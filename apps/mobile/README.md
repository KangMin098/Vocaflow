# @vocaflow/mobile

Expo (React Native) 앱. 디자인 토큰은 `@vocaflow/design-tokens` 의 JS 객체를 사용 — CSS Variables 가 아닌 `tokensLight` / `tokensDark` 를 `useTokens()` 훅으로 분기.

## 개발

```bash
pnpm --filter @vocaflow/mobile start
```
