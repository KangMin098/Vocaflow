// apps/web/src/lib/admin/help/video.ts
//
// VFP(영상 공장) 화면도움말.
//
// 이 화면의 관리자가 가장 먼저 묻는 것은 **"왜 여기서 실행이 안 되지?"** 다.
// 다른 파이프라인 콘솔에는 실행 버튼이 있는데 여기만 명령을 복사하라고 하기 때문이다.
// summary 가 그것부터 답한다 — 렌더는 브라우저를 띄워 프레임을 한 장씩 찍는 일이라
// 서버에서 돌릴 수 있는 종류가 아니다.

import type { HelpRegistry } from './types'

export const VFP_HELP: HelpRegistry = {
  video: {
    title: '영상 공장 (VFP)',
    screen: {
      summary:
        '플랫폼 구성요소(소개·장점·커리큘럼·시리즈·문항 유형·학습 활동)마다 PR 영상 한 편을 찍어 공개 버킷에 올리는 파이프라인의 관측 화면. 영상 목록이 코드에 적혀 있지 않다 — 구성요소가 늘면 찍어야 할 영상도 자동으로 는다. 이 화면은 그 차이를 본다.',
      when: '플랫폼에 시리즈·유형·활동을 추가한 뒤, 또는 학습자 화면에서 영상이 안 보인다는 말이 나왔을 때.',
      steps: [
        {
          title: '현황에서 밀린 것을 센다',
          detail:
            '구성요소 수와 영상 수가 다르면 "안 만듦" 으로 뜬다. 파일이 실제로 살아 있는지는 스토리지를 직접 읽어 따로 센다 — manifest 는 "발행했다는 기록"이라 파일이 지워져도 그대로 남는다.',
          done: '문제가 0건이면 경보 칸이 아예 안 펴진다.',
        },
        {
          title: '내보내기에서 명령을 가져간다',
          detail:
            '전량과 "밀린 것만" 두 벌을 준다. 밀린 것만 돌리면 이미 찍은 편을 다시 찍지 않는다. 명령은 **로컬 저장소**에서 돌린다 — 서버에서 실행되지 않는다.',
          done: '실행이 끝나고 이 화면을 새로고침하면 수치가 움직인다.',
        },
        {
          title: 'manifest 를 커밋한다',
          detail:
            '발행(publish)까지 끝나도 `apps/web/src/lib/video/manifest.json` 을 커밋해 배포하기 전에는 **학습자 화면에 한 편도 안 뜬다.** 화면이 보는 것은 버킷이 아니라 이 파일이다.',
          done: '상단의 붉은 경고(발행 기준 URL 없음)가 사라진다.',
        },
      ],
      fields: [
        {
          label: '파일 살아 있음',
          detail:
            'manifest 에 적힌 mp4 가 버킷에 실제로 있는 편 수. 규격 3종이 **전부** 있어야 한 편으로 센다. 이 값이 「영상 있음」보다 작으면 그 편은 학습자 화면에서 깨진 채 뜬다.',
        },
        {
          label: '재생 시작',
          detail:
            '누적 재생 시작 횟수(`video_started`). **0 이면 경고색이다** — 62편을 찍어 올려도 본 사람이 0 이면 공급만 늘린 것이기 때문이다. 편별 수치는 구성요소 탭의 「재생」 칸에 있다.',
        },
        {
          label: '고아',
          detail:
            '영상은 발행돼 있는데 그 구성요소가 플랫폼에서 사라진 경우. 화면에서 죽은 링크가 되지는 않지만(그 자리가 없으니) 버킷 용량을 계속 먹는다.',
        },
        {
          label: '못 잼 (?)',
          detail:
            '스토리지를 못 읽었다는 뜻이지 0 이 아니다. 0 으로 뭉개면 "파일이 없다" 는 거짓 경보가 되므로 구분해서 그린다.',
        },
      ],
      cautions: [
        '렌더 전량은 약 90분·177MB 다. 중간에 끊어도 안전하지만, 이미 찍은 편을 건너뛰지는 않는다(덮어쓴다) — 밀린 것만 돌리려면 내보내기 탭의 두 번째 명령을 쓴다.',
        '`work/` 와 `out/` 은 커밋하지 않는다. 번들에는 그날의 재고 수치가 박혀 있어서, 묵은 것으로 다시 찍으면 **틀린 수치로 광고를 만들게 된다.**',
        'Remotion 은 4인 이상 영리 법인에 유료다(좌석당 월 $25 또는 렌더당 $0.01). 팀이 넷이 되는 날 정산 대상이다.',
      ],
      drain: {
        what: '구성요소마다 나레이션을 굽고 영상을 찍어 공개 버킷에 올린다. LLM 이 채우는 칸은 없다 — 문구는 전부 구성요소의 실제 이름·설명·실측 수치에서 파생된다.',
        procedure: [
          {
            title: '① 원료',
            detail: '`pnpm video:source` — DB 실측을 번들로 뽑는다.',
            done: '**재실행 안전** — 같은 DB 상태면 같은 파일이 나온다.',
          },
          {
            title: '② 음성',
            detail:
              '`pnpm video voice` — Edge TTS 로 나레이션을 굽고 낱말 타임스탬프로 컷 길이를 실측한다.',
            done: '**재실행 안전** — 문장·음성 설정이 같으면 건너뛴다("새로 0 · 건너뜀 N").',
          },
          {
            title: '③ 렌더',
            detail: '`pnpm video render-all` — 규격 3종을 찍는다. 전량 약 90분·177MB.',
            done: '**덮어쓴다**(건너뛰지 않는다). 밀린 것만 돌리려면 id 를 붙인다.',
          },
          {
            title: '④ 썸네일',
            detail: '`pnpm video thumbs` — YouTube 목록용 1280×720. 영상이 아니라 1프레임이라 빠르다.',
            done: '**재실행 안전** — 이미 있으면 건너뛴다.',
          },
          {
            title: '⑤ 포장',
            detail:
              '`pnpm --filter @vocaflow/video-factory package` — 포스터·WebVTT 자막·설명글·manifest·youtube.json.',
            done: '**재실행 안전** — 이미 있는 포스터는 건너뛴다.',
          },
          {
            title: '⑥ 발행',
            detail: '`pnpm --filter @vocaflow/video-factory publish` — 공개 버킷 업로드.',
            done: '**재실행 안전** — 같은 크기로 올라간 파일은 건너뛴다.',
          },
          {
            title: '⑦ manifest 커밋',
            detail:
              '`apps/web/src/lib/video/manifest.json` 을 커밋해 배포한다. 화면이 보는 것은 버킷이 아니라 이 파일이다.',
            done: '이 화면 상단의 붉은 경고가 사라지고 `/video` 에 카드가 뜬다.',
          },
        ],
        prerequisites: [
          '로컬 저장소와 `apps/web/.env.local` 의 `SUPABASE_SERVICE_ROLE_KEY`(발행용 — anon 키로는 버킷에 못 쓴다).',
          '첫 실행 때 헤드리스 크롬 113MB 를 내려받는다.',
        ],
        verify: [
          '`pnpm video stale` — 발행본이 설계도와 어긋나는지(없어진·안 올린·낡은 편).',
          '이 화면의 「파일 살아 있음」이 「영상 있음」과 같은지.',
          '공개 URL 이 200 을 주는지 — 버킷이 공개라 로그인 없이 확인된다.',
        ],
        recovery: [
          '렌더가 전부 즉시 실패하면 포트 문제다 — 앞 렌더가 포트를 붙들고 있을 수 있다. CLI 가 빈 포트를 찾아 쓰지만, 남은 프로세스가 있으면 먼저 정리한다.',
          '음성이 중간에 끊기면(Edge TTS 연속 호출 제한) 그냥 다시 돌린다 — 이미 구운 것은 건너뛴다.',
          '파일이 유실됐으면 `publish` 를 다시 돌린다. 크기가 같은 것은 건너뛰므로 빠진 것만 올라간다.',
        ],
      },
    },
    tabs: {
      현황: {
        summary:
          '구성요소·발행본·실제 파일 **셋을 겹쳐** 본다. 셋이 어긋나는 방식이 각각 다른 사고를 뜻한다 — 안 만듦(플랫폼은 자랐는데 영상이 안 따라왔다) · 유실(기록은 있는데 파일이 없다) · 고아(구성요소가 사라졌다).',
        when: '정기 점검, 또는 "영상이 안 보인다" 는 말이 나왔을 때 가장 먼저.',
        fields: [
          {
            label: '발행본 기준 시각',
            detail:
              'manifest 를 만든 때. 이 화면의 모든 수치는 그때의 발행본을 말한다 — 그 뒤에 구성요소를 추가했다면 「안 만듦」으로 잡힌다.',
          },
        ],
      },
      구성요소: {
        summary:
          '한 행이 **구성요소 하나**다(영상 하나가 아니라). 영상이 없는 구성요소도 행으로 남아야 "밀린 것" 이 보이기 때문이다. 편별 재생 수가 여기 있다.',
        when: '무엇을 더 만들지, 어떤 영상이 실제로 보이는지 정할 때.',
        fields: [
          {
            label: '규격 3/3',
            detail:
              '가로(YouTube 본편·학습자 화면) · 세로(Shorts·세로 광고) · 정사각(피드 광고). 하나라도 빠지면 그 채널에 못 내보낸다.',
          },
          {
            label: '썸네일·자막 두 점',
            detail:
              '앞이 썸네일, 뒤가 WebVTT 자막. 썸네일은 포스터 프레임과 다른 물건이다 — 목록에서 168×94px 로 줄어들면 본문 글자는 한 획도 안 남는다.',
          },
        ],
      },
      '수치 낡음': {
        summary:
          '발행된 영상에 박힌 수와 **지금 DB 값**을 맞대 본다. 영상은 찍은 날의 스냅샷이라 수치는 반드시 묵는다 — 그래서 "틀렸다" 가 아니라 얼마나 달라졌는지만 보여 준다.',
        when: '분기 진단 때, 또는 재고가 크게 늘어 영상이 실제보다 작게 말하고 있다고 느껴질 때.',
        fields: [
          {
            label: '임계값이 없는 이유',
            detail:
              '"20% 넘으면 경고" 같은 수를 근거 없이 정하면 그건 판단이 아니라 짐작이다. 대신 실제 차이를 크기순으로 낸다 — 다시 찍을지는 사람이 정한다.',
          },
          {
            label: '목록에 없는 근거',
            detail:
              '지금 값을 다시 셀 수 있는 것만 비교한다(사전·문항·도서·챕터 퀴즈·유형별 재고). 논문 인용이나 계산식 같은 근거는 비교 대상이 아니라 건너뛴다 — 억지로 0 으로 만들지 않는다.',
          },
          {
            label: '▲ / ▼',
            detail:
              '부호를 함께 낸다(색만으로 말하지 않는다). ▲ 는 지금이 더 크다 = 영상이 실제보다 **작게** 말하고 있다는 뜻이다.',
          },
        ],
      },
      내보내기: {
        summary:
          '실행할 명령을 그대로 건넨다. **서버에서 돌리지 않는 이유**는 렌더가 브라우저로 프레임을 한 장씩 찍는 일이라서다(전량 약 90분). 이 저장소의 다른 드레인과 같은 모양이다.',
        when: '밀린 것을 찍을 때, 또는 YouTube 채널에 올릴 때.',
        fields: [
          {
            label: '밀린 것만',
            detail:
              '「안 만듦」으로 잡힌 편의 id 만 넣은 명령. 전량(90분) 대신 그것만 돌린다. 이 블록은 밀린 것이 있을 때만 나타난다.',
          },
          {
            label: 'youtube.json',
            detail:
              '편당 제목·설명·태그·자막·썸네일과 본편(가로)·Shorts(세로) 경로. 설명란에 수치의 출처와 측정일이 함께 들어간다 — 근거 없는 수치를 채널에 올리지 않기 위해서다.',
          },
        ],
      },
    },
  },
}
