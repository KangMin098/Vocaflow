// apps/web/src/lib/admin/help/textbook.ts
//
// 원문 적격 화면도움말.
//
// ⚠️ 2026-09-06 에 TBP 관측판(`/admin/textbook`)을 지우면서 그 도움말도 함께 지웠다 —
//    파이프라인이 「TBP Pipeline」과 「교재 공장」 둘로 보였고, 둘 다 같은 교재를 말하는데
//    한쪽은 조회만 한쪽은 공정이라 관리자가 어디로 가야 하는지가 안 갈렸다. 고유 자산 셋은
//    공정으로 옮겼다: 브랜드 규격 → ⑧ 조판 · 초·중 원문 재고 → ④ 소재 · 평가 요소 15 → ② 기획.
//    「원문 적격」은 원천 재고·적격 판정·처리 절차를 잇는 ④ 소재의 작업 화면이다.

import type { HelpRegistry } from './types'

export const TBP_HELP: HelpRegistry = {
  'csat-sources': {
    // 메뉴 라벨과 같은 이름이어야 한다 — 다르면 관리자가 자기가 어디 있는지 알 방법이 없다.
    // 2026-09-23 「원문 적격」 → 「소재 적격」(DD-74): 이 화면의 출력은 **④ 소재의 입력**이고,
    // 이름이 그 자리를 말하게 했다. 라우트(csat/sources)와 도움말 슬러그는 그대로다.
    title: '소재 적격',
    tabs: {
      '원천 관리': {
        diagrams: [{ kind: 'flow', caption: '원천에서 개별 원문으로', nodes: [{ label: '검색·필터', says: '문제 조건으로 범위를 좁힌다' }, { label: '검토', says: '근거와 준비 상태를 확인한다' }, { label: '원문 검수', says: 'ACP에서 실제 원문을 연다' }] }],
        summary: '원천을 검색하고 확인할 항목으로 좁힌 뒤 **검토 → 원문 검수하기**로 이동합니다. 검색·필터·정렬·선택은 URL에 남아 새로고침과 뒤로가기로 복원됩니다.',
        steps: [
          { title: '맨 위 수를 지금 값으로', detail: '맨 위 「교재에 실을 수 있는 원문」은 **지금 DB 에서 센 수**입니다(판정 규격 v4 · DB 함수 csat_source_live_rollup). 「지금 다시 세기」를 누르면 그 자리에서 다시 셉니다 — 약 0.2초, 읽기만 하고 아무것도 바꾸지 않아 몇 번 눌러도 됩니다. 「센 시각」은 지금이고 「판정 시각」은 원문마다 판정을 매긴 때입니다 — 판정을 새로 하려면 드레인을 돌려야 하고, 단추는 **세기만** 합니다.', done: '「지금 DB 기준 · ○○에 셈」이 바뀌면 다시 센 것입니다.' },
          { title: '원천별 작업 진행표 읽기', detail: '행은 원천, 열은 단계입니다 — ① 모음 → ② 원문 점검(보관 판정) → ③ 발췌 → ④ 학년 분석 → ⑤ 내용 판정 → ⑥ 실을 수 있음. 칸마다 값 · 막대 · 한 줄(남은 수)과 상태(끝 · 남음 · 막힘 · 해당 없음)가 있습니다. 처음에는 **남은 일이 가장 많은 원천 8개**만 보이고 나머지는 한 줄로 접힙니다. 숫자는 DB 함수 csat_source_pipeline_live 가 세며, 원본/조각 구분과 보관 상태는 발췌기와 **같은 규칙**(gate-rules.mjs)입니다 — 그래서 「③ 발췌 대기」는 발췌기가 지금 자를 수 있는 원본 수와 같습니다.', done: '남은 수가 0 인 칸은 「끝」입니다. 전체의 1% 이하가 남으면 「끝」으로 칠하되 남은 수를 적습니다.' },
          { title: '칸을 눌러 하는 법 보기', detail: '칸을 누르면 표 아래에 그 원천 · 그 단계의 하는 법이 열립니다 — 지금 수 · 먼저 끝나야 하는 것 · 실행 줄(위에서부터, **예행이 먼저**) · 「명령 복사」 · 「Claude 에게 맡기기(지시문 복사)」 · 다시 돌려도 되는지. ② 원문 점검 칸은 그 원천의 **판정자 일치도(κ)** 와 대량 판정 가능 여부(최근 두 회차 κ 가 모두 0.6 이상)를 docs/source-check/round-*.md 에서 읽어 보여 줍니다.' },
          { title: '검토 대상 찾기', detail: '「자세히 보기」를 펴면 예전 화면(관문 7개 · 원천 관리 · 적격 판정 · 원문 조회 · 처리 안내)이 그대로 있습니다. 주소에 탭·조건이 있으면 펴진 채로 열립니다. 검색어와 확인할 항목으로 원천을 좁힙니다. 조치 필요 우선은 실패 → 법적 제약 → 발췌 경로 → 분석 → 판정 기록 → 차단 기록 순입니다.' },
          { title: '근거 확인', detail: '검토를 눌러 준비 상태와 근거를 확인합니다. 항목 수는 서로 겹치므로 더하지 않습니다.' },
          { title: '원문 가져오기 (가져오는 법)', detail: '상세 패널의 「가져오는 법」을 펴면 그 원천의 수집 스크립트와 단계별 명령(읽기 전용 dry-run → 소량 → 본 수집 → 분석 큐 처리 → 다시 세기)이 나옵니다. 각 명령은 복사할 수 있고, 「Claude Code 에 맡기기」는 쓰기 단계 앞에서 dry-run 결과를 보여 주고 확인을 받도록 적힌 지시문을 복사합니다. 화면은 아무것도 실행하지 않습니다. 모든 스크립트는 `--commit` 없이는 쓰지 않고 source_id 로 중복을 건너뛰어 **재실행 안전**합니다. 담긴 글은 queued 라 분석을 거쳐야 적격 판정 대상이 됩니다.', done: '「지금 다시 세기」 뒤 그 원천의 재고가 늘었습니다.' },
          { title: '원문 검수', detail: '해당 원천의 모든 상태를 포함한 ACP 검수 목록을 엽니다. 수정·보관·삭제는 그 화면에서 진행합니다.' },
        ],
        cautions: ['원문 점검 지시문은 **DB 에 쓰지 않는** 표본 회차입니다. 보관 판정을 대량으로 적재하는 것은 그 원천의 κ 가 두 회차 연속 0.6 이상이고 승인을 받은 뒤입니다(AGENTS.md).', '원천별 표는 이제 지금 DB 이고(「지금 다시 세기」가 함께 센다) **적격 판정 탭의 학년별 표만 스캔 결과**입니다.', '검수 대기(ready)와 학령·내용 판정 기록은 적격 통과가 아닙니다. 원천별 적격 통과 수는 이 집계에 없습니다.', '마지막 등록은 created_at 기준이며 수정·검수 시각이 아닙니다. 재고와 적격 판정은 측정 시각과 대상이 다를 수 있습니다.'],
      },
      '적격 판정': {
        diagrams: [{ kind: 'keys', caption: '수집 상태와 적격은 별개', nodes: [{ label: '검수 대기', says: '아직 교재 적격을 보장하지 않는다' }, { label: '적격 7축', says: '법적 안전부터 어휘까지 판정한다' }, { label: '조판 가능', says: '그대로 사용·발췌해 사용만 허용한다' }] }],
        summary: '상단 수치나 검토 큐를 선택하면 개별 원문 목록으로 내려갑니다. 원문을 눌러 차단 사유·본문·발췌 후보·문항·교재 기록·이력을 확인하세요. 원문 정책 v3는 관리자·조판·학습에서 공유합니다.',
        steps: [
          { title: '관문에서 걸린 곳을 본다', detail: '화면 맨 위 「지금 해야 할 작업」이 원문이 지나는 관문을 순서대로 세웁니다. **어느 관문에 걸렸는지가 곧 다음에 할 일**입니다. 「드레인으로 열림」만 지금 돌릴 수 있고, 「정책 결정 필요」는 갈아 넣어도 안 열립니다 — 병목 표시는 그래서 열 수 있는 것 중에서만 고릅니다. **걸린 편수는 합산하지 않습니다**(한 원문이 추출 대기이면서 학령 초과일 수 있습니다). 번호가 건너뛰면 그 관문에 걸린 원문이 없다는 뜻입니다.' },
          { title: '숫자를 눌러 그 원문을 본다', detail: '관문의 편수와 위쪽 세 칸(판정 대상 · 관문 통과 · 적격·재료 있음)은 전부 단추입니다. 누르면 아래 조회 콘솔이 그 조건으로 걸리고 화면이 그리로 내려갑니다. 관문에서 내려온 「사유」 조건은 거르개 지우기로 안 풀리므로 그 줄의 **사유 조건 풀기**를 씁니다.' },
          { title: '영향부터 확인', detail: '반려 원문·문항 연결 큐부터 확인합니다. 연결 문항 수가 많은 원문을 먼저 보여 줍니다. 과거 교재 manifest가 없으면 노출 없음으로 판단하지 않습니다.' },
          { title: '본문과 사유 대조', detail: '품질 신호는 오탐을 포함합니다. 발췌창은 후보이고 문항 연결은 승인된 범위의 증거가 아닙니다. 실제 지문·위치·검수를 확인하세요.' },
          { title: '최신 원문으로 재검증', detail: '현재 원문을 다시 읽어 파생 판정 캐시만 갱신합니다. 원문·내용 verdict·정답은 보존하며 같은 상태로 재실행할 수 있습니다. 원문 revision이 바뀌면 이전 캐시는 학습에서 사용하지 않습니다.' },
          { title: '마지막 드레인 확인', detail: '조회 콘솔 위 한 줄이 이 단계에서 마지막으로 돈 드레인입니다. **건너뜀 수가 재실행 안전의 증거**입니다 — 두 번째 실행인데 건너뜀이 0이면 같은 것을 다시 쓰고 있다는 뜻이므로 적재를 멈추고 원인을 봅니다. 「기록 표가 없다」와 「한 번도 안 돌렸다」는 다르게 적히며, 앞엣것은 마이그레이션 미적용입니다.' },
        ],
        cautions: [
          '큐는 서로 겹치므로 합산하지 않습니다. 내용 검토 큐에는 다른 축에서 이미 차단된 미판정 원문도 포함됩니다.',
          '재검증은 내용 판정을 새로 작성하거나 본문을 정제하는 액션이 아닙니다. 본문 변경은 연결 문항·앵커 영향 검토가 먼저입니다.',
          '교재 재료로 거르면 **아직 판정이 안 실린 원문은 안 걸립니다**(태그가 없으므로). 「재료가 없다」가 아니라 「아직 안 봤다」일 수 있으니, 재료 거르개를 풀고 미판정 수를 함께 보세요.',
          '**「교재 재료 실림」과 「적격 · 재료 있음」은 다른 수입니다.** 앞엣것은 등급을 보지 않아서, 막힌 원문도 포함합니다. 교재 생성에 넘길 수 있는 것은 뒤엣것뿐이고, 그 칸이 0이면 재료 거르개로 아무리 찾아도 **교재에 못 쓰는 원문만** 나옵니다.',
        ],
      },
      '처리 안내': {
        diagrams: [{ kind: 'flow', caption: '실행 전에 범위를 확인', nodes: [{ label: '집계 갱신', says: 'DB 읽기·로컬 스냅샷 갱신' }, { label: '병목 확인', says: '명령의 대상과 전제 조건을 읽는다' }, { label: '처리·재확인', says: '각 절차에 따라 실행하고 다시 잰다' }] }],
        summary: '집계 갱신과 판정·발췌 절차를 확인합니다. 이 탭은 명령을 실행하지 않습니다. 단계의 조건을 읽고 대상 원천에 맞는 절차를 선택하세요.',
        steps: [
          { title: '집계 갱신', detail: '두 명령은 DB 읽기와 로컬 스냅샷 갱신입니다. 재실행 가능하며 완료 후 화면을 새로고침합니다.' },
          { title: '처리 조건 확인', detail: '다음 한 걸음에서 병목과 실행 전 조건을 읽습니다. 명령이 특정 원천과 전체 재고 중 무엇을 대상으로 하는지 확인합니다.' },
          { title: '문항 계획과 검수', detail: '유형별 문항과 보충 계획, 검수 드레인 결과를 펼쳐 확인합니다. DB 적재는 각 드레인의 절차와 재실행 조건을 따릅니다.' },
        ],
      },
    },
    screen: {
      summary:
        '**교재·학습에 사용할 수 있는 원문인가**를 확인하고 개별 원문까지 검토하는 곳입니다. `source-eligibility.ts:evaluateSource`가 판정 정본이며, 원천 집계 JSON과 DB 파생 판정 캐시의 측정 시각을 구분합니다. 재검증은 원문을 보존하고 파생 캐시만 갱신합니다.',
      when: '권을 새로 조판하기 전. 또는 드레인(분석·게이트·발췌 채점)을 돌린 뒤 재고가 실제로 늘었는지 확인할 때.',
      diagrams: [
        {
          kind: 'flow',
          caption: '원문이 권으로 가는 길 — 권수를 막는 것은 대개 원문이 아니다',
          nodes: [
            { label: '적격 7축', actor: 'auto', says: '장르 · 저작권 · 어수창 · 어휘 · 자립성' },
            { label: '유형 재고', actor: 'auto', says: '권수는 가장 얇은 유형이 정한다' },
            { label: '채울 몫', actor: 'auto', says: '결정론과 생성형을 가른다 — 비용 100배' },
            { label: '청크를 뽑는다', actor: 'script', says: '이미 뽑힌 것부터 — 바로 집필 가능' },
            { label: '문항을 쓴다', actor: 'claude', says: '쓴 뒤 게이트에 막힌 것을 본다' },
          ],
          loop: '원문을 늘려도 유형 격차는 안 줄어든다 — 얇은 유형을 먼저 채운다.',
        },
      ],
      fields: [
        {
          label: '유형 재고 — 병목이 원문이 아닐 수 있다',
          detail:
            '일곱 축을 다 통과한 원문이 아무리 많아도, **시중이 내는 유형을 우리가 못 내면** 교재는 시중을 못 따라간다. 조판 로그가 오래 「시장 유형 적합도 99.4%(가진 유형 안에서) · **시장 전체 기준 30.1%**」라고 말해 왔는데 그 격차의 정체가 이 표다. ' +
            '실측 2026-09-07: 시장 목표 **14%**(V5~7 1위)인 `blank` 는 재고 **28개**인데, 목표 **1%** 인 `blank_word` 는 **219,810개**다. **원문을 아무리 늘려도 이 격차는 안 줄어든다.** ' +
            '⚠️ **「재고가 있다」로 세지 않는다** — 유형당 26개만 있어도 「있다」가 되기 때문이다. 한 권은 20단원 × 6문항 = 120문항이고 목표 14% 면 **권당 17개**가 필요하므로 26개는 한 권 쓰면 바닥이다. 그래서 재는 것은 **「몇 권까지 갈 수 있는가」** 이고, 그 권수는 **가장 얇은 유형**이 정한다 — 그것을 늘리기 전에는 다른 유형을 아무리 늘려도 권수가 안 는다. ' +
            '목표 비중의 정본은 `rungMix`(시중 79종 실측에서 유도) — 화면이 다시 계산하지 않는다. 갱신: `pnpm dlx tsx scripts/textbook/type-inventory-scan.mjs`(읽기만 하므로 재실행 안전 · 약 70초).',
        },
        {
          label: '채울 몫 — 그래서 무엇부터 쓰는가',
          detail:
            '위의 유형 재고는 「몇 권인가」까지만 답한다. 그 다음 질문(「무엇부터」)을 화면이 안 답하면 **관리자가 표를 눈으로 세게 되고, 눈으로 세면 눈에 띄는 것만 센다.** 실측 2026-09-08: 재고표를 보고 「제목·주제·빈칸 셋이 병목」이라 적었는데 계산해 보니 병목은 **생성형 유형 전부**였다(내용일치·주장·심경도 같이 비어 있었다). ' +
            '이 표는 **비용이 100배 다른 둘을 갈라 놓는다** — `order`·`grammar_fix` 같은 결정론 유형은 생성기 한 번이면 되고(이미 수천 권 분량이 쌓여 있다), `title`·`topic`·`blank`·`content_match`·`claim`·`mood` 같은 **생성형 유형은 글을 읽어야 만든다.** 권수를 올리는 것은 뒤쪽뿐이다. ' +
            '「지금 시작 가능」은 **이미 뽑혀 있고 아직 안 채워진 청크** 수다 — 그만큼은 DB 를 건드리지 않고 바로 집필에 들어갈 수 있다. 나머지는 `item-drain-export.mjs` 로 먼저 뽑아야 한다. ' +
            '갱신: `pnpm dlx tsx scripts/textbook/item-fill-plan.mjs`(재고 스냅샷을 읽기만 하므로 재실행 안전 · DB 를 안 본다). 목표 권수는 `--volumes N`. 뽑을 명령까지 보려면 `--export-cmds`.',
        },
        {
          label: '원천 생존율 — 편수는 공급이 아니다',
          detail:
            '이 화면의 다른 표는 「몇 편 있는가」를 센다. 그런데 **편수만 세면 없는 공급을 있다고 세게 된다.** 실측 2026-09-13: 집필 배치 다섯이 독립으로 같은 것을 보고했다 — 같은 밴드·같은 규격인데 **원천이 다르면 지문 생존율이 스무 배 갈린다.** PLOS/Europe PMC 초록은 게이트 통과 **5.6%**(topic·blank V7 기준 2/36), Gutenberg 고전 산문은 **100%**(implication V7 16/16 · content_match V6 40/40). ' +
            '사인은 둘뿐이다 — **논문 서식**(`Citation:` · `Funding:` · `Objective To …` · `95% CI`)과 **어수 상한 초과**(학술 초록은 190어 언저리에 몰려 있는데 V7 상한은 178어). 둘 다 지문을 고쳐서는 못 푼다. ' +
            '그래서 「9만 편 있다」는 상위 밴드 독해에 대해 참이 아니다. **어느 원천을 더 수확할지, 어느 밴드에 어느 원천을 붙일지가 이 표에서 갈린다** — 그것이 「원문 선택의 기준」이다. ' +
            '⚠️ **표본이다.** 밴드 하나가 3만 편을 넘고 본문이 1.3GB 라 전수는 못 잰다. 칸마다 최대 `--per`(기본 40)편을 보고 **표본 수를 함께 찍는다** — 5편 본 칸의 80% 와 40편 본 칸의 80% 는 같은 말이 아니다. 표본은 결정론으로 고르므로 같은 재고면 같은 표본이고, 값이 흔들리면 그것은 재고가 바뀐 것이다. ' +
            '⚠️ **안 쟀으면 표가 아예 안 보인다** — 0% 로 칠하면 화면이 「모든 원천이 못 쓴다」고 말하게 된다. ' +
            '갱신: `pnpm dlx tsx scripts/textbook/source-yield-scan.mjs`(읽기만 하므로 재실행 안전 · 표본 본문을 받으므로 몇 분 걸린다). 유형 창을 바꾸려면 `--type`, 표본을 늘리려면 `--per`.',
        },
        {
          label: '쓰고도 못 싣는 것',
          detail:
            '**게이트는 시간이 지나며 엄해지는데 청크와 산출은 게이트보다 오래 산다.** 밴드별 어수창(8/31) · 기사 껍데기 차단(9/6) · 해설의 인용·오답 언급 요구가 차례로 들어왔고, 그 전에 쓴 문항이 **조용히** 적재에서 막힌다. 아무도 안 보면 그 일을 두 번 하게 된다. ' +
            '실측 2026-09-08: 이미 채운 **1,377문항 중 315(22.9%)** 가 막혀 있었다. ' +
            '⚠️ **그 315를 한 수로 보면 안 된다** — 「315개를 다시 써야 한다」로 읽혀 아무도 손대지 않는다. 갈라 보면 **142는 한국어 해설 한 줄**이면 살아나고(선택지·정답은 이미 검증됐다), 173은 지문이 문제라 집필로 못 고친다. 비용이 100배 다르다. ' +
            '해설이 걸리는 흔한 자국 둘: 약어(`"B.S., M.S."`)만 인용해 「4자 이상 영단어 둘이 이어진 자리」를 못 만든 경우, 그리고 왜 나머지가 아닌지를 안 짚은 경우(`오답`·`나머지`·`①②③④⑤` 중 하나가 있어야 한다). ' +
            '갱신: `pnpm dlx tsx scripts/textbook/item-drain-audit.mjs`(읽기만 하므로 재실행 안전 · DB 를 안 본다). 고칠 파일과 칸 번호까지 보려면 `--fixable`.',
        },
        {
          label: '발췌가 어디까지 와 있는가',
          detail:
            '미판정의 처방은 「발췌 경로로 가야 한다」인데, **그 발췌는 이미 뽑혀 있을 수 있다.** 실측 2026-09-07: 11,601편이 이미 뽑혀 있었고 **전부 `queued` · 학령 분석 0** 이라 조판 가능이 되지 못하고 있었다 — 안 만든 것이 아니라 만들어 놓고 다음 단계에서 서 있었다. ' +
            '그래서 「다음 한 걸음」이 뽑힌 편수 · 분석이 붙은 편수 · 기다리는 편수를 함께 말한다. **분석이 붙어야 조판 가능이 될 수 있다.** 다음 명령은 `scripts/acp/process-queue.mjs --feed plos-extract --commit --limit N` — 결정론 경로라 **LLM 비용이 없고** 편당 약 5초다. ' +
            '⚠️ 편당 어휘 100~120개를 쓰므로 전량(1만여 편)은 누적 쓰기가 100만 행대다. 이 저장소는 2026-09-06 에 쓰기 폭주로 25분 정지를 겪었다 — `--limit` 으로 나눠 돌릴 것.',
        },
        {
          label: '추출 결함',
          detail:
            '**일곱 축이 통과시킨 뒤에도 남는 것.** 축은 「이 원문을 써도 되는가」를 묻고 그 질문은 본문이 온전하다는 것을 전제한다 — 장르도 저작권도 어수도 맞는데 첫 문단이 사이트 안내문이거나 초록이 두 번 들어 있는 편은 축이 못 잡는다. 그대로 조판하면 **그 문자열이 학생이 읽는 지문에 인쇄된다.** ' +
            '적격과 **다른 스캔**이라 잰 시각도 따로 보인다 — `pnpm dlx tsx scripts/textbook/extraction-defect-scan.mjs --all`(전수일 때만 스냅샷을 덮는다 · 읽기만 하므로 재실행 안전). ' +
            '⚠️ **비율만 보고 판단하지 말 것** — 한 원천이 그 결함의 80% 이상을 차지하면 표가 그 사실을 함께 말한다. 실측 2026-09-06: 「문단 통째 중복」이 전체의 59.4% 였는데 12,917건 중 **12,878건이 plos 하나**였고 모양도 하나였다(초록이 본문 앞에 한 번, `Abstract` 뒤에 다시 한 번). 「본문 절반이 깨졌다」와 「한 원천의 수확기가 한 군데서 겹쳐 붙인다」는 처방이 아예 다르다. ' +
            '이 스캔은 **고치지 않는다** — 어디에 몇 편 있는지만 센다. `==` 는 수식에도 나오고 `Media` 는 본문 낱말로도 나오므로, 무엇을 지울지는 소스별 추출기를 고칠 때 사람이 정한다. ' +
            '⚠️ **여기 잡히는 발췌는 2026-09-15 이전에 담은 것이다** — 자르는 쪽을 그날 고쳤다(경계에서 멈춘다). 실측 책 8권에서 「경계넘음」 21 → **0**, 대신 조각 수 −16.2%. 새로 담는 것에는 안 생기지만 **이미 담긴 것은 그대로 남아 있다** — 다시 자르는 것은 본문 덮어쓰기라 따로 승인이 필요하다. ' +
            '⚠️ **「이야기 경계를 넘은 발췌」는 수가 하한이다** (2026-09-15 추가). 선집을 고정 길이 창으로 자르면 한 지문에 두 이야기가 담기는데(실물: 앞 5문장이 Ivan·Koshchei 이야기의 끝, 뒤 5문장이 다른 아이 이야기의 시작 — 밑줄 5개가 두 이야기에 갈려 있었다), 이 규칙은 **여는 정형구를 쓴 경계만** 본다. 정형구 없이 바뀌는 경계(`Oeyvind was his name.`)는 **안 걸린다** — 정형구 적중에도 정상 인용 등의 오탐이 있으므로 문맥 확인이 필요하다. 실제로 경계를 넘었다고 확인한 편만 다시 자르며 미적중도 품질 승인은 아니다.',
        },
        {
          label: '소스별 원문 — 판정이 아니라 재고',
          detail:
            '위 표들이 「이 원문을 써도 되는가」를 말한다면 이 표는 **「그 원천에서 언제 몇 편 받았고 지금 어떤 상태인가」**를 말한다. 2026-09-16 까지 이 화면에는 그 질문의 답이 없었다 — 원천 이름을 스물한 번 부르면서 판정 결과만 냈다. ' +
            '**다른 스냅샷을 읽는다**: `source-inventory-scan.mjs` 는 본문을 안 받으므로 실측 **9초**다(적격 스캔은 76~200초). 그래서 훨씬 자주 갱신할 수 있고, 잰 시각도 따로 보인다. **읽기만 하므로 재실행 안전.** ' +
            '⚠️ **「판정 받음」을 「조판 가능」으로 읽지 말 것** — 전자는 `csat_fit.gate.verdict` 가 붙은 편수고, 후자는 일곱 축을 다 통과한 편수다. 정본은 「등급 분포」 표다. 이 표가 판정을 다시 세면 같은 화면의 두 표가 다른 답을 하게 된다. ' +
            '⚠️ **「마지막 등록」은 `created_at` 이다.** `source_fetched_at` 열이 따로 있지만 수집기가 안 채워 실측 2026-09-15 기준 PLOS 45,096편이 **전부 비어 있다** — 그것을 쓰면 화면이 「한 번도 GET 안 함」이라고 거짓말한다. ' +
            '검토를 누르면 확인할 항목과 그 근거가 나옵니다. 원천 이름만으로 처리 명령을 추천하지 않습니다. 처리 안내에서 명령의 대상과 전제 조건을 확인합니다. ' +
            '수집·판정 실행 버튼은 없다. 실행 절차는 처리 안내에서 확인하고 원문 변경은 ACP에서 진행한다.',
        },
        {
          label: "원문 조회와 변경의 경계",
          detail: "적격 판정 탭에서 본문·사유·발췌·연결·이력을 조회하고 파생 캐시를 재검증합니다. 본문·내용 verdict·발행 상태 변경은 ACP에서 수행합니다. 변경 전 문항과 앵커 영향을 확인하세요. 원문 revision 변경 후에는 재검증까지 학습에서 사용되지 않습니다.",
        },
        {
          label: '조판 가능',
          detail:
            '`그대로 사용` + `발췌해 사용` 두 등급의 합. **이 둘만이다** — 나머지 넷은 “지금은 쓸 수 없다” 는 뜻이고, 조판에 넣으면 근거 없이 고른 것이 된다. 등급을 하나 더 허용하려면 `COMPOSABLE_GRADES` 를 고쳐야 하고, 회귀가 그 변경을 잡는다.',
        },
        {
          label: '등급을 처방으로 가르는 이유',
          detail:
            '“쓸 수 있다/없다” 둘로 가르면 **고칠 수 있는 것과 못 고치는 것이 한 칸에 뭉친다.** 어수가 넘치는 글(자르면 된다)과 라이선스가 막힌 글(영영 못 쓴다)은 처방이 정반대다. 그래서 등급은 **다음에 무엇을 해야 하는가**로 가른다 — 각 줄 오른쪽에 그 처방이 적혀 있다.',
        },
        {
          label: "내용 판정 없음 (unjudged)",
          detail: "내용 verdict가 없어 학습·교재 사용을 기다립니다. raw는 발췌 후 판정, 게이트 누락은 내용 검토 대상입니다. 캐시 재검증만으로 내용 판정을 만들어 주지는 않습니다.",
        },
        {
          label: '조회로 찾기 (2026-09-23)',
          detail:
            '큐 14개는 서로 곱해지지 않아 「usable 인데 문항이 없는 것」·「논지 재료가 있는 B1 원문」 같은 질문이 안 됐습니다. ' +
            '이제 큐·등급·판정·학령·CEFR·교재 재료를 **AND 로 겹쳐** 겁니다. 표 위 한 줄이 지금 걸린 조건을 그대로 적습니다. ' +
            '결과가 0편이면 조건이 틀린 게 아니라 정말 없는 것입니다 — 조건이 틀리면 목록 대신 사유가 뜹니다(빈 목록으로 삼키지 않습니다).',
        },
        {
          label: '교재 재료 8종은 수능 목록이 아니다',
          detail:
            '논지·구조·시간순·심경·사실·어휘·어법·구어. 정본은 시중 교재 79종(textbook/market-spec.json)에서 뽑았고 ' +
            '수능 유형만을 위한 목록이 아닙니다. 태그가 **비어 있음([])** 과 **아직 안 실림(표시 없음)** 은 다른 뜻입니다 — ' +
            '전자는 반려돼 뽑을 재료가 없다는 뜻이고, 후자는 아직 판정이 안 내려갔다는 뜻입니다.',
        },
        {
          label: "긴 글의 후보와 조건부 사용 (규격 v3)",
          detail: "문항 보유는 실제 발췌 지문이 존재한다는 신호입니다. 원문 정책을 통과해도 조건부이며 개별 문항의 규격·정답·위치·검수가 필요합니다. 발췌창만 있는 원문은 사용 대기입니다.",
        },
        {
          label: '「문항 없는 원글 N편」은 두 몫이다 (2026-09-07)',
          detail:
            '조판 로그의 그 줄은 오래 **한 덩어리**였고 처방을 하나만 말했다 — 「`store-new-types.mjs` 를 돌려야 쓰인다」. ' +
            '그런데 실측(V7)에서 2,985편 중 그 명령으로 채울 수 있는 것은 **506편**뿐이었다. 나머지 2,479편은 **내용 판정을 못 받아 게이트가 막으므로 문항을 만들어도 안 실린다** — 돌리면 시간만 쓴다. ' +
            '지금은 갈라 찍는다: 「N편은 **지금 채울 수 있다**(store-new-types) · M편은 **내용 판정을 먼저 받아야 한다**」. 두 몫은 다음에 할 일이 다르다(문항 생성 vs 판정). ' +
            '전 밴드 실측 2026-09-07 — 판정을 통과했는데 문항이 없는 글 **3,218편**(V6 2,034 · V5 672 · V7 506 · V4 6). 이 몫은 즉시 채울 수 있는 순이익이다.',
        },
        {
          label: '문항이 붙은 원문 vs 조판 가능',
          detail:
            '두 수의 **차이가 곧 “판정 없이 만들어진 문항” 의 분모**다. 문항이 붙었다는 것은 그 원문에서 이미 지문이 잘려 나왔다는 뜻인데, 그 원문이 판정을 통과하지 못하면 근거 없이 쓰인 것이다. ' +
            '실측 2026-09-06: 문항 보유 **19,202편** · 조판 가능 **2,211편**.',
        },
        {
          label: '“게이트를 돌려도 안 풀린다” 줄',
          detail:
            '미절단 원본(`purpose=raw`)은 **게이트를 돌려도 판정이 안 붙는다** — `gate-rules.mjs` 의 `PURPOSE_RULE.raw.verdicts` 가 빈 집합이라 `decide()` 가 판정 전에 되돌아온다(“자르기 전에는 무엇도 게시 불가”). ' +
            '실측: `purpose=raw` 36,337편이 **전부** 판정자 `rule` · verdict 없음이다. 미판정 19,333편 중 **13,459편(70%)이 여기 해당**하므로, 이 줄이 없으면 관리자가 돌지 않을 배치를 돌린다. ' +
            '처방은 **보관 판정 → 발췌** 순서다(2026-09-24): `plos-raw-triage-export.mjs` 로 전문을 V-Level 낮은 것부터 20편씩 뽑아 판정 → `gate-mixed-import --input` 이 `gate.retain` 에 적재 → 보관된 것만 `plos-extract` 가 자른다. 보관 판정은 게시를 열지 않는다.',
        },
        {
          label: "미절단 원본의 처리 경로",
          detail: "raw 게시 제한은 발췌 작업이 필요하다는 뜻입니다. 내용 reject·학령/CEFR 제한을 무시하는 예외가 아닙니다. 순서는 ① 보관 판정(본문 전문을 읽고 gate.retain 에 use/narrative/reject — 길이·어휘·V-Level 로는 버리지 않고 순서에만 씁니다) ② 보관된 원본만 plos-extract 로 발췌 ③ 발췌본을 전문 판정입니다. 보관 판정이 없는 raw 는 보관 미결정(undecided)으로 셉니다 — 예전처럼 전량을 '추출 대기 보관'으로 세지 않습니다. ⚠️ 보관 판정을 앞 800어만 읽혀 하면 안 됩니다: 30편 대조에서 보관할 논문 17편 중 12편을 버렸습니다(2026-09-24). 여기서 버린 원본은 다시 읽히지 않으므로 망설여지면 보관 쪽으로 둡니다.",
        },
        {
          label: '연령 × 유형별 원문 요건 (2026-09-06 추가)',
          detail:
            '위 표들이 “지금 몇 편인가” 를 말한다면 이 표는 **“무엇을 갖춰야 하는가”** 를 말한다 — 둘이 함께 있어야 “이 지문을 왜 이 학년 이 유형에 썼나” 에 답할 수 있다. ' +
            '어느 학년에 어느 유형이 열리는지는 `SERIES_SPINE` 7단이 정하고, 창은 **유형 계열**(수능 짧은 지문 90~200어 · 수능 장문 260~400 · 학교 문단 40~200 · 학교 문장 6~40 · 초등 3종은 지문 없음)이 정한 뒤 ' +
            '**그 학년대 시중 분포 p10~p90**(`market-spec.json` · 79종 실측)이 좁힌다. ' +
            '**DB 를 안 본다** — 정본에서 바로 펴므로 스냅샷이 낡아도 이 표는 늘 지금 규격이다.',
        },
        {
          label: '“유형 창 그대로” 표시',
          detail:
            '학년이 창을 **못 좁혔다**는 뜻이다. 교차가 비면 `itemWordSpec` 이 유형 창을 그대로 쓴다 — 좁히려다 재료를 0 으로 만들지 않기 위해서다. ' +
            '좁혀진 척 보이면 근거가 거짓이 되므로 화면이 그대로 적는다. 장문·문장 단위·초등 3종이 여기 해당한다(시중 분포의 꼬리거나 잴 지문이 없다).',
        },
        {
          label: '되돌리기 불가',
          detail:
            '법적·게재 안전 두 축인데 **처방이 서로 다르다.** ' +
            '철회된 연구·제목에 드러난 민감 소재는 고칠 방법이 없으므로 **빼는 것이 유일한 처방**이다. ' +
            '라이선스(`cc_by_nd`·`restricted`·`display_only`)는 **원문의 문장을 싣지 못한다는 뜻이지 쓰지 못한다는 뜻이 아니다** — ' +
            '사실·논지 구조만 읽고 우리 문장으로 새로 쓰는 경로(R3·R4)가 있고, 그 산출물은 자체 저작(CC0)이라 게이트를 통상 통과한다. ' +
            '`original` 이 바로 그 경로로 만들어졌다(21원천 채점 1위). **여기 쌓인 원문은 「손대지 말 것」이 아니라 재저작 입력 대기다**(DD-75).',
        },
        {
          label: '잰 시각 · N일 전',
          detail:
            '이 화면은 실시간 집계가 **아니다.** `library_articles` 는 본문이 1.3GB 라 조건부 `count: exact` 가 8초 statement timeout 에 걸린다(오류 message 가 빈 문자열로 와서 원인이 안 보인다). 전수 훑기는 커서 페이징으로 **212초**(2026-09-15 실측 · 재고 87,626편) — 매 요청에 할 일이 아니다. 그래서 스냅샷을 읽고 **언제 잰 값인지 항상 함께** 말한다. 7일이 넘으면 줄이 경고색으로 바뀐다.',
        },
      ],
      cautions: [
        '**조판이 이 판정을 강제한다 — 2026-09-06 저녁부터 기본값이다.** `volume-pool.mjs` 가 권마다 판정을 돌려 통과한 원문만 싣고, `원문 적격 N/M (X%)` 을 항상 인쇄한다. ' +
          '오래 「경고」였던 근거는 재고였다(「강제하면 V2 는 3편 · V3 는 1편만 남는다」). 기사 5,245편을 전부 판정한 뒤 **권을 실제로 뽑아 비교했더니** 여섯 학년이 같은 권을 냈다 — 문항 풀이 98% 잘려도(V6 157,622→3,024) 뽑는 것은 120문항이라 결과가 안 바뀌고, **V2 는 오히려 9/10 → 10/10 으로 좋아졌다.** ' +
          '⚠️ **(갱신 2026-09-25)** 지금 PLOS V7 조각은 **1,045개 · 전부 내용 판정됨 · 쓸 수 있음 799** 이고, V7 발췌 대기 원본은 **1편**뿐이다 — 아래 옛 기록의 「59편」은 그 뒤 발췌·판정으로 채워졌다. V7 을 더 늘리는 길은 이제 발췌가 아니라 **판정 전 원본 31,220편의 원문 점검**이다(원천별 작업 진행표 ②). ' +
          '(옛 기록) **V7 만 다르다**: 판정된 원문이 59편뿐이라(2,468편이 미판정 plos raw) 20단원이 아니라 **6단원**이 나오고 채점이 8/10 로 떨어진다. 그런데 그것이 사실이다 — 지금까지의 20단원 V7 권은 **미판정 원문 위에 세워져 있었다.** 얇은 권은 결함이 아니라 재고가 얇다는 표시이고, 되살리는 길은 강제를 끄는 것이 아니라 `scripts/csat/plos-extract` 다. ' +
          '현재 v3 정책에서는 강제를 끌 수 없습니다. 전후 비교는 읽기 전용 audit을 사용합니다.',
        '원문 정책 v3는 항상 강제됩니다. `VOCAFLOW_SOURCE_STRICT=0`은 실행 오류이며 전후 비교는 읽기 전용 audit으로 수행합니다.',
        '조판 로그의 적격 분모는 **문항이 붙은 원글**이다 — 이 화면의 분모(밴드 전량)와 다르다. 밴드 전체 select 에 판정 열을 넣었더니 V4(856편) 조판이 **statement timeout 으로 죽었다**(jsonb 를 행마다 detoast 한다). 문항 없는 원글은 그 권에 아무것도 기여하지 않으므로 분모에서 빼는 것이 맞기도 하다.',
        '스냅샷이 낡으면 없는 재고를 있다고 말한다. 드레인을 돌린 뒤에는 **반드시 스캔을 다시** 돌린다 — 아래 절차 마지막 단계다.',
        '`판정 규격 v` 가 바뀐 뒤의 옛 스냅샷은 **다른 자로 잰 값**이다. 화면이 그 경우 경고를 띄우지만, 경고를 무시하고 인용하면 규칙을 고친 효과를 거꾸로 읽게 된다.',
      ],
      drain: {
        what: '조판 후보(`ready`·`published`) 전체를 일곱 축으로 판정한 스냅샷 — 등급 분포 · 탈락 축 · 학령별 적격 · 원천별 막힌 편수.',
        prerequisites: [
          'DB 캐시는 승인된 eligibility SQL 적용이 전제입니다. 일괄 적재 전 최신 문항 연결 수·dry run diff·checkpoint·배치 백업을 확인합니다.',
          '`apps/web/.env.local` 에 `SUPABASE_SERVICE_ROLE_KEY` 가 있어야 한다 — 스캔은 서비스 키로 읽는다.',
          '판정 규칙을 고쳤다면 `ELIGIBILITY_SPEC_VERSION` 을 먼저 올린다. 안 올리면 옛 스냅샷과 새 스냅샷이 같은 자로 잰 값처럼 보인다.',
        ],
        procedure: [
          {
            title: '전수 판정 스캔',
            detail:
              '`pnpm dlx tsx scripts/textbook/source-eligibility-scan.mjs` — `status in (ready, published)` 전량을 커서 페이징으로 훑어 판정하고, **인자 없이도 이 화면이 읽는 스냅샷을 갱신한다**(터미널에만 보려면 `--no-write`). **읽기만 한다 — 재실행 안전**이고, 몇 번을 돌려도 DB 가 바뀌지 않는다. ⏱ **재고에 비례한다** — 2026-09-06 재고 35,889편에 31.9초였고, 2026-09-15 재고 **87,626편에 212초**다. 「금방 끝난다」고 적어 두면 돌리다 말고 끊는다. PostgREST 가 한 쪽 1,000행으로 강제하고 집계 함수가 꺼져 있어(PGRST123) 커서 페이징 말고 다른 길이 없다.',
            done: '콘솔 마지막에 `→ apps/web/src/lib/textbook/source-eligibility-snapshot.json` 이 찍히고, 이 화면의 “잰 시각” 이 오늘로 바뀐다.',
          },
          {
            title: '한 밴드만 다시 재기',
            detail:
              '`--band 5` 를 붙이면 그 밴드만 훑는다. 한 학년 드레인을 돌린 직후 빠르게 확인할 때 쓴다. **밴드 인자를 주면 스냅샷을 덮지 않는다** — 그 결과로 덮으면 화면이 그 밴드만 있는 세상을 말하기 때문이다. 굳이 쓰려면 `--json <경로>` 를 명시해야 한다.',
            done: '콘솔의 `대상` 줄이 `V5` 로 찍힌다.',
          },
          {
            title: '막힌 축을 푼다',
            detail:
              '화면 맨 위 “다음 한 걸음” 이 가리키는 축의 드레인을 돌린다. ' +
              '**내용 판정** — 감사 보고서에서 다른 차단 원인이 없는 ID를 고른 뒤 `gate-article-export.mjs --ids-file <UUID 목록> --output <새 JSON>`으로 최대 100편의 전체 본문·revision·해시를 뽑습니다(읽기 전용, 이미 판정된 원문은 건너뜀). ' +
              '⚠️ **길이는 원문 적격 기준이 아닙니다**(2026-09-23 결정) — 원문이 지문 창보다 긴 것은 결함이 아니라 원문의 정상 상태입니다. 지문을 뜨는 것은 교재 생성 단계의 별도 공정이고, 그 공정은 유형마다 다른 창을 씁니다(초등 3종은 지문 자체가 없고, 문장 단위 6~40어, 학교 문단 40~200, 수능 일반 90~200, 수능 장문 260~400). 예전에는 수능 창 하나로 원문을 걸러 **장문 창에 그대로 맞는 22,209편이 「발췌 미생성」으로, 학교 문단 창에 맞는 108편이 「너무 짧다」로** 빠져 있었습니다. 그 축을 제거한 뒤 조판 가능 13,737(15.7%) → 28,162(32.1%). ' +
              '⚠️ **그래도 판정이 곧 교재는 아닙니다** — 판정은 내용이 쓸 만한가만 답합니다. 발췌·문항 생성은 뒤 공정이고, 그 큐는 `excerpt_materialization` 에서 셉니다. ' +
              '⚠️ **판정이 한 칸 밀려도 검사기는 통과합니다** — `id`·revision·해시를 청크에서 인덱스로 복사하므로 식별자는 전부 맞고 판정만 엉뚱한 글에 붙습니다. 실측 153청크 중 6건 발생(전부 가드 도입 전 앞쪽 45청크 · 이후 108청크는 0건). 에이전트는 판정을 따로 나열해 두었다가 나중에 짝짓지 말고 **청크 배열을 직접 순회**해 만들고, 쓰기 전에 흩어진 인덱스 15개 이상을 본문과 눈으로 대조해야 합니다(정본 `.claude/agents/csat-source-judge.md`). 임시 파일명에는 청크 이름을 넣습니다 — 병렬 판정 중 공용 이름이 덮어써진 사고가 두 번 있었습니다. ' +
              '판정 파일은 적재 전에 `gate-reviews-verify.mjs <export> <reviews>`로 대조합니다(빠진 편·해시 불일치·모순 장르를 파일 단위로 보고, `reject`가 차단 장르를 쓰는지도 봅니다). ' +
              '판정 행은 **일곱 키**입니다 — `uses` 가 「이 원문으로 어떤 교재를 만들 수 있는가」를 답합니다(정본 `gate-rules.SOURCE_USES` 8종: argument·structured·sequence·mood·factual·vocab·grammar·spoken). **수능 목록이 아니라** `textbook/market-spec.json`(시중 교재 79종)의 표준 발문에서 편 것이고, **길이로 고르지 않습니다** — 어수창·밴드는 기계가 잽니다. 검사기는 `uses` 가 없어도 통과시키되 `usesFilled` 를 늘 찍습니다(그 전 판정분 호환). ⚠️ **`usesFilled` 가 행수보다 작은데 넘어가면 그만큼이 빈 채로 쌓입니다** — 2026-09-23 에 1,900행이 그렇게 남아 뒤늦게 전량 백필했습니다. ' +
              '⚠️ **보관 여부는 게시 가능 여부와 다른 축입니다** — `publishable:false` 를 「버릴 것」으로 읽으면 안 됩니다. plos 원본 31,220편은 전량 `oversize-raw` 로 게시가 막혀 있지만 **추출 대기 재고**이고, export 가 `purpose: raw` 를 일부러 제외합니다(문항이 붙은 raw 만 `--include-raw-with-items` 로 예외). A 버킷이 빈 청크를 1,736개 낸 것도 고장이 아니라 이 제외 때문입니다 — 「추출 실패」로 읽고 없는 고장을 고치지 마십시오. 보관 축은 `gate-rules.retentionOf` 가 파생으로 답하고 감사가 확보 전량에 대해 셉니다. ' +
              '⚠️ **이미 적재한 청크를 고쳐 다시 넣을 때 조용히 실패하는 자리가 셋입니다**(2026-09-23 실측): ① 드레인 드라이버는 `cache/<tag>.jsonl.commit-*` 영수증이 있으면 **건너뜁니다**, ② `import/<tag>.json` 이 남아 있으면 **옛 입력을 재사용**합니다 — 둘 다 성공으로 보고하면서 DB 는 안 바뀝니다. ③ 앞선 적재가 그 행의 `updated_at` 을 올려 두었으므로 적재기 CAS 가 `Review revision changed` 로 막습니다. 영수증·입력을 치우고, 리비전은 **DB 본문 해시를 다시 계산해 판정 파일과 일치할 때만** 현재 값으로 다시 묶습니다(해시가 다르면 그 행은 재판정 대상이지 리비전 문제가 아닙니다). 검사기는 리뷰↔청크를, 적재기는 리뷰↔DB 를 보므로 **둘을 같이** 맞춰야 합니다. ' +
              '에이전트가 본문을 읽고 별도 JSON에 판정·근거를 쓴 뒤 `gate-mixed-import.mjs --input <판정 JSON>`으로 미리봅니다. checkpoint와 소량 검증 후 같은 명령에 --commit을 붙입니다. ' +
              '원문·발행 상태·다른 분석 키는 보존하며 같은 판정 재실행은 변경 0입니다. 본문 해시나 수정 시각이 달라지면 다시 내보내 검토합니다. 적재 후 영향 ID의 캐시를 재검증합니다. ' +
              '기존 책/제목 단위 청크는 참고 자산입니다. 이름이 같은 서로 다른 기사의 근거로 재사용하지 않습니다. legacy mixed 무범위 --commit은 중지되었습니다. ' +
              '⚠️ `gate-make.mjs` 는 **발췌창(`csat_fit.make`)을 채우는 결정론 스크립트라 판정과 무관하다** — 그걸 돌리고 “판정했다” 고 믿으면 미판정이 그대로 남는다. ' +
              '**학령 분석** — queued는 `scripts/acp/process-queue.mjs`, 이미 ready인 글은 대상 ID의 누락 원인부터 확인합니다(기존 어휘가 있으면 전체 재분석 대신 해당 계산만 복구). **발췌 자리** — `scripts/textbook/store-new-types.mjs`의 문항 생성·검수 경로입니다. ' +
              '**되돌릴 수 없는 축(법적·안전)은 드레인이 없다** — 그 원문들은 빼는 것이 처방이다.',
            done: '해당 축의 “지금 탈락” 이 줄어든다.',
          },
          {
            title: '권수를 올린다 — 문항 집필 드레인',
            detail:
              '적격 원문이 아무리 늘어도 **생성형 유형이 비면 권수는 0 이다.** 무엇을 얼마나 써야 하는지는 「채울 몫」 표가 세고, 그 몫을 채우는 절차가 이것이다. ' +
              '① `pnpm dlx tsx scripts/textbook/item-fill-plan.mjs --export-cmds` 로 몫과 뽑을 명령을 낸다(**읽기만 한다 — 재실행 안전**, DB 도 안 본다). ' +
              '② 모자란 칸만 `scripts/textbook/item-drain-export.mjs --type <유형> --band <학년> --need <문항수>` 로 뽑는다 — **이미 그 유형이 붙은 지문은 건너뛰므로 재실행 안전**이고, 읽기만 한다. ⚠️ `--need` 를 빼면 기본 한 자리 작업량만 나간다. ' +
              '⚠️ **유형이 안 얹히는 지문도 뽑히지 않는다**(`type-fit.ts`). 「이 지문이 이 유형을 떠받칠 수 있는가」는 재고와 다른 질문이다 — 소설에 「글의 목적」을 물으면 정답이 장면 요약이 되고, 서술문에 「필자의 주장」을 물으면 정답이 요지가 된다. 규칙이 있는 유형은 지금 **`purpose`·`claim`** 둘이고, **규칙이 없는 유형은 통과가 아니라 미판정**으로 센다(안 본 것과 보고 괜찮은 것은 다르다). 실측 2026-09-15: 저장된 `purpose` 86건 중 적합 **0**, `claim` 52건 중 **7(13.5%)**. ⚠️ 뺀 수가 크다고 놀랄 일이 아니다 — `claim` V5 는 16,479편 중 12,925편을 빼지만 남는 후보가 **535편**이라 수요의 수십 배다. 게이트는 막는 것이 아니라 **뽑기를 옳은 지문 쪽으로 돌린다**. ' +
              '⚠️ **2026-09-15 부터 추출 결함 지문은 뽑히지 않는다.** 규칙 일곱(HTML 속성 혼입 · 위키 마크업 · 브라우저 안내 · 문단 중복 · 수식 소실 · 이야기 경계 · 공유 버튼)이 오래 스캔 안에만 있어 **세기만 하고 아무도 안 썼고**, 그래서 결함 지문 위에 문항이 얹혔다. 이제 뽑기가 같은 규칙을 부르고, 뺀 편수를 규칙별 근거와 함께 출력한다(V7 빈칸 실측 14편). 판정은 **실제로 실릴 창**에 걸므로 원글 저 끝의 잡티 때문에 멀쩡한 창을 잃지 않는다. 뺀 수가 크면 그 원천의 추출기를 고쳐야 한다는 신호다. ' +
              '③ **에이전트(Claude Code 또는 Codex)가 `chunk-NN.json` 을 읽고 `chunk-NN.out.json` 으로 채운다.** 집필 규격의 정본은 `scripts/textbook/item-drain-brief.md` 한 벌이다 — 규칙을 새로 짜지 말 것(손으로 짠 검사기가 없는 규칙을 만들어 멀쩡한 요약 문항 넷을 헛되이 다시 쓰게 한 일이 있다). **이미 `.out.json` 이 있는 청크는 건너뛴다 — 재실행 안전.** ' +
              '④ `pnpm dlx tsx scripts/textbook/item-selfcheck.mjs --file <청크>.out.json --type <유형> --band <학년>` — 적재 관문(`item-gate.ts`)과 **같은 함수**를 부르므로 여기서 통과하면 적재에서도 통과한다. **DB 를 안 타고 아무것도 안 쓴다 — 재실행 안전.** ' +
              '⑤ `pnpm dlx tsx scripts/textbook/item-drain-import.mjs --dir scripts/textbook/item-drain/<유형>-v<학년> --commit` — 유일키가 `(kind, ref_id, type, paragraph_idx)` 라 **이미 있으면 건너뛴다 — 재실행 안전.** ⚠️ `--commit` 없이 먼저 돌려 건너뛴 수와 이유를 본다. ' +
              '⚠️ **폴더 이름이 정본이다** — `--dir` 만 주고 유형·학년을 기본값으로 두었다가 요지 9문항이 `topic`/V3 로 잘못 적힌 일이 있다(2026-08-31). 지금은 어긋나면 넣기 전에 멈춘다.',
            done: '`item-fill-plan.mjs` 를 다시 돌리면 그 칸의 「써야 할 문항」이 줄고, 재고 스캔 뒤 그 밴드의 「만들 수 있는 권」이 오른다.',
          },
          {
            title: '파생 판정 캐시를 갱신한다',
            detail: 'source-policy-refresh.mjs --item-counts <JSON> --output <JSONL>은 읽기 전용입니다. 100편 이하는 --ids-file <UUID 목록>으로 현재 문항 수까지 확인합니다. --plan <JSONL> --limit 100의 전후 값·영향을 확인하고 checkpoint 후 --commit <JSONL> --limit 1로 시험합니다. 검증 후 --commit <JSONL> --limit 500 --all로 확대합니다. 동일 입력은 변경 0, 실패한 범위는 nextOffset·백업을 확인합니다. 잘못된 시각·24시간 지난 측정·변경된 원문/문항은 거부합니다. 내용 판정은 별도 UUID/revision/본문 해시 검증 드레인이며 재검증만으로 생성되지 않습니다.',
            done: '캐시가 규격·원문 revision과 일치하고 적격에 blocker가 없습니다.',
          },
          {
            title: '판정 하나로 열리는 원본을 먼저 비운다',
            detail:
              '캐시 갱신만으로는 미판정이 줄지 않습니다(같은 입력 → 같은 판정 → 변경 0). 미판정 중 **차단 사유가 `raw_content_unjudged` + `base_judgement` 둘뿐이고 문항이 붙은 원본**만 내용 판정 한 번으로 적격이 됩니다. `gate-raw-with-items-ids.mjs`(읽기 전용)가 그 교집합을 100개씩 청크로 뽑고, `gate-article-export.mjs --ids-file <청크> --output <JSON> --include-raw-with-items`가 본문·revision·해시를 담아 냅니다. 에이전트가 판정을 쓴 뒤 `gate-reviews-verify.mjs <export> <reviews>`로 해시·리비전·모순을 먼저 대조하고, `gate-mixed-import.mjs --input <판정>` 예행 → `--commit` → **그 다음에** 같은 청크로 source-policy-refresh 감사 → --plan → --commit 순서입니다. 셋 다 재실행 안전이며 같은 판정을 다시 넣으면 변경 0입니다. ⚠️ 판정 적재가 원문 `updated_at`을 올리므로 **적재와 같은 호흡에 감사를 돌리면** 측정 시각이 리비전보다 이르러 거부됩니다(순서를 지키면 됩니다). 남은 미판정은 `cefr_above_band`가 섞인 것이라 판정으로 열리지 않습니다 — 밴드·V-Level 결정이 필요합니다.',
            done: '`gate-raw-with-items-ids.mjs`의 withItems가 0이 되고, 그만큼 적격 문항 수가 늘어 있습니다(2026-09-20 실측: 기사 309편 → 문항 105,294 → 119,559 · 11.97% → 13.59%).',
          },
          {
            title: '원문 점검 회차(보관 판정 기준 다듬기)',
            detail:
              '⚠️ **대량 보관 판정은 회차 루프가 먼저다**(docs/source-check/criteria.md §10 · 2026-09-24 사용자 결정). ' +
              '① `node --tls-max-v1.2 scripts/csat/source-round-export.mjs --round N`(예행, 소스별 후보 수) → `--write`: 보관 판정이 없는 원천을 소스당 20건씩 청크로 뽑는다 — 읽기 전용 · 같은 회차는 같은 표본(재실행 안전) · 열 청크 중 하나는 이중 판정. ' +
              '② 판정자(`csat-source-judge`)가 정본을 읽고 보관·보류·폐기 + 쓰임새 칸 + 샘플 문단 가공 시도를 쓴다. ' +
              '③ `gate-reviews-verify.mjs` → `node scripts/csat/source-round-report.mjs --round N` 이 `docs/source-check/round-N.md`(소스·칸 집계 · κ · 사람 확인 표본)를 쓴다. ' +
              '④ **회차마다 멈추고** 사람 확인 → 오판 분석 → 기준 개정(버전 · changelog) → `--compare` 로 재판정 변동 수. 적재(`gate-mixed-import --input`)는 그 회차 판정이 확정된 뒤다.',
            done: '`round-N.md` 가 쓰였고 사람 확인·오판 분석이 채워졌다. 대량 판정은 κ 가 안정되고 승인을 받은 뒤 아래 드레인으로 한다.',
          },
          {
            title: 'PLOS 원본 보관 판정 드레인 (회차 안정·승인 후)',
            detail:
              '① `node --tls-max-v1.2 scripts/csat/plos-raw-triage-export.mjs`(예행, 편수만) → `--write --max N`: **전문**을 V-Level 낮은 것부터 20편씩 `scripts/csat/plos-raw-triage/` 에 뽑는다 — 읽기 전용, **이미 보관 판정됐거나 이미 청크에 든 원본은 건너뛰므로 재실행 안전**. 후보는 적격 캐시에서 고르므로 새로 수확한 원본은 캐시 갱신 뒤에 보인다. ' +
              '② 에이전트(`csat-source-judge`)가 `scripts/csat/plos-raw-triage-brief.md` 를 따라 `chunk-NNN.out.json` 을 쓴다(`kind:"retain"` · `basis:"full"` · 20편에 약 26만 토큰 추정). 열 청크 중 하나는 두 번째 판정자가 따로 판정하고 `gate-reviews-agreement.mjs` 로 κ 를 잰다 — 0.6 미만이면 그 배치는 적재하지 않는다. ' +
              '③ `gate-reviews-verify.mjs <chunk> <out>` → `gate-mixed-import.mjs --input <out>` 예행 → `--commit`. 적재기는 `gate.retain` 한 키만 더하고 `gate.verdict` 는 두므로 게시 판정이 바뀌지 않는다. 같은 판정 재적재는 변경 0(재실행 안전). ' +
              '④ 보관이 쌓이면 `plos-extract` 를 돌린다 — 보관 판정 없는 원본은 「보관 판정 없음·폐기」로 건너뛴다.',
            done: '`csat-sources-audit` 의 보관 미결정(undecided) 중 plos 몫이 0 이다.',
          },
          {
            title: '스캔을 다시 돌린다',
            detail:
              '드레인은 DB 만 바꾸고 스냅샷은 안 바꾼다. **다시 재지 않으면 화면이 어제 값을 계속 보인다.** 전수 적격·재고 스캔을 다시 실행한다.',
            done: '규격·원문 revision이 일치하고 DB·캐시·스냅샷 차이가 설명됩니다.',
          },
        ],
        verify: [
          'scripts/audit/csat-sources-audit.mjs --check가 exit 0인지 확인합니다. reject·분석 누락·CEFR 초과·발췌창만 있는 원문이 적격으로 통과하면 안 됩니다.',
          '적격 비율을 올리는 것이 목표가 아닙니다. 등급 줄의 편수 합이 전체와 같은지(합이 안 맞으면 스냅샷이 밴드 인자와 함께 만들어진 것이다).',
          '“되돌릴 수 없는 부적격” 이 0 이 아니면 그 편수만큼은 **아무리 드레인을 돌려도 안 줄어든다** — 조판에서 빼는 것이 유일한 처방이다.',
          '학령별 표에서 조판 가능이 0 인 칸이 있는지. 있으면 **그 학년 교재를 지금 만들 수 없다**는 뜻이고, 재고가 있어도 마찬가지다.',
        ],
        recovery: [
          '캐시 적재 실패 시 .agent-logs의 배치 백업과 범위를 확인합니다. 원문 revision이 달라졌다면 최신 입력으로 다시 내보내 재검증합니다. 원문·정답 전체를 덮지 않습니다. checkpoint after/diff를 남깁니다.',
          '스캔이 `재고 조회 — 500 …` 으로 죽으면 statement timeout 이다. 페이지 크기(1000)를 줄이지 말고 잠시 뒤 다시 돌린다 — 커서 페이징은 중단해도 안전하고, 처음부터 다시 돌리면 된다.',
          '스냅샷 파일이 없으면 화면이 뜨지 않는다(빌드 시 import 한다). 1단계를 한 번 돌리면 생긴다.',
          '수치가 화면에 안 반영되면 개발 서버를 다시 띄운다 — JSON 은 번들에 들어가므로 파일만 바꾸면 갱신이 늦을 수 있다.',
        ],
      },
      seeAlso: [
        { label: '교재 공장 — 공정 현황판', href: '/admin/csat' },
        { label: '원문 관리 (ACP 콘솔)', href: '/admin/articles' },
        { label: '원문 재고 실측 리포트', doc: 'docs/reports/kid-source-plan-20260904.md' },
        { label: '게시 게이트 규칙', doc: 'scripts/csat/gate-rules.mjs' },
      ],
    },
  },
}
