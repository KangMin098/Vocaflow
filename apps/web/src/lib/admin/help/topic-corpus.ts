// apps/web/src/lib/admin/help/topic-corpus.ts
//
// TCP(주제 코퍼스 파이프라인) 화면도움말.
//
// 이 화면의 관리자는 "왜 원문이 안 보이지?" 를 가장 먼저 묻는다 — 라이선스상 저장하지
// 않기 때문인데, 그걸 모르면 파이프라인이 고장 난 줄 안다. summary 가 그것부터 말한다.

import type { HelpRegistry } from './types'

export const TCP_HELP: HelpRegistry = {
  'topic-corpus': {
    title: '주제 코퍼스 (TCP)',
    screen: {
      summary:
        '주제가 붙은 외부 글을 훑어 "어떤 단어가 어떤 주제에서 두드러지는가" 를 관측하고, 그 결과를 사전 주제 분류로 승격하는 곳. 원문은 저장하지 않는다 — 라이선스(TED = CC BY-NC-ND)상 보관이 불가해서, 토큰화는 메모리에서 끝내고 카운트만 남긴다.',
      when: '사전 주제 분류를 넓히고 싶을 때, 또는 특정 주제의 어휘 갭을 찾고 싶을 때.',
      steps: [
        {
          title: '주제 큐에 적재',
          detail:
            '주제 행의 적재를 누르면 해당 주제 페이지가 노출하는 강연 목록을 큐에 넣는다. 이미 큐에 있던 항목은 다시 넣지 않으므로 여러 번 눌러도 안전하다.',
          done: '대기 수치가 올라간다. 옆의 미수집 수치는 아직 안 담긴 편수다.',
        },
        {
          title: '드레인',
          detail:
            '큐에서 몇 편씩 가져와 자막을 읽고 단어 빈도를 센다. 편당 약 1.2초 간격을 두므로 100편이면 2분 이상 걸린다. 중단해도 다음 실행이 남은 것부터 이어서 처리한다.',
          done: '수확 수치가 오르고 대기가 줄어든다. 갭 수치는 사전에 없던 단어 수다.',
        },
        {
          title: '승격 미리보기 (필수 선행)',
          detail:
            '임계값으로 몇 개 단어가 카테고리에 붙을지 세어만 본다. 아무것도 쓰지 않는다. **이걸 보기 전에는 승격 버튼이 잠겨 있고**, 본 뒤에 임계값을 고치면 다시 잠긴다 — 화면에 뜬 대상 수가 그 임계값의 것임을 보장하기 위해서다.',
          done: '작업 칸에 "미리보기 N개 대기 중" 이 남고 승격 버튼이 열린다.',
        },
        {
          title: '승격 적용 (확인 한 번 더)',
          detail:
            '승격을 누르면 바로 쓰지 않고 **대상 건수·카테고리·임계값을 다시 보여 주는 확인 칸**이 열린다. 거기서 실행을 눌러야 실제로 붙는다. 붙는 링크는 출처가 corpus-derived 라 나중에 출처만으로 골라 되돌릴 수 있지만, 그 되돌리기는 SQL 로만 된다 — 화면에는 취소가 없다.',
          done: '승격 수치가 올라가고 미리보기 표시가 지워진다(다시 승격하려면 다시 본다).',
        },
      ],
      fields: [
        {
          label: '미수집',
          detail:
            '소스가 밝힌 총 편수에서 큐에 담은 편수를 뺀 값. 주제 페이지는 한 번에 16편만 노출하므로 이 값이 보통 크게 남는다 — 0 이 아니면 그 주제는 아직 전량이 아니다. 전량이 필요하면 URL 목록을 직접 붙여 넣는 경로를 쓴다. **총 편수는 소스가 알려 준 경우에만 진행 기록에 찍힌다** — 안 알려 주면 그 문장이 아예 안 나온다(예전엔 소스를 가리지 않고 "TED 총 undefined편" 이 찍혔다).',
        },
        {
          label: '갭',
          detail:
            '추출됐지만 사전에서 표제어를 못 찾은 단어 수. 사전 갭 백로그(pending_words)에 코퍼스 출처로 쌓이며, 학습자가 신고한 갭과는 구분된다.',
        },
        {
          label: '두드러짐 하한',
          detail:
            '배경(다른 모든 주제) 대비 로그오즈비. 1.0 이면 약 2.7배 과대표집을 뜻한다. 낮추면 흔한 단어까지 딸려 오고, 높이면 그 주제의 핵심어만 남는다.',
        },
        {
          label: '최소 등장 글 수',
          detail:
            '서로 다른 글 몇 편에 나와야 인정할지. 3 이면 강연 한 편의 특이한 어휘가 카테고리를 만들지 못한다.',
        },
        {
          label: '잠긴 버튼이 말하는 것',
          detail:
            '드레인은 대기 0 이면, 미리보기는 관측 단어 0 이면, 승격은 유효한 미리보기가 없으면 잠긴다. 커서를 올리면 무엇이 모자라 잠겼는지 뜬다. **액션이 성공하면 화면이 현황을 다시 읽으므로**(헤더에 "현황 갱신 중" 이 잠깐 뜬다) 잠금이 풀리기를 기다렸다가 다음을 누르면 된다 — 새로고침은 필요 없다.',
        },
        {
          label: '표가 비어 있을 때',
          detail:
            '"소스 레지스트리가 비어 있다" 는 큐가 빈 것과 다르다. 소스는 화면에서 만들지 않고 마이그레이션(20260816170000_topic_corpus_local_sources.sql)이나 topic_corpus_sources 직접 INSERT 로 등록한다. 위에 붉은 오류가 함께 떠 있으면 목록이 없는 게 아니라 **못 읽은 것**이다.',
        },
      ],
      cautions: [
        '승격 적용은 사전의 주제 분류를 바꾸는 쓰기다(한 번에 최대 500단어). 되돌리려면 corpus-derived 출처의 링크를 SQL 로 지워야 한다 — 그래서 화면이 미리보기 선행과 확인 단계를 강제한다. 잠긴 승격 버튼을 억지로 여는 경로는 없다.',
        '기존 imported 링크(28,079건)는 어떤 경우에도 덮어쓰지 않는다. 승격은 없는 링크를 추가할 뿐이다.',
        '원문 저장 금지는 스키마로 지킨다 — 수확 원장에 본문 컬럼이 없다. 본문 컬럼을 추가하는 변경은 라이선스 판단을 다시 해야 하는 사안이다.',
      ],
      drain: {
        what: '주제별 어휘 관측치(주제×표제어 빈도)와 사전 갭 목록.',
        prerequisites: [
          '해당 주제의 대기 수치가 0 보다 클 것 (없으면 적재를 먼저 누른다).',
          'SUPABASE_SERVICE_ROLE_KEY 가 설정돼 있을 것 — 없으면 드레인 API 가 500 을 낸다. CLI 는 환경변수로 줘도 된다.',
          '⚠️ **적재는 주제 발견(discover)으로 한다 — 카탈로그 전량 투입 금지.** 실측 2026-08-26: 카탈로그로 넣은 ted:catalog 는 수확률 4.7%(done 2,622 / skipped 53,582)인데, 주제 페이지 발견으로 넣은 ted:sleep 은 15/15 였다. 실패 사유는 전부 "자막 없음" 이고, 자막 유무는 /transcript 를 받아 봐야 알아 선필터가 불가능하다. 즉 전량 투입은 4만 건을 넣고 2천 편을 건지려고 20시간 넘게 도는 일이다. (표본 15건이라 100% 를 그대로 믿을 수는 없지만, 4.7% 와의 차이는 표본으로 설명되지 않는다. TEDx 여부는 원인이 아니었다 — TEDx 2.9% · 그 밖 5.0% 로 둘 다 낮다.)',
        ],
        procedure: [
          {
            title: '드레인 반복 호출 (화면)',
            detail:
              '화면의 드레인 버튼은 한 번에 최대 10편을 처리하고, 큐가 마를 때까지 자동으로 다시 호출한다. 응답의 drained 가 true 면 멈춘다. **탭을 닫으면 멈춘다** — 큰 큐에는 아래 CLI 를 쓴다.',
            done: '대기 0 · 수확 수치가 적재한 편수에 도달.',
          },
          {
            title: '무인 드레인 (CLI · 큰 큐)',
            detail:
              'pnpm tcp:drain — 큐가 마를 때까지 사람 없이 돈다. 옵션은 -- --batches N(맛보기) · -- --source ted:ai(주제 한정) · -- --max 1~10(배치당 편수). 화면과 **같은 함수**를 부르고(lib/topic-corpus/drain.ts), claim 이 FOR UPDATE SKIP LOCKED 라 화면과 동시에 돌려도 같은 강연을 두 번 잡지 않는다. **재실행 안전** — Ctrl+C 로 끊고 다시 켜면 이어서 마른다. 10배치마다 편/분과 남은 시간을 찍는다. ⚠️ 실측 2026-08-26: 수확률이 11.4%(자막 없는 강연이 대부분)라 85,179건은 약 49시간이다. 이 낭비는 피할 수 없다 — 자막 유무는 /transcript 를 받아 봐야 알고, 적재 목록도 그 힌트를 주지 않는다. 다만 skipped 는 다시 claim 되지 않으므로 **한 번만** 치른다.',
            done: '"[tcp] 큐가 말랐다." 또는 대기 0.',
          },
          {
            title: '갭 처리 (VCB)',
            detail:
              '수확이 끝나면 사전 갭이 pending_words 에 쌓여 있다. /admin/pending-words 에서 확인한 뒤 VCB enrichment 로 넘긴다.',
            done: '갭 항목의 상태가 pending 에서 벗어난다.',
          },
        ],
        verify: [
          '대기·처리중이 모두 0 인가.',
          '수확 편수 × 평균 unique 가 관측 단어 수와 어긋나지 않는가 (크게 적으면 자막을 못 읽은 편이 많다는 뜻).',
          '실패 수치가 0 인가 — 0 이 아니면 그 항목의 사유를 본다.',
        ],
        recovery: [
          '재실행 안전하다. 같은 강연을 다시 수확해도 통계에 두 번 더해지지 않는다 (이미 수확한 문서는 건너뛴다).',
          '처리중이 30분 넘게 멈춰 있으면 다음 드레인이 자동으로 회수해 대기로 되돌린다.',
          '3회 실패한 항목은 실패로 고정돼 더 이상 잡히지 않는다 — 사유를 확인하고 필요하면 큐에서 직접 되돌린다.',
        ],
      },
      seeAlso: [
        { label: '사전 갭 백로그', href: '/admin/pending-words' },
        { label: '어휘 사전', href: '/admin/vocabulary' },
      ],
    },
  },
}
