// apps/web/src/lib/admin/help/quality.ts
//
// 품질 게이트·판정 (/admin/quality*) 화면도움말.
// 스키마·작성 원칙은 ./types.ts 참조. 화면을 바꾸면 이 파일도 같은 커밋에서 고친다.

import type { HelpRegistry } from './types'

export const QUALITY_HELP: HelpRegistry = {
  // ── /admin/quality — 품질 지표 ──────────────────────────────────────────────
  quality: {
    title: '품질 지표',
    screen: {
      summary:
        '파이프라인 단계별 품질 수치를 하루 한 번 찍어 두고 추이로 보는 화면 — 값을 고치는 곳이 아니라 어제와 오늘을 비교하는 곳이다.',
      when: '추출·발행·사전 쪽을 손댄 뒤 지표가 어느 방향으로 움직였는지 확인할 때. 그 전에 스냅샷이 최소 2회는 쌓여 있어야 비교가 된다.',
      fields: [
        {
          label: '지금 수집',
          detail:
            'cron 을 앞당기는 게 아니라 스냅샷 1건을 지금 더 쌓는다. 하루에 여러 번 눌러도 되고, 누른 만큼 추이 점이 촘촘해진다. role=admin 만 통과 — curator 나 dev-bypass 세션은 "admin only" 로 거부된다.',
        },
        {
          label: '수집 N회 보관',
          detail:
            '이 화면이 읽는 최근 400행 안에 들어온 스냅샷 수. 스냅샷 1회가 9행 안팎이라 대략 최근 40여 회분이고, 그보다 오래된 기록은 DB 에 남아 있어도 여기엔 안 나온다.',
        },
        {
          label: '스파크라인 / ▲▼',
          detail:
            '수집 2회째부터 선이 그려진다. ▲▼ 는 직전 스냅샷과의 차이이며 _pct 지표는 %p 로 읽는다 — 누적 변화가 아니다.',
        },
        {
          label: 'published · ready 배지',
          detail:
            '같은 지표를 콘텐츠 상태별로 따로 잰 것(dims.status). 발행분만 볼 때와 게시 대기분까지 볼 때가 갈린다.',
        },
        {
          label: 'stage 배지 gate',
          detail:
            '품질 게이트 불변식의 위반 건수 추이. 이건 별도 nightly(content-gate-nightly, KST 03:25)가 적재하므로 "지금 수집" 으로는 갱신되지 않는다. 한글 라벨이 없어 불변식 이름(I1·I5 …)이 그대로 뜬다.',
        },
        {
          label: '발행세트 SSoT 드리프트 (도서 / 단어)',
          detail:
            '이미 발행된 챕터 단어장이 **지금의 추출 결과와 다른** 정도. 0 이 아니면 그 책은 옛 단어를 학습자에게 주고 있다는 뜻이라 재발행이 필요하다 — 저절로 맞춰지지 않는다. 추출·사전 로직을 고치면 다음 수집에서 반드시 오른다. 어느 책인지는 카드 아래 dims 의 `drifted` 에 {책 이름: 건수} 로 있다. 조치는 `pnpm dlx tsx scripts/lcp/republish-books.mjs --drifted-only`(먼저 `--dry-run`).',
        },
        {
          label: '카드 아래 회색 키/값',
          detail: '그 수치를 낸 측정 모수(dims) — 분모·기준 집합 등. 값이 튀면 여기부터 본다.',
        },
      ],
      cautions: [
        'quality_metrics 는 admin RLS read 라, 권한이 없으면 오류 대신 0행이 와서 "수집된 품질 지표가 없어요" 만 뜬다. 첫 수집 전인지 세션 문제인지는 "지금 수집" 을 눌러 보면 갈린다.',
        '스냅샷은 이 화면에서 지우거나 되돌릴 수 없다. 파이프라인이 깨진 상태에서 수집하면 그 점도 추이에 그대로 남는다.',
        'SSoT 드리프트는 발행 도서마다 추출을 다시 돌려서 잰다 — 수집 1회가 20초 안팎 걸리는 이유다(나머지 지표는 즉시). "지금 수집" 을 눌렀는데 응답이 늦은 건 고장이 아니다.',
        '드리프트는 **발행 도서만** 센다. 미발행 도서의 세트는 RLS 가 학습자에게 가리므로 제외한다 — 그 숫자까지 보려면 러너의 `--drifted-only --dry-run` 을 쓴다.',
      ],
      seeAlso: [
        { label: '품질 게이트', href: '/admin/quality/gates' },
        { label: '추출 판정', href: '/admin/quality/judge' },
      ],
    },
  },

  // ── /admin/quality/gates — 품질 게이트 ──────────────────────────────────────
  'quality-gates': {
    title: '품질 게이트',
    screen: {
      summary:
        '학습자에게 나갈 산출물이 맞는 단어·뜻·레벨로 뽑혔는지 결정론 불변식으로 검사한다. 전역 현황을 위에서 보고, 게시할 콘텐츠 하나를 아래에서 따로 찍어 본다.',
      when: '큐레이션이 ready 로 끝난 콘텐츠를 게시하기 직전, 또는 추출·사전 로직을 바꾼 뒤 전역 영향을 확인할 때.',
      steps: [
        {
          title: '전역 배너부터',
          detail:
            '전역 검사는 화면을 열 때 그 자리에서 돈다 — 실행 버튼이 따로 없고 새로고침이 곧 재검사다. 사전DB·단어추출·LCP·ACP 묶음을 한 번에 본다.',
          done: '"전역 게이트 통과 — 게시 신뢰 가능" 배너 + critical FAIL 0',
        },
        {
          title: '콘텐츠 하나를 골라 실행',
          detail:
            '드롭다운에는 발행분뿐 아니라 [ready]·[queued] 미발행도 들어 있다. 검사 대상이 상태에 따라 다르다 — 미발행이면 현재 추출이 뱉는 단어를, 발행분이면 이미 나간 발행 세트를 본다.',
          done: '초록 "게시 신뢰 가능" 또는 붉은 "게시 차단 후보 N건"',
        },
        {
          title: 'FAIL 이면 상세 원인 보기',
          detail:
            'critical FAIL 이 하나라도 있을 때만 버튼이 나타난다. 문제 단어 · 이유 · 고칠 힌트까지 준다. 단어 단위가 아닌 실패(레벨·register·드리프트)는 목록이 비고 안내 문구만 뜬다.',
          done: '문제 단어 목록, 각 줄에 "→ 고침:" 힌트',
        },
        {
          title: '고친 뒤 다시 실행',
          detail:
            '게이트는 검사만 하고 아무것도 고치지 않는다. 사전DB나 파이프라인을 고치고 같은 버튼을 다시 누른다. 도서/아티클 토글이나 드롭다운을 건드리면 직전 결과는 지워진다.',
        },
      ],
      fields: [
        {
          label: 'PASS / WARN / FAIL',
          detail:
            'FAIL 은 severity=critical, WARN 은 severity=warning. 오른쪽 숫자는 위반 건수이고 PASS 는 항상 0 이다.',
        },
        {
          label: 'critical FAIL',
          detail:
            '게시 차단 후보. 발행·재발행 RPC 가 content_gate_publishable 로 실제로 막는다 — 화면 경고가 아니라 서버 차단이다.',
        },
        {
          label: 'I7 노이즈 register — 구 단어장 예외',
          detail:
            '구·관용어(phrase_unit)는 보통 노이즈지만, 컴포저의 구동사·관용어 유형(curation_query.blueprint=phrasal-idiom)으로 만든 세트에서는 그것이 표제어 자체라 세지 않는다. 예외는 그 한 조합뿐이다 — 다른 유형에서는 구가 섞이면 그대로 FAIL 이고, 같은 세트라도 archaic·brand·proper_noun 은 면제되지 않는다.',
        },
        {
          label: 'WARN',
          detail:
            '위생 항목(추적용). 게시를 막지 않으므로 WARN 이 남아 있어도 전역 배너는 "통과" 로 뜬다.',
        },
        {
          label: 'I10 발행세트 SSoT 드리프트',
          detail:
            '발행 세트가 현재 추출 로직의 산출과 어긋났다는 뜻(=stale). 추출을 개선하면 발행분이 한꺼번에 여기 걸린다. 유일하게 게시 차단에서 제외된 critical 이며, 재발행(republish_book_word_sets / republish_article_word_set)으로만 풀린다.',
        },
      ],
      cautions: [
        '전역 게이트는 화면을 열 때마다 전 범위를 스캔한다 — statement_timeout 을 60초까지 올려 둔 무거운 쿼리다. 연속 새로고침 대신 결과를 읽고 움직인다.',
        '게이트를 무시하고 내보내는 우회로는 없다. critical FAIL 상태에서 발행·재발행 RPC 를 부르면 예외로 끊긴다(I10 만 예외).',
        'I10 을 푸는 재발행은 발행 세트의 단어를 지우고 현 추출로 다시 채운다. set_id 와 구독은 보존되지만 학습자가 보던 단어 목록 자체는 바뀐다. 대형 도서는 한 statement 에 몰지 말고 한 권씩.',
        '게이트 통과는 게시를 자동으로 하지 않는다. 실제 게시는 큐레이션 화면에서 따로 누른다.',
      ],
      seeAlso: [
        { label: '품질 지표 추이', href: '/admin/quality' },
        { label: '추출 판정', href: '/admin/quality/judge' },
      ],
    },
  },

  // ── /admin/quality/judge — 추출 판정 ────────────────────────────────────────
  'quality-judge': {
    title: '추출 판정',
    screen: {
      summary:
        '시스템이 고른 단어와 탈락 직전 후보를 출처를 가린 채 섞어 보여 주고, 관리자의 선택을 골든 라벨로 쌓는다 — 추출 순위·게이트를 바꿀 때의 회귀 기준이 여기서 만들어진다.',
      when: '추출 순위나 게이트 규칙을 손대기 전후로 기준선을 쌓을 때. 대상은 발행된 도서·아티클뿐이고, 도서는 챕터가 1개 이상이어야 목록에 뜬다.',
      steps: [
        {
          title: '소스와 모드 먼저',
          detail:
            '표본을 부르면 선택 영역이 사라진다 — 절대 판정 / 쌍대 비교는 부르기 전에 정한다. 바꾸려면 "다른 표본" 으로 돌아와야 한다.',
        },
        {
          title: '표본 불러오기',
          detail:
            '그 챕터·아티클의 추출을 그 자리에서 다시 계산한다(저장된 발행 세트가 아니다). 후보가 안 나오면 "추출 후보가 나오지 않았어요" 로 끝나는데, 챕터 번호가 실제 범위를 넘긴 경우가 대부분이다.',
          done: '단어 카드 16개(또는 쌍대 8쌍)가 뜸',
        },
        {
          title: '판정',
          detail:
            '절대 판정은 카드마다 ✓(가치 있음) 또는 ?(애매)를 찍고, 쌍대 비교는 두 단어 중 하나를 고른다. 같은 버튼을 다시 누르면 해제된다.',
          done: '절대 판정은 카드 테두리 색, 쌍대 비교는 고른 쪽 보라 테두리',
        },
        {
          title: '판정 제출 → 공개',
          detail:
            '제출한 뒤에야 시스템의 선택(in-cap)이 공개된다. 저장은 단어 하나당 서버 왕복 한 번이라 16개면 16번 — 몇 초 걸리고 그동안 버튼이 잠긴다.',
          done: 'precision / recall / 라벨 수 3칸 + 단어별 "나 : 시스템" 비교 목록',
        },
      ],
      fields: [
        {
          label: '표본 16개',
          detail:
            '현재 순위 1~8위(cap 안)와 41~48위(cap 40 바로 밖)를 섞은 것. 같은 챕터를 다시 불러도 이 16개는 그대로이고 순서만 다시 셔플된다 — 반복 판정으로는 새 정보가 안 쌓인다.',
        },
        {
          label: '절대 판정',
          detail:
            '단어를 하나씩 독립으로 본다. 표본 전체가 라벨로 저장되므로 precision·recall 둘 다 나온다.',
        },
        {
          label: '쌍대 비교',
          detail:
            '두 단어 중 나은 쪽만 고른다. 고른 쪽은 가치 있음, 진 쪽은 가치 없음으로 저장된다. 표본이 홀수면 마지막 하나는 쌍이 안 돼 빠지고, 한 쌍도 안 고르면 제출 버튼이 잠긴다.',
        },
        {
          label: 'in-cap / #NN',
          detail:
            'in-cap = 현재 추출에서 40위 안(학습자에게 실제로 나가는 범위). in-cap 이 아니면 그때의 순위를 #NN 으로 보여 준다.',
        },
        {
          label: 'precision / recall',
          detail:
            'precision = 내가 고른 것 중 in-cap 비율, recall = in-cap 중 내가 고른 비율. 이번 회차 표본에 대한 값이고 누적 성적이 아니다.',
        },
        {
          label: '다른 표본 / 다음 표본 판정',
          detail: '저장 없이 선택 화면으로 되돌린다. 이미 제출한 라벨은 그대로 남는다.',
        },
      ],
      cautions: [
        '절대 판정에서 아무것도 찍지 않은 카드는 "가치 없음" 으로 저장된다. 훑어만 보고 제출하면 그 회차 전부가 부정 라벨로 쌓인다.',
        '제출은 단어별 순차 저장이라 중간에 실패하면 앞부분만 저장된 채로 남는다. 그 상태에서 그대로 다시 제출하면 앞부분이 한 번 더 쌓인다 — 같은 단어 재판정을 막는 제약이 없다.',
        '쌓인 라벨을 화면에서 지우거나 고칠 방법은 없다. extraction_judgments 는 덧쓰기만 되므로 잘못 쌓은 회차는 DB 에서 직접 지워야 한다.',
        '표본을 띄워 둔 사이에 추출·사전이 바뀌어 그 단어가 현 추출에서 빠지면 저장이 "not in current extraction" 예외로 끊긴다. 표본을 다시 불러서 판정한다.',
      ],
      seeAlso: [
        { label: '품질 게이트', href: '/admin/quality/gates' },
        { label: '품질 지표 추이', href: '/admin/quality' },
      ],
    },
  },
}
