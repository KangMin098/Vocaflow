# 교재 공장 감사 — 실행·검증 보고 (2026-09-16)

> 실행 범위: **T1**(⑦L2) · **A**(④ 게이트 라벨) · **T3**(자유도 출처) · **T4**(「조판」 두 이름) · **T2**(② 판정 가능/증거 부족) · **T5**(리포트 나이 표시) · T6 은 **전제가 틀려 철회**(진짜 구멍 1건만 수정).
> 남은 것: 리포트 재생성(기존 파일을 덮음 · 승인 대기) · 결정 필요 10개. **화면에 남은 출처 어긋남 0/5 · 드레인 관문 11/11.**
> 진단 [01-asis.md](./01-asis.md) · 재설계안 [02-tobe.md](./02-tobe.md)

---

## 1. 변경 전후 공정 현황판 — **통과 4/8 → 5/8**

실측(`loadFactoryLine()` 직접 호출 · 2026-09-16):

| 공정 | 변경 전 | 변경 후 | 상태 |
|---|---|---|---|
| ① 기출 원천 | 29/29 · `pass` | 29/29 · `pass` | 그대로 |
| **② 기획** | 구속점 EBS 1.199 · `short`(생산 부족처럼 보임) | 판정 가능 구속점 **쎄듀 1.200** · 「증거 부족 — EBS」(못 잼) · `unmeasured` · 리포트 나이 노트 | **판정 규칙 바뀜 (T2·T5)** · 병목 위치 그대로 |
| ③ 설계 | 21/21 · 5/5 · `pass` | 21/21 · 5/5 · `pass` | 그대로 |
| **④ 소재** | 4/4 + **못 잼 주입** · `unmeasured` | 4/4 · 조판 풀 87,556 · **`pass`** | **↑ 해소 (A)** |
| ⑤ 집필 | 90/90 · 880,247 · `pass` | 동일 · `pass` | 그대로 |
| ⑥ 해설 | 880,185/880,247 · `short` | **875,528**/880,247 · `short` | ↓ **다른 세션 작업** (§1-1) |
| **⑦ 검수** | L1 19/19 · **L2 802/802** · L3 16/19 · L4 10/28 · `short` | L1 19/19 · **L2 4/408** · L3 16/19 · L4 10/28 · `short` | **눈금만 정직해졌다 (T1)** |
| ⑧ 조판·발행 | 7/7 · 7/7 · `pass` | 동일 · `pass` | 그대로 |
| **전체** | **4/8 통과 · 병목 ② 기획** | **5/8 통과 · 병목 ② 기획** | **+1** |
| ④ 후보 눈금 | 「조판 풀 87,556」 | 「조판 후보 원문 (적격 판정 전) 87,556」 + 노트 「그중 조판기가 싣는 조판 가능 30,508」 | **이름만 바뀜 · 판정 그대로 (T4)** |
| (자유도 패널) | 스냅샷 · 재고 시각 **2026-09-15 13:35Z** · 단일유형 23/66 · 혼합권 20 | 집계표 · 재고 시각 **30분 주기** · 단일유형 23/66 · 혼합권 20 | **수치 같음 · 시각만 바뀜 (T3)** |

**⑦의 상태는 `short` 그대로다** — 바뀌기 전에도 L4(10/28)가 이미 그 칸을 막고 있었기 때문이다.
즉 이 변경으로 **통과하던 것이 막히지 않았다.** 바뀐 것은 **거짓말 하나가 사라진 것**뿐이다:
L2 가 「802/802 = 100%」라고 말하던 자리에서 이제 「4/408」이라고 말한다.

**④는 지문을 한 편도 더 수확하지 않고 통과했다.** 재고는 처음부터 87,556편으로 차 있었고,
막고 있던 것은 **이름 두 줄**이었다(§4-1 A).

### 1-2. 시중 비교 리포트 재생성 (승인 2026-09-17)

기존 두 벌을 백업한 뒤 **임시 경로에 먼저 만들어 비교하고** 제자리로 옮겼다(창고 8분 40초 · 권 11분 57초).
`gen-benchmark-report.mjs --check` 통과(마커 밖 무수정).

| | 09-01 | 09-17 | |
|---|---|---|---|
| 창고 범위 | 427,592문항 | **880,252문항** | 두 배 |
| 창고 구속점 | 쎄듀 1.114 | 쎄듀 **1.144** | A6 개선 |
| **권 구속점(리포트)** | EBS 1.199 | EBS 1.199 | 그대로 — 여전히 증거 부족 |
| 판정 가능 구속점(현황판 ②) | 쎄듀 1.200 | 쎄듀 1.200 | 그대로 |
| **권 합본** | 1.424 | **1.340** (자 정정 후 · 정정 전 1.325) | **↓ 0.084** |
| 합본 A5 유형 다양성 | 못 잼 | **시중 16 · 우리 15 → 0.938 ❌** | **새로 잰 축 · 진다 — 빠진 유형은 `purpose`** |
| 합본 A7 선택지 수 규격 | 우리 100% | 92.7% → **자 정정 후 100%** | 후퇴가 아니었다(아래) |
| NE능률 A6 지문 어수 | 1.025 | **1.086** | 개선 |

**원인 추적 (2026-09-17 · 읽기만 · `market-benchmark --volume all --json`)**

- **A7 은 품질 후퇴가 아니었다 — 자의 불일치였다.** 규격 밖 16건이 **전부** 초등 사전 유형
  (`word_meaning` 10 · `rhyme` 6)의 4지선다였다. 09-01 이후 조립 규칙이 바뀌어 이 유형들이 **V2 권에도**
  실리게 됐는데, 자는 V1 만 학년으로 빼고 V2 의 같은 유형은 **초3~6 독해서의 5지 기준**으로 쟀다.
  `elementary.ts` 의 `ELEMENTARY_CHOICES` 는 바로 그 적용을 금지한다고 적고 있고(파닉스는 장르가 다르다),
  같은 자의 A4 는 초등 3종을 **유형으로** 뺀다. A7 도 유형으로 빼도록 고쳤다 → **16건 제외 · A7 100% ·
  다른 축은 한 자리도 안 변함.** (제외 수는 리포트에 매번 찍힌다.)
  ⚠️ 앞 판에서 이것을 「실제 품질 후퇴」라고 적었다 — **틀렸다.** 판 구성이 바뀐 것을 품질로 읽었다.
- **A5 는 진짜 생산 격차다.** 시중에만 있는 유형은 `purpose`(글의 목적) 하나. 재고 86건(V3 16 · V5 27 ·
  V6 20 · V7 23)이 있지만 **전량 `typeMisfit`** 으로 빠진다 — 09-15 게이트(`type-fit.ts` · 평가원 설계 규칙
  §R-PURPOSE 「화자와 수신자가 분명해야 목적이 성립한다」)가 옳고, 86건 중 편지·안내문 지문에 쓴 것이 **0건**이다.
  풀려면 **화자·수신자가 있는 지문을 먼저 확보**해야 한다 — 코퍼스(논문·뉴스)에 거의 없는 형식이라
  원글 집필(`write-drain`)이 필요한 생산 과제다.

현황판 판정(5/8 · ② 못 잼)은 안 바뀌었다(합본 1.340 은 목표 이상).

⚠️ EBS 관련 작업(정답해설 코퍼스 추가)은 지시에 따라 **제외**했다 — 「증거 부족 — EBS」는 그대로 남는다.

### 1-3. A5 생산 — 서신 90편 · 「글의 목적」 90문항 (승인 「권장안으로」 2026-09-17)

게이트(`type-fit.ts`)는 건드리지 않았다. 게이트가 요구하는 글 — 부르는 사람·받는 사람과 요청 행위가 있는 편지·공지 —
를 `write-drain --mode correspondence` 로 **새로 썼다.**

| 밴드 | 서신 적재 | 적재 전 밴드 적중 | 서신 적합(문항 창) | 게이트 판정 | 목적 문항 | 권의 `purpose` |
|---|---|---|---|---|---|---|
| V5 | 30 | 30/30 | 30/30 | use 30 · 격리 0 | 30 | 6 |
| V6 | 30 | **30/30** | 30/30 | use 30 · 격리 0 | 30 | 7 |
| V7 | 30 | **30/30** | 30/30 | use 30 · 격리 0 | 30 | 13 |

(적중·적합은 `write-drain-verify` · `fit-probe` · `write-drain-import` 예행 · 권 수는 `build-volume --band N` 출력.
목적 문항 적재 시 정답 번호 분포 V6 6·6·6·6·6 · V7 6·6·6·6·6 · 정답이 유일한 최장 V5 10.5% · V6 6.1% · V7 0.0%.)

권 리포트 재생성(백업 후 · 임시 경로에서 비교 후 교체 · `gen-benchmark-report --check` 통과):

| | 09-17 오전 | 09-17 A5 생산 후 |
|---|---|---|
| 합본 A5 유형 다양성 | 시중 16 · 우리 15 → **0.938 ❌** | 시장 표준 **16/16 — 천장** · 전체 24종(표준 밖 8종 포함) → **1.5 ✅** |
| 권 합본 지수 | 1.340 | **1.433** |
| 권 구속점 | EBS 1.199 (증거 부족) | EBS 1.199 — **그대로** |
| NE능률 A6 | 1.086 | 1.081 |

현황판 판정은 안 바뀐다 — ②가 읽는 판정 가능 구속점(쎄듀)과 EBS 「증거 부족」은 A5 와 무관한 축이다.
⚠️ V7 권은 120문항 중 `purpose` 가 13개로 몰렸다 — 조판기의 배분이고 이 작업은 배분 규칙을 바꾸지 않았다(관찰만).

### 1-1. ⑥ 해설이 뒤로 갔다 — 이 감사의 변경이 아니다

해설 보유가 880,185 → **875,528**(미보유 62 → **4,719**)로 줄었다. 총 문항은 880,247 로 같다.
이 감사는 해설·문항을 건드리지 않았다(변경 파일 §3 — `factory.ts` · `factory-model.ts` · 회귀 · 문서뿐).

같은 워크스페이스에서 **다른 세션이 동시에 작업 중**이고, 이 감사의 커밋 뒤로 9개가 쌓였다.
그중 `7b22baba`(고유명사 가드가 재고에 닿지 못하고 있었다 — 수리 선택자를 넓힌다) 등이
문항을 수리하면서 해당 문항의 해설이 다시 빈 것으로 보인다. **DB 실측이고 추정은 원인 쪽뿐이다.**
⑥은 어차피 `short` 였으므로 공정 판정은 바뀌지 않았다.

---

## 2. 검증

| 검사 | 결과 |
|---|---|
| `tsc --noEmit -p apps/web/tsconfig.json` | **오류 0** |
| `next lint` (변경 파일 전부) | **No ESLint warnings or errors** |
| `factory.integration.test.ts` (실 DB) | **22/22 통과** (자유도 출처 · ② 판정 가능/증거 부족 검사 추가) |
| `market-gate.test.ts` | **신설 · 전부 통과** (실제 리포트 모양 · 진짜 미달 비가림 · 모르는 것을 못 닿음으로 빼지 않음 · 리포트 나이) |
| `gate-mixed-import.mjs` 예행 | 1,290조각 · 건너뜀 0 · **쓴 것 0** |
| `factory-model.test.ts` | **56/56 통과** (실패하던 것이 A 로 초록) |
| `freedom-view.test.ts` · `freedom-panel.test.tsx` | **17/17 · 9/9 통과** |
| csat·textbook 회귀 전량 (58파일 904건) | 902 통과 · **2 실패 — 전부 이 변경과 무관** (§4-4) |

### 변이 검사 — 회귀가 실제로 잡는가

새 회귀는 값의 모양이 아니라 **두 화면이 같은 출처를 보는가**를 잠근다. 셋 다 잡혔다:

| 변이 | 잡혔나 | 무엇이 잡았나 |
|---|---|---|
| ① 옛 버그 되살리기(L2 를 다시 `csat_coverage()` 로) | ✓ | 라벨 검사 — `'교재 문항' 없음` |
| ② **라벨만 새것으로 두고 수는 기출** | ✓ | 수 검사 — `expected 802 to be 4` |
| ③ L2 를 푸는 명령 제거 | ✓ | 명령 검사 — `L2 명령이 없다` |

②가 중요하다. 라벨만 검사하면 이름을 바꾸고 출처는 틀린 채로 통과한다 —
**이번에 고친 버그가 정확히 그 모양이었다**(2026-09-13 에 한쪽만 고쳤다).

| 변이 (T3 · A · 계측기) | 잡혔나 | 무엇이 잡았나 |
|---|---|---|
| ④ 자유도 로더가 다시 저장소 스냅샷을 읽게 한다 | ✓ | 통합 회귀 — `expected '2026-09-15T13:35:54Z' to be '2026-09-17T00:30:00Z'` |
| ⑤ ④ `gateGauges` 를 옛 이름으로 되돌린다 | ✓ | 계측기 게이트 정렬 — ④ 하나만 ✗ |
| ⑥ 소재 화면 글자를 「조판 풀」로 되돌린다 | ✓ | `pool-vocabulary.test` — 파일:줄을 짚는다 |
| ⑦ ② 를 옛 판정(리포트 구속점 그대로)으로 되돌린다 | ✓ | 통합 회귀 — `expected [] to deeply equal ['증거 부족 — EBS']` |

---

## 3. 변경 파일

커밋 `2bdf6ced` (감사 산출물) · `86a5aa93` (T1) · `df3ed1ed` (보고) · `234bd97e` (A) · `ca018630` (계측기) · `15f52a09` (T3) · `96422167` (T4) · `ded38caa` (T2·T5·T6) · `ab1207a7` (리포트 재생성) · 이번 커밋 (A7 자 정정)

| 파일 | 무엇 |
|---|---|
| `docs/factory-audit/01-asis.md` | 진단서 (질문 1~5 판정 + 8칸 매트릭스 + 병목 9위) |
| `docs/factory-audit/02-tobe.md` | 재설계안 (제안 8 + 브랜드/시리즈 초안 + 축별 목표표 + 검수 매핑표 + CLAUDE.md diff) |
| `docs/factory-audit/03-report.md` | 이 문서 |
| `scripts/audit/factory-sources.mts` | **신설** 출처 대조 계측기 (읽기 전용 · 재실행 안전) |
| `scripts/audit/factory-commands.mts` | **신설** 명령 위생 계측기 (읽기 전용 · 재실행 안전) |
| `scripts/audit/*.result.json` | 두 계측기의 산출 |
| `apps/web/src/lib/csat/factory.ts` | ⑦L2 출처·라벨 교체 + L2 명령 3단 추가 |
| `apps/web/src/lib/csat/__tests__/factory.integration.test.ts` | 회귀 2건 교체·신설 |
| `apps/web/src/lib/csat/__tests__/fixtures.ts` | 표본의 L2 를 실측 모양으로 |
| `apps/web/src/lib/csat/factory-model.ts` | ④ `gate`·`gateGauges` 를 `factory.ts` 라벨에 맞춤 (A) |
| `apps/web/src/lib/textbook/freedom-view.ts` | 순수 함수화 — 재고를 인자로 받고 배합은 `rungMix` 에서 계산 (T3) |
| `apps/web/src/lib/textbook/freedom-load.ts` | **신설** 서버 로더 — `loadDcpInventory` 한 번 (T3) |
| `apps/web/src/app/admin/csat/page.tsx` · `components/admin/textbook/FreedomPanel.tsx` | 자유도를 같은 물결에서 받고, 집계 시각·못 읽음 경보를 적는다 (T3) |
| `apps/web/src/lib/textbook/__tests__/freedom-view.test.ts` · `app/admin/csat/__tests__/freedom-panel.test.tsx` | 스냅샷을 고정 표본으로 · 라이브 경로 동치 검사 4건 · 못 읽음 경보 (T3) |
| `apps/web/src/lib/admin/help/csat.ts` | 「갈린다」 경보의 뜻 정정 · 「재고는 어디서 오나」 신설 (T3) · ④ 흐름도·필드를 후보/가능으로 (T4) |
| `apps/web/src/lib/admin/help/textbook.ts` | 「조판 가능」 용어 · **같은 목록 안의 모순** 정정(「나머지 넷만 경고」 vs 「09-06부터 강제」) (T4) |
| `apps/web/src/lib/csat/factory.ts` (T4) · `sourcing/SourceClient.tsx` · `sourcing/BandStrip.tsx` · `sources/NextStepPipeline.tsx` · `sources/SourceInventoryTable.tsx` | 「조판 풀」 → 판정 전은 「조판 후보」, 적격 통과는 「조판 가능」 |
| `apps/web/src/app/admin/csat/__tests__/pool-vocabulary.test.ts` | **신설** — 화면·도움말의 보이는 글자에 옛 이름이 돌아오는 것을 막는다 |
| `apps/web/src/lib/csat/market-gate.ts` · `__tests__/market-gate.test.ts` | **신설** — ② 판정 가능/증거 부족 분리 · 리포트 나이 (T2·T5) |
| `apps/web/src/lib/csat/factory.ts` (T2·T5) | ② 눈금 재구성 · ②·⑦L4 에 리포트 생성 시각 노트 |
| `apps/web/src/lib/admin/help/csat.ts` (T2) | 「② 기획 — 증거 부족 눈금과 판정 구속점」 신설 |
| `scripts/csat/gate-mixed-import.mjs` (T6 정정) | 빈 판정을 조용히 건너뛰던 것 — 건너뛴 수 출력 · 낯선 판정 경고(적용 의미는 그대로) |
| `scripts/audit/factory-commands.mts` | 드레인 관문을 파일 이름이 아니라 동작(dry-run + 건너뛴 수)으로 센다 |

**DB 스키마·마이그레이션·학습자 앱 경로·기존 리포트 파일은 건드리지 않았다.** `--commit` 을 쓴 데이터 변경도 없다.

---

## 4. 미착수 — 그리고 이 감사가 틀렸던 곳

### 4-0. 이 감사가 **일곱 번** 틀렸다 (전부 정정함)

| 무엇 | 왜 틀렸나 | 지금 |
|---|---|---|
| ④ 소재를 「통과」로 적었다 | 눈금(4/4)만 읽고 `state()` 가 주입하는 「못 잼」을 셈에 안 넣었다. 화면을 본 쪽이 옳았다 | **「못 잼」으로 정정 → 그 뒤 A 로 실제 해소** |
| ⑦ L3 를 「19/19」로 적었다 | SQL `jsonb is not null` 이 **JSON 의 `null` 을 「있음」으로 센다.** 앱은 JS 로 읽어 3건을 없는 것으로 센다 | **「16/19」로 정정** |
| 「현황판에 도움말이 없다」 | 키를 `^  '이름':` 로만 훑는 정규식이 **따옴표 없는** `csat:` 를 못 봤다 | **결함 아님 — 취소 (§4-3)** |
| P2 「사다리 밖 164,564 vs 스냅샷 고아 0」 | 스냅샷의 `orphanItems` 는 **원글이 사라진 문항**(댕글링)이다 — 다른 개념끼리 견줬다. 「패널이 841,826 을 띄운다」도 틀렸다(패널은 그 수를 안 찍는다) | **P2 취소 · P1 서술 정정** |
| 「드레인 게이트 2/11」 → T6 「검사기 4개 신설」 | `*-drain-validate.mjs` 라는 **파일 이름**만 셌다 — 적재기 안에 dry-run·건너뛴 수 출력이 이미 있었다. 진짜 구멍은 `gate-mixed` 의 조용한 건너뛰기 1건 | **11/11 로 정정 · T6 철회 · 1건 수정** |
| 「A7 실제 품질 후퇴」 | 판 구성 변화(초등 사전 유형이 V2 권에 실림)를 문항 품질로 읽었다. 자가 그 유형을 학년으로만 빼고 있었다 | **자 정정 · A7 100%** |
| 「표지·아이콘 자산 없음」 | 이미지 파일이 없는 것을 표지가 없는 것으로 읽었다 — `cover.ts` 가 조판식 표지를 만들고 네 곳이 공용한다 | **정정 · 브랜드 결정 7 → 5** |

계측 규칙도 두 번 틀렸다(`writeFileSync` 를 DB 쓰기로 셈 13→0 · 블라인드 평가 세트를
드레인으로 셈 19→11). **여덟 번 모두 코드가 아니라 자(또는 읽는 나)가 틀렸다.** 그중 하나(T6)는 틀린 수 위에 **작업 제안까지** 세웠다 — 구현 직전에 적재기를 열어 보고 멈췄다.

교훈 셋을 자에 박았다:
1. **판정은 `state()`/`judgeStage` 를 태워서 낸다** — 눈금을 직접 읽으면 주입된 「못 잼」을 놓친다.
2. **SQL 로 앱 수치를 검증할 때 `jsonb_typeof(x) <> 'null'`** — `is not null` 은 JSON null 을 「있음」으로 센다.
3. **「없다」고 말하기 전에 정본을 실행해서 확인한다** — 정규식으로 센 「없음」은 대개 정규식이 없는 것이다.
4. **두 수를 견주기 전에 두 필드의 정의를 읽는다** — 이름이 비슷하다고 같은 양이 아니다(`orphanItems`).

### 4-1. 해소한 것 — A (④ 게이트 라벨 정합)

`factory-model.ts` 의 `gateGauges` 가 「게이트가 있는 밴드 중 지문 보유」인데 `factory.ts` 가 내는
라벨은 「지문으로 채우는 밴드 중 재고 보유」였다. 이름이 안 맞으면 `state()` 가 「못 잼」 눈금을
꽂도록 돼 있으므로, ④는 **재고 87,556편을 두고 「못 잼」**으로 서 있었다.

**어느 이름이 옳은지는 물어보지 않고 git 이력으로 답했다:**

| 커밋 | 날짜 | 무엇 |
|---|---|---|
| `f8ef73af` | 2026-09-06 | 게이트와 눈금을 **이름으로 묶는** 장치를 넣으며 옛 라벨로 `gateGauges` 를 적었다 |
| `5982ac67` | 2026-09-15 | ④의 셈법을 **게이트의 `metric` 에 맡기도록** 바꾸며 `factory.ts` 라벨을 고쳤다. 커밋 본문이 이유를 적고 있다 — S5 의 합격선은 `listening` 하나뿐이라 **지문을 수확해서 채우는 칸이 아닌데** 옛 셈법이 그 칸을 늘 빨갛게 세웠다 |

즉 **나중 것이 의도된 변경**이고 `factory-model.ts` 만 안 따라갔다. `gate` 문장도 함께 맞췄다 —
문장만 옛 뜻으로 두면 눈금과 다시 따로 놀고, 그 어긋남을 잡는 장치는 **이름**뿐이라 문장은 아무도 안 본다.

결과: `factory-model.test.ts` **56/56 통과**(실패하던 회귀가 초록) · ④ `unmeasured` → `pass`.

### 4-2. 남은 것

| # | 발견 | 증거 | 왜 안 했나 |
|---|---|---|---|
| C | ~~A7 회복~~ | §1-2 | **자의 불일치로 판명 · 정정 완료** |
| C′ | ~~A5 — `purpose` 문항을 편지·안내문 지문에 쓴다~~ | §1-3 | **완료** — V5~V7 서신 90편 · 문항 90 · 합본 A5 16/16(1.5) |
| — | EBS 정답해설 코퍼스 추가 | — | **지시에 따라 제외** |
| E | 결정 필요 10개 (브랜드 5 · 커버리지 1 · 차단조건 3 · 신고경로 1) | 02-tobe §2·§3-1·§4 | 「결정은 다음에」 |

### 4-3. 취소된 발견 — B 는 **오탐이었다**

한때 「현황판에 화면도움말이 통째로 없다(`screen="csat"` 인데 레지스트리에 키 없음)」를 결함으로
적었다. **틀렸다.** 도움말은 `help/csat.ts:181` 에 **따옴표 없는** `csat:` 로 있다.

원인은 키를 `^  '이름':` 로만 훑는 정규식이 하이픈 없는 키를 못 본 것이다. 레지스트리를 실제로
import 해 세어 보니 **쓰이는 키 53개가 전부 정의돼 있었다(빠진 것 0)**. 계측기
`factory-commands.mts` 에도 같은 버그가 있어 도움말 화면을 9개로 세고 있었다 — 고쳐서 **10개**다.

> 그 정규식을 믿었으면 **17개 화면에 「도움말 없음」이라는 거짓 결함**을 보고했을 것이다.
> 「없다」고 말하기 전에 **정본을 실행해서** 확인해야 한다.

### 4-4. 이 변경과 무관한 기존 실패

| 실패 | 원인 | 지금 |
|---|---|---|
| `factory-model.test.ts` 「소재 이 선언한 눈금이 실측 코드에 있다」 | 위 A | **해소됨 (56/56)** |
| `copyright-boundary` 회귀 | 다른 세션이 **지금 편집 중인** 지문 골격 데이터(`skeleton-data/*.json` · `passage-skeleton.ts`, 이 감사 변경 목록에 없음) | 다른 세션 소관 |
| `db-health/silent-noop` 「stale 로 센 표는 실제로 통계가 없다」 | 실 DB 의 표 통계 상태에 기대는 회귀 — `lib/admin/db-health` 는 이 감사가 건드리지 않았다 | 다른 영역 · 기록만 |
| `sources-screen.test.tsx` 「계획이 재고보다 앞선 값으로 계산되지 않았다」 | `item-fill-plan.json`(13:05Z)이 `type-inventory-snapshot.json`(13:35Z)보다 **먼저** 계산됐다 — 다른 세션이 스냅샷을 갱신하고 계획을 다시 안 돌렸다 | 그대로 실패. 다른 세션 소관 |

---

## 5. 다음 루프에 그대로 넣을 지시문

아래를 `/loop` 나 `/goal` 에 그대로 붙여 넣으면 이어진다.

```
/goal
## 대상
docs/factory-audit/02-tobe.md 의 승인 대기 항목 + 03-report.md §4-2 의 미착수 A~E.
근거는 scripts/audit/factory-sources.mts · factory-commands.mts 재실행 결과만 쓴다
(손으로 적지 않는다). 진단은 01-asis.md 를 출발점으로 삼되 수치는 다시 잰다.

## 이미 끝난 것 (다시 하지 않는다)
- ⑦L2 교재 문항화(T1) · ④ 게이트 라벨 정합(A) — 공정 통과 5/8
- 자유도 패널을 공정 ⑤·⑥과 같은 집계표로(T3)
- 「조판」 두 수의 이름을 갈랐다(T4 — 후보 87,556 / 조판기가 싣는 「조판 가능」 30,508) — 화면에 남은 어긋남 0/5
- ② 판정을 「판정 가능한 출판사 중 최저」 + 「증거 부족」(못 잼)으로 갈랐다(T2) · ②·⑦L4 에 리포트 나이 노트(T5)
- 드레인 관문 11/11 확인 — T6 은 철회, `gate-mixed` 조용한 건너뛰기만 고쳤다
- **A5 생산** — V5~V7 서신 각 30편 · 목적 문항 각 30개 · 권 리포트 재생성(합본 A5 16/16 · 합본 1.433)
- 계측기 `factory-sources.mts` 에 **게이트 정렬 검사**(이름이 어긋나 `state()` 가 「못 잼」을
  꽂는 공정을 찾는다 — 변이로 ④ 되돌리기를 잡는 것 확인)와 **⑦ 층별 JS 셈법**(jsonb null 함정 회피)을 넣었다

## 되돌리기 싸고 하류 영향 큰 것부터 (각각 승인 확인)
- **V7 권의 `purpose` 쏠림(120 중 13)** — 조판기 배분 규칙을 볼지 결정 필요(§1-3 관찰).
- 리포트는 다시 낡는다 — 생산을 바꾼 뒤에는 **재생성을 같은 루프에서** 한다(기존 파일을 덮으므로 백업 후).
- **EBS 관련 작업은 제외**(사용자 지시 2026-09-17).
- **T7** L1·L3 눈금을 「기록 유무」에서 「통과율」로 — 조판기 출력 변경이 필요해 비싸다.
- **T8** A5(유형 다양성) 측정기 — 4개 출판사 전부 못 재는 축.

## 결정이 필요해 멈춰 있는 것 — 물어보고 진행
02-tobe.md 의 결정 필요 10개: 브랜드 5(교재 해설 문체 · 판형/제본/용지 ·
시리즈별 유형 계열 문서 · 시리즈 간 중복 규칙 · 개정 주기) ·
커버리지 목표 1(시중 22 시리즈를 다 덮는 것이 목표인가) ·
배포 차단 조건 3(L2 표본 하한 · 정답 쏠림을 차단으로 승격할지 · L4 미측정 취급) ·
탈출 오류 신고 경로 1.

## 규칙
- 학습자 앱 배포 경로·DB 스키마·기존 리포트 파일은 승인 없이 수정 금지.
- 판정마다 근거 파일 경로와 실측 수치. 수치는 스크립트 재실행 결과만.
- 회귀를 붙였으면 **변이를 주입해 잡히는지 확인**하고 그 결과를 보고에 적는다.
- 커밋은 `git commit --only <파일 개별 지정>` (워크스페이스 공유).

## 완료 조건
- 승인된 항목이 구현·검증됐고 현황판에 나빠진 지표가 없다
- 03-report.md 에 변경 전후 표와 다음 지시문이 갱신돼 있다
```

---

## 6. 질문 1~5 최종 판정 요약

| 질문 | 판정 | 핵심 수치 |
|---|---|---|
| 1 플랫폼 적합성 | **적합(조건부)** | 출처 5갈래 · 진단 시 실제 어긋남 **3건** → **화면에 남은 것 0/5**(T1·T3·T4) · 남은 위험은 사람이 돌리는 리포트의 낡음(②·L4) |
| 2 Claude Code 작업 적합성 | **적합** | 명령 **24/25** 건전 · 드레인 관문 **11/11**(1건 수정) · 도움말 드레인 6/10 · 「재실행 안전」 25회 |
| 3 브랜드·시리즈 완결성 | **적합(부분)** | 13항목 존재(표지·권 마크 포함) · 빠진 것 **해설 문체 지침 · 유형 계열 문서** |
| 4 시중 대비 우위 | **부적합** | 잰 축 **10/28(35.7%)** · EBS 구조적으로 판정 불가(「증거 부족」) · 판정 가능 구속점 쎄듀 1.200(경계) · A7 후퇴는 자의 불일치로 정정 · **합본 A5 15 vs 16 → 생산으로 16/16(1.5)** · 합본 1.424 → 1.340 → **1.433** |
| 5 검수 완전성 | **부적합** | 내용 오류 4종을 **L2 혼자 0.98%** 로 본다 · 탈출 오류 **발견 경로 없음** · L4 는 차단 조건 아님 |
