# 정기 플랫폼 진단 (Platform Audit)

> **주기: 분기 1회 (1·4·7·10월 첫 주)** + 아래 트리거 발생 시 즉시.
> 목적은 "제품이 잘 만들어졌는가"가 아니라 **"이 제품이 목표 규모(10만 학습자)에 도달할 산술이 성립하는가"** 를 재검증하는 것.
> 최초 수행: 2026-08-16. 결과 리포트: `https://claude.ai/code/artifact/a36b68f6-5fdc-4395-b735-a9fd83fce574`

---

## 0. 왜 정기적으로 하는가

이 프로젝트의 실패 모드는 **"공급망 비대 / 수요 검증 0"** 이다.
콘텐츠 파이프라인은 AI 페어로 무한히 확장되지만, 학습자 수·리텐션·지불 의사는 자동으로 늘지 않는다.
그 격차는 매일 조금씩 벌어지고 **분기 단위로만 눈에 띈다**. 그래서 달력에 박아 둔다.

2026-08-16 최초 진단에서 확인된 격차:
앱 코드 81,747줄 · 어휘 데이터 253만 행 · **가입자 3명 · 누적 학습기록 604건**.

---

## 1. 즉시 트리거 (주기와 무관하게 재진단)

| 트리거 | 이유 |
|---|---|
| 새 학습 모듈 추가 검토 시 | 모듈 추가는 항상 F5(인지 부하·완성도 n등분)를 악화시킨다. 추가 전에 진단 필요 |
| 모바일 앱 착수 검토 시 | D7 미검증 상태의 앱스토어 출시는 6개월 매몰 |
| 외부 투자·지원사업 지원 직전 | 실사에서 나올 질문을 먼저 자문 |
| 결제/PG 연동 직전 | 전환율 가정이 실측으로 바뀌는 시점 |
| 가입자 100 / 1,000 / 10,000 돌파 | 퍼널 가정치를 실측치로 교체 |
| 경쟁사 대형 이벤트 (투자·피벗·폐업) | 지형 재평가 |

---

## 2. 절대 규칙 — 문서를 근거로 쓰지 않는다

`CLAUDE.md` · `docs/*.md` 의 수치는 **드리프트한다**.
2026-08-16 실측에서 `CLAUDE.md` 는 `library_books 20` / `vocabularies 5,896` 이라 적고 있었으나
실제는 **401 / 2,200** 이었다 (20배 과소 · 2.7배 과대).

> **진단은 DB 직접 질의 + 저장소 실계수 + 1차 공개자료로만 수행한다.**

---

## 3. 실측 절차 A — 운영 DB

Supabase project `jajenrevcbmrpaliomxv`. `mcp__supabase__execute_sql` 로 실행.

```sql
-- A1. 수요 측 (가장 중요 — 맨 먼저 본다)
select 'auth_users' k, count(*)::text v from auth.users
union all select 'learning_records', count(*)::text from public.learning_records
union all select 'reading_sessions', count(*)::text from public.reading_sessions
union all select 'daily_activity',   count(*)::text from public.daily_activity
union all select 'scores',           count(*)::text from public.scores
order by 1;

-- A2. 공급 측 (행 수 상위 — 어디에 노동이 들어갔는지)
select relname, n_live_tup
from pg_stat_user_tables
where schemaname='public' and n_live_tup > 0
order by n_live_tup desc limit 20;

-- A3. 학습자에게 실제로 보이는 카탈로그 (발행률 병목)
select status, count(*) n from library_books group by 1 order by 2 desc;

-- A4. 신규 가입 추이 (코호트가 생기면 여기부터 리텐션 계산)
select date_trunc('month', created_at)::date m, count(*) n
from auth.users group by 1 order by 1;
```

**판정 기준** — 공급 측 상위 10개 테이블 합계 대비 수요 측 합계 비율.
2026-08-16 기준 253만 : 728 = **3,480 : 1**. 이 비율이 개선되지 않으면 그 분기는 실패로 기록한다.

## 4. 실측 절차 B — 저장소

```bash
find apps/web/src -name "*.ts" -o -name "*.tsx" | wc -l          # 파일 수
find apps/web/src -name "*.ts" -o -name "*.tsx" | xargs wc -l | tail -1   # LOC
find apps/web/src/app -name "page.tsx" | wc -l                   # 페이지 라우트
find "apps/web/src/app/(main)" -maxdepth 1 -type d | wc -l       # 학습자 표면 수 ★
find apps/mobile/src -type f | wc -l                             # 모바일 실체
grep -ril "amplitude\|mixpanel\|posthog\|gtag\|analytics" apps/web/src | wc -l   # 계측
grep -ril "toss\|portone\|iamport\|billing" apps/web/src | wc -l  # 결제 PG
```

★ **학습자 표면 수는 낮을수록 좋다.** 2026-08-16 = 22개 (사용자 3명). 목표는 4개.

**목업 수치 감사 (매 분기 필수)** — 공개 라우트에 실측 아닌 상수가 있는지:

```bash
grep -rn "12,000\|4.8 / 5\|MRR\|누적\|평점" "apps/web/src/app/(marketing)" apps/web/src/app/admin/billing
```

2026-08-16 적발: `/pricing` 의 `12,000+ 학습자` · `4.8/5 평점` · `34 학교·기관` · 교사 추천사 (실제 3/0/0/0),
`/admin/billing` 의 `MRR ₩1.84M` · `활성 구독 184` 하드코딩.
→ **표시광고법(거짓·과장 표시) 리스크. 발견 즉시 제거 또는 "예시" 명시.**

---

## 5. 경쟁 지형 재조사 (분기 1회 웹 검색)

| 대상 | 확인할 것 | 2026-08 기준선 |
|---|---|---|
| 국내 영어앱 MAU 순위 | 순위 변동·신규 진입 | 듀오링고 62만 / 말해보카 29만 / 똑똑보카 26만 / 스픽 24만 |
| 클래스카드 | 교사·학생 수 (학교 채널 봉쇄 정도) | 교사 11만 · 학생 230만 · 영어교사 1/3 |
| 말해보카(이팝소프트) | 매출·영업이익 (카테고리 경제성 상한) | 매출 256억(’25) · 영업이익 1.27억(’24, 첫 흑자) |
| 스픽 | 국내 매출·투자 | 국내 60억(’23) · Series C $78M @ $1B |
| 뤼이드 | 생존 여부 (기술 우위 ≠ 유통 우위의 반례) | 투자 2,000억 · 매출 50억 · 영업손실 421억 |
| 듀오링고 | 유료 전환율 (업계 상한) | 유료 1,220만 / MAU 1억 3,300만 = **9.2%** |
| AI 디지털교과서 | B2G 채널 개폐 | 2025.8 교육자료 격하 · 채택률 32% · 손배소 2,000억 → **닫힘** |
| 사교육비 조사 | 영어 지출이 어디로 흐르는지 | 총액 27.5조 · 영어 참여학생 월 28.1만 원 |

---

## 6. 산술 모델 — 매 분기 같은 식에 최신 값을 넣는다

```
가입 N
  × MAU/가입 (기본 가정 10~20%, 실측 생기면 교체)
  × 유료전환/MAU (기본 5~9%, 듀오링고 9.2%가 업계 상한 — 신생은 절반 이하)
  × ARPU (현재 /pricing Pro ₩9,900)
  = MRR
```

**2026-08 산출** — 가입 10만 → MAU 1~2만 → 유료 500~1,800 → **연매출 0.6~2.1억**.

```
LTV  = ARPU × 평균 구독 개월(기본 8)          = ₩79,200
CAC  = 가입당 획득비 ÷ (유료/가입 전환율)
허용 CAC(가입당) = LTV ÷ 3 × (유료/가입 전환율)  = ₩400 내외
```

> **결론(변하지 않는 한 유효)**: 가입 1명을 400원 이하로 데려올 유료 채널은 없다.
> → 10만은 광고가 아니라 **교사 3,500명 × 학급 30명** (CAC 0) 으로만 도달한다.
> 클래스카드 교사 기반의 3.2%.

---

### 6-1. 활성화·리텐션은 이제 화면에서 읽는다 (2026-08-16)

F4 를 "이벤트 6종 수집" 으로 적어 뒀지만, 실측해 보니 **6종 중 5종이 이미 기존 테이블에서
파생**된다(가입=`auth.users` · 첫 학습/세션/단어장/퀴즈=`learning_records`+`scores`).
빠진 것은 **순수 재방문(학습 없는 페이지 조회)** 하나뿐이고, 그것은 **일부러 수집하지 않는다** —
학습 제품에서 조회만 한 방문은 가치가 아니고 쫓으면 지표가 실제 학습과 멀어진다.
(조회 리텐션이 필요해지는 시점은 유료 전환 퍼널을 붙일 때다.)

즉 F4 의 실체는 "수집 장치가 없다" 가 아니라 **"아무도 계산해 본 적이 없다"** 였다.
→ `/admin` 상단 "학습자 활성화 · 리텐션" 패널에서 상시 판독한다
(`lib/admin/retention.ts` · 새 테이블·쓰기 경로 0).

**1회차 판독 (2026-08-16)** — 가입 3 · 활성화 3 · **가입→첫 학습 중앙값 55일** ·
D1 0/3 · D7 1/3 · D30 1/3 · 최근7일 3 · 최근28일 3.

> ⚠️ **이 3명은 개발·검증 계정이다. 사용자 행동이 아니다.** 여기서 읽을 것은 비율이 아니라
> **장치가 돈다는 사실** 하나다. 그래서 패널은 분모 20명 미만이면 퍼센트를 아예 그리지 않는다
> — 작은 표본의 퍼센트는 근거가 아니라 착시이고, 이 문서 §2 가 금지하는 "수치를 근거로 쓰기" 가
> 거기서 시작된다.
>
> 다만 **55일**은 표본이 작아도 방향을 말한다: 문제는 리텐션 이전의 **활성화**다.
> 가입하고 두 달 뒤에 첫 학습을 한다면 D1/D7 은 애초에 잴 대상이 아니다.

## 7. 진단 결과 기록 형식

매 회차마다 아래 표에 한 행을 추가한다 (삭제 금지 — 추세가 자산이다).

| 회차 | 일자 | 가입자 | 학습기록 | 발행 도서 | 학습자 표면 | 계측 | 결제 | 공급:수요 비 | 판정 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 2026-08-16 | 3 | 604 | 13 / 401 | 22 | 없음 | 없음 | 3,480 : 1 | **risk** — 수요 검증 0 |

판정은 Memory Decay 4색 어휘를 그대로 쓴다: `stable` / `shaky` / `risk` / `new`.

---

## 8. 상시 유효한 7개 진단 (해소될 때까지 매 회차 재확인)

| ID | 결함 | 해소 조건 |
|---|---|---|
| F1 | 공급망 100% / 수요 검증 0% | 외부 학습자 30명 이상 · D7 측정 가능 |
| F2 | 모바일 부재 = 리텐션 장치(푸시·위젯) 부재 | D7 검증 **후** 앱 착수 (순서 역전 금지) |
| F3 | 콘텐츠–시장 부적합 (PD 고전 ↔ 내신·수능·토익·회화) | 지불 동기와 연결된 콘텐츠 축 확보 |
| ↳ F3 진행 (2026-08-17) | **BYO 축이 열렸다** — TextFit 이 임의 지문(교과서·프린트·모의고사)을 학습자 기준으로 판정한다. 내신은 교과서 저작권 때문에 콘텐츠 공급이 원천 불가라, 공급이 아니라 **처리**로 푸는 것이 유일한 경로였다. 남은 것은 큐레이션 축(S4 소스 8건)과 검증(교사 10명) | 교사 10명 실사용 + S4 소스 ≥ 40 |
| F4 | ~~계측 부재~~ → **계측 장치 완료 (2026-08-16)**. 남은 것은 표본 | 분모 20명 이상에서 D1/D7/D30 · 활성화율 판독 |
| F5 | 학습자 표면 22개 = 인지 부하 + 완성도 n등분 | 표면 4개 이하 (삭제 아닌 플래그 off) |
| F6 **(재정의 2026-08-17)** | 발행률 3.2% — 파이프라인이 카탈로그로 안 이어짐. **다만 원인은 게이트가 아니었다** (아래 §8-1) | ~~published ≥ 60~~ → **published ≥ 60 이면서 그중 절반 이상이 학습자 목표 대역(V6–8)의 논설·설명문** |
| F7 | 문서–현실 드리프트 (자기 계측 실패) | `CLAUDE.md` DB 통계를 스크립트 생성으로 교체 |
| ~~F8~~ **(발견·해소 2026-08-17)** | ~~교사 채널의 핵심 기구가 껍데기였다~~ — 산술상 10만은 교사 3,500명 × 학급 30명(CAC 0)으로만 도달하는데, `classes` 는 `id·teacher_id·name·invite_code` 가 전부였고 `class_members` 는 **코드에서 테스트 파일에만** 등장했다(RPC 도 권한 헬퍼·가입뿐). 학급을 만들어 30명을 초대해도 학생에게 전달되는 것이 없었다 | **해소** — `20260817130204_class_assignments` 적용. 교사가 지문에서 뽑은 단어를 학급에 보내고 → 학생이 단어장에 담고 → 교사가 수행을 본다. 지문은 저장하지 않는다(CHECK 가 구조적으로 강제). RLS 공격 회귀 11건. ⚠️ **남은 것은 실사용** — 두 테이블 모두 아직 0행이라, "기구가 있다" 가 "쓰인다" 는 아니다 |

---

### 8-1. F6 은 왜 재정의됐나 — 숫자 목표가 잘못된 행동을 시킨다 (2026-08-17)

F6 을 해소하려고 `ready` 303권을 발행하려다, **먼저 그게 무엇인지 쟀다.**

**① 게이트는 막고 있지 않았다.** `run_content_quality_gates('book', …)` 를 표본에 직접 돌렸더니
critical 전항목 PASS(경고 I6 하나). 303권 전부 저작권 통과 · V-Level 301 · 챕터·단어 303.
즉 파이프라인이 끊긴 게 아니라 **아무도 발행을 누르지 않았다.** 기술 문제가 아니었다.

**② 그런데 발행하면 F3 이 나빠진다.** 303권의 정체:

| 출처 | 권수 | 평균 V | 평균 단어 |
|---|---|---|---|
| `standard_ebooks` | 285 (V7↑ 270권) | 7.7 | 106,657 |
| `storyweaver` | 18 (V0–3 17권) | 2.6 | 361 |

목표 대역(V6–8)에 245권이 있지만 평균 **75,255 단어 · 30.6 챕터** — 고2 가 끝낼 분량이 아니다.
30,000 단어 이하로 좁히면 43권인데, 그 목록이 이렇다:
*Sonnets from the Portuguese · Antigone · Ajax · Electra · Philoctetes · Agamemnon ·
Macbeth · The Comedy of Errors · Poetry(3종)* — **소포클레스·셰익스피어·시집**이다.

**③ 결정타** — `csat_stage_catalog` 에 등재된 도서 13건 중
**논설·설명문 0건 · 서사 12건.** 도서 코퍼스 전체가 서사다.
수능 영어는 사실상 논설·설명문 전용이므로, **책을 더 발행해도 그 축은 1권도 안 늘어난다.**

**그래서 목표를 고쳤다.** `published ≥ 60` 은 소포클레스 47권을 더 발행하면 달성된다 —
카탈로그는 두꺼워지고 F3 은 그대로거나 악화된다. 그건 이 문서 §0 이 경고하는
**"공급망 비대"** 를 지표가 오히려 부추기는 꼴이다.
→ 숫자 뒤에 **문종 조건**을 붙였다. 셀 수 있는 목표는 반드시 그 방향으로 갈 수 있는 목표여야 한다.

⚠️ **일반 규칙**: 이 문서의 해소 조건에 숫자만 있는 항목을 볼 때마다 물을 것 —
**"이 숫자를 가장 싸게 달성하는 방법이 제품을 낫게 하는가?"** 아니라면 조건이 틀린 것이다.

---

## 9. 수행 방법 (Claude Code)

```
/  이 문서를 첨부하고: "docs/PLATFORM_AUDIT.md 절차대로 정기 진단 수행"
```

절차: §3 DB 질의 → §4 저장소 계수 → §5 웹 재조사 → §6 산술 재계산 → §7 표에 행 추가 → §8 재확인
→ 결과는 아티팩트 리포트로 발행하고, 이 문서 §7 표와 `CHANGELOG.md` 에 한 줄씩 남긴다.

**주의** — 진단은 코드를 고치는 작업이 아니다. 같은 턴에 수정까지 하면 측정이 오염된다.
측정 → 기록 → (별도 결정) → 수정 순서를 지킨다. 단, §4 의 목업 수치 적발만은 예외로 즉시 제거한다.

---

*docs/PLATFORM_AUDIT.md — 최초 작성 2026-08-16. 다음 예정: 2026-10 첫 주.*
