# 하이라이트 동기 강의 TTS — 결정 기록

> 지시문: [docs/csat-lecture-tts-brief.md](../csat-lecture-tts-brief.md). 지시문이 "질문하지 않고 여기에 적는다"고 정했다.
> 한 줄 = 한 결정. **지시문과 다르게 간 것은 왜 다른지를 함께 적는다.**

| # | 날짜 | 결정 | 근거 |
|---|---|---|---|
| L1 | 09-17 | `/frontend-design` 스킬은 이 저장소에 없다 → 정본인 `vocaflow-design` 을 따른다 | CLAUDE.md 「UI 작업 전 그 스킬을 먼저 읽는다」. 외부 취향 스킬은 학습 화면에서 비활성(§1 라우팅) |
| L2 | 09-17 | `/csat/item/[slug]` 에는 「레이어 패널」이 없다(그건 `/csat/overlay` 의 것) → 재생 바를 **머리글 바로 아래**에 둔다 | 지시문 [D] 가 가리킨 자리가 이 화면에 존재하지 않는다. 대상 화면은 지시문이 명시한 `/csat/item/[id]` 다 |
| L3 | 09-17 | 대본은 **커밋된 회차 JSON**(`lib/csat/lecture-data/`)에 두고, 재생을 누른 뒤 `/api/csat/lecture` 로만 받는다 | 서버 렌더 props 로 넘기면 RSC 페이로드가 HTML 에 박혀 F2 가 깨진다. DB 에 두면 마이그레이션 승인이 끼고, 골격·앵커가 이미 같은 방식(커밋 JSON)이다 |
| L4 | 09-17 | 접근성 자막(A1 opt-in)은 **이번에 만들지 않는다** | 청각이 막힌 학습자에게 같은 내용이 이미 화면에 글로 있다(해설 블록 전부). 대본을 자막으로 켜는 길은 A1 의 「미렌더」 경계를 여는 일이라 수요가 확인된 뒤에 연다 |
| L5 | 09-17 | TTS 가 걸린 검사는 **진짜 Chrome·Edge 를 띄워 CDP 로 붙는다** | 실측: Playwright 가 띄운 브라우저는 headless·headed·channel 모두 `getVoices()` **0개**. 직접 띄우면 Chrome 19 · Edge 321 |
| L6 | 09-17 | Gate 0 PASS = 재 본 브라우저 **전부** 통과 | 한 브라우저만 통과하면 학습자 절반이 끊긴 강의를 듣는다 |
| L7 | 09-17 | 단어 단위 강조는 **만들지 않는다** | 지시문 전제(「Chrome 안정」)가 이 기기에서 틀렸다: Chrome(Google 한국의)은 `onboundary` **0회**, Edge 만 6회. 보너스를 위해 브라우저별 분기를 들이지 않는다 — 동기 단위는 큐 |
| L8 | 09-17 | 낭독 속도 계수 = 한국어 **초당 4.0음절** · 영어 초당 2.6단어 | Gate 0 실측 4.70(Chrome) · 3.88(Edge). 처음 어림 5.6 은 20~44% 빨랐다. 큐 상한 20초는 **느린 목소리에서도** 지켜져야 한다 |
| L9 | 09-17 | 큐 간 지연 = **들리는 초과 침묵**(다음 첫 소리 − 앞 마지막 소리 − 대본의 쉼)으로 잰다 | 처음 지표(붙든 시간 끝 → 첫 소리)는 Chrome 에서 396ms(한계 400 바로 밑)였다. 그건 네트워크 음성의 준비 시간이 쉼 위에 얹힌 것이다 |
| L10 | 09-17 | 엔진이 발화 준비 시간(이동평균, 상한 300ms)만큼 **다음 큐를 먼저 출발**시킨다 | 들리는 쉼 = 대본의 쉼. 적용 후 초과 침묵 Chrome ≤108ms · Edge ≤107ms(소리 켠 실행) |
| L11 | 09-17 | 멈춤 = **지금 문장 취소**, 재개 = **그 문장부터 다시** | `speechSynthesis.pause()` 가 음성마다 다르게 군다. 문장은 짧아(대개 3초) 한 문장 되풀이가 「어디까지 들었지」보다 낫다 |
| L12 | 09-17 | 한국어 조각에는 **아라비아 숫자·원문자·로마자를 남기지 않는다**. ①은 「1번」이 아니라 「일 번」 | 지시문 C5 는 ①→「1번」인데 C3 은 숫자 읽기 모호함을 금지한다. 「3번째」를 목소리가 「삼번째」로 읽는 것이 실제 위험이라, 검사를 「숫자 0」으로 단순화했다. 「세 번째」 같은 고유어 수는 쓰는 쪽이 한글로 쓴다 |
| L13 | 09-17 | `est_sec`·`total_sec_est` 는 **적재가 계산해 덮는다** | 쓰는 쪽(모델)의 어림을 믿으면 길이 게이트가 자기신고가 된다 |
| L14 | 09-17 | 소리 켠 실행을 본 결과로, 음소거 실행을 `mutedRun` 으로 함께 남긴다 | 음소거가 끊김 이슈를 가릴 수 있다. 둘 다 PASS, 40초 발화는 둘 다 안 끊겼다 |
| L15 | 09-17 | 원고는 **약식**(`{r, t, p, s}`)으로 쓰고 적재가 펼친다 | 802편을 쓸 사람의 토큰이 곧 비용이다. id·order·길이는 기계가 채운다 |
| L16 | 09-17 | 루브릭 80점 몫은 **매번 새 심사관**(독립 서브에이전트)이 매기고, 앞 채점 파일을 못 보게 한다 | 지시문 C6 「별도 호출」. 같은 심사관이 고친 글을 다시 보면 앞 점수에 닻을 내린다 |
| L17 | 09-17 | 심사관이 짚은 **사실 오류는 점수와 상관없이 고치고 다시 채점**한다 | 1차 6편 전부 85~89로 통과였지만 한 편에 지문과 어긋나는 말이 있었다. 통과가 오류를 덮으면 안 된다 |
| L18 | 09-17 | `pointing` 검사를 더했다 — 「여기 N번째 문장」이라 말하면 그 막대가 켜져 있어야 한다 | 2차 심사관이 한 건을 짚었고, 검사를 만들어 돌리니 **8건**이 나왔다(심사관이 본 것은 그중 2건). 사람 눈이 놓치는 종류라 기계가 막는다 |
| L19 | 09-17 | `stale-grade` 검사 — 심사관은 원고 **스냅숏**을 읽고, 적재는 스냅숏과 지금 원고가 같을 때만 점수를 인정한다 | 파일럿에서 세 번 고쳐 쓰는 동안 「고친 글에 옛 점수」가 붙을 틈이 실제로 있었다 |
| L20 | 09-17 | 「쉽죠·어렵죠」 금지를 **문제·문항에 대한 판정**으로 좁혔다 | 「속마음이 새면 읽기는 오히려 쉽죠」(지문 논리 풀이)가 걸렸다. 막을 것은 무근거 난이도 판정이다 |
| L21 | 09-17 | Gate 1 을 3회차에서 닫는다 — 사실 오류 0 · 전부 ≥ 80. 남은 지적(문장 번호 더 말하기·결정 근거 먼저)은 **작성 지침**으로 옮긴다 | 심사관을 바꿀 때마다 새 트집이 나온다(1차 67~70 · 2차 64~71 · 3차 68~73). 게이트 기준을 넘은 뒤의 반복은 파일럿 6편이 아니라 지침에 투자한다 |
| L22 | 09-17 | 분석 원고에도 오독이 있다(M2706#34 ④ 「시장을 밀어낸다」). 대본은 **지문에 대해** 심사한다 | 강의가 분석을 그대로 물려받으면 오류가 소리로 증폭된다. 분석 수정은 이번 범위 밖 — 원장에 남긴다 |
| L23 | 09-17 | 강의 대본은 **서버 렌더에 길이(초)만** 싣고, 무대(`LectureStage`)가 재생을 누른 뒤 API 로 받는다. 블록은 서버가 그리고 무대가 `data-lecture-state` 만 단다 | 블록을 전부 클라이언트로 바꾸면 해설 HTML(I6)이 무너진다. 상태를 DOM 속성으로 두면 CSS 한 벌로 active·path·inside·dim 이 선다(바깥 블록이 옅어지면 안의 active 까지 옅어지는 문제를 path 가 막는다) |
| L24 | 09-17 | 접힌 층(`<details>`) 안을 가리키면 무대가 **편다** | 닫힌 채 빛나면 학습자는 아무것도 못 본다 |
| L25 | 09-17 | 재생 바는 **재생 중에만** 위에 붙는다(sticky). 눈금 줄만 가로로 넘친다 | 평소에는 해설을 가리지 않는다. 큐 14개 × 44px 은 375px 에 안 들어간다 — 본문이 아니라 그 줄만 넘긴다 |
| L26 | 09-17 | 계측 2종(`csat_lecture_played` · `csat_lecture_ended`) + 허용 목록 마이그레이션 `20260917150000` 을 **같은 커밋**에. 적용은 승인 뒤 | 앞 사고(보내는데 안 쌓임)의 재발 방지. 마이그레이션 자동 적용 금지(CLAUDE.md) |
| L27 | 09-17 | **타이밍을 재는 동안에는 `src/` 를 고치지 않는다.** 하네스는 브라우저마다 따로 구운 세션을 쓴다 | 1차 Gate 2 실행이 6/6 「실행 문맥 파괴」로 죽었다 — 재생 중 `speakable.ts` 를 고쳐 dev 서버가 화면을 다시 불렀다. 화면 탓이 아니라 측정 탓이었다 |
| L28 | 09-17 | Gate 3 은 **두 번의 다른 호출**로 잰다 — ① 학습자 역할은 지문·들리는 말·켜지는 자리만 받고(정답표·분석 없음) 설명을 쓴다 ② 심사 역할이 그 설명을 정답표·지문과 대조해 항목별 0~100 | 「이 대본만 듣고 설명할 수 있는가」를 한 호출이 채점하면 그 호출은 이미 정답을 안다. 모르는 쪽이 써야 완결성이 잰다 |
| L29 | 09-17 | Gate 4 는 서브에이전트 드레인 — 작성자가 **스스로 기계 검사를 통과시키고** 넘긴다(`WRITER.md`), 심사관은 작성자 둘 몫을 한 번에(`GRADER.md`). 청크 12문항 · 동시 작성 5 | 802편 × (읽기 + 쓰기 + 별도 채점)은 토큰이 곧 한도다. 들여쓰기 없는 청크 JSON 으로 읽는 비용을 줄였다 |
| L30 | 09-17 | **Gate 3 PASS(86.1) → 전체 적용 승인.** 학습자 역할이 강의만 듣고 6/6 정답을 맞혔다 | 통과선 85 에 1.1점 차이라 얇다. 심사가 짚은 구멍(근거↔정답 낱말 짝짓기 생략 · 논리 한 칸 건너뛰기 · 순환 배제 · 공식이 제 문항의 결정 단서와 어긋남)을 `WRITER.md` 에 옮기고 Gate 4 에 들어간다 |
| L31 | 09-17 | M2706#34 ④ 의 해석 차이(「첫 문장이 시장을 밀어낸다」)는 **판정하지 않고 남긴다** | 대본 심사관 한 명은 오독, 완결성 심사관은 더 강한 근거라 했다. 분석 원고도 후자다. 한 호출의 판단으로 다른 호출을 뒤집지 않는다 — 분석 원장을 다시 볼 때 사람이 가른다 |
| L32 | 09-17 | Gate 2 쉼 검사는 **엔진이 그때 정한 붙들기 시간**(`cue-end.holdMs`)과 견준다 | 처음에는 강의가 끝난 뒤의 앞당김 값으로 쟀다. Edge 에서 앞당김(이동평균)이 강의 중 약 280 → 170ms 로 내려가 앞쪽 큐가 −100ms, 뒤쪽이 +100ms 로 보였다 — 엔진이 아니라 자가 틀렸다 |
| L33 | 09-17 | 붙들기를 **100ms 틱 → 타이머 하나**로 바꿨다(멈추면 남은 시간을 떼어 두고 재개 때 다시 건다) | 자를 고친 뒤에도 한 큐가 +157ms 였다 — 이건 엔진이었다. 틱이 남은 시간을 올림해 쉼이 최대 100ms + 지터만큼 길어졌다. 회귀 2종 추가 |
| L34 | 09-17 | 발화가 감시 시한을 넘기면 **끊고 한 번 더** 읽는다. 두 번째도 막히면 오류. 살린 것은 `recovered` 로 리포트에 남긴다 | Gate 2 3차: Edge 온라인 음성의 발화 하나가 끝 신호를 안 줬다. 감시 시한이 강의 멈춤은 막았지만 그 발화를 끊지 않아(다음 발화와 겹칠 수 있다) 다시 읽지도 않았다(문장이 빠진다). 학습자 망에서도 일어날 일이라 게이트가 아니라 엔진을 고친다 |
| L35 | 09-17 | 재채점은 **고친 문항만**, 별도 파일(`chunk-X.regrade.json`)에 쓰고 `merge-regrade.mts` 가 합친다 | 재채점 심사관이 기존 grade.json 을 열면 앞 판정에 닻을 내린다(L16). 따로 쓰게 하면 열 이유가 없다. 합친 파일은 `.regrade.merged.json` 으로 옮겨 두 번 합치지 않는다 |
| L36 | 09-17 | 재작업 뒤에도 **큐 한두 개의 사실 오류**로 떨어진 문항은 표적 수정을 **한 번 더** 한다. 그래도 떨어지면 `failed.json` 으로 | C6 의 「재생성 1회」는 대본 전체를 다시 쓰는 것이다. 지금 떨어지는 것은 대부분 한정어 하나(can·often)다 — 이것 때문에 문항을 버리면 F5(≥95%)가 무너지고, 고치는 비용은 한 줄이다. 전체 재생성 횟수는 여전히 1회 |
| L37 | 09-17 | 마이그레이션 `20260917150000`(csat_lecture_played·ended) **적용** — 사용자 승인 | 적용 전에는 강의 계측이 수신구에서 204 로 조용히 버려졌다. 적용 뒤 제약에 두 이름이 있음을 확인 |
| L38 | 09-17 | 작가 지침에 **떨어진 풀이 사례**를 계속 더한다(부재 단정 · 한정어 삭제 · 칸 수 셈). 진행 중인 작가에게는 메시지로 알린다 | Gate 4 사실 오류의 대부분이 번호가 아니라 풀이였다. 삽입 청크 두 개에서 7건이 「앞에 없다」, 요지 청크 하나에서 7건이 한정어 삭제 — 같은 실수가 청크마다 되풀이됐다 |
| L39 | 09-17 | 큐에 **말한 문장 번호**(`focus`)를 싣고, 지도가 켜려던 막대에 그 번호가 없으면 **말한 막대를 켠다**(`lecture/focus.ts`). 도입·전략·정리 블록을 가리키는 큐는 제외 | 실측: 색인 775 분석큐 중 145큐(120문항)가 「N번째 문장」이라 말하며 다른 막대를 켰다 — A3(말과 화면이 한 곳)의 구조적 구멍. 배제 큐는 규칙상 오답 칩을 가리켜야 해서 타깃을 바꿀 수 없다. 판정은 **그릴 때** 한다(골격이 나중에 바뀌어도 어긋나지 않게). 기존 568강의는 `backfill-focus.mts` 로 채움(2,164큐, 막대가 달라지는 큐 1,257 — 대부분 지도 전체를 가리키는 구조 큐). 두 번 돌려 0 — 재실행 안전 |
| L40 | 09-17 | 표적 수정을 두 번 받고도 사실 오류가 남은 문항은 **실패 확정**(`failed.json`) — 2023#31 · 2024#37 · M2409#36 · M2606#20 · M2406#20 | L36 의 한도. 다섯 모두 큐 한 곳의 한정어·위치 말이라 사람이 한 줄 고치면 살지만, 같은 심사를 세 번 거친 뒤 계속 고치면 「통과할 때까지 굴린다」가 된다 |
