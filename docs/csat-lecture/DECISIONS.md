# 하이라이트 동기 강의 TTS — 결정 기록

> 지시문: [docs/csat-lecture-tts-brief.md](../csat-lecture-tts-brief.md). 지시문이 "질문하지 않고 여기에 적는다"고 정했다.
> 한 줄 = 한 결정. **지시문과 다르게 간 것은 왜 다른지를 함께 적는다.**

| # | 날짜 | 결정 | 근거 |
|---|---|---|---|
| L1 | 09-17 | `/frontend-design` 스킬은 이 저장소에 없다 → 정본인 `vocaflow-design` 을 따른다 | CLAUDE.md 「UI 작업 전 그 스킬을 먼저 읽는다」. 외부 취향 스킬은 학습 화면에서 비활성(§1 라우팅) |
| L2 | 09-17 | `/csat/item/[slug]` 에는 「레이어 패널」이 없다(그건 `/csat/overlay` 의 것) → 재생 바를 **머리글 바로 아래**에 둔다 | 지시문 [D] 가 가리킨 자리가 이 화면에 존재하지 않는다. 대상 화면은 지시문이 명시한 `/csat/item/[id]` 다 |
| L3 | 09-17 | 대본은 **커밋된 회차 JSON**(`lib/csat/lecture-data/`)에 두고, 재생을 누른 뒤 `/api/csat/lecture` 로만 받는다 | 서버 렌더 props 로 넘기면 RSC 페이로드가 HTML 에 박혀 F2 가 깨진다. DB 에 두면 마이그레이션 승인이 끼고, 골격·앵커가 이미 같은 방식(커밋 JSON)이다 |
| L4 | 09-17 | 접근성 자막(A1 opt-in)은 **이번에 만들지 않는다** | 청각이 막힌 학습자에게 같은 내용이 이미 화면에 글로 있다(해설 블록 전부). 대본을 자막으로 켜는 길은 A1 의 「미렌더」 경계를 여는 일이라 수요가 확인된 뒤에 연다 |
| L5 | 09-17 | TTS 가 걸린 검사는 **진짜 Chrome·Edge 를 띄워 CDP 로 붙는다** | 실측: Playwright 가 띄운 브라우저는 headless·headed·channel 모두 `getVoices()` **0개**. 직접 띄우면 Chrome 19 · Edge 321 |
| L6 | 09-17 | Gate 0 PASS = 재 본 브라우저 **전부** 통과 | 한 브라우저만 통과하면 학습자 절반이 끊긴 강의를 듣는다 |
| L7 | 09-17 | 단어 단위 강조는 **만들지 않는다** | 지시문 전제(「Chrome 안정」)가 이 기기에서 틀렸다: Chrome(Google 한국의)은 `onboundary` **0회**, Edge 만 6회. 보너스를 위해 브라우저별 분기를 들이지 않는다 — 동기 단위는 큐 |
| L8 | 09-17 | 낭독 속도 계수 = 한국어 **초당 4.0음절** · 영어 초당 2.6단어 | Gate 0 실측 4.70(Chrome) · 3.88(Edge). 처음 어림 5.6 은 20~44% 빨랐다. 큐 상한 20초는 **느린 목소리에서도** 지켜져야 한다 |
| L9 | 09-17 | 큐 간 지연 = **들리는 초과 침묵**(다음 첫 소리 − 앞 마지막 소리 − 대본의 쉼)으로 잰다 | 처음 지표(붙든 시간 끝 → 첫 소리)는 Chrome 에서 396ms(한계 400 바로 밑)였다. 그건 네트워크 음성의 준비 시간이 쉼 위에 얹힌 것이다 |
| L10 | 09-17 | 엔진이 발화 준비 시간(이동평균, 상한 300ms)만큼 **다음 큐를 먼저 출발**시킨다 | 들리는 쉼 = 대본의 쉼. 적용 후 초과 침묵 Chrome ≤108ms · Edge ≤107ms(소리 켠 실행) |
| L11 | 09-17 | 멈춤 = **지금 문장 취소**, 재개 = **그 문장부터 다시** | `speechSynthesis.pause()` 가 음성마다 다르게 군다. 문장은 짧아(대개 3초) 한 문장 되풀이가 「어디까지 들었지」보다 낫다 |
| L12 | 09-17 | 한국어 조각에는 **아라비아 숫자·원문자·로마자를 남기지 않는다**. ①은 「1번」이 아니라 「일 번」 | 지시문 C5 는 ①→「1번」인데 C3 은 숫자 읽기 모호함을 금지한다. 「3번째」를 목소리가 「삼번째」로 읽는 것이 실제 위험이라, 검사를 「숫자 0」으로 단순화했다. 「세 번째」 같은 고유어 수는 쓰는 쪽이 한글로 쓴다 |
| L13 | 09-17 | `est_sec`·`total_sec_est` 는 **적재가 계산해 덮는다** | 쓰는 쪽(모델)의 어림을 믿으면 길이 게이트가 자기신고가 된다 |
| L14 | 09-17 | 소리 켠 실행을 본 결과로, 음소거 실행을 `mutedRun` 으로 함께 남긴다 | 음소거가 끊김 이슈를 가릴 수 있다. 둘 다 PASS, 40초 발화는 둘 다 안 끊겼다 |
