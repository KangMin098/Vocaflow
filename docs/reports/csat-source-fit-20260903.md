# 「적합 원문 5만」 — 세는 자를 바꾸자 재고가 14,252편에서 4,161편이 됐다

**실측일 2026-09-03** · 도구 `scripts/csat/topic-gap.mjs` · `scripts/csat/lib-topic.mjs`
· 근거 `library_articles` 실질의 · 기출 302지문(`measure-topic.mjs`)

---

## 1. 결론부터

**목표를 「`csat_fit.pass > 0` 인 원문 5만 편」으로 세면 28.5% 왔고, 「기출 배합을 지키며 쓸 수
있는 편수 5만」으로 세면 8.3% 왔다.** 뒤가 옳은 자다.

| 세는 법 | 현재 | 1단계 1만 | 2단계 3만 | 3단계 5만 |
|---|---|---|---|---|
| 적합 원문 (모양+담화) | 14,252 | 달성 142.5% | 47.5% | 28.5% |
| **균형 사정권** (+ 소재 배합) | **4,161** | **41.6%** | 13.9% | **8.3%** |

`score-articles.mjs` 는 **모양(어수·문장길이·낱말길이)과 담화(연결사·지시어)** 만 잰다 —
그 파일이 스스로 밝혀 둔 계약이다. 소재는 안 잰다. 그래서 `pass > 0` 을 「적합」이라 부르면
**절반만 말한 것**이고, 교재를 실제로 조립할 때 비는 자리는 그 나머지 절반에서 나온다.

---

## 2. 소재 격차 — 우리 재고는 이과 편중이다

기출 302지문의 소재 배합(`measure-topic.mjs`, 회차×소재 독립 · 순열검정 p=0.26 →
**고정 배합**)을 목표로 두고, 적합 원문 표본 3,000편에 **같은 분류기**를 댄 결과:

| 소재 | 기출% | 재고% | 배율 | 추정 재고 |
|---|---|---|---|---|
| 과학·자연 | 31.6 | 42.0 | 1.33 | 5,991 |
| 심리·인지 | 15.8 | 10.1 | 0.64 | 1,439 |
| 사회·경제 | 15.4 | 11.5 | 0.74 | 1,634 |
| 기술·매체 | 7.4 | 23.4 | **3.18** ← 과잉 | 3,340 |
| 예술·문화 | 14.0 | 5.9 | **0.42** ⚠️ | 846 |
| 역사·인류 | 3.5 | 1.1 | **0.30** ⚠️ | 152 |
| 교육·언어 | 7.0 | 4.4 | 0.63 | 627 |
| 철학·윤리 | 5.3 | 1.5 | **0.29** ⚠️ | 219 |

**균형 사정권 = min_t (재고_t / 목표비율_t) = 4,161편.** 병목은 **철학·윤리**(219편).
5만 편이 있어도 전부 생물학이면 기출 배합으로 교재를 짤 수 없다 — 병목 소재가 전체를 정한다.

### 소재를 정하는 것은 소스다

| 소스 | 표본 | 과학 | 심리 | 사회 | 기술 | 예술 | 역사 | 교육 | 철학 |
|---|---|---|---|---|---|---|---|---|---|
| plos | 2,374 | 36% | 11% | 13% | 27% | 7% | 1% | 4% | 2% |
| futurity | — | 67% | 13% | 6% | 10% | 1% | 2% | 1% | 1% |
| usgs | — | 92% | 0% | 0% | 6% | 0% | 0% | 3% | 0% |
| elife | — | 93% | 0% | 0% | 0% | 0% | 0% | 7% | 0% |

**현재 배선된 소스 중 인문 소재를 내는 곳이 하나도 없다.** PLOS 를 아무리 더 긁어도
철학·윤리 219편은 안 늘고, 따라서 **균형 사정권도 안 는다.** 지금까지 "적합 원문이 늘었다"
고 세던 것은 이미 넘치는 칸을 더 채운 것이었다.

---

## 3. 위키백과로는 못 메운다 — 소재 게이트를 붙여 봤고, 실패했다

`score-articles.mjs` 는 위키백과를 **모양 적합률 94.6% 로 전 소스 1위**라 재 놓고도
"소재가 수능이 아니다"(*Judge (sumo)* · *True Blue (album)*)라며 대량 확보를 보류했다.
그 보류가 **소재 분류기를 게이트로 쓰면 풀리는지** 실측했다 — 풀리지 않는다.

재고 위키백과 92편(모양적합 87)에 `classify()` 를 대고 확신도(1위-2위 득점 차)로 걸러 보면:

```
margin  소재        제목
  29    예술·문화   Serenity (Clara)
  26    예술·문화   The Dawn of Love (painting)
  23    예술·문화   Status Update Music
  17    예술·문화   True Blue (album)        ← 앨범이다. 수능 예술 지문이 아니다
```

**확신도가 높을수록 오히려 앨범·곡·인물 문서가 올라온다.** 수능의 「예술·문화」는 미학·예술론·
문화인류학이지 디스코그래피가 아닌데, 키워드 분류기는 둘을 구분하지 못한다(`art`·`music`·
`album` 이 같은 칸에 있다). margin 20 이상으로 조여도 87 → 7편이고 그 7편에 *True Blue* 가 남는다.

→ **기존 보류 판단은 옳다. 되돌리지 말 것.** 이 실측을 여기 적어 두는 이유가 그것이다.

---

## 4. 병목을 실제로 메울 수 있는 곳 — OpenStax (CC BY 4.0)

OpenStax 공개 API(`/apps/cms/api/v2/pages/?type=books.Book`, 실측 129권)에 병목 소재가 그대로 있다:

| 병목 | OpenStax 교재 |
|---|---|
| 철학·윤리 (배율 0.29) | Introduction to Philosophy · Business Ethics |
| 역사·인류 (0.30) | World History Vol 1 (to 1500) · Vol 2 (from 1400) · U.S. History · Introduction to Anthropology |
| 사회·경제 (0.74) | Introduction to Sociology (3판) · Introduction to Political Science |
| 심리·인지 (0.64) | Psychology · Psychology 2e |

라이선스는 **CC BY 4.0** — `license_class=cc_by` 로 발행 가능한 등급이고, 결이 **교과서 설명문**
이라 수능 지문의 사용역과 맞는다(PLOS 처럼 방법·통계 절 노이즈가 없다).

**아직 안 메워지는 칸: 예술·문화(0.42).** OpenStax 에 미술사 교재가 없다. Smarthistory 는
CC BY-**NC**-SA 라 제외된다. 이 칸은 다음 사이클에서 따로 재야 한다 — 지금 답이 없다고 적어 둔다.

---

## 5. PLOS 를 5만 편까지 긁는 비용은 문제가 아니었다

부수적 실측 하나. PLOS Solr 는 `fl=body` 로 **본문 전문을 목록 응답에 그대로 준다**
(실측: 1건 30,429자 · `numFound` **399,344**). 지금 경로는 목록을 받은 뒤 글마다 HTML 을
다시 GET 하는데, 그럴 필요가 없다 — 한 요청에 50편씩 본문째로 온다.

즉 **PLOS 쪽 공급과 속도는 병목이 아니다.** 병목은 §2 의 소재 배합이다. 이 발견은 다음
사이클에서 「본문을 받아 그 자리에서 채점하고, 통과한 것만 적재」하는 경로(= 적재 시점
적합도 100%)로 쓴다.

---

## 6. 이번에 바꾼 것

- `scripts/csat/lib-topic.mjs` **신설** — 소재 분류표를 `measure-topic.mjs` 에서 꺼냈다.
  기출과 재고에 **같은 자**를 대야 격차가 격차로 읽힌다(복사본이 있으면 한쪽만 고쳐졌을 때
  분류표 차이를 격차로 오독한다). 리팩터 동치 검증: 기출 302지문 **불일치 0**.
- `scripts/csat/topic-gap.mjs` **신설** — 위 표와 균형 사정권을 낸다. 읽기 전용.
- `scripts/csat/data/topic-distribution.json` 갱신 — 기출 코퍼스가 300 → 302지문으로 늘어난 값.

---

## 7. 3단계 5만까지 소재별 소요량 — 어디서 몇 편이 모자란가

배율(§2)은 **지금 재고 안의 비율**이라 "과학은 과잉" 으로 읽히지만, 5만 편 규모로 늘리면
**절대 부족분은 과학·자연이 가장 크다.** 두 값을 같이 보지 않으면 "PLOS 는 그만 긁어도 된다"
는 잘못된 결론이 나온다. 목표 배합 × 50,000 − 추정 재고:

| 소재 | 목표(50,000) | 추정 재고 | **부족** | 공급선 |
|---|---|---|---|---|
| 과학·자연 | 15,800 | 5,991 | **9,809** | PLOS(`numFound` 399,344) · futurity · usgs · elife ✅ |
| 심리·인지 | 7,900 | 1,439 | **6,461** | PLOS · futurity · OpenStax Psychology ✅ |
| 예술·문화 | 7,000 | 846 | **6,154** | ⚠️ §9 참조 |
| 사회·경제 | 7,700 | 1,634 | **6,066** | PLOS(pgph) · OpenStax Sociology·Political Science ✅ |
| 교육·언어 | 3,500 | 627 | **2,873** | ⚠️ 부분 |
| 철학·윤리 | 2,650 | 219 | **2,431** | OpenStax Philosophy·Business Ethics(§8) — 일부만 |
| 역사·인류 | 1,750 | 152 | **1,598** | OpenStax World History·Anthropology(§8) — 일부만 |
| 기술·매체 | 3,700 | 3,340 | 360 | 이미 거의 찼다 — **상한을 걸어야 한다** |

**PLOS 는 계속 긁어야 한다. 다만 31.6% 상한을 걸고.** 지금까지 상한이 없어 기술·매체가
배율 3.18 까지 부풀었다.

---

## 8. OpenStax 실측 — 붙일 만하지만 병목을 다 메우지는 못한다

`scripts/csat/probe-openstax.mjs` (적재 없음 · 재고 채점과 **같은 자** `lib-fit.mjs`).
REX 가 본문을 서버렌더하므로 페이지 HTML 에서 문단을 꺼낼 수 있다. 책당 14쪽 표본:

| 교재 | 전체 쪽 | 적합률 | 추정 적합 | 그중 목표칸 |
|---|---|---|---|---|
| Introduction to Philosophy | 135 | 36% | 48 | 철학 39 |
| World History, Volume 1 | 207 | 50% | 104 | 역사 41 |
| Introduction to Anthropology | 219 | 50% | 110 | 역사 16 |
| Business Ethics | 122 | 67% | 81 | 철학 31 |
| **합계 4권** | 683 | — | **343** | **127** |

인문 계열 교재를 다 붙여도 **1,000편 안쪽**이다. §7 의 인문 3칸 부족분 10,183편의 **약 10%**.
→ **필요하지만 충분하지 않다.** 붙이되, 이것만으로 됐다고 세지 말 것.

⚠️ 소재 판정에서 **예술·문화가 28/50 으로 과다**하게 나왔다 — `culture`·`tradition`·`art` 가
인류학·세계사 산문에 흔해서다. 분류기의 알려진 약점이고(`lib-topic.mjs`), 같은 편향이 기출
쪽에도 들어 있다. 그래서 §2 의 「예술·문화 14.0%」도 순수 예술론만은 아니다 — 두 값을
견주는 데는 성립하지만, **"예술론 지문 7,000편이 필요하다" 로 읽으면 과잉 해석**이다.

---

## 9. 예술·문화·인문 대량 공급선 — 타진했고, 쉬운 길은 없다

| 후보 | 결과 | 판정 |
|---|---|---|
| **DOAJ API** | 200 · philosophy **184,078** · history **447,918** · Fine Arts 45,841 · education 660,696 | 색인은 열려 있다. 그러나 전문 링크 25편이 **약 20개 호스트로 흩어지고**(czasopisma.kul.pl · hrcak.srce.hr · teorievedy.flu.cas.cz …) 저널 언어가 **EN 38 / ES 20 / PT 12 / DE 9 …** 로 섞인다. 호스트마다 추출기가 필요하고 언어 게이트도 있어야 한다 — **가능하나 비싸다** |
| **MDPI** (Arts · Humanities · Philosophies · Religions) | **403** (Cloudflare) | CC BY 전문이지만 **막혀 있다.** 우회하지 않는다 |
| **Open Library of Humanities** | 200 (93KB) | 열려 있다 — 다음 사이클에서 볼륨·적합률 측정 |
| Smarthistory | — | CC BY-**NC**-SA → 제외 |
| Gutenberg(gutendex) | 연결 실패 | PD 이나 결이 고문이라 후보에서 낮다 |

---

## 10. 적재 전 채점 경로 — `harvest-plos.mjs` (2026-09-03 추가)

「적합도 100%」를 자를 좋게 만들어 얻는 게 아니라 **버릴 것을 담지 않아서** 얻는다.
받아들이는 모든 행이 `csat_fit.pass > 0` 이고 **몫이 남은 칸에만** 들어간다.

| | 지금 경로 | 적재 전 채점 |
|---|---|---|
| 적재 후 채점률 | 58% (24,870 중 14,252) | **100%** |
| 배합 준수 | 없음 → 기술·매체 배율 3.18 | 칸별 몫 상한 |
| GET 횟수 | 글마다 1회 | 50편에 1회 (`fl=body`) |

### 실측으로 잡은 함정 둘

**① `sort=id asc` 로 훑으면 적합률이 2.5% 가 된다.** cursorMark 는 유일 정렬키만 요구해서
`id asc` 가 되기는 하는데, PLOS 의 id 는 DOI 라 오름차순 머리에 `10.1371/annotation/…` 과
issue image 가 몰린다(표본 제목이 *PLoS Computational Biology Issue Image*).
`publication_date desc,id desc` 로 바꾸자 같은 질의가 **62.5%** 가 됐다. 25배다.

**② 질의한 칸으로 안 떨어진 글을 버리면 순 수율이 22.5% 로 주저앉는다.** Psychology 질의
200편 중 적합 125편인데 그중 심리·인지 판정은 45편뿐이고, 나머지 80편도 **전부 적합한 글**이다
(기술·매체 31 · 과학·자연 26 · 교육·언어 10 · 사회·경제 9 …). 자기 칸에 몫이 남아 있으면
담도록 바꿔 **62.5%** 로 되돌렸다.

⚠️ 몫을 훑는 자리에서 올리므로, **중복이라 안 들어간 글의 몫은 돌려줘야 한다** — 안 돌려주면
그 칸이 실제보다 찬 것으로 세어져 몫이 영영 안 찬다.

### PLOS 가 채울 수 있는 몫 (3단계 기준)

| | 편수 |
|---|---|
| PLOS 로 채울 수 있는 몫 | **22,683** (과학 9,798 · 심리 6,456 · 사회 6,085 · 기술 344) |
| **다른 소스가 필요한 몫** | **13,069** (예술 6,172 · 교육 2,882 · 철학 2,413 · 역사 1,602) |

상류 총량은 넉넉하다 — Psychology∪Neuroscience 만 **97,038편**, 전체 399,344편.

### 파일럿 56편 — 경로가 끝까지 통했다 (2026-09-03, DB 복구 후)

| 항목 | 값 |
|---|---|
| 훑음 / 적재 | 600편 / **56편** |
| **적재된 것 중 적합** | **56 / 56 = 100%** ← 「적합도 100%」의 실증 |
| 재고 변화 | 24,870 → 24,926 · 적합 14,252 → **14,308** |
| `library_articles` | 353,656,832 → 355,123,200 B |

**⚠️ 앞서 적은 저장 용량 수치가 틀렸다. 실측으로 바로잡는다.**

- DB 전체는 **5,315 MB** 다. "최근 606 → 350MB 회수" 라 적은 것은 `library_articles`
  **테이블**(337 MB) 이야기였고 DB 전체가 아니었다.
- 원문 1편의 실제 비용은 본문만이 아니다:

| 항목 | 편당 | 근거 |
|---|---|---|
| 본문 (`library_articles`) | **26.2 KB** | 파일럿 실측 (+1,466,368 B ÷ 56편) |
| 파생 어휘 (`library_article_vocabularies`) | **92.3 KB** | 11,015,221행 ÷ 19,516편 = 편당 564행 |
| **합계** | **약 118 KB** | |

→ 22,683편이면 본문만 **594 MB**, 어휘까지 하면 **약 2.6 GB** 다. 앞서 적은 680 MB 는
**본문만 세고 어휘를 빠뜨린 값**이라 3.8배 틀렸다.

**다만 저장소가 이미 이 문제를 풀어 뒀다.** `process-queue.mjs --fit-only` 가
「수집 → 채점 → **적합만** 분석」 순서를 만들고(2026-08-30 도입), `prune-vocab-sentences.mjs`
가 `first_sentence` 사본을 축출한다(heap 의 71%, 되살릴 수 있는 캐시). 우리 수확은
**적재 시점에 이미 100% 적합**이라 그 순서에 그대로 들어맞는다 — 본문은 `queued` 로 들어가고,
어휘는 실제로 교재에 쓸 때 뽑으면 된다. **보관(594 MB)과 분석(2 GB)은 분리된 결정이다.**

### 중복 벽 — 최근 창은 이미 덮여 있다

파일럿에서 600편 중 **326편(54%)이 이미 있는 글**이었다. 기존 PLOS 재고 19,332편 중
**18,082편이 2025년 이후**라 최근 창이 촘촘하기 때문이다. 상류 399,344편의 대부분은 앞쪽에 있다.

`--until 2024-06-01` 로 창을 옮기자 **적합률 63.7% → 88%**. 오래된 글이 더 잘 통과하기까지 한다.

⚠️ 커서 키에 날짜창을 넣었다 — 창이 다르면 결과 집합이 달라서, 같은 키를 쓰면 다른 창의
커서를 물려받아 **엉뚱한 자리부터 훑는다**(오류가 안 나서 안 보인다).

---

## 11. OLH 실측 — 품질은 최고, 볼륨은 부족 (2026-09-03)

`scripts/csat/probe-olh.mjs`. OAI-PMH 가 열려 있어 **전량 열거가 된다**(경로는 `/api/oai/` —
`/oai` 는 404 를 HTML 로 내서 "OAI 가 없다" 로 오독하기 쉽다).

| | |
|---|---|
| 저널 | 14개 (OAI 열림 13~14, 실행마다 1개 타임아웃) |
| 전량 | **3,159~3,309편** |
| **적합률** | **90%** (9/10) — 측정한 모든 소스 중 최고 |
| **쓸 수 있는 비율** | **42%** — 24편 중 13편이 서평·에디토리얼(153~1,076어), 1편이 **cc-by-nc** |
| 소재 | 역사·인류 3 · 예술·문화 3 · 사회·경제 2 · 기술·매체 2 |

**기여 추정 약 1,184편** (역사 355 · 예술 355 · 사회 237 · 기술 237).

두 가지를 기록해 둔다:

- **적합률만 보면 100% 로 보인다.** 처음 12편을 쟀을 때 12/12 였다. 표본을 24편으로 넓히자
  서평이 들어오면서 75% 가 됐고, 서평을 빼자 90% 가 됐다. **소스가 나쁜 게 아니라 지문이 아닌
  글을 표본에 넣은 것**이었다 — 어수(1,500) 게이트로 가른다.
- **OLH 가 전부 CC BY 는 아니다.** 표본에 `cc-by-nc` 가 1편 있었다(*Black Feminisms in Roman
  Archaeology*, 12,888어 — 어수만 보면 최상급 후보다). 소스 단위로 "CC BY" 라 적어 두면 NC 가
  섞여 들어온다. **글마다 확인해야 한다.**

---

## 12. 인문 결손은 아직 안 풀렸다 — 정직한 합계

| 공급선 | 인문 기여(추정) |
|---|---|
| OpenStax (CC BY 4.0) | ~800–1,000 |
| OLH (CC BY, NC 제외) | ~1,184 |
| **합계** | **~2,000–2,200** |
| **필요** | **13,069** |
| **남는 결손** | **약 11,000편 (84%)** |

**PLOS 로 22,683편은 길이 뚫렸지만(§10), 인문 13,069편 중 16%밖에 못 메웠다.**
이것이 5만 목표의 실제 병목이고, 다음 사이클이 여기에 매달릴 자리다.

### 다음에 시험할 가설 — OJS 단일 추출기

§9 에서 DOAJ 전문 링크가 "25편에 약 20호스트" 로 흩어져 비싸다고 적었는데, 그 호스트들을 다시
보면 `hrcak.srce.hr` · `czasopisma.kul.pl` · `revistas.ups.edu.ec` · `periodicos.unifesp.br` 로
**전부 OJS(Open Journal Systems)** 다. OJS 는 HTML 구조와 OAI 경로가 규격이라, 호스트가 20개여도
**추출기는 하나면 될 수 있다.** 이게 맞으면 인문 결손 11,000편의 길이 열리고, 틀리면 그때
목표 배합 자체를 사용자와 다시 정해야 한다. **먼저 시험한다.**

---

## 13. OJS 단일 추출기 가설 — **시험했고, 틀렸다** (2026-09-03)

`scripts/csat/probe-ojs.mjs`. DOAJ "philosophy" **184,020편**에서 24편을 열어 봤다.

| 물음 | 결과 |
|---|---|
| ① OJS 비율 | 7/12 **58%** — 가설의 이 부분은 맞다 |
| ② **HTML 전문** | **2/12 (17%)** — 나머지는 **PDF 뿐** ← 여기서 막힌다 |
| ③ 영어 | 3/12 **25%** |
| ④ 채점 가능분의 적합 | 1/1 (표본이 1편이라 수치가 아니다) |
| **순 수율** | **1/12 = 8%** |

그리고 **24편 중 12편이 아예 안 열렸다** — `403`(MDPI·onomazein) · `418`(hrcak) · `404` ·
`500` · TLS 오류 · 타임아웃. 호스트 21개에 실패 방식이 제각각이다.

**가설이 깨진 지점은 OJS 여부가 아니라 「HTML 전문」이었다.** OJS 는 규격이 맞지만
대부분의 인문 저널이 **PDF 갤리만** 낸다. 추출기를 하나로 만들어도 뽑을 HTML 이 없다.
거기에 영어 25% 와 호스트별 차단이 겹치면 **8%** 다 — 1편을 얻으려 12번 두드린다.

→ **DOAJ/OJS 는 인문 대량 공급선이 아니다.** §9 의 "가능하나 비싸다" 를
   **"실측했고, 안 된다"** 로 바꾼다.

---

## 14. 그래서 5만은 지금 공급선으로 도달할 수 없다 — 천장을 계산했다

균형 사정권은 병목 소재가 정한다. 확보 가능한 인문 재고를 다 더해도:

| 소재 | 현재 | + OpenStax | + OLH | 합계 | 목표비율 | **그 칸이 허용하는 총량** |
|---|---|---|---|---|---|---|
| 철학·윤리 | 219 | ~400 | ~155 | ~774 | 5.3% | 14,604 |
| 역사·인류 | 152 | ~400 | ~355 | ~907 | 3.5% | 25,914 |
| **예술·문화** | **846** | 0 | **~355** | **~1,201** | **14.0%** | **8,579** ← 병목 |
| 교육·언어 | 627 | ~100 | ~310 | ~1,037 | 7.0% | 14,814 |

**천장 ≈ 8,579편.** 1단계 10,000 에도 14% 모자란다. 과학·기술 쪽은 PLOS 로 얼마든지
늘릴 수 있지만 **예술·문화가 전체를 잠근다.** OpenStax 에 미술사 교재가 없고, Smarthistory 는
NC 고, MDPI 는 403 이고, DOAJ 는 PDF 다.

### 남은 길은 셋이고, 고르는 것은 사용자다

1. **배합 요건을 푼다** — 소재를 안 맞추면 `pass > 0` 기준으로 5만은 PLOS 만으로 도달한다
   (상류 399,344편). 대신 그 5만으로는 기출 배합의 교재를 못 짠다.
2. **목표를 천장에 맞춘다** — 균형 8,579편(≈1단계)까지 하고 멈춘다. 지금 공급선의 정직한 상한.
3. **예술·문화 지문을 짓는다** — 저장소가 이미 그 구조를 갖고 있다(`article_compose_*` ·
   `original` 524편 · CLAUDE.md §🤖 "LLM 작업 = Claude Code 배치"). 저작권이 처음부터 깨끗하고,
   `lib-fit.mjs` 로 같은 자를 대어 검수할 수 있다. 다만 **6,000편은 큰 드레인**이다.
   ⚠️ 기존 `original` 524편의 적합률은 **3.8%** 였다 — 그건 초·중용 짧은 글이라 그렇고,
   수능 대역을 겨냥해 지으면 달라지지만 **그 전제를 먼저 파일럿으로 확인해야 한다.**

---

## 15. 작문 파일럿 — **3안이 열렸다** (2026-09-03)

§14 의 셋째 길(예술·문화 지문을 짓는다)을 10편으로 시험했다.
채점기는 `scripts/csat/score-composed.mjs` — **수확 원문과 같은 자**(`lib-fit.mjs`)다.

| 회차 | 결과 |
|---|---|
| 1차 (한 번에 씀) | 10편 중 **적합 7 (70%)** |
| 떨어진 3편의 이유 | **전부 「낱말이 길다」** (5.56 · 5.67 · 5.71자 · 상한 5.36) |
| 2차 (평이하게 고쳐 씀) | **과교정** — 4.40 · 4.42 · 4.49자로 이번엔 **하한 4.69 미달** |
| 3차 (중앙 5.01 겨냥) | **5.20 · 4.82 — 대역 안, 2/2 적합** |

**결론: 대역은 좁지만 조절 가능한 손잡이다.** 한 번에 쓰면 70%, **측정-수정 루프를 한 바퀴
돌리면 붙는다.** 이것이 3안의 전제였고, 확인됐다.

⚠️ 표본 10편이다. "된다/안 된다" 를 가르는 데는 충분하지만 **적합률 수치로 인용하면 안 된다.**

### 왜 이 결과가 §14 의 천장을 푸는가

병목은 예술·문화 6,172편이었고, 그 칸에는 **수확할 수 있는 CC BY 영어 전문 공급선이 없다**
(OpenStax 미술사 없음 · Smarthistory NC · MDPI 403 · DOAJ PDF 27%·영어 33%). 지어 쓰면
저작권이 처음부터 깨끗하고, 소재를 원하는 칸에 정확히 놓을 수 있고, 같은 자로 검수된다.

곁가지 관찰 하나 — 지은 10편의 소재 판정은 예술·문화 5 · 심리·인지 2 · 철학·윤리 1 ·
역사·인류 1 · 교육·언어 1 이었다. **다섯 칸이 전부 부족한 칸이다.** 인문을 겨냥해 쓰면
어느 칸으로 떨어져도 낭비가 아니다.

### 규모

인문 결손 약 11,000편이다. 저장소는 이미 이만한 드레인을 완주한 적이 있다
(ScriptQuiz 챕터 퀴즈 **1,292문항** · 기출 분석 **802문항** 3인 검수). 다만 11,000편은
그보다 한 자릿수 크므로, **드레인 구조(export → 채움 → 채점 → 재작성 → import)를
먼저 세우고 규모를 단계로 나눠야 한다.**

---

## 16. 1구간 수확 완료 — 그리고 이 목표의 가장 중요한 수치

| | |
|---|---|
| 겨냥 수확 적재 | **3,370편** (과학 1,500 · 사회 1,200 · 기술 344 · 심리 326) |
| 그중 적합 | **3,370 / 3,370 = 100%** |
| 재고 | 24,870 → **28,258** · 적합 14,252 → **17,622** |
| `library_articles` | 339 → **407 MB** |
| **균형 사정권** | 4,161 → **4,189** |

**적합 원문을 3,370편 늘렸는데 균형 사정권은 28편 늘었다.**

이것이 우연이나 결함이 아니라 **정의상 그렇게 된다.** 균형 사정권은 병목 소재가 정하고
(`min_t 재고_t / 목표비율_t`), 병목은 여전히 **역사·인류 147편**이다. 수확은 몫을 지켜
과학·사회·기술에만 들어갔고 — 그 칸들도 3단계 기준으로는 부족하니 낭비는 아니다 — 그러나
**병목을 건드리지 않는 한 이 수치는 안 움직인다.**

→ **PLOS 수확은 필요하지만 임계 경로가 아니다.** 임계 경로는 §15 의 작문뿐이다.
   앞서 작문을 "인문 결손의 나머지 16%" 로 적었는데, 그건 규모의 이야기였고
   **진척의 이야기로는 작문이 100%다.**

### 재측정된 소재 분포 (표본 3,000편)

| 소재 | 기출% | 재고% | 배율 | 추정 재고 | 3단계 부족 |
|---|---|---|---|---|---|
| 과학·자연 | 31.6 | 40.6 | 1.29 | 7,155 | 8,634 |
| 기술·매체 | 7.4 | 22.8 | **3.10** 과잉 | 4,024 | 0 |
| 사회·경제 | 15.4 | 13.3 | 0.86 | 2,350 | 5,369 |
| 심리·인지 | 15.8 | 10.5 | 0.67 | 1,856 | 6,039 |
| 예술·문화 | 14.0 | 6.0 | 0.43 | 1,057 | 5,961 |
| 교육·언어 | 7.0 | 4.3 | 0.62 | 764 | 2,745 |
| 철학·윤리 | 5.3 | 1.5 | 0.29 | 264 | 2,368 |
| **역사·인류** | 3.5 | 0.8 | **0.24** | **147** | 1,607 ← **병목** |

### 고친 비효율 하나

1구간 로그에서 **심리·인지가 11쪽 연속 「받음 0」** 이었다. 바로 앞서 돈 과학·자연이 **같은
날짜창**을 훑으며 Psychology·Neuroscience 질의에도 걸리는 글을 이미 넣어 버렸기 때문이다
(PLOS 논문은 주제를 여럿 단다). 중복 판정은 제대로 걸렸지만 **550편을 받아 왔다가 전부
되돌린** 셈이다. → `--slot all` 을 넣어 **한 번 훑고 몫이 남은 칸에 나눠 담는다.**

---

## 17. 작문 드레인 — 구조를 세웠다 (2026-09-03)

CLAUDE.md §🤖 의 3단 구조. `compose-drain-export` → **Claude Code 가 채움** → `-import --commit`.

| 단계 | 하는 일 |
|---|---|
| export | 남은 몫을 `compose-drain/chunk-NN.json` 으로. 지침·대역을 청크 안에 넣어 파일 하나만 열면 되게 |
| Claude Code | 읽고 채워 `chunk-NN.out.json` |
| import | 채점 → **붙은 것만 적재** · 떨어진 것은 `chunk-NN.reject.json` 에 **이유와 함께** |

**재실행 안전**: export 는 채운 청크를 건너뛴다 · import 는 내용 해시를 `source_id` 로 써서
같은 글을 두 번 안 넣는다 · **빈 값·짧은 값은 세지 않는다**(넣으면 다음 export 가 "완료" 로
세어 구멍이 영영 남는다 — CLAUDE.md §🤖).

### 배분 기준을 고쳤다 — 같은 함정을 두 번 밟을 뻔했다

처음에는 **절대 부족분이 큰 칸부터** 채우게 짰다. 그러면 예술·문화(5,961)가 먼저 나온다.
그런데 균형 사정권은 `min_t (재고_t / 목표비율_t)` 이므로 **지금 병목인 칸**에 넣어야 오른다.
두 기준은 실제로 갈린다:

```
절대 부족  예술·문화 5,961 > 교육·언어 2,745 > 철학·윤리 2,368 > 역사·인류 1,607
병목 순서  역사·인류 4,190 < 철학·윤리 5,016 < 예술·문화 7,531 < 교육·언어 10,887
```

부족분 기준으로 예술·문화를 5,961편 **다 지어도 균형 사정권은 4,190 에서 안 움직인다** —
역사·인류가 그대로이기 때문이다. §16 에서 수확 3,370편이 사정권을 28편밖에 못 올린 것과
**같은 함정**이고, 여기서 다시 밟으면 12,681편을 짓고도 수치가 제자리다.
→ 병목 순서로 배분하도록 고쳤다. 첫 청크가 역사·인류로 나온다.

### 스키마 제약

`source='original'` 은 CHECK `chk_original_needs_batch` 때문에 `compose_batch_id` +
`composed_spec` 이 **둘 다 있어야** 들어간다. import 가 청크마다
`article_compose_batches` 행을 먼저 만든다. 그 제약은 출처 없는 합성 글이 조용히 끼어드는
것을 막는 장치이므로 우회하지 않는다.

---

## 18. 첫 청크를 돌렸다 — 그리고 소재 조준이 빗나갔다 (2026-09-03)

역사·인류 12편을 지어 드레인을 끝까지 돌렸다. **12편 전부 적재.** 구조는 작동한다.

| | |
|---|---|
| 1차 채점 | 12편 중 **붙음 9 (75%)** · 떨어진 3편은 **전부 「낱말이 길다」**(5.40~5.60) |
| 고쳐 쓴 뒤 | **12/12 (100%)** |
| 적재 | **12편** (`source='original'` · 배치 자동 생성) |

파일럿(§15)이 말한 그대로다 — 한 번에 75%, 측정-수정 한 바퀴로 100%.

### ⚠️ 그런데 병목은 안 찼다 — 소재 조준이 빗나갔다

12편의 소재 판정: **과학·자연 3 · 교육·언어 3 · 사회·경제 3 · 심리·인지 2 · 역사·인류 1.**
역사·인류를 채우려고 지었는데 **1편만 그 칸에 떨어졌다.**

원인을 두 갈래로 의심했고, 하나는 **틀렸다**:

- ❌ **길이 편향 가설** — 기출 지문(중앙 155어)과 재고 원문(6,000자≈1,000어)에 같은 분류기를
  대니 긴 글에서 흔한 낱말을 가진 칸이 이기는 것 아닌가. **재 보니 아니었다** — 같은 글을
  앞 150어만 잘라 재도 판정이 같다(150/300/600/전문 모두 역사·인류 1).
- ✅ **소재 목록이 문제였다** — 내가 뽑아 둔 소재가 「사료 비판」·「구전 계보의 정확성」처럼
  **역사학의 방법론**이었다. 그렇게 쓰면 글이 인식론·사회학 어휘로 채워진다.
  청동기 문명 붕괴처럼 **시대·문명·변화 자체**를 쓰자 **역사·인류 margin 7** 로 깨끗이 떨어졌다.

→ 소재 목록을 「내용」으로 바꿨다(청동기 붕괴 · 중세 삼포식 농업 · 실크로드 · 흑사병 이후의
  임금과 토지 · 신석기 혁명과 건강 …). export 지침에도 한 줄 넣었다:
  **「그 칸의 내용을 써야 그 칸으로 떨어진다.」**

⚠️ **적재한 12편은 버리지 않는다** — 떨어진 칸(교육·언어 3 · 사회·경제 3 · 심리·인지 2)이
전부 부족한 칸이라 낭비가 아니다. 다만 **균형 사정권은 안 오른다**(역사·인류 1편분만).

### 소재 풀이 몫보다 한참 작다 — export 가 경고한다

```
예술·문화  남은 몫 5,961편 · 소재 풀 14개
철학·윤리  남은 몫 2,368편 · 소재 풀 10개
교육·언어  남은 몫 2,745편 · 소재 풀  8개
역사·인류  남은 몫 1,607편 · 소재 풀 24개
```

같은 소재를 되풀이하면 비슷한 글이 쌓인다(첫 판에서 실제로 청크 안에 2개가 겹쳤다 —
청크 안 중복은 막았다). **12,681편을 지으려면 소재 풀부터 그만큼 넓혀야 한다.**

---

## 19. 소재를 「내용」으로 바꾸자 조준이 맞았다 (2026-09-03)

같은 칸(역사·인류)을 **새 소재 목록**으로 12편 더 지었다. 소재는 청동기 붕괴 · 중세 삼포식
농업 · 고대 제국의 도로망 · 초기 야금술 · 점토 표식 · 유목 제국 · 인류의 확산 · 고대 화폐 ·
제국의 변경 · 동굴 벽화 연대 · 기후와 정착 · 초기 국가의 곡물 창고.

| | 1차 청크(방법론 소재) | 2차 청크(내용 소재) |
|---|---|---|
| 역사·인류 판정 | **1 / 12** | **8 / 24 누적** (2차만 보면 7/12) |
| 최종 적합 | 12/12 | 12/12 |

**소재 목록 하나로 조준이 7배 좋아졌다.** 자를 건드리지 않고 입력을 바꿔서 얻은 것이다.

### 세 번째 실패 모드 — 길이와 문장 편차

2차 청크의 1차 채점에서 3편이 떨어졌고, 이번엔 이유가 낱말 길이만이 아니었다:

```
early metalworking   242어 · 문장 8 29 8 36 16 13 34 20 10 9 49  → shape 1, pass 0
ancient spread       231어 · 낱말길이 5.364 (상한 5.35625)        → shape 0
climate shifts       232어 · 문장 31 34 16 13 9 27 28 23 25 26   → shape 1, pass 0
```

**붙은 글은 280~310어였고 떨어진 글은 231~242어였다.** 짧으면 창이 한 개 반밖에 안 나와
한 번 실패하면 그대로 끝난다. 문장 길이가 8~49어로 튀는 것도 같은 문제를 키운다.
→ 지침에 넣었다: **300~340어 · 문장 18~25어로 고르게.** 고쳐 쓰자 **24/24** 가 됐다.

### 누적

| | |
|---|---|
| 작문 적재 | **24편** (2청크 · 전부 `csat_fit.pass > 0`) |
| 소재 | 역사·인류 8 · 사회·경제 7 · 과학·자연 3 · 교육·언어 3 · 심리·인지 2 · 예술·문화 1 |

---

## 20. 다음 사이클

1. **소재 풀 확장** — 이게 다음 병목이다. 예술·문화는 몫 5,961편에 풀 14개다.
   칸마다 최소 100개가 필요하고, 그 목록 자체가 「내용」이어야 한다(§18·§19).
2. **역사·인류를 계속** — 병목이 실제로 내려가는지 `topic-gap` 재측정으로 확인.
3. **PLOS 수확 계속** — `--slot all` 로.
4. **OpenStax ingester** — World History · Anthropology 는 애초에 「내용」이라 조준이 맞는다.
