# S6 — 수능(CSAT) · 스크립트 퀴즈 · 연습 감사

감사 시각 2026-09-05 · 브랜치 `pc2-20260720-1` · 코드 실독 + DB 실측(`jajenrevcbmrpaliomxv`)

## 요약

- **검사한 화면: 8개**
  - `/csat` · `/csat/[typeId]` · `/csat/item/[slug]` · `/csat/plan`
  - `/scriptquiz` · `/scriptquiz/play`
  - `/practice` · `/practice/dcp`
- **결함: blocker 4 · major 10 · minor 8**

### 담당 질문에 대한 결론 (먼저)

| 질문 | 답 |
|---|---|
| ① 문항 풀이 프로세스 — 되돌아갈 수 있나 | **ScriptQuiz: 못 한다.** 답하면 1,000ms 뒤 강제로 다음 문항. 이전 문항 재열람 수단 0. 결과 화면도 정답을 보여 주지 않는다 → **[B3]** |
| ① 중간 이탈 시 진행이 남나 | **ScriptQuiz: 전량 소실**(Esc 한 번, 확인 없음) → **[M1]**. **DCP: attempt 는 서버에 남지만 화면은 항상 1번부터 다시** → **[M4]** |
| ② /csat 3단이 결정을 받는 단계인가 | **그렇다.** 목록→유형→문항 모두 고유 내용이 있고 경유 화면이 없다. `/csat/plan` 도 별도 산출물. **다만 유형 화면의 「이 유형의 기출」 목록이 68%를 «준비 중»으로 잘못 표시해 3단째로 가는 문을 스스로 닫고 있다** → **[B2]** |
| ② /practice → /practice/dcp | `/practice` 는 실 결정 화면. 다만 lead 카드가 클라 fetch 후 다른 면으로 바뀌고 → **[M9]**, `/practice/dcp` 의 뒤로가기가 `/practice` 가 아니라 `/hub` 다 → **[M5]** |
| ③ 빈 상태 (D4) | `/scriptquiz` · `/practice/dcp` 는 다음 걸음이 있다(양호). **`/csat` · `/csat/plan` 은 막다른 문장 한 줄뿐** → **[m1]** |
| ④ 오답 피드백 금지사항 | 모달 오버레이로 학습 중단 **없음**(O/X 오버레이는 `pointer-events-none`). 정답률 빨간 압박은 결과 화면 「오답」 타일이 `--error-light`/`--error-ink` 로 경계선. **OX 문항은 정오 판정이 색상 단독 + aria-live 없음** → **[M8]** |
| ⑤ 낭비 — 정답이 클라이언트로 가나 | **간다. 그리고 채점도 클라이언트가 한다.** 문항 전체 + `correctIndex` 가 RSC 페이로드로 HTML 에 실린다 → **[B1] blocker** |
| ⑤ 같은 데이터 중복 요청 | 챕터 퀴즈 카탈로그를 `/scriptquiz` 한 번 그릴 때 **2회**, `/scriptquiz/play` 마다 **1회 더** 전량 조회 → **[M3]** |

---

## 결함 목록

### [B1] ScriptQuiz 가 정답표를 브라우저에 실어 보내고 채점도 클라이언트가 한다 (blocker · 축 A5)

- **위치**
  - `apps/web/src/lib/scriptquiz/questions.ts:39-52` (`rowToQuestion` — `correctIndex: r.correct_index`)
  - `apps/web/src/app/(main)/scriptquiz/play/page.tsx:37,68` (server → client prop)
  - `apps/web/src/components/game/scriptquiz/ScriptQuiz.tsx:59,137`
- **증상**: `/scriptquiz/play?book=…&ch=…` 를 열고 **시작하기를 누르기 전에** 페이지 소스(RSC flight 페이로드)를 보면 5~10문항 전부의 정답 인덱스와 `sourceSnippet` 이 그대로 있다. 개발자도구 없이 `Ctrl+U` 로 재현된다. 점수는 그 값으로 브라우저가 계산해 `scores` 에 넣는다.
- **근거**
  ```ts
  // ScriptQuiz.tsx:137 — 채점 주체가 브라우저다
  const isCorrect = idx === currentQ.correctIndex
  ```
  ```
  select_book_chapter_quiz → TABLE(id, q_order, type, question, question_ko,
                                   options, correct_index, source_snippet, source_sentence_idx)
  ```
  (`pg_get_function_result` 실측). `ScriptQuiz` 는 `'use client'` 이므로 `session` prop 전체가 직렬화되어 HTML 에 남는다.
- **같은 저장소가 이 규칙을 이미 알고 있다** — `lib/learner/dcp-actions.ts:160-162`:
  > 문항 테이블은 학습자가 못 읽는다(admin 정책 하나뿐). **열어서도 안 된다** — 같은 행에 `answer_key` 가 있어서 정책을 열면 브라우저에서 정답이 보인다. 그래서 `textbook_practice_items` RPC 가 정답을 뺀 열만 내준다.
  DCP 는 지키고 ScriptQuiz 만 어긴다.
- **고칠 방향**: DCP 와 같은 모양으로 맞춘다. ① `select_book_chapter_quiz` 를 `correct_index`·`source_snippet` 없는 열만 내는 RPC 로 바꾸거나 두 RPC 로 쪼갠다. ② `grade_dcp_item` 과 짝이 되는 `grade_chapter_quiz_item(p_question_id, p_selected_index)` 를 만들어 서버가 채점하고 그때 정답·근거를 함께 돌려준다. ③ `ScriptQuiz.handleAnswer` 를 server action 호출로 바꾸고 `QuizQuestion.correctIndex`·`sourceSnippet` 을 타입에서 제거한다(타입이 재발을 막는다).
- **영향 화면**: `/scriptquiz/play`

---

### [B2] 「해설 있음」 판정이 PostgREST 1,000행 상한에 걸려 802문항 중 544문항을 «준비 중» 으로 잘못 표시한다 (blocker · 축 A5/A1)

- **위치**: `apps/web/src/lib/csat/learner.ts:372-397` (`loadCsatTypeItems`), 화면은 `app/(main)/csat/[typeId]/page.tsx:159-183`
- **증상**: `/csat/[typeId]` 의 「이 유형의 기출」에서 대부분의 문항이 `· 준비 중` 으로 붙고 머리글이 «해설 12 / 41» 처럼 낮게 뜬다. **그런데 그 «준비 중» 문항을 눌러 `/csat/item/[slug]` 로 들어가면 해설이 멀쩡히 다 나온다.** 학습자는 그 문항을 안 누른다 — 화면이 없다고 말했으니까.
- **근거**
  ```ts
  // learner.ts:379 — type 필터도, 페이지네이션도, .order() 도 없다
  db.from('csat_item_analyses').select('item_id, choice_analysis, version').eq('status', 'published'),
  ```
  프로젝트 자신의 규칙(`lib/supabase/paged-select.ts:13`): "**개수를 세거나 전량이 필요하면 여기 헬퍼를 쓴다.** 직접 `.limit()` 을 적지 않는다."
  DB 실측(2026-09-05):
  | 값 | 실측 |
  |---|---|
  | `csat_item_analyses` published | **2,234행** (상한 1,000 초과) |
  | 실제로 해설이 붙은 문항 | **802 / 802** (전부) |
  | 화면이 «해설 있음» 으로 세는 문항 | **258** |
  | 잘못 «준비 중» 으로 표시되는 문항 | **544 (68%)** |
  같은 원인이 `loadCsatTypeCards:87` (`csat_items_public` 전량 802행)·`loadCsatTypeDetail:138` 에도 있다. 지금은 802 < 1,000 이라 **아직** 안 틀리지만 같은 결함이고 같은 커밋에서 고쳐야 한다.
- **고칠 방향**: `learner.ts:379` 를 `pagedSelect` 로 감싸되, 더 나은 것은 **필요한 것만 받는 것**이다 — `csat_items_public` 에서 이 유형의 `item_id` 를 먼저 뽑고 `.in('item_id', ids)` 로 좁힌다(한 유형은 최대 수십 문항이라 상한과 무관해진다). `loadCsatTypeCards`/`loadCsatTypeDetail` 의 전량 select 3곳도 같은 커밋에서 `pagedSelect` 로 감싼다.
- **영향 화면**: `/csat/[typeId]` (직접), `/csat` · `/csat/plan` (같은 원인 · 잠복)

---

### [B3] ScriptQuiz 는 정답을 읽을 시간을 주지 않고, 되돌아가 다시 볼 수단이 하나도 없다 (blocker · 축 A2/A6)

- **위치**: `apps/web/src/components/game/scriptquiz/ScriptQuiz.tsx:44,152-193,586-597,621-748`
- **증상 (재현)**: 4지선다를 틀리면 정답 선지에 초록 테두리 + 노란 체크가 붙고 「스크립트 근거」 블록이 **선택지 아래**(390px 에서는 접힘선 아래)에 나타난다. **1,000ms 뒤 화면이 다음 문항으로 갈아치워진다.** 영어 문장을 스크롤해 읽을 시간이 없다. 되감기·일시정지·이전 문항 버튼이 없다. 세션이 끝난 뒤 결과 화면의 「다시 만나봐요」는 **문제 문장과 근거 인용만** 보여 주고 **정답이 무엇이었는지, 내가 무엇을 골랐는지는 끝까지 안 나온다.** 즉 틀린 문항의 정답을 학습자가 알 수 있는 경로가 전 세션에 걸쳐 없다.
- **근거**
  ```ts
  const FEEDBACK_DURATION = 800  // :44
  setTimeout(() => { setFeedback(null)
    setTimeout(() => { … setCurrentIdx((i) => i + 1) … }, 200)   // :153-192  합계 1,000ms 자동 전진
  }, FEEDBACK_DURATION)
  ```
  ```tsx
  {answerState === 'answered' && question.sourceSnippet && ( … )}   // :588 — 그 1,000ms 동안만 존재
  ```
  ```tsx
  {wrongQs.map((q) => ( … {q.question} … “{q.sourceSnippet}” … ))}  // :733-745
  // q.options[q.correctIndex] 도, answers 의 selectedIndex 도 렌더하지 않는다
  ```
- **고칠 방향**: 자동 전진을 없애고 **「다음」 버튼**으로 학습자가 넘기게 한다(`FEEDBACK_DURATION` 은 O/X 오버레이에만 남긴다). 피드백 상태에서 정답 선지와 내가 고른 선지를 나란히 두고 근거를 **선택지 위**로 올린다. 결과 화면 `wrongQs` 항목에 `내 답 / 정답` 두 줄을 추가하고, 항목을 눌러 그 문항 피드백으로 되돌아가는 경로를 만든다. (`answers` 배열에 `selectedIndex` 가 이미 있으므로 데이터는 다 있다.)
- **영향 화면**: `/scriptquiz/play`

---

### [B4] DCP 채점이 실패하면 세션이 「채점 중」에 영구 고착해 학습자가 진행할 수 없다 (blocker · 축 A1)

- **위치**: `apps/web/src/components/practice/DcpPlayer.tsx:43-50` · `apps/web/src/lib/learner/dcp-actions.ts:239-252`
- **증상 (재현)**: `/practice/dcp` 에서 제출한 순간 네트워크가 끊기거나 server action 이 거부되면(예: 세션 만료 → `Forbidden`) **제출 버튼이 회색으로 잠긴 채 아무 일도 일어나지 않는다.** 다시 누를 수도, 다음으로 갈 수도 없다. 새로고침이 유일한 탈출인데 그러면 [M4] 때문에 1번 문항부터 다시다.
  두 번째 갈래: RPC 가 `error` 를 돌려주는 경우(거부·타임아웃)에는 화면이 **「아쉬워요 — 정답 흐름을 함께 볼게요」라고 오답 판정을 내리고**, `answerKey` 가 null 이라 정답 공개는 안 나오며, 「어떤 점이 어려웠나요?」만 뜬다. **맞힌 학습자에게 틀렸다고 말한다.**
- **근거**
  ```ts
  // DcpPlayer.tsx:43-50 — try/catch 가 없다. reject 되면 setPhase('graded') 에 도달하지 못한다.
  async function handleSubmit(answer) {
    setPhase('grading')
    const res = await gradeDcpItem(item.id, answer)
    setResult(res); if (res.correct) setCorrectCount(c => c + 1); setPhase('graded')
  }
  ```
  ```ts
  // dcp-actions.ts:246 — 조회 실패와 「틀렸다」를 같은 값으로 뭉갠다
  if (error || !data) return { correct: false, attemptId: null, answerKey: null }
  ```
  같은 파일 `fetchTextbookPracticeItems:206` 은 정반대로 적어 뒀다: "조회 실패를 빈 목록으로 뭉개지 않는다 — 화면이 «아직 문항이 없어요» 로 거짓말하게 된다." 채점 경로만 그 규칙을 안 따른다.
- **고칠 방향**: `DcpGradeResult` 에 `ok: boolean`(또는 `status: 'graded' | 'failed'`)을 더해 실패를 오답과 **다른 상태**로 만든다. `handleSubmit` 을 `try/catch` 로 감싸 실패 시 `phase` 를 `'answering'` 으로 되돌리고 "채점을 못 했어요 — 다시 제출해 주세요" + 재시도 버튼을 띄운다. 이때 학습자의 선택은 그대로 남겨 둔다(입력 유실 금지).
- **영향 화면**: `/practice/dcp`

---

### [M1] ScriptQuiz 중간 이탈 = 답한 것 전량 소실. 확인 절차가 없다 (major · 축 A6)

- **위치**: `components/game/scriptquiz/ScriptQuiz.tsx:65-72,153-193` · `components/layout/SessionFrame.tsx:166-179,271-285`
- **증상**: 10문항 중 8문항을 푼 상태에서 **Esc 키 한 번** 또는 헤더 X 를 누르면 즉시 `/scriptquiz` 로 나가고 8문항의 답이 전부 사라진다. 확인 대화도, "이어서 풀기" 도 없다. `scores` 는 마지막 문항 완료 시점에만 한 번 쌓이므로 서버에도 흔적이 남지 않는다.
- **근거**: 상태는 전부 `useState`(`answers`·`currentIdx`) 이고 어떤 지속 저장도 없다. `recordGameScore` 는 `currentIdx + 1 >= totalQ` 분기 안에서만 호출된다(`:156-183`). `SessionFrame` 의 Esc 핸들러는 무조건 `router.push(closeHref)` 다.
- **고칠 방향**: (a) 최소 — 세션 진행 중에는 Esc/X 에 확인을 붙인다(`answers.length > 0` 일 때만). (b) 제대로 — 문항마다 답을 `sessionStorage` 에 적어 두고 재진입 시 "이어서 풀까요?" 를 준다. [B1] 을 고쳐 서버 채점으로 옮기면 문항 단위 attempt 가 서버에 남으므로 (b) 가 거의 공짜가 된다.
- **영향 화면**: `/scriptquiz/play`

---

### [M2] 문항을 못 찾으면 「The Great Gatsby」 데모 퀴즈로 조용히 떨어지고, 그 결과가 실 학습 기록으로 적재된다 (major · 축 A1)

- **위치**: `app/(main)/scriptquiz/play/page.tsx:32-47` · `components/game/scriptquiz/ScriptQuiz.tsx:60,172-183` · `components/game/scriptquiz/ScriptQuizQueue.tsx:172-177`
- **증상**: ① `?ch=abc` 처럼 챕터가 숫자가 아니거나 ② 해당 챕터에 문항이 0개거나 ③ 파라미터 없이 `/scriptquiz/play` 로 들어오면 **Gatsby 목업 5문항**이 실제 퀴즈처럼 뜬다. 완주하면 `recordGameScore` 가 `module:'scriptquiz'` 로 `scores` 1행을 넣는다 — Hub 「최근 활동」·`daily_activity`·주간 리포트에 실 학습으로 섞인다.
  이 경로는 **화면이 스스로 권한다**: 카탈로그가 비었을 때 빈 상태의 유일한 CTA 가 「샘플 체험」 → `/scriptquiz/play`(파라미터 없음)다.
- **근거**
  ```ts
  const session = sessionProp ?? MOCK_SESSION            // ScriptQuiz.tsx:60
  void recordGameScore({ module: 'scriptquiz', … })      // :172 — 목업 여부를 보지 않는다
  ```
  `lib/scores/record-score.ts:69-80` 은 로그인만 확인하고 무조건 INSERT 한다. DB 실측: `scores where module='scriptquiz'` 23행 · Gatsby 메타 0행(아직 안 터졌을 뿐 경로는 살아 있다).
  프로젝트 상시 규칙(CLAUDE.md §4️⃣): "공개 라우트의 목업/허위 수치는 발견 즉시 제거".
- **고칠 방향**: `ScriptQuiz` 에 `isSample` 을 명시적으로 넘긴다. 샘플일 때 ① `recordGameScore` 를 호출하지 않고 ② 화면 안에 「샘플입니다 — 기록되지 않아요」를 상시 표시한다. 파라미터가 있는데 문항이 0개인 경우(②)는 목업이 아니라 **빈 상태**로 처리한다 — "이 챕터는 아직 확인 문항이 없어요" + 다른 챕터로 가는 링크.
- **영향 화면**: `/scriptquiz/play`, `/scriptquiz`(빈 상태 CTA)

---

### [M3] 챕터 퀴즈 카탈로그를 한 화면에 두 번, 플레이할 때마다 또 한 번 전량 조회한다 (major · 축 A5)

- **위치**: `app/(main)/scriptquiz/page.tsx:36-39` · `lib/scriptquiz/queue.ts:200` · `lib/scriptquiz/questions.ts:71-77`
- **증상**: `/scriptquiz` 를 한 번 그릴 때 `list_book_chapter_quiz_catalog` RPC 가 **2회** 실행된다. `Promise.all` 로 동시에 나가므로 캐시도 안 먹는다. `/scriptquiz/play` 는 문항 조회와 **나란히 카탈로그 전량을 또 받는데, 그 결과에서 쓰는 것은 책 제목 한 줄과 챕터 제목 한 줄뿐**이다.
- **근거**
  ```ts
  // page.tsx:36 — 오른쪽 fetchChapterQuizCatalog 가 왼쪽 안에서 이미 호출된다
  const [q, catalog] = await Promise.all([
    fetchScriptQuizQueue(client, user?.id ?? null),   // queue.ts:200 → fetchChapterQuizCatalog(client)
    fetchChapterQuizCatalog(client),
  ])
  ```
  ```ts
  // questions.ts:71-77 — 제목 두 줄 때문에 카탈로그 전량
  const [{ data: qData … }, { data: catData }] = await Promise.all([
    rpc('select_book_chapter_quiz', {…}),
    rpc('list_book_chapter_quiz_catalog'),
  ])
  ```
  DB 실측: 카탈로그 행 = 퀴즈 있는 (도서, 챕터) 쌍 **342행**(`library_chapter_quiz` 2,453문항). 같은 파일 `:122-124` 가 이 RPC 의 1,000행 상한을 이미 경고해 뒀는데 `fetchChapterQuizSession` 의 호출만 `pagedSelect` 를 안 쓴다 — **342행이 1,000을 넘는 순간 그 뒤 도서는 제목이 조용히 「스크립트」/「Chapter N」 으로 떨어진다.**
- **고칠 방향**: `fetchScriptQuizQueue` 가 이미 받은 카탈로그를 돌려주게 하거나(`QuizQueue` 에 `hasCatalog` 를 실어 보낸다) `fetchChapterQuizCatalog` 를 `React.cache()` 로 감싼다. `fetchChapterQuizSession` 은 제목 두 줄만 필요하므로 카탈로그 대신 **책·챕터 하나만 돌려주는 RPC** 를 쓰거나 `select_book_chapter_quiz` 반환에 제목을 넣는다.
- **영향 화면**: `/scriptquiz`, `/scriptquiz/play`

---

### [M4] DCP 는 오늘 이미 푼 5문항을 재방문 때 그대로 다시 내고, `/practice` 의 「대기 5」도 줄지 않는다 (major · 축 A6/A1)

- **위치**: `lib/learner/dcp-actions.ts:122-149` · `prescribe_today` RPC 본문 · `lib/learner/prescription-actions.ts:154,164-165` · `app/(main)/practice/PracticeChooser.tsx:74-80`
- **증상**: `/practice/dcp` 를 끝까지 마치고 「홈으로」를 눌렀다가 다시 `/practice` → Syntax 로 들어오면 **방금 푼 것과 완전히 같은 5문항**이 1번부터 다시 나온다. 「오늘 다 했어요」 같은 상태가 없다(빈 상태는 S3 미만이거나 문항 0개일 때만 뜬다). 중간에 이탈했다 돌아와도 항상 1번부터다 — 3번까지 풀었어도 그 3번을 다시 푼다(`csat_item_attempts` 에 같은 날 중복 시도가 쌓인다).
  `/practice` 카드도 "대기 5" 를 하루 종일 그대로 유지한다.
- **근거** (RPC 본문 실측)
  ```sql
  SELECT i.id, i.type, i.paragraph_idx, i.payload FROM csat_dcp_items i
  JOIN csat_stage_catalog c ON …
  WHERE … AND i.type IN ('order','insert') …
  ORDER BY md5(i.id::text || current_date::text) LIMIT 5
  ```
  날짜 시드로 5개를 고정하고 **이미 시도한 문항을 빼지 않는다.**
  ```ts
  const practiceCount = Array.isArray(practiceItems) ? practiceItems.length : 0   // prescription-actions.ts:154
  ```
  = 처방된 총수이지 남은 수가 아니다. `PracticeChooser:77` 이 그것을 그대로 "대기" 로 렌더한다.
- **고칠 방향**: `prescribe_today` 의 practice 블록에 `AND NOT EXISTS (SELECT 1 FROM csat_item_attempts a WHERE a.item_id = i.id AND a.user_id = p_user_id AND a.created_at::date = current_date)` 를 더한다(마이그레이션 필요 — 승인 후). 그러면 `practiceCount` 가 자동으로 「남은 수」가 되고, 이탈 후 재진입이 이어서 풀기가 되며, 다 푼 날은 `active=false` 로 떨어져 `/practice/dcp` 가 이미 있는 빈 상태를 쓴다. 다만 그 빈 상태 문구가 "학습 단계가 무르익으면 열려요"(잠김 안내)라서 "오늘 몫을 다 했어요" 와 갈라 써야 한다.
- **영향 화면**: `/practice/dcp`, `/practice`

---

### [M5] `/practice/dcp` 의 뒤로가기가 온 곳(`/practice`)이 아니라 `/hub` 로 간다 (major · 축 A2/A4)

- **위치**: `app/(main)/practice/dcp/page.tsx:26-33,53-58` · `components/practice/DcpPlayer.tsx:87-93` · `app/(main)/practice/PracticeChooser.tsx:63`
- **증상**: `/practice` 의 Use 카드에서 「Syntax」 칩을 눌러 `/practice/dcp` 에 들어가면 화면 좌상단 링크가 「← 오늘」이고 `/hub` 로 간다. 빈 상태 CTA 「오늘로 돌아가기」도, 완주 CTA 「홈으로」도 전부 `/hub` 다. **`/practice` 로 돌아가는 명시적 수단이 이 화면에 하나도 없다** — 다른 면을 연습하려던 학습자는 사이드바를 다시 찾아야 한다.
- **근거**
  ```tsx
  <Link href="/hub" …>← 오늘</Link>                    // dcp/page.tsx:26-33  (세 곳 모두 /hub)
  ```
  ```tsx
  ? { ...TOOLS, use: [{ label: 'Syntax', href: '/practice/dcp', … }, …] }   // PracticeChooser.tsx:63
  ```
  `dcp/page.tsx:3` 머리말이 "hub «오늘» 처방 ④ 연습 블록의 진입점" 이라고 적혀 있다 — 두 번째 진입점(`/practice`)이 나중에 생겼는데 복귀 링크가 갱신되지 않았다. 라우트 계층으로도 `/practice/dcp` 의 부모는 `/practice` 다.
- **고칠 방향**: `/practice` 에서 오는 링크에 `?from=/practice` 를 붙이고 `dcp/page.tsx` 가 `sanitizeInternalPath`(`lib/layout/session-return.ts` 에 이미 있다 — ScriptQuiz 가 쓴다)로 읽어 복귀 목적지·라벨을 정한다. `DcpPlayer` 완주 CTA 도 같은 값을 받는다.
- **영향 화면**: `/practice/dcp`

---

### [M6] DCP 는 맞힌 문항의 해설을 절대 보여 주지 않는다 (major · 축 A6)

- **위치**: `components/practice/DcpPlayer.tsx:188` · `:255-265`(`ExplanationNote`)
- **증상**: 순서/삽입/5지선다를 **맞히면** 「정확해요!」 한 줄과 「다음」 버튼만 나온다. 정답 순서도, 왜 그렇게 이어지는지도 안 나온다. 찍어서 맞힌 학습자는 근거를 못 배우고 넘어간다.
- **근거**
  ```tsx
  {!correct && result.answerKey && <Reveal item={item} answerKey={result.answerKey} />}   // :188
  ```
  `<ExplanationNote>` 는 `Reveal` 안에만 있으므로 정답 경로에서는 렌더 자체가 안 된다. 같은 파일 `:285` 주석은 "해설은 **왜 나머지가 아닌지**까지 적혀 있다 — 오답 노트가 따로 필요 없게 만든 자리다" 라고 그 가치를 스스로 적어 뒀다.
- **고칠 방향**: `:188` 의 `!correct` 게이트를 풀고, 정답일 때는 정답 공개 블록을 접힘(`<details>`) 기본으로 두되 `ExplanationNote` 는 항상 편다(맞혔을 때 필요한 것은 "정답이 무엇이었나" 가 아니라 "왜" 다).
- **영향 화면**: `/practice/dcp`

---

### [M7] 이 슬라이스 8화면에 계측 이벤트가 하나도 없다 (major · 축 A8 · D2)

- **위치**: `lib/analytics/events.ts`(정본) 대비 `app/(main)/csat/**`, `app/(main)/scriptquiz/**`, `app/(main)/practice/**`, `components/game/scriptquiz/**`, `components/practice/**` 전량
- **증상**: 진입도 상호작용도 셀 수 없다. `/csat` 이 실제로 열리는지, 유형 카드가 눌리는지, ScriptQuiz 세션이 몇 %에서 이탈하는지(위 [M1] 이 정확히 그 지점이다), `/practice` 의 lead 카드가 눌리는지 — 전부 관측 불가.
- **근거**: `grep -rn "track\|analytics" ` 를 위 경로 전체에 돌려 **매칭 0건**(`tracking-*` Tailwind 클래스만 걸린다). `events.ts` 의 `PublicEvent` 유니온에도 csat/scriptquiz/practice 계열 이벤트가 없다. 세션 완주는 `scores` 로 파생되지만 **시작·이탈·중도 지점은 어떤 테이블에도 남지 않는다.**
- **고칠 방향**: `PublicEvent` 에 닫힌 열거형으로 4종만 더한다 — `csat_type_opened{ready:boolean, recentItems:number}` · `quiz_session_started{questions:number, sample:boolean}` · `quiz_session_abandoned{answered:number, total:number}` · `practice_facet_chosen{facet: FacetId, measured:boolean}`. 전부 숫자·불리언·닫힌 열거형이므로 D3 계약을 지킨다(자유 문자열 0). [M1] 을 고치는 커밋에 `quiz_session_abandoned` 를 같이 넣는다.
- **영향 화면**: 8개 전부

---

### [M8] OX 문항은 정오 판정이 색상 단독이고, 정오 결과를 읽어 주는 것이 없다 (major · 축 A7)

- **위치**: `components/game/scriptquiz/ScriptQuiz.tsx:445-504` (OX 분기) vs `:505-576` (4지선다 분기), `:240-257` (오버레이)
- **증상**: OX 문항에서 답하면 **테두리·배경색만** 바뀐다(`--success`/`--error`). 4지선다 분기에는 체크 아이콘·X 아이콘이 있는데 OX 분기에는 없다. 버튼 안의 거대한 O/X 는 **선지 라벨**이지 판정이 아니고 `aria-hidden` 이다. 적록색약 학습자는 자기가 맞혔는지 알 수 없다. 화면 낭독기 사용자는 어느 유형에서도 정오를 못 듣는다 — 전체 화면 O/X 오버레이가 `aria-hidden`(`:244`) 이고 `aria-live` 영역이 없다.
- **근거**
  ```tsx
  // :470-480 OX — 판정은 클래스 색뿐, 아이콘 0
  showCorrect ? 'border-[var(--success)] bg-[var(--success-light)]'
    : showWrong ? 'border-[var(--error)] bg-[var(--error-light)]' : …
  ```
  ```tsx
  // :555-571 4지선다 — 여기엔 있다
  {showCorrect && (<span aria-label="정답"><svg …/></span>)}
  {showWrong && <X size={20} … />}
  ```
  ```tsx
  <div className="… fixed inset-0 z-50 …" aria-hidden>   // :242-245
  ```
  비교 대상: `DcpPlayer.tsx:164-174` 는 같은 일을 `aria-live="polite"` + 색 + 아이콘으로 제대로 한다.
  CLAUDE.md 절대 금지: "색상만으로 정보 전달 (색맹 대응 위반)" · 항상 지킬 것: "정답/오답: 색상 + 아이콘 + 애니메이션 3중 피드백".
- **고칠 방향**: OX 분기에도 4지선다와 같은 체크/X 배지를 넣는다(선지 기호 O/X 와 시각적으로 겹치지 않게 버튼 모서리에). 오버레이 옆에 `<p className="sr-only" aria-live="polite">{isCorrect ? '정답입니다' : '오답입니다'}</p>` 를 둔다.
- **영향 화면**: `/scriptquiz/play`

---

### [M9] `/practice` 의 1차 CTA 가 로드 후 다른 면·다른 링크로 바뀐다 (major · 축 A1/A5)

- **위치**: `app/(main)/practice/PracticeChooser.tsx:57-71,110-116` · `components/wordvault/hooks/useFacetSummary.ts:22-44`
- **증상**: 서버는 `/practice` 를 `recognize` 면을 강조한 채(「여기서부터 · Recognize · Flashcard 로 연습」) 그린다. `/api/wordvault/facets` 응답이 도착하면 **강조 카드가 다른 면으로 통째로 바뀌고**(예: 「지금 가장 무른 곳 · Use · Syntax 로 연습」) 1차 버튼의 목적지도 `/flashcard` → `/practice/dcp` 로 바뀐다. 로딩 표시가 없어서 학습자는 언제 확정되는지 모르고, 빨리 누르면 의도와 다른 도구로 간다. 응답이 실패하면(비로그인·서버 오류) 조용히 `recognize` 로 남아 「여기서부터」로 뜨는데 그 사실을 화면이 말하지 않는다.
- **근거**
  ```ts
  const facets = useFacetSummary()
  const ready = facets.status === 'ready' ? facets.data : null   // :57-58 — loading 과 unavailable 을 같게 다룬다
  const lead: FacetId = weakestHasTool ? weakest! : 'recognize'  // :70
  ```
  ```tsx
  {isMeasured ? '지금 가장 무른 곳' : '여기서부터'}              // :189
  ```
  `useFacetSummary` 의 초기 상태는 `{status:'loading'}` 이고, 서버 페이지는 이 값을 미리 가져다 주지 않는다(`practice/page.tsx:60-66` 은 큐·게임풀·처방만 받는다).
- **고칠 방향**: 서버에서 면 요약을 함께 읽어 `PracticeChooser` 에 초기값으로 넘긴다(같은 계산이 `lib/framework/word-progress-query` 에 있고 API 는 그것을 감싼 것뿐이다). 그러면 첫 페인트가 곧 정답이고 클라 왕복이 사라진다. 서버로 못 옮긴다면 최소한 `status==='loading'` 동안 강조 카드의 버튼을 비활성/스켈레톤으로 두고, `'unavailable'` 은 「지금은 무른 곳을 못 재요」로 구분해 적는다.
- **영향 화면**: `/practice`

---

### [M10] 유형 기출 목록 조회가 실패하면 섹션이 통째로 사라지고 아무 말도 안 한다 (major · 축 A1)

- **위치**: `lib/csat/learner.ts:381` · `app/(main)/csat/[typeId]/page.tsx:154,184`
- **증상**: `csat_items_public` 또는 `csat_exams` 조회가 실패하면 `/csat/[typeId]` 의 「이 유형의 기출」 섹션 전체가 **아무 흔적 없이 없어진다.** 학습자는 이 유형에 기출이 없다고 읽는다(문항 해설로 가는 유일한 문이 그 섹션이다). 페이지 상단의 「기출 {detail.items}문항」은 다른 쿼리라 여전히 «41문항» 을 보여 주므로 화면 안에서 서로 모순된다.
- **근거**
  ```ts
  if (itemsRes.error || examsRes.error) return []      // learner.ts:381 — 오류와 «없음» 이 같은 값
  ```
  ```tsx
  {items.length ? ( <section> … </section> ) : null}   // page.tsx:154,184
  ```
  같은 파일의 다른 로더들(`loadCsatTypeCards:92`, `loadCsatTypeDetail:148`, `loadCsatPlan:266`)은 전부 `error` 를 밖으로 내보내 화면이 「불러오지 못했어요」를 그린다. `loadCsatTypeItems` 만 삼킨다.
- **고칠 방향**: 반환 타입을 `{ items, error }` 로 맞추고 화면이 세 상태(오류 / 비어 있음 / 목록)를 갈라 그린다. 비어 있음도 지금은 `null` 이라 침묵이다 — "이 유형의 기출을 아직 연결하지 못했어요" 한 줄이 필요하다.
- **영향 화면**: `/csat/[typeId]`

---

### [m1] `/csat` · `/csat/plan` 의 빈 상태·오류 상태에 다음 한 걸음이 없다 (minor · 축 A3 · D4)

- **위치**: `app/(main)/csat/page.tsx:49-59` · `app/(main)/csat/plan/page.tsx:56-60,131-135`
- **증상**: 네 문장 전부 한 줄짜리 막다른 문단이다 — 「지금은 분석을 불러오지 못했어요. 잠시 뒤 다시 열어 주세요.」 / 「아직 준비된 유형이 없어요.」 / 「지금은 계획을 불러오지 못했어요…」 / 「아직 계획을 세울 회차가 없어요.」 버튼도 링크도 없다. `/csat` 은 슬라이스의 진입면이라 여기서 막히면 되돌아갈 곳이 사이드바뿐이다.
- **근거**: 위 네 블록 모두 `<p>` 한 개로 끝나고 형제 요소가 없다. 대조군으로 `/practice/dcp:43-59` 와 `ScriptQuizQueue:179-200` 은 같은 상황에 CTA 를 붙여 놓았다 — 같은 저장소 안에서 기준이 갈린다.
- **고칠 방향**: 오류에는 「다시 시도」(`useRouter().refresh()` 또는 `reset`), 빈 상태에는 「읽을 지문 고르기 → /library/books」 같은 옆길을 붙인다. `/csat/plan` 의 빈 상태는 `/csat` 으로 되돌리면 충분하다(이미 상단에 「← 유형 목록」이 있으므로 우선순위는 낮다).
- **영향 화면**: `/csat`, `/csat/plan`

---

### [m2] 44px 미만 터치 타깃 4곳 (minor · 축 A7)

- **위치**
  - `components/practice/DcpItems.tsx:153` — 삽입 위치 슬롯 버튼 `min-h-[36px]` (문항 하나당 3~6개, **문항을 푸는 주된 조작**)
  - `components/practice/DcpPlayer.tsx:201` — 「어떤 점이 어려웠나요?」 원인 칩 5개 `min-h-[36px]`
  - `components/practice/DcpPlayer.tsx:237` — 원인 팁의 「복습하기」 링크 `min-h-[36px]`
  - `components/game/scriptquiz/ScriptQuiz.tsx:361-367` — 시작 화면 「돌아가기」 링크, 높이 지정 없음(13px 텍스트 = 약 20px)
- **증상**: 390px 실기에서 오조작이 난다. 특히 삽입 슬롯은 세로로 촘촘히 쌓여 있어(`gap-1`) 인접 슬롯을 누르기 쉽다.
- **근거**: CLAUDE.md 절대 금지 「44px 미만 터치 타겟」. 같은 저장소가 이 규칙을 명시적으로 지킨 자리가 바로 옆에 있다 — `DcpItems.tsx:79` 의 `MoveButton` 은 `h-11 w-11`, `dcp/page.tsx:28` 주석은 "41×18 이었다 — 44px 미만 탭 대상이었다(CLAUDE.md 절대 금지 · 실측 390px)".
- **고칠 방향**: 네 곳 모두 `min-h-[44px]`. 슬롯 버튼은 시각적 높이를 유지하고 싶다면 `py` 대신 투명 패딩이나 `::before` 확장으로 히트 영역만 넓힌다.
- **영향 화면**: `/practice/dcp`, `/scriptquiz/play`

---

### [m3] 한글 제목에 Lora(`font-editorial`) (minor · 프로젝트 규칙)

- **위치**: `app/(main)/practice/PracticeChooser.tsx:92` (「연습」) · `components/game/scriptquiz/ScriptQuizQueue.tsx:60` (「읽은 것 확인하기」)
- **근거**: `tailwind.config` : `editorial: ["var(--font-serif)", "Lora", …]`. CLAUDE.md 절대 금지 「한글에 Lora」. (같은 클래스가 영어 면 이름 `Recognize`·`Use` 등에 쓰이는 것은 규칙에 맞다 — `lib/framework/axes.ts:55-96` 확인.)
- **고칠 방향**: 두 h1 만 `font-display` 로 내리거나, 규칙 자체를 바꿀 생각이면 CLAUDE.md 를 먼저 고친다. **판단이 필요한 항목이다** — 저장소 여러 화면이 같은 패턴을 쓰므로 이 슬라이스만 고치면 오히려 어긋난다.
- **영향 화면**: `/practice`, `/scriptquiz`

---

### [m4] 오답 복습 목록만 영어 문항을 산세리프로 그린다 (minor · 프로젝트 규칙)

- **위치**: `components/game/scriptquiz/ScriptQuiz.tsx:738` vs `:434`
- **근거**: 문제 화면은 `font-english`(Lora — 규칙대로), 결과 화면의 같은 문장은 `font-display`(Plus Jakarta 산세리프). 한 세션 안에서 같은 텍스트가 두 서체로 나온다. CLAUDE.md 절대 금지 「영어에 산세리프」.
- **고칠 방향**: `:738` 을 `font-english` 로 바꾼다.
- **영향 화면**: `/scriptquiz/play`

---

### [m5] `transform`/`opacity` 아닌 속성을 애니메이트한다 (minor · 모션 예산)

- **위치**: `ScriptQuiz.tsx:410` (`transition-[width]`), `:678` (`transition: 'stroke-dashoffset 1s var(--ease-out)'`), `DcpPlayer.tsx:109` (`transition-[width]`)
- **근거**: CLAUDE.md 「`transform`·`opacity` 만 애니메이트」. 점수 링은 **1,000ms** 로 "총 1초 초과 금지" 상한에 정확히 닿는다. (`prefers-reduced-motion` 은 `globals.css:311-323` 전역 규칙이 `transition-property` 를 색·opacity 계열로 좁혀 두므로 감소 모드에서는 안전하다 — 그 전역 규칙 자체가 페이드까지 죽여 정본과 어긋나지만 `globals.css:1005-1007` 이 이미 알고 있고 이 슬라이스 밖의 문제다.)
- **고칠 방향**: 진행률 바는 `scaleX` + `transform-origin:left` 로, 점수 링은 `stroke-dashoffset` 을 고정하고 `opacity`/`rotate` 로 등장시키거나 지속시간을 `--dur-slow`(≤300ms) 로 내린다.
- **영향 화면**: `/scriptquiz/play`, `/practice/dcp`

---

### [m6] 잘못된 `?book` 값이 전역 오류 화면을 띄우고 「다시 시도」가 영원히 실패한다 (minor · 축 A1/A4)

- **위치**: `lib/scriptquiz/questions.ts:78` · 폴백은 `app/error.tsx`
- **증상**: `/scriptquiz/play?book=abc&ch=1` (링크 공유·주소 손편집으로 도달 가능)로 들어가면 RPC 가 `invalid input syntax for type uuid` 를 돌려주고 `throw` → 전체 화면 「문제가 발생했어요」. 「다시 시도」 버튼은 같은 URL 을 다시 렌더하므로 몇 번을 눌러도 같은 화면이다.
- **근거**
  ```ts
  if (qErr) throw new Error(qErr.message)     // questions.ts:78
  ```
  `select_book_chapter_quiz(p_book_id uuid, …)` 실측 시그니처.
- **고칠 방향**: `play/page.tsx` 에서 `book` 이 uuid 모양인지 먼저 검사하고, 아니면 [M2] 에서 만들 「이 챕터에는 확인 문항이 없어요」 빈 상태로 보낸다(목업 폴백이 아니라). `fetchChapterQuizSession` 의 throw 는 서버 오류 전용으로 남긴다.
- **영향 화면**: `/scriptquiz/play`

---

### [m7] 아직 필요 없는 추천을 시작 화면 마운트에 미리 계산한다 (minor · 축 A5)

- **위치**: `components/game/scriptquiz/ScriptQuiz.tsx:63` · `lib/recommend/use-next-action.ts:79-91` · `lib/recommend/get-next-action.ts:173-181`
- **증상**: `useNextAction()` 이 컴포넌트 최상단에 있어 **시작 화면이 뜨는 순간** server action 이 나가고 `vocabularies` 카운트 2회 + `user_stats` 1회가 실행된다. 그 결과는 **결과 화면에서만** 쓰인다 — 시작 화면에서 이탈한 학습자에게는 전부 헛수고다. 반대로 세션이 20분 걸리면 "위급 단어 N개" 의 N 은 20분 전 값이다.
- **근거**
  ```ts
  const recommendation = useNextAction()     // :63 — screen 상태와 무관하게 마운트 시 1회
  ```
  ```tsx
  {screen === 'result' && (<ResultScreen … recommendation={recommendation} />)}   // :229-238
  ```
- **고칠 방향**: `useNextAction` 에 `enabled` 인자를 주거나 `ResultScreen` 안으로 호출을 옮긴다(결과 화면 마운트 시 1회).
- **영향 화면**: `/scriptquiz/play`

---

### [m8] 게임 화면에 하드코딩 색이 남아 있고 그중 하나는 대비가 의심스럽다 (minor · 프로젝트 규칙)

- **위치**: `ScriptQuiz.tsx:296` (`from-[#5BC8F5] to-[#1A7AB8]` QUIZ 워드마크), `:416` (`#FFD9D9`), `:563` (`stroke="#FFE234"`)
- **증상**: `#FFE234`(노랑) 체크마크가 `--success-light`(연녹) 배경 위에 놓인다 — 정답 표시의 유일한 아이콘인데 눈에 잘 안 띈다. `data-theme="dark"` 에서 세 값 모두 고정이다.
- **근거**: CLAUDE.md 「CSS Variables 로 테마 제어 — 하드코딩 금지 (**게임 전용 예외**)」. ScriptQuiz 는 게임 계열이라 워드마크 그라디언트는 예외로 볼 여지가 있지만, **정오 판정 아이콘의 색은 정보 전달이라 예외 대상이 아니다.**
- **고칠 방향**: `:563` 의 `stroke` 를 `var(--success)`(또는 `--on-p`)로 바꾼다. 나머지 둘은 게임 예외로 남겨도 되나 `data-theme="dark"` 실측은 한 번 필요하다.
- **영향 화면**: `/scriptquiz/play`

---

## 결함 없음으로 확인한 것 (억지로 채우지 않기 위해 남긴다)

- **모달로 학습 중단 없음** — ScriptQuiz 의 O/X 오버레이는 `pointer-events-none` 이고 800ms 뒤 사라진다. DCP 는 인라인 피드백이다.
- **인증 게이트 정합** — `/csat`·`/scriptquiz`·`/practice` 모두 `PROTECTED_PREFIXES`(`lib/auth/protected-routes.ts:25-57`)에 있고 미들웨어가 `?next=` 로 복귀시킨다. `/scriptquiz/play` 의 "비로그인 게이트는 미들웨어" 주석은 사실이다.
- **`/scriptquiz` 진입면의 빈 상태 3갈래** — 「다 확인했다 / 아직 안 읽었다 / 퀴즈가 없다」를 갈라 각각 다른 CTA 를 준다(`ScriptQuizQueue.tsx:156-200`). 이 슬라이스에서 가장 잘 된 빈 상태다.
- **`/csat/item/[slug]` 의 정답 미상 처리** — 정답표가 없는 회차에 추정 정답을 적지 않고 그 사실을 말한다(`csat/item/[slug]/page.tsx:67-73`). Empathetic Feedback 과 정확성을 둘 다 지켰다.
- **DCP 는 서버 채점** — `grade_dcp_item` RPC 가 `answer_key` 를 들고 있고 클라이언트에는 채점 뒤에만 내려온다. [B1] 이 고쳐야 할 목표 상태가 이미 같은 저장소 안에 있다.
- **`csat_items.passage` 미노출** — `csat_items_public` 뷰에 원문 컬럼 자체가 없고 학습자 로더가 service_role 을 쓰지 않는다(`lib/csat/learner.ts:9-23`). 저작권 경계는 코드 구조로 지켜져 있다.
- **`/csat` 3단에 경유 화면 없음** — 목록·유형·문항 각각이 다른 산출물을 낸다.
- **`memory_state` / `mastery_progress` / `last_days` / `next_days` 사용 0** — 슬라이스 전체 grep 무매칭.
