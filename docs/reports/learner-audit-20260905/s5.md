# S5 — 허브 · 셸(네비게이션) · 대시보드 · 계획 · 리포트 · 진단 · 설정 감사

감사일 2026-09-05 · 브랜치 `pc2-20260720-1` · 소스 무수정(읽기만)

## 요약

- **검사한 화면 (12)**: `/hub` · `/dashboard` · `/plan` · `/reports` · `/diagnostic` · `/diagnostic/history` · `/my`(→리다이렉트) · `/my/books` · `/my/texts`(→리다이렉트) · `/my/words`(→리다이렉트) · `/settings` · `/sitemap`
- **검사한 셸 (5)**: `(main)/layout.tsx` · `Sidebar`(데스크톱) · `MobileTabBar`(390px) · `MobileUtilityBar`(390px) · `CompassRibbon`+`WayfinderPanel` · `SessionFrame`
- **결함: blocker 2 · major 12 · minor 11**
- **셸에서 3번 이내로 닿지 않는 화면: 데스크톱 3개 · 모바일(390px) 8개** (§부록 A 표)

라우트 실계수: `app/(main)` + `app/(app)` = **학습자 라우트 77개**(`page.tsx` 기준). 과제문의 "91개"와 다르다 —
`(marketing)` 7 + `(auth)` 4 를 더해도 88 이다. 아래 계산은 실측 77 기준이다.

---

## 결함 목록

### [B1] 설정 12개가 아무 데도 저장되지 않는데 화면은 "저장됨"이라고 두 번 말한다 (blocker · 축 A1/A6)

- **위치**: `apps/web/src/app/(main)/settings/page.tsx:38-50`(`useStateWithSave`) · `:56-83`(`SaveIndicator`) · `:195-215`(12개 상태) · `:597`(푸터 문구)
- **증상**: `/settings` 에서 테마·모션 감소·집중 모드·TTS 속도 등 12개를 바꾸면 우측 상단에 **「저장됨」 배지**가 뜨고
  푸터에 **「변경 사항은 자동으로 저장됩니다.」** 가 인쇄돼 있다. 새로고침하거나 다른 화면에 갔다 오면 **전부 초기값으로 돌아간다.**
  DB·localStorage 어디에도 쓰지 않는다.
- **근거**:
  ```ts
  // :35-37 파일 자신의 주석
  // (실제 백엔드 저장은 Phase 2 — 현재는 시각 피드백만 제공)
  const wrapped = (v: T) => {
    setValue(v)
    window.dispatchEvent(new CustomEvent('settings:saved'))   // :44 — 이것이 "저장"의 전부
  }
  ```
  `const [theme, setTheme] = useStateWithSave<...>('system')` (`:205`) — 초기값이 상수 `'system'` 이라
  **다크를 쓰고 있는 학습자에게도 항상 System 이 선택돼 보인다.**
- **파생 결함(같은 원인)**: 로그인한 학습자에게 **테마를 바꿀 수단이 앱 전체에 없다.**
  실제 테마 정본은 `localStorage['vocaflow-theme']` 이고 이를 쓰는 곳은
  `app/layout.tsx:105-108`(적용) · `app/(auth)/layout.tsx:17-26`(로그인/가입 화면의 토글) ·
  `app/dev/components/page.tsx` **뿐이다**. 즉 다크로 바꾸려면 **로그아웃해서 로그인 화면으로 가야 한다.**
- **고칠 방향**: (a) 최소 조치 — `theme`·`reducedMotion` 두 개만이라도 `localStorage` 정본(`vocaflow-theme`)에
  읽기/쓰기로 배선하고, (b) 나머지 10개는 「준비중」 규약(같은 파일 `:470-500` 이 이미 쓰는 방식)으로 내리고
  `SaveIndicator` 와 푸터 문구를 제거한다. **눌리는데 아무 일도 없는 것보다 나쁜 것이 "됐다"고 말하는 것이다** —
  이 파일이 `:519-523` 주석에서 스스로 내린 판단이다.
- **영향 화면**: `/settings` (+ 테마 문제는 학습자 전 화면)

---

### [B2] 진단 40문항 응답이 메모리에만 있고 셸이 열려 있다 — 오클릭 한 번에 전부 소실 (blocker · 축 A6/A2)

- **위치**: `apps/web/src/components/diagnostic/DiagnosticClient.tsx:196`(`responses` state) · `:288-292`(누적) ·
  `:296-352`(submit) · `:374-390`(에러 화면) · `:584`(그만두기)
- **증상**: 진단은 최대 40문항이고 답은 전부 React state 에만 쌓인다.
  `/diagnostic` 는 `lib/layout/full-screen-routes.ts` 목록에 **없어서** 진단 중에도 사이드바·나침반 띠·하단 탭이
  그대로 떠 있다. 문항 30개를 푼 상태에서
  ① 셸 링크를 잘못 누르거나 ② 새로고침하거나 ③ 뒤로가기를 누르면 **30개가 전부 사라지고 확인 대화도 없다.**
  ④ 마지막 문항을 답한 뒤 `insert`/RPC 가 실패하면 에러 화면의 유일한 버튼이 「처음으로」(`:381-382`)라
  **40문항을 처음부터 다시 푼다.**
- **근거**:
  ```ts
  const [responses, setResponses] = useState<Response[]>([])          // :196
  const next: Response[] = [...responses, { question_id: q.id, knew }] // :288
  ```
  `beforeunload`·`localStorage`·`sessionStorage` 는 이 파일에 **0건**(grep 확인). 진행 중 저장 지점이 없다.
  ```ts
  if (insertErr || !insertData) { setError(insertErr?.message ?? '결과 저장 실패'); return }  // :316-319
  ```
  실패 시 `responses` 는 살아 있지만 화면이 `error` 분기로 전환되면서 **재시도 버튼이 없다.**
- **고칠 방향**: ① `responses`+`selectedTest.id`+`currentIdx` 를 `sessionStorage` 에 매 답마다 저장하고
  `start` 페이즈 진입 시 "이어서 하기" 를 제시한다. ② 에러 화면에 **「다시 제출」**(같은 `finalResponses` 로
  `submit` 재호출)을 「처음으로」보다 먼저 둔다. ③ `/diagnostic` 를 `ACTIVITY_FULL_SCREEN` 에 넣거나
  진행 중 셸 이탈에 확인을 붙인다(둘 중 하나 — 셋 다 할 필요는 없다).
- **영향 화면**: `/diagnostic`

---

### [M1] 셸이 모든 학습자 라우트에서 `vocabularies` 전량을 페이지네이션한다 — 풀스크린 세션 6곳에서는 결과를 통째로 버린다 (major · 축 A5)

- **위치**: `apps/web/src/app/(main)/layout.tsx:26` → `lib/learner/wayfinder-query.ts:57-62` →
  `lib/learner/growth-stats.ts:76-88`
- **증상**: `(main)` 레이아웃은 **모든** 하위 라우트에서 `fetchWayfinder()` 를 부르고, 그 안의
  `fetchGrowthStats()` 가 학습자의 `vocabularies` 전 행을 1,000행씩 끊어 **끝까지** 읽는다.
  그런데 `/flashcard/play` · `/spellforge/play` · `/pairflip/play` · `/scriptquiz/play` · `/dictate/session` ·
  `/wordvault/browse` 여섯 라우트는 `(main)` 아래에 있으면서 `isFullScreenRoute` 라
  `CompassRibbon`·`Sidebar`·`MobileTabBar`·`MobileUtilityBar` 가 **전부 null 을 반환한다** —
  즉 세션 시작 때마다 이 조회가 돌고 **한 픽셀도 그리지 않는다.**
- **근거**:
  ```ts
  // growth-stats.ts:79-88 — .limit() 이 아니라 전량 페이징이다
  pagedSelect<{ stability: number|null; last_review_at: string|null }>(
    (from, to) => lc.from('vocabularies').select('stability, last_review_at')
                    .eq('user_id', user.id).range(from, to),
    'growth-stats vocabularies')
  ```
  ```tsx
  // CompassRibbon.tsx:100 — 데이터를 다 받은 뒤에 버린다
  const hidden = isFullScreenRoute(pathname) || !data
  ```
- **고칠 방향**: 레이아웃에서 `fetchWayfinder()` 를 호출하기 전에 서버에서 경로를 판정한다
  (`headers()` 의 `x-invoke-path` 또는 `(main)` 아래 세션 라우트를 별도 route group 으로 분리).
  분리가 가장 깨끗하다 — `(app)/layout.tsx` 가 이미 그 형태다(`SessionFrame` 만 감싼다).
- **영향 화면**: `/flashcard/play` · `/spellforge/play` · `/pairflip/play` · `/scriptquiz/play` ·
  `/dictate/session` · `/wordvault/browse`

---

### [M2] `fetchTodayPrescription` 이 `cache()` 되지 않아 `/hub` 에서 `prescribe_today` RPC 가 2회 돈다 — 파일 주석이 반대로 적혀 있다 (major · 축 A5)

- **위치**: `lib/learner/prescription-actions.ts:91`(`export async function`, `cache()` 없음) ·
  `lib/learner/wayfinder-query.ts:8-19`(주석) · `app/(main)/hub/page.tsx:60-67`
- **증상**: `/hub` 를 한 번 열면 `prescribe_today`(SECURITY DEFINER · 학습자 stage 도출) 가 **두 번** 실행된다 —
  한 번은 셸(`fetchWayfinder`)이, 한 번은 페이지가.
- **근거**: `wayfinder-query.ts` 머리 주석은 이렇게 약속한다.
  > · 처방(prescribe_today) → `fetchTodayPrescription` 재사용 (cache)
  > 즉 이전 `fetchTodayStatus` 와 **같은 왕복 수**로 여섯 질문을 전부 답한다.

  그러나 `prescription-actions.ts` 는 `'use server'` 파일이고 `import { cache }` 가 없다(grep: `cache(` 0건).
  같은 계열로 `fetchStudyPlanItems`(`plan-actions.ts:147`)·`fetchReadingRoom`(`reading-room-actions.ts:43`)·
  `fetchManageOverview`(`manage-overview.ts:41`) 도 캐시되지 않는다.
- **파생**: `auth.getUser()` 는 `@supabase/ssr` 에서 GoTrue `/user` 왕복이다. `/hub` 한 번에
  미들웨어 1 + 처방 2 + growth-stats 1 + touched 1 + dcp 1 + reading-room 1 + plan-items 1 + gateway 1 = **9회**.
  `lib/supabase/server.ts` 의 `createClient` 도 캐시되지 않는다.
- **고칠 방향**: `fetchTodayPrescription`·`fetchStudyPlanItems`·`fetchReadingRoom` 을 `cache()` 로 감싼 순수
  조회 함수로 뽑고(`'use server'` 액션은 그 위에 얇게 둔다), `createClient` 와 `getUser()` 를 `cache()` 로 한 번만
  돌게 한다. 주석의 약속과 코드를 맞춘다.
- **영향 화면**: `/hub`(2배) · 나머지 학습자 라우트 전부(`auth.getUser` 다중 호출)

---

### [M3] `/dashboard` 가 `vocabularies` 를 서버에서 두 번 전량 읽고, 클라이언트가 다시 10개 쿼리로 같은 것을 읽는다 (major · 축 A5)

- **위치**: `app/(main)/dashboard/page.tsx:66` · `lib/learner/memory-horizon.ts:140-166` ·
  `lib/learner/growth-stats.ts:79` · `components/dashboard/RecentActivity.tsx:60` · `hooks/useHubData.ts:178-245`
- **증상**: `/dashboard` 한 번 렌더에
  ① 셸이 `vocabularies` 전량(`stability,last_review_at`) 페이징,
  ② 페이지가 `fetchMemoryHorizon` 으로 `vocabularies` 전량(7컬럼) + `learning_records` 전량 ×2 페이징,
  ③ `fetchManageOverview` 로 `user_profiles`·`user_stats`·`daily_activity`·`weekly_reports`,
  ④ 그리고 **클라이언트에서** `RecentActivity` 가 `useHubData()` 로 **10개 쿼리를 더** 던진다
  (프로필·streak·오늘 카운트·전체 단어 수·정확도 분자/분모·복습 due·최근 텍스트·learning_records 50·scores 50).
- **근거**:
  ```tsx
  // RecentActivity.tsx:60-72 — 10개 쿼리를 받아 그중 하나만 쓴다
  const { data, isLoading } = useHubData()
  const activities = data?.recentActivities ?? []
  ```
  `useHubData` 의 반환 타입(`hooks/useHubData.ts:45-79`)은 `user`·`stats`·`vrl`·`continueCard`·`modules`·
  `recentActivities` 6묶음인데 이 화면은 마지막 하나만 읽는다. 나머지 5묶음(쿼리 8개)은 버려진다.
- **고칠 방향**: `RecentActivity` 를 서버 컴포넌트로 내리고 `learning_records`+`scores` 두 쿼리만 하는
  전용 조회를 쓴다(`fetchMemoryHorizon` 이 이미 `learning_records` 를 읽으므로 거기서 파생할 수도 있다).
  `useHubData` 는 그러면 `/hub-lab`(개발용)만 남는다 — 함께 정리 대상.
- **영향 화면**: `/dashboard`

---

### [M4] 처방이 폴백일 때 셸과 `/hub` 가 서로 다른 것을 말하고, 셸 CTA 가 **이미 진단한 학습자를 진단으로 되돌린다** (major · 축 A4)

- **위치**: `lib/learner/wayfinder-query.ts:76-85` · `lib/learner/wayfinder.ts:206-211` ·
  `components/home/TodayStage.tsx:66-72`
- **재현 조건**: 진단 완료(`isDiagnosed=true`) + `prescribe_today` RPC 실패로 폴백
  (`prescription.unavailable === true` — `prescription-actions.ts:142`. 이 저장소에서 실제로 3주간 있었던 상태다).
- **증상**:
  · 셸 나침반 띠: 계단 점이 사라지고, 단 하나뿐인 CTA 가 **「진단 시작」 → `/diagnostic`** 이 된다.
    이미 V-Level 을 가진 학습자가 **모든 화면 최상단에서** 진단을 다시 받으라는 말을 듣는다.
  · 같은 화면 `/hub`: 무대는 **5블록을 그대로 그리고** 「오늘의 흐름 0/5」를 인쇄한다.
  → "한 화면에 진행이 두 종류" — 이 저장소가 `today-blocks.ts:170-179` 주석에서 고쳤다고 적어 둔 바로 그 실패다.
- **근거**:
  ```ts
  // wayfinder-query.ts:78-85 — 폴백이면 blocks 를 비운다
  const blocks = prescription && isDiagnosed && !unavailable ? buildTodayBlocks(...) : []
  ```
  ```ts
  // wayfinder.ts:206 — blocks 가 비면 '미진단' 국면으로 떨어진다
  if (!input.isDiagnosed || total === 0) { phase = 'undiagnosed'; now = diagnosticStep(...) }
  ```
  ```tsx
  // TodayStage.tsx:66-68 — 무대는 unavailable 을 보지 않는다
  const blocks = prescription?.isDiagnosed ? buildTodayBlocks(prescription, ...) : []
  ```
- **부작용(계측)**: `wayfinder_opened`/`wayfinder_cta_clicked` 의 `phase` 속성이 이 경우
  `'undiagnosed'` 로 기록돼 **퍼널이 진단 완료자를 미진단으로 센다**(`CompassRibbon.tsx:114,146`).
- **고칠 방향**: `WayfinderPhase` 에 `'unavailable'` 을 더하고, `buildWayfinder` 가
  `isDiagnosed && total===0` 을 `'undiagnosed'` 로 접지 않게 한다. 그 국면의 문장은
  `TodayStage.tsx:150-157` 이 이미 쓰는 문구("지금 오늘 분량을 계산하지 못했어요")를 재사용하고,
  CTA 는 `/hub` 로 보낸다. 동시에 `TodayStage` 도 `prescription.unavailable` 을 보고 블록을 접는다.
- **영향 화면**: 학습자 전 화면(띠) + `/hub`

---

### [M5] `/sitemap` 은 "모든 화면"이라고 말하지만 77개 중 **18개**만 싣는다 — WCAG 2.4.5 의 두 번째 길이 원래 길이 있던 화면에만 생겼다 (major · 축 A4)

- **위치**: `app/(main)/sitemap/page.tsx:38`(설명) · `:124-127`(본문) · `:129-171`(렌더 소스)
- **증상**: 화면이 **「Vocaflow 의 모든 화면입니다.」** 라고 쓰지만, 실제 렌더 소스는
  `META_ITEMS`+`NAV_GROUPS`+`ASIDE_GROUP`+`FOOTER_ITEMS` — 즉 **사이드바가 이미 아는 목록 그대로**다.
  중복·쿼리변형을 제외한 실제 링크 대상은 **18 라우트**(아래 §부록 A). 나머지 59개는 여기에 없다.
- **근거**: 이 화면이 스스로 적은 존재 이유가 자기 구현과 어긋난다.
  > 전수 계측에서 학습자 52 측정 중 **43 이 "내비게이션만 있고 둘 다 없음"** 이었다 (`:14-17`)

  그런데 목록을 내비게이션에서 그대로 파생하므로, **원래 내비에 없던 화면은 여전히 길이 하나뿐이다.**
  2.4.5 를 놓치고 있던 43개가 실제로는 그대로 남는다.
- **고칠 방향**: 목록 사본을 만들지 말라는 파일의 규칙은 옳다. 대신 **정적 라우트 매니페스트**를
  빌드타임에 만들어(`app/sitemap.ts` 가 이미 132 URL 목록을 갖고 있다 — `app/sitemap.ts:49-50` 참조)
  거기에 사이드바 그룹을 겹쳐 그린다. 목록은 여전히 한 곳(매니페스트)에서 온다.
  최소 조치로도 §부록 A 의 "셸이 안 파는 화면"(59개)만 「그 밖의 화면」 한 묶음으로 추가하면 된다.
- **영향 화면**: `/sitemap` (+ 셸에서 안 팔리는 59개 화면 전부)

---

### [M6] 27개 화면이 `<main>` 안에 `<main>` 을 그린다 (major · 축 A7)

- **위치**: `components/ui/ios/Screen.tsx:55-57` · `app/(main)/layout.tsx:58`
- **증상**: 레이아웃이 `<main id="main-content" tabIndex={-1}>` 로 children 을 감싸는데,
  `Screen` 은 `asMain` 기본값이 `true` 라 그 안에서 또 `<main>` 을 연다.
  스크린리더는 `main` 랜드마크를 2개로 읽고(중첩된 `main` 은 HTML 명세 위반), 랜드마크 탐색
  (NVDA `D`, VoiceOver 로터)이 두 번 멈춘다. 「본문으로 건너뛰기」 링크는 바깥쪽에 착지하므로
  안쪽 `main` 은 순수 잡음이다.
- **근거**:
  ```tsx
  // Screen.tsx:55-57
  asMain = true,
  const Inner = asMain ? 'main' : 'div'
  ```
  `app/(main)` 의 `<Screen>` 사용 파일 **28개** 중 `asMain={false}` 를 넘기는 곳은
  `app/(main)/sitemap/page.tsx:114` **하나뿐**이다. 여기에 더해 raw `<main>` 을 직접 여는 화면이 6개 더 있다
  (`my/books/page.tsx:19` · `csat/page.tsx:26` · `csat/[typeId]/page.tsx:37` ·
  `csat/plan/page.tsx:40` · `csat/item/[slug]/page.tsx:42` · `text/new/page.tsx:155` · `wordvault/page.tsx:109`).
- **고칠 방향**: `(main)` 레이아웃이 `main` 을 소유하므로 `Screen` 의 기본값을 `false` 로 뒤집고,
  `main` 이 필요한 다른 레이아웃(admin·marketing)에서만 `asMain` 을 켠다. raw `<main>` 7곳은 `div` 로.
- **영향 화면**: S5 전부(`/hub` `/dashboard` `/plan` `/reports` `/diagnostic` `/diagnostic/history`
  `/settings` `/my/books`) + `(main)` 나머지

---

### [M7] `/plan` 은 자료가 없으면 막다른 화면이다 — 파일 전체에 서재로 가는 링크가 없다 (major · 축 A3/D4)

- **위치**: `components/plan/PlanClient.tsx:495-498`
- **재현 조건**: 신규 학습자(도서 enroll 0 · 내 본문 0 · 구독 세트 0)가 `/dashboard` → Plan 카드로 진입.
- **증상**: 자료 선택 패널에 **「표시할 자료가 없어요.」** 한 줄만 남는다. 계획을 만들 방법이 없고,
  자료를 어디서 구하는지 알려 주는 링크가 화면에 없다.
- **근거**:
  ```tsx
  {tabMaterials[activeTab].length === 0 ? '표시할 자료가 없어요.' : '이 분류에 자료가 없어요.'}
  ```
  이 파일에서 `href=` 가 나오는 곳은 4군데뿐이며(`:1036` 열기 · `:1806`·`:1851` 활동 실행) 전부
  **이미 계획에 담긴 항목**을 여는 링크다. `/library`·`/text`·`/library/vocab` 로 가는 링크는 0건(grep).
- **고칠 방향**: 빈 상태 문구 아래에 탭별 목적지 링크를 둔다 —
  book→`/library/books` · article→`/library/scripts` · word_set→`/library/vocab` · script→`/text/new`.
  `MATERIAL_LABEL` 을 이미 import 하고 있으므로 목적지 표 하나만 더하면 된다.
- **영향 화면**: `/plan`

---

### [M8] `/dashboard` 히어로의 빈 상태에 다음 한 걸음이 없다 (major · 축 A3/D4)

- **위치**: `components/dashboard/DurabilityLadder.tsx:41-59`
- **증상**: 신규 학습자의 `/dashboard` 최상단 히어로가
  「단어를 담으면 여기서 기억이 자라는 걸 볼 수 있어요.」 / 「글을 읽고 모르는 단어를 담는 것이 첫걸음이에요.」
  **문장 두 줄로 끝난다.** 버튼도 링크도 없다. 바로 아래 `RescuedWords`(`:41-56`)는 같은 상황에서
  CTA 를 그리고 있어 형제끼리도 규약이 다르다.
- **근거**: `if (onLadder === 0) return (<section>…<p/><p/></section>)` — 자식에 `Link` 가 없다.
- **고칠 방향**: `RescuedWords.tsx:47` 이 쓰는 것과 같은 패턴으로 `/library/books`(또는 `/hub`) 링크를 붙인다.
- **영향 화면**: `/dashboard`

---

### [M9] `SessionFrame`('use client')이 모든 학습자 화면 번들에 게임 마크 19개의 JSX 를 끌고 온다 (major · 축 A5)

- **위치**: `components/layout/SessionFrame.tsx:15`(`'use client'`) · `:30`(import) · `:53-59` ·
  `app/(main)/layout.tsx:59-61` · `lib/game/catalog.tsx:127-149`
- **증상**: `(main)` 레이아웃이 **모든** children 을 `SessionFrame` 으로 감싸고, 그 클라이언트 모듈이
  `GAME_CATALOG` 를 import 한다. 같은 모듈에 `GAME_MARKS: Record<GameSlug, ReactNode>` 가
  19개 인라인 SVG JSX 로 들어 있어, `/settings`·`/reports`·`/plan` 같은 게임과 무관한 화면의
  클라이언트 번들에 함께 실린다. 루트/앱 `package.json` 에 `sideEffects` 플래그가 없어
  webpack 이 이 모듈을 부작용 있는 것으로 보고 통째로 유지한다.
- **근거**: 이 위험을 저장소가 이미 알고 있다 —
  ```
  // lib/layout/full-screen-routes.ts:24-27
  // 레지스트리는 `game/catalog` 를 거쳐 `GAME_MARKS`(ReactNode)까지 끌고 온다.
  // 사이드바·FlowNav·SessionFrame 이 그것을 import 하면 그 JSX 가 전 화면 번들에 딸려 온다.
  ```
  그 파일은 목록을 손으로 복사해 회피했는데, **`SessionFrame` 은 실제로 import 하고 있다**(`:30`).
- **고칠 방향**: `SessionFrame` 이 필요한 것은 `slug → {name, emoji, closeHref}` 뿐이다.
  `lib/game/session-meta.ts` 같은 JSX 없는 파생 상수 모듈을 만들고(빌드타임 생성 또는 catalog 에서 분리),
  `GAME_MARKS` 는 `lib/game/marks.tsx` 로 떼어 낸다. 드리프트는 기존 단위 테스트로 막는다.
- **영향 화면**: `(main)` 전 화면

---

### [M10] `/my/books`(512줄)는 셸이 "내가 소유한다"고 선언해 놓고 셸·사이트맵 어디에서도 갈 수 없다 (major · 축 A4)

- **위치**: `components/layout/sidebar-config.ts:158-160`(`owns: ['/my/books']`) ·
  `lib/framework/axes.ts:267`(`owns: [..., '/my/books']`) · `lib/text-viewer/workspace-href.ts:14-20` ·
  `components/workspace/WorkspaceBookContext.tsx:74`
- **증상**: 사이드바 `My Library` 항목은 `/my/books` 를 자기 소관으로 선언하지만 `href` 는 `/text` 다.
  그리고 `/text` 화면(`TextHubContent`)에는 `/my/books` 로 가는 링크가 **한 줄도 없다**(grep: 0건).
  `/sitemap` 에도 없다(사이드바 목록만 싣기 때문 — M5).
  유일한 진입은 **본문 워크스페이스 안의 breadcrumb** 이고, 그 라벨이 `axes.ts` 가 은퇴시킨 이름 **「BookVault」** 다.
- **근거**:
  ```tsx
  // WorkspaceBookContext.tsx:74-81 — 앱에서 /my/books 로 가는 단 하나의 상시 링크
  <Link href="/my/books" aria-label="책 목록으로 돌아가기"> <ArrowLeft/> BookVault </Link>
  ```
  ```ts
  // workspace-href.ts:16-18 — /my/books/[bookId] 는 "이론상 unreachable" 폴백에서만 쓰인다
  return t.nextTextId ? `/text/${t.nextTextId}?mode=read` : `/my/books/${t.bookId}`
  ```
  `app/(main)/my/books/page.tsx:12` 의 metadata `title` 도 `'BookVault'` — 은퇴한 이름이 화면 제목에 남아 있다.
- **고칠 방향**: 둘 중 하나로 정한다 — (a) `/my/books` 를 `/text?view=books` 로 합치고 라우트는
  redirect 로 남긴다(`/my/texts`·`/my/words` 가 이미 그 형태다), 또는 (b) 사이드바 `My Library` 의
  서브메뉴 `Books` 를 `/my/books` 로 돌리고 `/text?view=books` 를 은퇴시킨다.
  어느 쪽이든 「BookVault」 라벨과 metadata title 은 `Texts`/`My Library` 규약으로 교체한다.
- **영향 화면**: `/my/books` · `/my/books/[bookId]` · `/text`

---

### [M11] 학습자 화면 77개 중 진입 이벤트가 있는 화면 = 0 (major · 축 A8/D2)

- **위치**: `lib/analytics/events.ts:29-160` · `lib/analytics/funnel.ts:36-42`
- **증상**: `node scripts/analytics/check-instrumentation.mjs` 실행 결과
  정의된 공개 이벤트는 14종이고 **14종 전부 호출부가 존재한다**(미사용 이벤트 0 — 확인함).
  그러나 **그 14종 중 학습자(로그인 후) 화면에 붙은 것은 `wayfinder_opened`·`wayfinder_cta_clicked` 둘뿐이고,
  둘 다 셸의 「나의 자리」 패널 상호작용이지 화면 진입이 아니다.**
  나머지 12종은 전부 `(marketing)`·`/fit`·공용 단어장 서가에 있다.
  → **학습자 라우트 77개 중 진입 이벤트를 가진 화면은 0개.**
  `funnel_events` 쪽도 `teacher_hub_view`(`/teacher`) 하나가 전부다.
- **근거**(스크립트 실측 출력):
  ```
  정의된 이벤트 14종: fit_viewed · fit_analyzed · fit_shared · fit_share_opened ·
    fit_signup_clicked · fit_worksheet_printed · landing_viewed · landing_cta_clicked ·
    catalog_viewed · volume_previewed · landing_demo_moved · landing_section_reached ·
    wayfinder_opened · wayfinder_cta_clicked
  자체 DB(funnel_events): landing_viewed 26 · catalog_viewed 19 · teacher_hub_view 3 · fit_viewed 3
  ```
  호출부 전수(grep):
  | 이벤트 | 호출 파일 |
  |---|---|
  | fit_* 6종 | `components/textfit/*` |
  | landing_* 4종 | `components/marketing/*` |
  | catalog_viewed·volume_previewed | `components/library/vocab/VocabSetGrid.tsx` |
  | wayfinder_* 2종 | `components/layout/CompassRibbon.tsx:114,146` |
- **결과**: 진단 §6 이 요구하는 "가입 → 첫 학습 → 재방문" 중 **첫 학습 이후 구간이 관측 불가**다.
  `/hub` 를 봤는지, `/plan` 을 세웠는지, `/diagnostic` 을 시작했다가 중간에 나갔는지(B2 와 직결) 셀 수 없다.
- **고칠 방향**: 이벤트 계약(숫자·불리언·닫힌 열거형)을 지키면서 최소 3종을 더한다 —
  `screen_viewed { surface: SurfaceId }`(닫힌 4열거형) · `diagnostic_started { questions:number }` ·
  `diagnostic_abandoned { answered:number, total:number }`.
  마지막 둘은 B2 의 유실이 실제로 얼마나 일어나는지 재는 유일한 방법이다.
- **영향 화면**: 학습자 77개 전부

---

### [M12] `/hub-lab` 은 어디에서도 갈 수 없는 고아 라우트인데 학습자 보호 라우트로 배포돼 있다 (major · 축 A4)

- **위치**: `app/(main)/hub-lab/page.tsx` · `lib/auth/protected-routes.ts:29-30` · `app/robots.ts:36`
- **증상**: `/hub-lab` 을 가리키는 링크가 저장소 전체에 **0건**이다(자기 화면의 변형 전환
  `LabBar.tsx:30` `/hub-lab?v=` 제외). 사이드바·사이트맵·`/dev` 인덱스 어디에도 없다.
  그런데 로그인 게이트를 지나 `(main)` 셸(M1 의 전량 조회 포함)을 그대로 달고 서비스된다.
- **근거**:
  ```
  grep '/hub-lab' → app/(main)/hub-lab/LabBar.tsx:30 · app/robots.ts:36 · lib/auth/protected-routes.ts:29-30
  ```
- **고칠 방향**: 설계 랩이므로 `app/dev/` 아래로 옮기거나(`/dev` 인덱스가 이미 개발 화면 목록을 판다)
  프로덕션 빌드에서 제외한다. 남긴다면 `/dev/page.tsx` 목록에 한 줄 추가해 고아 상태를 없앤다.
  부수 효과로 `hooks/useHubData.ts`(457줄)의 유일한 나머지 사용처가 `RecentActivity` 하나가 되어 M3 정리가 쉬워진다.
- **영향 화면**: `/hub-lab`

---

### [m1] 한글 문장 3곳이 Lora(`font-english`)로 조판된다 (minor · 프로젝트 규칙)

- **위치**: `components/layout/WayfinderPanel.tsx:60` · `components/reports/ReportsClient.tsx:149` ·
  `components/plan/PlanClient.tsx:386`
- **증상**: `font-english` = `["var(--font-serif)","Lora","Iowan Old Style","Georgia","serif"]`
  (`tailwind.config.ts:128-129`). Lora 에는 한글 글리프가 없으므로 세 곳의 한국어가
  폴백 세리프로 떨어져 주변 `font-body` 문장과 서체가 어긋난다.
  - `WayfinderPanel` `Says()` 가 받는 문자열은 `forecastSentence`·`reachSentence`·`pastSentence`
    (`lib/learner/wayfinder.ts:255-266`) — 전부 한국어다.
  - `ReportsClient:149` — `report.empathetic_note`(한국어 격려 문구)
  - `PlanClient:386` — "자료를 고르고 — 챕터·활동·요일을 클릭, 주간 보드에 쌓여요."
- **주의**: CLAUDE.md 가 스스로 충돌한다 — 「철학 ③ … Lora italic "사람의 말투"」 대 
  「🚫 절대 하지 않을 것 · Typography · 한글에 Lora」. **어느 쪽이 정본인지 먼저 정해야** 고칠 수 있다.
  정본이 후자라면 세 곳을 `font-body italic` 으로 바꾼다.
- **영향 화면**: 학습자 전 화면(패널) · `/reports` · `/plan`

### [m2] `prefers-reduced-motion` 전역 규칙이 keyframe 애니메이션을 "낮추기"가 아니라 "끄기"로 처리한다 (minor)

- **위치**: `apps/web/src/app/globals.css:311-324`
- **근거**: `animation-duration: 0.01ms !important; animation-iteration-count: 1 !important;`
  transition 쪽은 `--dur-fast` 로 낮추고 `opacity`/색 계열만 남겨 **페이드는 보존**되지만,
  `@keyframes` 기반(스켈레톤 `animate-pulse`·스피너 `animate-spin`)은 완전히 죽는다.
  같은 파일 `:1005-1006` 이 이 어긋남을 이미 적어 두었다.
- **고칠 방향**: 전역 규칙에서 `animation-*` 를 빼고, 무한 루프 장식만 선택자로 지정해 끈다.
  로딩 스피너/스켈레톤은 상태 표시라 남겨야 한다(현재는 정지해 로딩 중임을 알 수 없다).

### [m3] `text-white` 하드코딩 (minor · 프로젝트 규칙)

- **위치**: `components/reports/ReportsClient.tsx:84`
- `bg-[var(--p)]` 위에 `text-white` — 토큰 `--on-p` 가 있는데 쓰지 않았다. 같은 줄의
  `motion-reduce:transition-none` 도 m2 와 같은 "끄기" 패턴이다.

### [m4] 모든 `(main)` 화면이 최소 한 화면보다 길다 — 빈 화면에서도 스크롤이 생긴다 (minor · A5)

- **위치**: `components/ui/ios/Screen.tsx:61`(`min-h-dvh`) · `app/(main)/layout.tsx:52-77` ·
  `components/layout/MobileTabBar.tsx:82-83`(`h-[var(--tabbar-h)]` 스페이서)
- 세로 flex 컬럼에 `MobileUtilityBar` + `CompassRibbon` + `min-h-dvh` 본문 + 탭바 스페이서가 차례로 쌓인다.
  390px 에서 문서 높이는 항상 `100dvh + 리본 + 유틸리티바 + 탭바` 가 되어, 내용이 한 줄뿐인 화면
  (예: 비로그인 `/dashboard` 안내)에서도 스크롤바가 생긴다.
- 고칠 방향: `Screen` 의 `min-h-dvh` 를 `min-h-0 flex-1` 로 바꾸거나 레이아웃 쪽에서 높이를 잡는다.

### [m5] `Segment` 가 `role="radio"` 를 쓰면서 화살표 키를 다루지 않는다 (minor · A7)

- **위치**: `app/(main)/settings/page.tsx:157-190`
- `role="radiogroup"` + `role="radio"` 를 선언했으므로 WAI-ARIA 상 방향키로 이동하고
  그룹 전체가 Tab 정지 하나여야 한다. 실제로는 각 버튼이 개별 Tab 정지이고 방향키 처리가 없다.
  라디오 3개짜리 그룹이 4개(테마·회상 대기·보이스) 있어 Tab 12번을 먹는다.
- 고칠 방향: 방향키 핸들러 + `tabIndex` roving 을 넣거나, `role` 을 떼고 그냥 버튼 그룹으로 둔다.

### [m6] `MobileUtilityBar` 는 2항목 전제로 쓰였는데 지금 4항목을 줄바꿈·스크롤 없이 그린다 (minor · A7)

- **위치**: `components/layout/MobileUtilityBar.tsx:24-27`(주석) · `:60-63`(컨테이너)
- 주석은 「항목이 둘뿐이다. 열고 닫는 상태를 만들면…」 이라고 근거를 대는데,
  `FOOTER_ITEMS` 는 그 뒤 CSAT Types · Sitemap 이 추가돼 **4개**가 됐다(`sidebar-config.ts:290-345`).
  컨테이너는 `flex items-center justify-end gap-1` 로 `flex-wrap`·`overflow-x-auto`·truncate 가 전부 없다.
  각 항목이 `min-h-[44px] px-3` + 16px 아이콘 + 라벨(가장 긴 것이 "CSAT Types")이라 390px 에서 여유가 거의 없다.
- 고칠 방향: `overflow-x-auto` + `shrink-0` 를 주거나(가로 스크롤 허용) 라벨을 아이콘+`aria-label` 로 접는다.
  실제 390px 렌더 측정으로 넘침 여부를 먼저 확인할 것.

### [m7] `/diagnostic/history` 가 DB 오류 메시지를 학습자에게 그대로 인쇄한다 (minor · A1)

- **위치**: `app/(main)/diagnostic/history/page.tsx:68-72`
- `<p>{error.message}</p>` — PostgREST/RLS 원문(영문 + 정책명)이 노출된다.
  게다가 오류가 나도 아래 `HistoryTimeline` 이 빈 배열로 함께 렌더돼 "기록 없음"과 "읽기 실패"가 구분되지 않는다.
- 고칠 방향: 사용자 문구로 감싸고, 오류일 때는 타임라인 대신 재시도만 보여 준다.

### [m8] `useHubData` 는 오류를 콘솔로만 남긴다 — 화면에 실패 상태가 없다 (minor · A1)

- **위치**: `hooks/useHubData.ts:161-175`(`unwrap`) · `components/dashboard/RecentActivity.tsx:71-72`
- 10개 쿼리 중 어느 것이 실패해도 `console.error` 뒤 `null` 로 접히고, 화면은
  "아직 학습 활동이 없어요" 를 보여 준다 — **읽기 실패와 진짜 빈 상태가 같은 그림**이다.
  `prescription-actions.ts:50-61` 이 `unavailable` 을 도입해 고친 것과 정확히 같은 결함이다.

### [m9] 진단 「시작」 버튼에 로딩 상태가 없다 (minor · A1)

- **위치**: `components/diagnostic/DiagnosticClient.tsx:266-284`(`startTest`)
- `startTest` 는 `vrl_diagnostic_questions` 를 다 받은 뒤에야 `setPhase('question')` 을 하는데
  진행 중 상태 플래그가 없다. 느린 회선에서 버튼을 눌러도 화면이 그대로라 학습자가 다시 누른다
  (`start` 페이즈의 목록 로딩(`:524-528`)에는 스피너가 있어 형제끼리 규약이 다르다).

### [m10] `TodayQueue` 빈 상태에 다음 걸음이 없다 (minor · A3)

- **위치**: `components/hub/TodayQueue.tsx:159-164`
- 「오늘 만날 단어가 없어요. 단어장을 추가하거나 새 라운드를 시작해보세요.」 — 두 행동을 말해 놓고
  **둘 다 링크가 아니다.** 사용처는 `/flashcard`·`/spellforge` 허브(S5 밖이지만 `components/hub/` 소유).

### [m11] `funnel.ts` 는 자유 문자열·임의 객체를 받는다 — D3 계약이 `events.ts` 에만 걸려 있다 (minor · A8/D3)

- **위치**: `lib/analytics/funnel.ts:44-47`
- `RecordOptions { surface?: string; meta?: Record<string, unknown> }`.
  `events.ts` 는 "자유 문자열이 하나도 없다는 것이 지문이 샐 수 없다의 구조적 근거" 라고 못 박는데
  같은 폴더의 이 파일에는 그 방어가 없다(현재 호출부 2곳은 리터럴 `'/teacher'` 라 실제 유출은 없다).
  고칠 방향: `surface` 를 `SurfaceId`·라우트 열거형으로 좁히고 `meta` 에 `isSafeProps` 를 태운다.

---

## 부록 A — 셸 도달 거리

### A-1. 셸이 직접 파는 목적지 (18 라우트)

| 표면 | 데스크톱 사이드바 | 모바일 하단탭 | 모바일 유틸리티바 | `/sitemap` |
|---|---|---|---|---|
| `/hub` | ✓ META | ✓ Today | | ✓ |
| `/dashboard` | ✓ META | ✓ Growth | | ✓ |
| `/library`(→`/library/books`) | ✓ | ✓ Library | | ✓ |
| `/library/scripts` `/library/vocab` `/library/textbooks` | 펼침 자식 | | | ✓ |
| `/text` (+`?view=` 4면) | ✓ | | | ✓ |
| `/wordvault` | ✓ | ✓ Vault | | ✓ |
| `/practice` | ✓ | | | ✓ |
| `/arcade` | ✓ Game Lab | | | ✓ |
| `/scriptquiz` | ✓ | | | ✓ |
| `/dictate` | ✓ | | | ✓ |
| `/comics/adapted` `/comics/restored` | ✓ ASIDE | | | ✓ |
| `/csat` `/teacher` `/settings` | ✓ FOOTER | | ✓ | ✓ |
| `/sitemap` | ✓ FOOTER | | ✓ | (자기 제외) |

**데스크톱 = 18 · 모바일 하단탭 = 4 · 모바일 유틸리티바 = 4.**
모바일에서 `/practice` `/arcade` `/scriptquiz` `/dictate` `/text` `/comics/*` 로 가는 상시 길은
**`/sitemap` 한 곳뿐**이다(사이드바가 `hidden md:flex`). 즉 폰에서는 그 6개가 전부 한 단계 깊어진다.

### A-2. 3번 이내로 닿지 않는 화면

계수 규칙: 셸(사이드바 항목·하단탭·유틸리티바·사이트맵) 클릭을 1로 세고, 그 뒤 화면 안 링크를 더한다.
학습자 상태에 의존하지 않는 경로만 센다.

**데스크톱 (1280px) — 3개**

| 화면 | 최단 경로 | 클릭 |
|---|---|---|
| `/hub-lab` | (경로 없음 — 인바운드 링크 0건) | ∞ |
| `/library/textbooks/[step]/practice` | Library① → Textbooks 탭② → `[step]`③ → 연습④ | 4 |
| `/my/books/[bookId]` | My Library① → 본문② → BookVault③ → 책④ | 4 |

**모바일 (390px) — 8개** (위 3개 + 사이드바 부재로 한 단계씩 밀린 5개)

| 화면 | 최단 경로 | 클릭 |
|---|---|---|
| `/hub-lab` | — | ∞ |
| `/library/textbooks/[step]/practice` | Library① → Textbooks② → `[step]`③ → 연습④ | 4 |
| `/my/books` | Sitemap① → My Library② → 본문③ → BookVault④ | 4 |
| `/my/books/[bookId]` | 위 + 책⑤ | 5 |
| `/flashcard/play` | Sitemap① → Practice② → Flashcard③ → 시작④ | 4 |
| `/spellforge/play` | Sitemap① → Practice② → SpellForge③ → 시작④ | 4 |
| `/pairflip/play` | Sitemap① → Practice② → PairFlip③ → 시작④ | 4 |
| `/text/[id]/echo` · `/text/[id]/comic` | Sitemap① → My Library② → 본문③ → 모드 필④ | 4 |

> ⚠️ 위 표는 **상시 내비 경로**만 센다. `/flashcard/play` 등 세션 라우트는 그날의 처방이 그 블록을 지목하면
> 나침반 띠 CTA 로 **1클릭**에 열린다(`CompassRibbon.tsx:139-152`). 그 지름길은 학습자 상태에 의존하므로
> 도달 보장으로 세지 않았다.

**상태 의존이라 보장되지 않는 경로** (참고):
`/diagnostic/history`(이전 스냅샷이 있어야 `/diagnostic` 에 「기록 보기」가 뜬다 — `DiagnosticClient.tsx:498-520`) ·
`/my/books`(enroll 한 책의 워크스페이스에 들어가야 breadcrumb 이 생긴다) ·
`/pairflip/results`·`/dictate/results`(세션 종료 후에만).

### A-3. 셸이 팔지 않는 화면 = 59개

`(main)`+`(app)` 77개 중 A-1 의 18개를 뺀 나머지. 전부 화면 안 링크로만 닿고, `/sitemap` 에도 없다(M5).
그중 리다이렉트 껍데기 4개(`/my` `/my/texts` `/my/words` `/comics`)와 아케이드 19종을 빼면
**실질 36개 화면이 "길이 하나뿐"** 이다 — `/sitemap` 이 해결하려던 WCAG 2.4.5 문제가 그대로 남아 있는 집합이다.

---

## 부록 B — 확인했으나 결함이 아닌 것

- **셸 최상위의 `'use client'`**: `(main)/layout.tsx` 는 서버 컴포넌트이고, 클라이언트 경계는
  `Sidebar`·`MobileTabBar`·`MobileUtilityBar`·`CompassRibbon`·`SessionFrame` 다섯 개로 국한돼 있다.
  "셸 최상위가 클라이언트라 전부 클라이언트가 되는" 구조는 아니다. (다만 `SessionFrame` 의 import 는 M9.)
- **정의됐지만 호출되지 않는 이벤트**: 0건. `EVENT_REGISTRY` 를 `Record<PublicEventName, true>` 로
  적어 둔 방어(`events.ts:120-137`)가 실제로 작동하고 있다. 문제는 미사용이 아니라 **커버리지**다(M11).
- **`memory_state` / `mastery_progress` / `last_days` / `next_days` 컬럼 사용**: S5 범위에서 0건.
  기억 상태는 전부 `getMemoryState()`(R(t) 동적 계산)를 거친다(`growth-stats.ts:97-113`).
- **v5 롱폼 색 토큰(`--color-*`)**: S5 범위에서 0건.
- **셸 상태 표면 중복**: `streak` 은 `CompassRibbon.tsx:163-173` 한 곳에서만 그려진다.
  ADR 0006 D2 는 지켜지고 있다.
- **`/hub` 의 "오늘 할 일" 정본 단일화**: `hasTodayPlan` → `TodayPlanCard`,
  진단완료 → `TodayStage`, 미진단 → `TodayFocus` 로 배타 분기한다(`hub/page.tsx:71-110`).
  같은 계산이 `/dashboard`·`/plan` 에 복제돼 있지 않다 — `/dashboard` 는 회고(`memory-horizon`),
  `/plan` 은 수동 계획(`study_plan_items`)으로 축이 분리돼 있다. **단, M4 의 폴백 경로에서만 갈라진다.**
- **44px 터치 타깃**: S5 범위에서 반복 점검 흔적(주석에 실측치 기록)이 많고, 표본 확인한
  `ManageSection:148` · `MobileTabBar:112`(h-14) · `Sidebar:479` · `ReportsClient:54` 모두 44px 이상이다.
