# 초·중 원문을 고등의 절반까지 — 먼저 「적합」의 자부터 실측으로 바꿨다

**시작 2026-09-04** · 목표: 초·중 밴드 원문을 **고등 재고의 절반** 규모로, 시중 교재 규격에
어긋나지 않게 확보한다. 이 문서는 사이클마다 이어 쓴다.

---

## 0. 목표를 숫자로 고정한다

「고등과 비슷하게 · 고등 목표량의 반절」을 이렇게 잰다 (전부 2026-09-04 DB 실측):

| | 편수 | 근거 |
|---|---|---|
| 고등 재고 (5~7단 = V5~V9) | **18,320** | `library_articles` ready+published · display_only 제외 |
| 그중 문항이 붙은 원문 | 17,786 | `csat_dcp_items` distinct ref_id |
| **초·중 목표 (절반)** | **9,160** | 위의 1/2 |

**적합의 정의 — 세 축을 동시에 통과해야 1편이다.**

| 축 | 자 | 출처 |
|---|---|---|
| ① 어수창 | 밴드별 시중 실측 창 | `market-spec.json` (⚠️ §3 — 오염 확인됨) |
| ② FK 밴드 | 초3~4 1.5~4.0 … 중3 8.5~12.0 | 시중 79종 FK 사다리 |
| ③ 교육과정 어휘 | 밖 % ≤ 시중 p90 | **본 사이클에서 실측으로 교체** (§2) |
| ④ 라이선스 | 재배포·파생 허용이 글 안에 | 소스별 확인 |

한 축만 보면 반드시 틀린다는 것은 이미 두 번 실측했다 — 어수만 보면 FK 15.37 짜리 NASA
사진설명이 초6 자리를 통과했고(45% 오분류), FK 만 보면 `Little Women`(1868)이 초6~중1로 나온다.

---

## 1. 현재 재고 — 3축을 동시에 걸면 **108편**이다

`grade-level-bench.mjs`(3축 판정 추가) 실측:

| 밴드 | FK창 | 어수창 | FK 통과 | +어수 | **+어휘 = 적합** | 시중 자리 |
|---|---|---|---|---|---|---|
| 초3~4 | 1.5~4.0 | 44~121 | 71 | 62 | **50** | 40.3 |
| 초5~6 | 3.5~5.5 | 44~121 | 79 | 62 | **47** | 60.9 |
| 초6~중1 | 4.5~7.0 | 44~121 | 47 | 6 | **4** | 66.3 |
| 중1~2 | 6.5~9.0 | 46~154 | 30 | 6 | **4** | 43.0 |
| 중3 | 8.5~12.0 | 42~153 | 24 | 17 | **3** | 66.4 |
| | | | | | **108** | |

**달성률 108 / 9,160 = 1.2%.**

탈락 사유(밴드 안 251편 기준): **어수창 98 · 어휘 52.**
`시중 자리`는 그 밴드 적합분의 어휘 난이도가 시중 분포 어디에 놓이는가다(50 = 시중 중앙).

---

## 2. 어휘 자를 짐작에서 실측으로 바꿨다

`curriculum.ts` 의 문턱은 `maxOutsidePct: 40` 하나였고, **그 파일이 스스로 "재서 정한 값이
아니다" 라고 적어 두고 있었다.** 목표가 "시중 교재에 부합" 인데 자가 짐작이면 부합 여부를
말할 수 없다. 그래서 쟀다.

### 시중 지문을 캐내는 도구 — `passage-mine.mjs` (신설)

기존 `passage-ruler.mjs` 는 출판사가 인쇄한 `129 words` 표식을 단서로 뽑는다. 정확하지만
표식 있는 쪽에만 쓸 수 있어 **초등 표본이 1~4편**이었다. 표식 없는 쪽에서도 캐내야 했다.

**어수·FK 는 이 도구로 못 잰다 — 시도했고 자가 검증에서 막혔다.** 표식 있는 쪽에서 캐낸
덩어리를 선언 어수와 맞대니 편차 중앙 **53.5%** · ±25% 안 25%였다. PDF 다단 조판이라 한 지문이
여러 덩어리로 갈리거나 옆 단이 섞인다. 그 상태의 문장 길이는 원문의 문장 길이가 아니다.

**그런데 교육과정 별표 적중은 덩어리 경계에 영향받지 않는다** — 어떤 낱말이 나왔는지만 세기
때문이다. 그래서 어휘 축 하나만 내도록 도구를 좁혔고, 검증도 그 축으로 바꿨다:

> 같은 쪽을 ①표식 기반 정확 추출 ②쪽 단위 추출 두 방법으로 재어 `outsidePct` 를 비교.
> **표본 49쪽 · 차이 중앙 1.2%p · ±7%p 안 90%.**

### 실측 결과 — 문턱 40 은 시중 분포의 **p75** 였다

시중 초·중 독해 교재 지문 실린 쪽 **196** (NE능률 8시리즈):

| 학교급 | 표본 | p05 | p25 | **p50** | p75 | **p90** | p95 |
|---|---|---|---|---|---|---|---|
| 초등 | 129 | 13.2 | 24.1 | **30.3** | 37.2 | **43.3** | 49.2 |
| 중등 | 67 | 20.0 | 29.7 | **34.7** | 41.7 | **44.0** | 48.1 |

**문턱 40 = 시중 p75.** 실제 시중 지문 4편 중 1편을 우리 게이트가 떨어뜨리고 있었다.
시중에 부합하는 것이 목표인데 **시중보다 좁은 자**를 대고 있었던 것이다.

→ 문턱을 **학교급별 시중 p90**(초등 43.3 · 중등 44.0)으로 교체했다. p95 로 하지 않은 이유는
§아래.

### 통과율만 보면 안 된다 — 분포가 겹쳐야 부합이다

p95 로 두면 시중 지문 95%가 통과하지만, **우리 재고가 분포의 가장 어려운 끝에 몰려도 전부
통과한다.** 통과율이 같아도 분포가 다르면 부합이 아니다. 그래서 문턱은 p90 으로 두고,
지문마다 **시중 분포에서의 자리**(`marketPercentile` 0~100)를 함께 낸다.

너무 쉬운 쪽은 **막지 않는다** — 막으면 낮은 칸에 쓸 글까지 잃는다(StoryWeaver L1 이 그 예다:
FK 1.42 로 초4 교재 1.81 보다도 아래지만 초3~4 칸에는 쓸 수 있다). 자리로 알리고 배치로 푼다.

### 자를 한 곳으로 모았다

`grade-level-bench.mjs` 가 `shared_dictionary.list_tags` 를 직접 읽어 **어휘 자의 사본**을
들고 있었다. FK 에서 이미 겪은 실수라(같은 공식 세 벌) 패키지 정본을 쓰도록 바꿨다.
`space-place-ingest.mjs` 도 목표 칸이 초등이면 초등 자를 대도록 고쳤다.

회귀 `curriculum.test.ts` **18종**(+8) · 패키지 전체 **1,250 tests 통과** · `tsc --noEmit` exit 0.

---

## 3. ⚠️ 어수창이 오염돼 있다 — 다음 사이클의 첫 일

3축 판정에서 탈락 1위가 **어수창 98편**인데, 그 창 자체가 시중과 어긋난다.
출판사가 **스스로 인쇄한 어수**(`passage-ruler.mjs` · n=59)와 우리 창을 나란히 두면:

| | 최소 | p10 | 중앙 | p90 | 최대 |
|---|---|---|---|---|---|
| **시중 선언 어수** (초등 6 · 중등 53) | **97** | 107 | **132** | 177 | 198 |
| 우리 창 초6 (`market-spec`) | 40 | 44 | 62 | 121 | 194 |
| 우리 창 중1 | 40 | 46 | 84 | 154 | 197 |

**시중은 97어 밑을 한 건도 선언하지 않는데 우리 창의 중앙은 62~84어다.** 즉 우리 게이트는
시중 규격에 맞는 글(132어)을 **떨어뜨리고** 시중에 없는 짧은 글(62어)을 통과시킨다.
`초6~중1` 밴드에서 47편 중 6편만 어수창을 통과한 것이 그 증상이다.

원인은 이미 기록돼 있다(`passage-length-recheck-20260903.md`) — 추출기가 선택지 묶음(40~50어)을
지문으로 세어 분포의 아래쪽이 오염됐다. 가드가 있지만 잡지 못하는 종류가 남아 있다.

→ **다음 사이클: 어수창을 선언 어수 기반으로 교체하고 재측정한다.** 창이 바뀌면 §1 의 108편도
바뀐다(늘 수도 줄 수도 있다 — 재서 안다).

---

## 4. 어수창도 두 벌이었다 — 그리고 베이스라인이 108 → 470 으로 정정됐다

§3 을 고치러 갔더니 **정본은 이미 있었다.** `readability.PASSAGE_WORDS = {100, 200}` 이
2026-09-03 선언 어수 실측에서 나와 있었고, `market-spec.json` 은 "다른 곳에서도 쓰는 규격이라
건드리지 않았다" 고 그때 적혀 있었다. 문제는 **그 정본을 안 쓰는 곳이 둘 있었다**는 것이다.

| 자 | 값 | 근거 |
|---|---|---|
| `readability.PASSAGE_WORDS` | 100~200 | 출판사 선언 어수 실측 (n=59) |
| `assemble-unit.PASSAGE_WORDS` | **120~250** | 2026-08-21 에 "넉넉히" 로 **정한** 값 |
| `grade-level-bench` 조회 창 | **42~173** | 오염된 `market-spec` 합집합 |

수집기는 100~200 으로 담고 조립기는 120~250 으로 판정한다 — **100~119어 글은 수집되지만
조립기가 막는다.** 오류가 안 나서 안 보이는 종류다. 벤치의 42~173 은 더 나빴다:
**시중 규격의 174~198어 지문이 조회에서부터 빠지고** 시중에 없는 62어 글이 들어왔다.

→ 셋을 실측 쪽으로 합쳤다. **같은 재고를 바른 자로 다시 재니 108 → 470편이다.**

| 밴드 | 어수창 | FK 통과 | +어수 | **적합** | 시중 자리 |
|---|---|---|---|---|---|
| 초3~4 | 100~200 | 48 | 48 | **43** | 35.1 |
| 초5~6 | 100~200 | 100 | 100 | **98** | 26.6 |
| 초6~중1 | 100~200 | 188 | 188 | **187** | 18.8 |
| 중1~2 | 100~200 | 131 | 130 | **130** | 14.9 |
| 중3 | 100~200 | 22 | 22 | **12** | 66.4 |
| | | | | **470** | |

**달성률 470 / 9,160 = 5.1%.**

---

## 5. 통과했는데 부합은 아니다 — 재고의 78%가 시중보다 쉽다

적합 470편을 소스별로 갈라 **시중 분포에서의 자리**를 보면:

| 소스 | 적합 | 시중 자리(중앙) | 어수(중앙) | 읽는 법 |
|---|---|---|---|---|
| **original**(우리 재저작) | **367** | **16.9** | 187 | 시중보다 **훨씬 쉽다** |
| space_place | 51 | 63.0 | 132 | 시중 결에 맞다 |
| storyweaver | 23 | 67.4 | 115 | 맞다 |
| nasa | 10 | 35.1 | 111 | 맞다 |
| futurity | 9 | 66.4 | 164 | 맞다 |
| voa · usgs · noaa | 10 | 4.3~19.9 | ~110 | 쉽다 |

**재고의 78%가 우리가 쓴 글이고, 그 글들이 시중 분포의 왼쪽 끝(자리 16.9)에 몰려 있다.**
게이트는 통과한다 — 게이트는 상한만 보기 때문이다. 그러나 "시중 교재에 부합" 을
분포로 물으면 **아직 아니다.**

이건 소스 문제가 아니라 **작문 지침 문제**다. 지침이 "쉽게 쓰라" 고만 말하고 어휘 대역을
말하지 않았다. 같은 종류의 문제를 수능 작문 드레인에서 이미 풀었다 — 낱말 길이가 대역을
벗어나 떨어지자 **측정-수정 루프 한 바퀴**로 붙였다(`csat-source-fit-20260903.md` §15).

→ 재저작·각색 지침에 **어휘 대역(시중 자리 35~65)** 을 넣고, 드레인 import 가 그 값을
   함께 채점하게 한다. 대역 밖이면 반려하고 이유를 숫자로 돌려준다.

---

## 6. 공급선 — PD 발췌가 주력이다 (실측 2026-09-04)

`longform-pd-probe.mjs` 를 **바뀐 자**(어수창 100~200 · 어휘 문턱 학교급별)로 다시 돌렸다.
돌리기 전에 두 가지를 고쳐야 했다:

- **겹치는 창을 세고 있었다.** 문단을 5칸씩 옮기며 창을 만드는데 창 하나가 3~8문단이라
  이웃 창끼리 문단을 나눠 갖는다. 그렇게 센 수는 수율이 아니다 — 적재하려면 조각끼리
  겹치면 안 된다(같은 문단이 두 지문에 실린다). **창을 만들면 끝 다음으로 건너뛰게** 고쳤다.
- **어휘 가드가 `outside <= 40` 을 박아 두고 있었다.** §2 에서 바꾼 문턱을 안 쓰고 있었다.

| 칸 | FK 적중 | 어휘가드 통과 | 탈락 | **책당** |
|---|---|---|---|---|
| 초3~4 | 376 | 232 | 38% | **23.2** |
| 초5~6 | 484 | 283 | 42% | **28.3** |
| 초6~중1 | 539 | 365 | 32% | **36.5** |
| 중1~2 | 612 | 457 | 25% | **45.7** |
| 중3 | 611 | 481 | 21% | **48.1** |
| 합계 | 2,622 | **1,818** | 31% | **181.8** |

**책당 비중복 182편.** 목표 9,160편은 **PG 아동물 51권** 발췌로 닿는다 — 전체 7,636권 중 51권이다.

| | |
|---|---|
| 라이선스 | PD. PG 가 "any commercial use, republishing, **making derivative works**" 를 명시 |
| **시중 자리** | p25 46.8 · **중앙 65.2** · p75 77.7 |

**이 값이 재저작문(16.9)과 정반대 방향이라는 것이 중요하다.** 둘을 섞으면 분포가 시중 중앙
(50) 근처로 모인다. 한쪽만으로는 어느 쪽이든 시중과 안 겹친다.

⚠️ **책당 182 는 하한이다** — 한 책을 400창까지만 본다(Little Women 3,778문단은 거기서 끊긴다).
⚠️ **아직 안 잰 축이 있다 — 소재와 시대.** 19세기 아동 소설이 시중 교재의 소재 결
(환경·기술·문화 설명문 + 현대 이야기)과 맞는지 재지 않았다. 어휘와 난이도가 맞아도
소재가 어긋나면 "학습 가치가 시중에 부합" 이라고 말할 수 없다. **다음 사이클의 첫 일.**

---

## 7. 쓰는 글에는 어휘 **대역**을 건다 (수확한 글에는 안 건다)

§5 의 처방을 배선했다. `AUTHORED_VOCAB_BAND` = 시중 **p25~p90**
(초등 24.1~43.3% · 중등 29.7~44.0%).

- 상한은 수확 게이트와 같다 — 그 위는 그 학년이 못 읽는다.
- **하한이 새로 생긴 것**이고, 근거는 부합이 아니라 **학습 가치**다. 밖 낱말이 15%인 글은
  30%인 글보다 새 낱말을 그만큼 덜 가르친다. 쉽게 쓰는 것은 배려가 아니라 가르칠 것을 뺀 것이다.
- 수확한 글에는 하한을 걸지 않는다 — 있는 글을 고르는 일과 쓰는 일은 다르다.
- 막을 때 **무엇을 하라고** 말한다("새 낱말을 더 넣어 다시 쓴다" / "별표 안 낱말로 바꾼다").
  수능 작문 드레인에서 낱말 길이 대역을 숫자로 주자 한 바퀴에 붙었던 것과 같은 설계다.

배선: `adapt-drain-export` 가 청크에 `vocabulary_band` 를 싣고, `adapt-drain-import` 가
**같은 자**로 채점해 대역 밖이면 이유를 숫자로 돌려준다.

---

## 8. 사이클 기록

```
[Cycle 1] 달성률: 1.2%  (근거: 3축 적합 108 / 목표 9,160 — 어수창이 틀린 채 잰 값)
✅ 어휘 문턱을 짐작(40)에서 시중 실측 p90(초등 43.3·중등 44.0)으로 교체 ·
   시중 지문 채굴기 신설(자가 검증 1.2%p) · 3축 동시 판정 · 어휘 자 사본 제거
❌ 어수창이 시중과 어긋남(시중 최소 97어 vs 우리 창 40어부터)
➡️ 어수창을 선언 어수로 교체 → 재측정

[Cycle 2] 달성률: 5.1%  (근거: 3축 적합 470 / 목표 9,160)
✅ 어수창 정본 통일(100~200) — 상수가 두 벌이던 것을 실측 쪽으로 합침 ·
   벤치 조회 창까지 교체 → 베이스라인 108 → 470 정정 · 1,250 tests 통과
❌ 적합분의 78%(original 367편)가 시중 분포 왼쪽 끝(자리 16.9)에 몰려 있다
➡️ ① 작문·각색 지침에 어휘 대역 추가 ② PD 발췌 수율 실측

[Cycle 3] 달성률: 5.1% (재고 변화 없음 — 이 사이클은 공급선을 골랐다)
✅ PD 수율을 **비중복**으로 다시 잼(겹치는 창을 세고 있었다) · 어휘 가드가 옛 문턱 40 을
   박아 두고 있던 것 교체 → 책당 181.8편 · **목표는 PG 51권으로 닿는다** ·
   시중 자리 중앙 65.2 (재저작문 16.9 와 반대 방향 — 섞으면 시중 중앙에 모인다)
❌ 소재·시대 축을 아직 안 쟀다 — 19세기 아동 소설이 시중 교재 소재와 맞는지 모른다
➡️ 소재 축 측정 + 적재 경로

[Cycle 4] 달성률: 5.1% (게이트·경로 정비 — 적재는 다음 사이클)
✅ `AUTHORED_VOCAB_BAND`(시중 p25~p90) 신설·배선 — 쓰는 글에만 걸고 수확한 글에는 안 건다.
   하한의 근거는 부합이 아니라 학습 가치다 · 1,256 tests 통과
✅ 각색 export 가 **재고 증가로 죽고 있던 것** 발견·수정 (본문 전량 조회 → statement
   timeout). PD 8,141편이 들어온 오늘부터 터졌다. 스캔은 메타만, 본문은 뽑힌 것만
❌ **PD 발췌를 적재하는 스크립트가 아직 없다** — probe 는 "몇 편 나오는가" 만 답한다
➡️ ① `harvest-gutenberg` 의 정제·커서 구조를 재사용한 초·중 발췌 적재기
   ② 소재·시대 축 측정(시중 지문의 소재 분포부터)

[Cycle 5] 달성률: 5.1% (적재는 했으나 4축 도입 전이라 아직 안 셈)
✅ 적재기 신설 · PD 발췌 906편 적재(25권 · 중복 0) · 처리 경로 확인 ·
   책당 상한 40(3권 815편 중 548편이 한 책이었다) · process-queue 에 --feed 필터
❌ 표본을 열어 보니 **6편 전부가 소설 대화 장면**이었다 — 세 축으로는 못 거른다
➡️ 네 번째 축(자립성)을 시중 실측으로 만든다

[Cycle 6] 달성률: **7.8%**  (근거: 4축 적합 715 / 목표 9,160)
✅ standalone.ts 신설 — 문턱을 시중 196쪽에서 쟀다(대화 p95 9% · 문두 지시 3%) ·
   적재기·벤치 4축 배선 · 미달 644편 정리(사용자 승인) · 남은 277편 전부 4축 통과 ·
   큐 0 · 1,267 tests 통과
❌ 정렬 키가 유일하지 않아 15편이 조회를 빠져나갔다(세 번째로 밟은 함정) — 고치고 재실행해 복구
➡️ 규모: 4축 수율 책당 11편 → 목표까지 **약 830권**. 초3~4 칸이 병목이라 각색과 병행

[Cycle 12~13] 각색 2차 · 수확 누적 158권
✅ 「가르칠 수 있는 새 낱말 밀도」 신호 신설 — **개수로 세면 거꾸로 나온다**(반려된 원문이
   23개로 최고, 통과분이 9~12개였다. 길이가 6,284자 대 690자다). 비율로 바꾸자 순서가
   뒤집혔다: Black Beauty 41% · … · NASA 공고문 8.4%(끝내 반려)
✅ 신호가 낮은 원문을 건너뛰고 5편을 채우니 **5/5 한 번에 통과**(1차는 6편 중 2편)
❌ 게이트로 만들지 않았다 — 표본 6편으로 문턱을 정하면 근거 없는 임계값이다

[Cycle 14~15] **다른 세션의 게시 게이트가 내 재고를 버리고 있었다**
❌ 설계 문서는 `narrative` 를 "**버리지 않는다**" 고 적는데 코드는
   `publishable = verdict === 'use'` 라 **서사 3,698편을 전부 archived** 로 내렸다.
   그중 1,241편이 초·중 교재용이었다
✅ **용도로 갈랐다** — 수능이 서사를 빼는 것과 교재가 이야기를 필요로 하는 것은 다른 판단이다.
   (다른 세션이 이 아이디어를 `purposeOf`: csat·kids·library·raw 로 일반화했다)
✅ 되돌림 경로 신설 + **재실행 안전의 오해를 고쳤다** — `publishable=true` 인데 archived 로
   남은 것이 **409편**이었다. 판정만 보고 skip 하니 상태 불일치가 영영 안 고쳐졌다.
   재실행 안전은 "다시 안 쓴다" 가 아니라 **"다시 돌리면 상태가 수렴한다"** 는 뜻이다
✅ 미판정 101권 판정(chunk-20·21·22) — narrative 87 · reject 8 · use 6

[Cycle 16~17] **게시 가능 4,879편 · 달성률 53.3%** (수확 220권 누적)
✅ 4축(어수·FK·어휘·자립성) + 게시 게이트(장르·교리·차별·폐기된 사실)를 **둘 다** 통과한 재고
⚠️ "적합도 100%" 는 그 문서 말대로 **"대역 통과 100%"** 로만 읽어야 한다 —
   교재로 쓸 수 있음 100% 가 아니다

---

## 12. 손으로 읽어야만 보이는 것 — 그리고 규칙으로는 못 올렸다

게이트를 통과한 발췌를 **네 번** 표본으로 읽었다(각 12편):

| 표본 | 쓸 만한 것 | 그 사이에 한 일 |
|---|---|---|
| 1차 | **3 / 12** | — |
| 2차 | 2 / 12 | 문두 접속부사 목록 확장 · 작은따옴표 대화 검출 |
| 3차 | 1 / 12 | 장 머리 우선 |
| **4차** | **6 / 12** | **훑는 주제를 소설 → 설명문으로** |

**규칙을 세 번 고쳐도 안 올랐고, 소스를 바꾸자 올랐다.**

### 시도했다가 되돌린 것 — 첫 문장 안의 되짚기 표지

`Glen knew that.` · `…past them shot a huge black mass` · `As the reader already knows…`
처럼 되짚기가 **문두가 아닌 자리**에 있어 문두 규칙이 못 잡는다. 그래서 첫 문장 전체에서
찾는 규칙을 넣어 봤더니 **시중 오탐이 3% → 초등 13% · 중등 19%** 로 뛰었다.
시중 지문도 `this account` · `the following day` 를 정상적으로 쓴다 — 지문 안에서
앞 **문장**을 되짚는 것과 앞 **문단**을 요구하는 것은 다른 일인데 표면형이 같다.

→ **넣지 않았다.** 넣을 것과 뺄 것을 가르는 것은 이 오탐 수치다. 같은 자로 잰
`opensAsRecord`(일지·항목 표제)는 오탐이 **초등 1% · 중등 0%** 라 넣었다.

### 남는 결론

네 축을 통과해도 **손으로 읽으면 더 걸러진다.** 그 비율은 규칙으로 못 올리므로
재고 수치와 함께 밝힌다:

| 수치 | 뜻 |
|---|---|
| 게시 가능 6,475편 | 대역·게이트 통과 |
| 그중 교재 즉시 사용 | **표본 기준 50%**(주제 변경 후) |

⚠️ 표본 12편이다. "된다/안 된다" 를 가르는 데는 충분하지만 **비율로 인용하면 안 된다.**

[Cycle 18~24] **게시 가능 5,630편 (61.5%)** · 수확 264권 · 각색 52편
✅ 몫 계산 결함 — 몫을 **적재 수**로 세니 중1~2·중3 이 1,832 로 "찼다" 고 보고하면서
   실제 게시 가능은 1,588·1,616 이었다. 격리분만큼 그 칸은 영영 못 채운다.
   → 격리 확정분만 몫에서 뺀다. 라운드로빈이 이제 병목인 초3~4 를 먼저 채운다
✅ 미판정 147권 판정(chunk-20~24) — narrative 126 · reject 17 · use 6.
   reject 가 다섯 갈래로 갈렸다: doctrine · mixed(독본·교본·활동집) ·
   poetry-drama(동요·오페라 가사·운문 우화) · bias · fragmentary(도해 참조)
   ⚠️ 두리틀 선생은 **발췌만 보면 무해**하다(원숭이 사냥 장면). 원본의 인종 서술은
     책을 알아야 안다 — **책 단위 판정 설계가 여기서 값을 했다**
✅ 재시도 부재를 **네 번째**로 잡았다(`gate-book-export`). 그리고 연쇄 결함 —
   재시도가 쪽을 500→250 으로 줄이는데 종료 조건이 `length < 500` 이라
   흔들린 다음 쪽에서 곧바로 "끝" 으로 읽고 나머지를 통째로 빠뜨렸다
❌ **DB 가 길게 흔들린다** — 다른 세션이 3만 편 전량 게이트를 돌리는 중이라 경합한다.
   수확·게이트가 재시도를 다 쓰고 죽는 일이 반복됐다. 백오프를 30초·6회로 늘렸다

[Cycle 7] 달성률: 7.8% (수확 계속 · 배분 결함을 잡았다)
✅ PD 발췌 519편 추가. 그런데 4축을 붙였는데도 **중3 226 · 중1~2 147 · 초3~4 17** —
   상위 두 칸이 73%였다. 책을 훑는 순서대로 담아 상위 칸이 독식하고 있었다
   → 남은 몫이 큰 칸부터 라운드로빈. 확인: 한 권이 다섯 칸에 8편씩 균등 배분
❌ 이것으로 초등 칸이 채워지지는 않는다 — 대부분의 책이 초등 조각을 아예 안 낸다
➡️ 초등 전용 공급선(StoryWeaver)에 같은 4축을 대고 연다

[Cycle 8] 달성률: **9.6%**  (근거: 4축 적합 875 / 목표 9,160)
✅ PD 발췌 555편 추가 · storyweaver-ingest 에 **어휘·자립성 게이트가 없던 것** 배선
   (그 스크립트는 FK·어수만 보고 담고 있었다)
❌ **StoryWeaver 가 429(rate limit)** — dry-run 16건 중 8건 실패. 막는 것은 코드가
   아니라 상류다. 간격을 늘려 천천히 돌려야 한다
➡️ PD 수확을 계속하며 초3~4 칸은 각색 드레인으로 병행

[Cycle 9] 달성률: 보유 기준 재정의
✅ PD 발췌 622편 추가(누적 80권). 배분이 고르게 나왔다 —
   초3~4 43 · 초5~6 86 · 초6~중1 140 · 중1~2 168 · 중3 185 (Cycle 7 의 24:237 대비)

[Cycle 10] **보유 3,092편 (33.8%)** · 분석 완료 1,537편 (16.8%)
✅ 계량을 바로잡았다 — `ready` 만 세는 것은 **손에 있는 원문을 없다고 세는 것**이었다
   (4축 판정은 본문만으로 한다). `--include-queued` 로 두 수를 나란히 낸다
❌ `queued` 를 전부 세면 3,131편인데 그 안에 **수능용 수확분 8,141편**이 섞인다 —
   우연히 규격을 만족해도 "초·중을 확보했다" 고 세면 하지 않은 일을 한 것으로 세는 것이다
❌ 재고가 커지자 측정 쿼리가 **세 번 timeout**. 페이징 넷 중 하나만 살아남았다
➡️ 처리 따라잡기 + 초3~4 병목

[Cycle 11] 각색 드레인 — **경로가 끝까지 통했다**
✅ 초등 6편을 각색해 **5편 적재 · 1편 반려**. 반려 이유가 숫자로 돌아온다 —
   "교육과정 밖 낱말이 15.7% 로 시중 초등 지문보다 쉽다(대역 24.1~43.3%)"
   측정-수정 한 바퀴로 4편이 붙었다(수능 작문에서 배운 그대로다)
❌ **반려된 1편은 다시 쓰지 않았다.** NASA 지원 프로그램 공고문이라 내용이
   추상어(idea·study·team·step)뿐이다 — 억지로 어려운 말을 넣으면 지표 조작이 된다.
   **소재가 추상적이면 각색으로도 대역에 못 든다 — export 단계에서 걸러야 한다**
❌ **두 자가 반대로 당긴다.** 내 어휘 대역은 "시중만큼 어렵게" 라 하고,
   기존 A2 게이트는 "V1 밴드 초과가 11~19%로 너무 어렵다" 고 경고한다.
   대조군이 다르다 — A2 게이트는 **News in Levels**(영어권 초급 뉴스), 내 것은
   **한국 시중 초등 교재**다. 목표가 "시중 교재 부합" 이므로 시중 쪽이 정본이어야
   하지만, 둘 중 하나를 고르는 것은 측정이 아니라 **결정**이라 여기서 하지 않는다.
   지금은 A2 쪽이 WARN(critical 아님)이라 통과하고 "사람이 본다" 로 남는다
✅ PD 발췌 622편 추가(누적 80권). **배분이 고르게 나왔다** —
   초3~4 43 · 초5~6 86 · 초6~중1 140 · 중1~2 168 · 중3 185 (Cycle 7 의 24:237 대비)
```

---

## 10. 네 번째 축 — 세 축을 통과하고도 지문이 아닌 글이 69%였다

적재기를 지어 906편을 담고 **표본 6편을 열어 봤다. 6편 전부가 소설 대화 장면이었다.**

> "Mrs. Grose tried to keep up with me. \"You mean you're afraid of seeing her again?\""
> "So she disturbed you, and, to see what she was looking at, you also looked—you saw."

어수·FK·어휘를 다 통과했는데 **지문이 아니다.** `Mrs. Grose` 도 `Flora` 도 설명 없이 나오고
문장이 앞 상황을 가리킨다. 교재 지문은 그 한 편이 스스로 선다.

**문턱은 짐작하지 않고 시중 지문 196쪽에서 쟀다** — 어휘 자를 만들 때와 같은 순서다:

| | 대화 비중 | 문두가 앞을 가리킴 |
|---|---|---|
| 시중 초등 (129쪽) | p50 **0** · p90 4.5 · p95 8.5 | **3%** |
| 시중 중등 (67쪽) | p50 **0** · p90 3.6 · p95 7.6 | **3%** |
| **우리 PD 발췌 (906편)** | **p50 17.9 · p75 48.5** | **53%** |

→ `standalone.ts` 신설. 문턱 = 시중 **p95(9%)**.

⚠️ 어휘 자는 p90 을 쓰는데 여기가 p95 인 것은 실수가 아니라 **분포가 달라서**다. 대화 비중이
0 에 몰려 있어 p90(4.5)으로 조이면 **대화가 조금 있는 정상 이야기 지문**이 무더기로 막힌다.
이 자는 대화를 나쁘다고 말하지 않는다 — 막는 것은 대화가 절반을 넘는 **장면 조각**이다.

### 적재분 정리 (사용자 승인)

미달 **644편 삭제 · 277편 유지**(전부 4축 통과). 지워도 잃는 것이 없다 — `source_id` 가
본문 해시라 같은 책을 다시 훑으면 같은 id 로 재수확된다.

⚠️ **정렬 키가 유일해야 한다 — 세 번째로 밟은 함정.** 1차 삭제(628편) 뒤 남은 수가
278이어야 하는데 **293** 이었다. `created_at` 만으로 페이지를 넘겼는데 배치 insert 라 같은
타임스탬프가 수두룩해 **15편이 조회를 통째로 빠져나갔다.** `.order('created_at').order('id')`
로 고치고 재실행해 16편을 마저 걷었다 — **재실행 안전하게 지어 둔 덕에 스스로 복구됐다.**

---

## 11. 지금 재고 — 4축 715편 (달성률 7.8%)

| 밴드 | FK통과 | +어수 | +어휘 | **+자립 = 적합** | 시중 자리 |
|---|---|---|---|---|---|
| 초3~4 | 56 | 56 | 51 | **42** | 30.2 |
| 초5~6 | 117 | 117 | 115 | **102** | 28.6 |
| 초6~중1 | 221 | 221 | 220 | **214** | 21.5 |
| 중1~2 | 225 | 224 | 224 | **222** | 22.9 |
| 중3 | 146 | 146 | 136 | **135** | 52.5 |
| | | | | **715** | |

**470(3축) → 542(4축 도입) → 715.** 남은 탈락 사유: 자립성 35 · 어휘 18 · 어수창 1.

### 규모 계산 — 몇 권이 필요한가

4축을 다 통과하는 비율은 25권 기준 **906 → 277편(31%)** = 책당 **11편**.
목표 9,160편이면 **약 830권**이 필요하다(PG 아동물 7,636권 중 11%).

⚠️ **초3~4 칸이 병목**이다 — 19세기 아동물이라도 문장이 길어 상위 칸으로 쏠린다.
이 칸은 각색 드레인(어휘 대역 게이트 붙은)이 함께 채워야 한다.

---

## 9. 다음에 지을 것 — 적재기 (재사용할 것이 이미 있다)

수능 쪽 `scripts/csat/harvest-gutenberg.mjs` 가 이미 갖고 있는 것:

| 갖춘 것 | 왜 필요한가 |
|---|---|
| curl 기반 수신 | node fetch 로는 gutenberg.org 에 못 붙는다(이 저장소가 세 번 겪음) |
| `cleanBookText` · `looksLikeBookMatter` | 목차·판권·주석을 지문으로 세지 않는다. **PD probe 에는 없는 가드다** |
| 커서 파일 + 본문 해시 `source_id` | 재실행 안전 — 같은 글이 두 번 안 들어간다 |
| 칸별 몫 상한 | 한 칸만 넘치게 담지 않는다 |

바꿀 것은 **채점기와 몫**뿐이다: `fitRecord`(수능 모양) → 3축(어수창 ∩ FK밴드 ∩ 어휘게이트),
소재 몫 → 학년 칸 몫. 자를 새로 만들지 않는다.

## Cycle 41 — `mixed` 를 풀려다 말았다

**가설**: 책 단위 `mixed` 판정(“쓸 것과 못 쓸 것이 섞인 책”)은 통권을 쓰는 수능 게이트에는 맞지만,
100~200어 조각만 4축으로 뽑는 kid 파이프라인에는 과하다. 초3~4 격리 170편 중 상당수가
여기 걸려 있으니, 풀면 병목이 뚫린다.

**측정**: 코드 없는 `mixed` 격리 292편에서 12편을 고르게 뽑아 손으로 판정했다.

| 결과 | |
|---|---|
| 쓸 만함 | **1/12 (8%)** — Scouting for Boys 발자국 관찰 활동 |
| 일반 kid 발췌 손판정(Cycle 38) | 6/12 (50%) |

버린 11편의 이유가 서로 달랐다 — 없는 삽화를 가리킴 · 다른 책을 소개하는 홍보문 ·
없는 시를 분석하는 운율 강의 · 수의학 처치(해열제·관장) · 소매치기 장면 · 폭죽 세는 동시 ·
중세 예법과 미사 · 인터뷰 답변.

**결론: 가설이 틀렸다.** `mixed` 는 조각 단위에서도 신호가 강하다. 292편을 풀었다면
게시 가능 수는 늘고 품질은 떨어졌을 것이다 — **한 지표를 좋게 만들며 다른 축을 파는** 전형이다.
풀지 않는다.

### 대신 얻은 것 — 자립성 자의 구멍

표본 #10 이 Audubon 항해일지였다. `Wednesday, 4th. Cloudy and coldish.` 로 시작하는데
자립성 자를 그대로 통과했다. 기존 `LOGBOOK` 은 달력 날짜(`November 10, 1851.`)만 잡는데,
**일지는 해가 바뀌지 않는 한 연도를 적지 않는다.**

`WEEKDAY_ENTRY` 를 더했다 — ‘요일 + 숫자 + 끝맺음’ 세 조건을 모두 요구한다.

| 잰 것 | 값 |
|---|---|
| 시중 지문 오탐(`opensAsRecord`) | **0.5% (1/207)** — 그 1건도 쪽 경계로 잘린 조각이지 요일 규칙이 친 것이 아니다 |
| 새로 걸러낸 재고 | 16편 (초3~4 6 · 초5~6 8 · 초6~중1 2) — 전부 이미 `archived` 라 게시 가능 수는 불변 |
| 회귀 | `standalone.test.ts` 12 (신규 2) · 패키지 타입체크 통과 |

수가 작은 것이 이 규칙의 값이다 — **자가 오탐을 안 만들면서 한 종류의 오염을 영구히 막는다.**

### 재고 (2026-09-05)

| 칸 | 적재 | 게시 가능 | 남은 몫 |
|---|---|---|---|
| 초3~4 | 619 | **449** | 1,383 |
| 초5~6 | 1,479 | 1,234 | 598 |
| 초6~중1 | 2,415 | 2,044 | 0 |
| 중1~2 | 2,371 | 2,075 | 0 |
| 중3 | 2,196 | 1,924 | 0 |

초3~4 의 격리율이 27%로 다른 칸(15~17%)보다 눈에 띄게 높다. 쉬운 글을 쓰는 PD 책일수록
동시·교훈담·잡지 모음이라 `mixed`·`poetry-drama` 에 많이 걸린다 — **이 칸은 수확이 아니라
각색으로 채워야 한다**는 신호다.

## Cycle 42 — 수확이 0편으로 끝난 이유는 공급이 아니라 **죽는 방식**이었다

각색 6편을 적재하고 재고를 쟀는데 게시 가능 수가 **한 편도 늘지 않았다**(7,782 그대로).
"초3~4 PD 공급이 말랐다"고 결론 내리기 직전에 로그를 봤다 — 수확기가 마지막 권의
중복 조회에서 `fetch failed` 로 **죽어 있었다.**

`dbRetry` 는 재시도 다섯을 다 쓰면 던진다. INSERT 는 그것을 `try/catch` 로 감싸
실패를 기록하고 넘어가는데, **중복 조회만 벗겨져 있었다.** 그래서 한 권의 조회 실패가
24권치 진행분을 통째로 버렸다. 재시도를 더 늘릴 문제가 아니다 — 한 권이 실패하면
그 권만 건너뛰면 된다. 건너뛴 조각은 본문 해시가 같으므로 다음 실행에서 그대로 다시 뽑힌다.

> **측정하지 않았으면 잘못된 결론을 내렸을 자리다.** "공급이 말랐다"와 "수집기가 죽는다"는
> 같은 숫자(증가 0)를 낸다.

### 계측을 스크립트로 고정했다

사이클마다 같은 수치를 임시 스크립트로 다시 세고 있었고, 그때마다 조건이 미묘하게 달라져
**같은 날 잰 값이 서로 어긋났다**(어떤 회차는 적재분, 어떤 회차는 게시 가능분).

- `scripts/textbook/lib/db.mjs` — 접속 + `dbRetry` 한 벌. 같은 결함을 여섯 번 손으로 다시
  썼다는 사실 자체를 주석에 남겼다. 돌고 있는 기존 다섯 스크립트는 건드리지 않았다.
- `scripts/textbook/kid-inventory.mjs` — 목표 대비 재고를 한 정의로만 센다. 분모 9,160,
  분자는 "격리(publishable=false)가 **아닌** 것" — 미판정 행을 빼면 달성률이 낮게 나온다.

### 각색 6편 — 대역을 맞추다 배운 것

| | |
|---|---|
| 쓴 것 | 달까지 거리 · 행성의 위성 · 핀휠 은하 · 저녁 하늘의 분홍 띠 · 부러질 때까지 날개 굽히기 · 알래스카 지진이 플로리다 우물을 흔들다 |
| 1차 통과 | 2/6 — 나머지 넷은 **너무 쉽게** 썼다(밖 10~22%, 하한 24.1%) |

밖% 는 **타입이 아니라 토큰** 기준이다. 그래서 "같은 낱말을 일부러 다시 쓴다"는 지시문이
밖% 를 직접 끌어내린다. 어려운 낱말 몇 개를 끼워 넣어도 토큰 50개 중 하나씩이라 거의 안 움직인다
(`orbit`·`average`·`horizon`·`laboratory` 는 전부 교육과정 **안**이었다).

**해법은 낱말을 끼우는 것이 아니라 주제어가 교육과정 밖인 각도로 다시 쓰는 것이다** —
`spacecraft`·`astronaut`·`ellipse`·`crater`(달), `twilight`·`arch`·`ridge`·`moonlight`(하늘),
`strut`·`brace`·`carbon`·`sensor`(날개). 그러면 반복이 오히려 밖% 를 **올린다**.
2차에 6/6 통과(23.2%는 한 문장 더해 25.8%).

⚠️ **남은 긴장**: `05-3` 은 어휘 대역은 통과했지만 import 가 *A2 밴드 초과 22.4%* 를 경고했다
(기준 10%, News in Levels Level 1 실측). 시중 한국 초등 교재의 밖 30% 와 CEFR A2 는 서로 다른 자다.
사용자 요구가 "시중 교재에 부합"이므로 시중 자를 따르되, **두 자가 어긋난다는 사실은 숨기지 않는다.**

## Cycle 43 — 달성률이 85.0% 가 아니라 88.2% 였다

고친 수확기가 **94편**을 담았다(직전 실행은 죽어서 0편). 그런데 게시 가능 수가 또
**정확히 그대로**였다(초3~4 449 · 초5~6 1,234). 두 번 연속 같은 자리에서 안 움직이니
"이 칸은 PD 공급이 말랐다" 로 결론 낼 참이었는데, 새로 담긴 40편의 판정 시각을 봤다.

**전부 미판정이었다.** 미판정은 격리가 아니므로 게시 가능에 들어가야 한다 — 그런데 안 들어갔다.

### 원인 — `.not(col, 'eq', 'false')` 는 NULL 을 떨어뜨린다

PostgREST 가 SQL `col <> 'false'` 로 번역한다. `col` 이 NULL 이면 비교가 UNKNOWN 이라
**그 행이 조용히 사라진다.** 두 스크립트가 주석에 의도를 정확히 적어 놓고
(“미판정은 일단 찬 것으로 센다”) 구현이 정반대였다.

| 초3~4 | |
|---|---|
| `.not(eq false)` 로 센 값 | 449 |
| 적재 652 − **명시적 격리 145** | **507** |
| 사라진 것 | 미판정 58 + `gate.publishable` 키 없는 행 |

빼기로 바꿨다 — NULL 은 어느 쪽 조건에도 안 걸린다. `kid-inventory.mjs` 와
`harvest-gutenberg-kid.mjs` 몫 조회 둘 다. 저장소 전체를 훑어 같은 꼴은 더 없다.

### 고친 뒤 재고 (2026-09-05)

| 칸 | 적재 | 게시 가능 | 격리율 | 남은 몫 |
|---|---|---|---|---|
| 초3~4 | 652 | **507** | 22% | 1,325 |
| 초5~6 | 1,556 | **1,365** | 12% | 467 |
| 초6~중1 | 2,413 | 2,138 | 11% | 0 |
| 중1~2 | 2,371 | 2,075 | 12% | 0 |
| 중3 | 2,196 | 1,924 | 12% | 0 |
| 각색 | 66 | 66 | — | — |
| **합계** | | **8,075** | | **/ 9,160 = 88.2%** |

> **두 사이클 연속 "증가 0" 이 서로 다른 원인이었다** — 앞은 수집기가 죽어서, 뒤는 계측기가
> 잘못 세서. 둘 다 "공급이 말랐다" 와 똑같은 숫자를 냈다. 재고가 모자란 것처럼 보일 때
> **먼저 세는 자를 의심할 것.**

격리율도 22%/12% 로, 앞서 초3~4 를 28% 로 적은 것은 같은 오계였다. 이 칸이 유난히
오염됐다는 이전 사이클의 진단은 **과장이었다** — 다른 칸(11~12%)보다 높긴 하지만 두 배는 아니다.

## Cycle 44 — 재는 자가 이야기 지문을 체계적으로 어렵게 재고 있었다

각색 `The Roti and the Thieves` 가 밖 **51.9%** 로 상한(43.3%)을 크게 넘었다. 낱말을 쉬운
것으로 두 번 바꿔도 52.8 → 51.9 로 꿈쩍하지 않아, 글이 아니라 **자**를 봤다.

`carried` · `took` · `grabbed` · `stopped` · `tired` — 전부 교육과정 **안** 낱말인데
'밖' 으로 세어지고 있었다. `stemLoose` 가 규칙 하나로 어간 **하나**만 만들기 때문이다:

| 낱말 | 만든 어간 | 찾아야 할 표제어 |
|---|---|---|
| `carried` | `carri` | `carry` |
| `tired` | `tir` | `tire` |
| `stopped` | `stopp` | `stop` |
| `took` | `took` | `take` (불규칙 — 규칙이 없다) |

**그리고 이 편향은 대칭이 아니다.** 과거시제가 몰린 **이야기** 지문에만 걸린다.
시중 초등 교재의 절반이 이야기인데 그 절반만 어렵게 측정된 셈이다.

### 고친 방법 — 어간을 하나로 정하지 않는다

`stemCandidates(w)` 가 후보를 모두 낸다(`carried` → carried · carri · carry · carrie).
하나라도 목록에 있으면 '안' 으로 센다. 되돌리기가 과할 위험은 낮다 — 후보가 실제 표제어가
아니면 목록에 없어서 그냥 안 걸린다. 불규칙은 가장 흔한 것만 표로 두었다(길게 늘리지 않는다 —
여기 없는 것은 여전히 밖으로 세어지고, 그것이 "적중률은 하한" 이라는 이 파일의 약속이다).

### **자를 고쳤으면 분포도 다시 잰다**

같은 시중 지문 207쪽을 새 자로 다시 쟀다. 자만 고치고 옛 문턱을 두면 **기준이 조용히 헐거워진다** —
43.3 은 옛 자의 p90 이지 새 자의 p90 이 아니다.

| 학교급 | 표본 | p25 | 중앙 | p75 | **p90(문턱)** |
|---|---|---|---|---|---|
| 초등 | 129 | 24.1 → **20.0** | 30.3 → **27.0** | 37.2 → **33.3** | 43.3 → **38.6** |
| 중등 | 78 | 29.7 → **23.5** | 34.7 → **28.6** | 41.7 → **35.4** | 44.0 → **41.6** |

새 p90(38.6)이 공교롭게 옛 짐작값 40 근처로 내려왔다. 그래도 짐작값을 쓰지 않는다 —
**값이 비슷한 것과 근거가 있는 것은 다르다.**

### 결과

`The Roti and the Thieves` 51.9% → **30.8%**. 어렵지 않았고 자가 틀렸다.
이미 적재한 6편도 새 자로 다시 재서 전부 새 대역(20~38.6%) 안임을 확인했다.
하한이 내려가며 미달이 된 2편은 다시 썼다(18.0 → 24.3 · 17.2 → 21.0).

### 곁가지 — 조용히 안 돌던 테스트 4파일

`vitest` 를 저장소 뿌리에서 돌리면 `path.resolve(process.cwd(), '../..')` 가 저장소 밖을
가리켜 네 파일이 ENOENT 로 통째로 실패하고 있었다. 파일 위치 기준(`import.meta.url`)으로
바꿨다. **624 통과 + 4 파일 실패 → 655 통과 · 실패 0** — 31개 테스트가 이제 실제로 돈다.
조용히 안 도는 테스트는 없는 테스트다.

## Cycle 45 — 고친 자로 재고를 수렴시켰다

DB 가 Cloudflare 522 로 응답을 못 하던 동안 밀렸던 일을 마쳤다.

### 고친 자가 기존 재고 판정을 어떻게 바꿨나

각 칸 400편씩 다시 판정:

| 칸 | 어휘 탈락 | 자립 탈락 |
|---|---|---|
| 초3~4 | 1.8% | 0% |
| 초5~6 | 1.3% | 0% |
| 초6~중1 | 1.5% | 0% |
| 중1~2 | 0.0% | 0% |
| 중3 | 0.5% | 0% |

**탈락이 적은 것이 이 변경이 옳았다는 증거다.** 자를 고치며 측정값(초등 중앙 30.3 → 27.0)과
문턱(43.3 → 38.6)이 함께 내려가 거의 상쇄됐다 — 정화가 아니라 **교정**이었다.

### 정리 스크립트가 두 축을 모두 수렴시킨다

`prune-kid-excerpts.mjs` 는 자립성만 봤다. 자가 바뀌면 옛 자로 들어온 것이 규격에 안 맞게
되는데, 그것을 걷어낼 곳이 없었다. 어휘 축을 더했다 — **자를 다시 고치는 날에도 이
스크립트 한 번이면 재고가 새 규격으로 맞춰진다.**

전수 판정 결과 **131편(1.4%)** 미달(자립성 0 · 어휘 131). 89편이 `queued` 라 달성률에
세어지고 있었다. 사용자 승인 후 삭제.

### 재고 (2026-09-05, 새 규격 기준)

| 칸 | 적재 | 게시 가능 | 격리율 | 남은 몫 |
|---|---|---|---|---|
| 초3~4 | 665 | 529 | 20% | 1,303 |
| 초5~6 | 1,607 | 1,431 | 11% | 401 |
| 초6~중1 | 2,358 | 2,097 | 11% | 0 |
| 중1~2 | 2,367 | 2,074 | 12% | 0 |
| 중3 | 2,190 | 1,919 | 12% | 0 |
| 각색 | 70 | 70 | — | — |
| **합계** | | **8,120** | | **/ 9,160 = 88.6%** |

숫자가 89.6 → 88.6 으로 **내려갔다.** 규격에 안 맞는 것을 세지 않기로 했으니 그래야 맞다 —
달성률은 지키라고 있는 것이 아니라 재라고 있는 것이다.

## Cycle 46 — 책당 예산의 대부분이 **버릴 것**에 쓰이고 있었다

수확 로그를 다시 읽었다. 30권에서 적합 조각 1,958개가 나왔는데 적재는 130편이었다.
사라진 곳이 두 군데였다:

```
책당 상한(40)을 넘어 안 담은 적합 조각 1052
몫이 찬 칸이라 버린 조각 776      ← 뽑은 뒤에 버린 것이다
```

**두 번째 줄이 결함이다.** 라운드로빈이 이미 몫이 찬 칸(초6~중1 · 중1~2 · 중3)에도
자리를 나눠 주고, 그렇게 뽑은 776조각을 적재 직전에 버렸다. 책당 40칸 예산의 대부분이
**버릴 것을 고르는 데** 쓰였다. 몫이 찬 칸을 선택 전에 빼자 그 줄이 **0** 이 됐다.

### 상한도 칸별로 바꿨다 — 다만 근거를 먼저 쟀다

상한 40은 "한 책이 서가를 물들이는 것"을 막으려는 값인데, **칸끼리 나눠 쓰고** 있었다.
칸은 서로 다른 코퍼스이므로 상한도 칸별이어야 한다. 올리기 전에 실제 편중을 쟀다:

| 칸 | 편수 / 책 수 | 권당 평균 | 최다 한 권 |
|---|---|---|---|
| 초3~4 | 687 / 220권 | 3.1 | 25편 (3.6%) |
| 초5~6 | 1,673 / 394권 | 4.2 | 15편 (0.9%) |

**실제 편중은 상한 근처에도 안 간다.** 칸별 40이면 한 책이 그 칸 목표(1,832)의 2.2%를
넘지 못한다 — 다양성 취지를 깨지 않는다.

### 효과 — 작지만 방향은 맞다

같은 12권으로 58편 → **62편**, 초3~4 17 → 18. 배분을 고쳐도 크게 안 뛰는 이유가 곧
다음 질문이다: **아주 쉬운 글(FK 1.5~4.0)의 실제 공급이 적다.** 상한은 더 이상 병목이
아니고, 이제 남은 것은 소재 축이다(학교 독본 실험이 그 답을 낸다).

## Cycle 47 — 목록 한 곳이 죽자 수확이 통째로 섰다

학교 독본 실험이 몇십 분을 매달려 있길래 멈추고 원인을 봤다.

```
gutendex.com      60초 타임아웃 · 0바이트
gutenberg.org     200 · 0.94초
```

**받을 책은 있는데 목록을 못 얻어서 못 받고 있었다.** 수확기가 책 목록을 `gutendex.com`
한 곳에서만 받기 때문이다. 목록 하나에 수확 전체를 묶어 둘 이유가 없다.

### 대체 경로 — Gutenberg 자체 카탈로그

`pg_catalog.csv.gz`(5.5MB) 한 번 받아 90,610권을 로컬에서 고른다. 이 경로가 **오히려 낫다**:

- 페이지네이션이 없다 — gutendex 페이징은 정렬 키가 없어, 이 저장소가 IA 수집에서
  같은 함정으로 **214건을 중복시키고 그만큼 누락**한 적이 있다.
- `Subjects` 와 `Bookshelves` 를 통째로 준다 — 주제를 우리가 직접 고른다.
- 캐시(7일) 뒤에는 네트워크가 필요 없다.

목록 조회가 주제 수만큼 연달아 실패하면 카탈로그로 전환한다.

### 그래서 얻은 답 — 학교 독본이 병목 칸의 수율을 올린다

| 소재 축 | 초3~4 수율 |
|---|---|
| 기본 (nature/science/animals/travel/children) | 1.0 → 1.4편/권 |
| **readers, fables** | **2.0편/권** (8권 → 16편) |

카탈로그가 세어 준 공급량도 함께 봤다: `readers` 128권(`The New McGuffey First Reader`
같은 실제 학년별 독본) · `fables` 75 · `fairy tales` 446 · `nature study` 18.
**애초에 학년별 지문으로 편집된 책**이라 조각이 그대로 지문이 되는 것이 이치에 맞는다.

두 칸이 16 · 16 으로 고르게 나온 것도 좋다 — 기본 소재는 초5~6 로 쏠렸다(41 대 18).

## Cycle 48 — 상한도 배분도 아니었다: 아주 쉬운 글의 공급이 병목이다

계수를 가르고 나서 첫 배치(16권)가 진실을 찍었다:

```
3축 적합 522 · 이미 몫이 찬 칸의 조각 475 · 상한 초과 0 · 적재 47 (초3~4 11)
```

**적합 조각의 91% 가 이미 찬 세 칸으로 간다.** 초3~4(FK 1.5~4.0)에 떨어지는 것은
9% — 권당 0.7편이다. 남은 1,189편을 이 속도로 채우려면 1,700권이다.

### 균등 분배 가정부터 의심했다 — 그런데 뒤집을 근거가 없었다

칸별 1,832는 내가 정한 값이지 잰 값이 아니다. 시중 코퍼스(SQLite)에서 학년대별
발행 종수를 세어 봤다: **초등 3시리즈 18권 · 중등 6시리즈 19권**. 균등에 가깝다.
37권짜리 코퍼스라 약한 증거지만, 균등을 버릴 만한 증거는 더더욱 없다. 유지한다.

### 그러면 쉬운 책을 골라야 한다 — 카탈로그 서가로 실측

Gutenberg 카탈로그의 `Bookshelves` 에서 아동 관련 서가를 뽑아 6권씩 dry-run 했다
(커서를 안 건드리므로 같은 책이 본 수확에 다시 쓰인다):

| 서가 | 풀 | 초3~4 편/권 |
|---|---|---|
| **Children's Instructional Books** | 80 | **3.8** — `The Elson Readers, Book 5` 한 권이 15편 |
| Children's Book Series | 468 | 2.0 |
| School Stories | 54 | 1.8 |
| Children's Picture Books | 140 | 1.7 |
| (기존) readers,fables 주제 검색 | — | 0.7~2.0 |
| (기존) nature/science/animals/travel/children | — | 0.7 |

**실제 학년별 독본이 5배를 낸다.** 애초에 학년 지문으로 편집된 책이라 조각이 그대로
지문이 되는 것이 이치에 맞는다. 기본 주제를 이 순서로 바꿨다(카탈로그 경로는 주제를
순서대로 소진하므로 순서가 곧 우선순위다). 풀이 작아(합 742권) 다 쓰면 다시 잰다.

### 재고 (2026-09-05 저녁)

8,476 / 9,160 = **92.5%** · 남은 몫 초3~4 **1,189** · 초5~6 **163** (세 칸은 참).

## Cycle 49 — 95.1%: 초5~6 가 찼다, 남은 것은 초3~4 하나

독본 서가 24권 배치가 **133편**을 담았다(초3~4 32 · 초5~6 101). 초5~6 가 정확히 1,832 로
차서 **네 칸이 찼다** — 이제 책당 예산 전부가 초3~4 로 간다. 각색 8편(v9)도 적재했다.

| | |
|---|---|
| 게시 가능 | **8,708 / 9,160 = 95.1%** |
| 남은 몫 | 초3~4 **1,116** (나머지 네 칸 0) |

### 우선순위가 무시되고 있었다

수율 3.8편/권인 `Children's Instructional Books` 를 맨 앞에 두었는데 24권 배치의 초3~4
수율이 1.3편/권이었다. 풀을 세니 그 서가 80권 중 **15권만 썼고** 나머지는 1.7~2.0 짜리
서가에서 왔다 — `catalogBooks` 가 네 서가를 합쳐 **책 번호순**으로 자르고 있었다.
주석에는 "주제를 순서대로 소진한다" 고 적어 놓고 구현이 그렇지 않았다.

정렬을 주제 순서 → 번호순으로 고쳤다. 고친 뒤 앞 70권의 분포: instructional 65 · series 5.

### 남은 풀 (처리 689권 기준)

instructional 65 · book series 393 · school stories 45 · picture 127 · fiction 303 ·
literature 346 · history 107 · anthologies 27 · nursery 6 — **합 1,419권.**
초3~4 1,116편을 1.5~3편/권으로 잡으면 400~750권. 풀 안에서 닿는다.

## Cycle 50 — **100.3%** · 합계 목표 도달

독본 서가 배치 세 번(30 · 40 · 40권)이 초3~4 에 **196 · 144 · 83편**을 담았다.
네 칸이 차자 책당 예산 전부가 한 칸으로 가면서 권당 수율이 6.5 → 3.6 → 2.1 로 내려간다 —
`Children's Instructional Books` 서가(65권)를 앞에서부터 소진하고 다음 서가로 넘어가는
모양 그대로다.

| | |
|---|---|
| 게시 가능 | **9,183 / 9,160 = 100.3%** |
| 칸별 | 초3~4 1,127 · 초5~6 1,827 · 초6~중1 2,091 · 중1~2 2,098 · 중3 1,958 · 각색 82 |
| 남은 칸 몫 | 초3~4 **705** (균등 분배 1,832 기준) · 초5~6 5 |

다른 세 칸의 수치가 몇 편씩 움직였다(중1~2 2,074 → 2,098 등) — 다른 세션의 게시 게이트가
책 판정을 새로 받아 되살리거나 격리한 것이다. 이 계측기는 그것을 그대로 반영한다.

### 목표 정의와 남은 일

목표는 **합계 9,160편**이었고 그것은 넘었다. 칸별 균등(1,832×5)은 내가 사다리를 끊지 않으려
정한 제약이지 사용자 목표가 아니다 — 다만 초3~4 가 다른 칸의 60% 라는 사실은 보고에 남긴다.
목표 스킬대로 한 사이클 더 돌려 초3~4 를 채우고 마친다. 그 뒤로는 같은 스크립트를
`--books N --commit` 로 돌리기만 하면 칸이 찬다(풀 1,300권 남음).

## Cycle 51 — 소재를 바꿔도 품질이 안 깎였다(오히려 올랐다), 대신 누수 둘

수율만 보고 소재 축을 바꿨으므로 **다른 축이 깎였는지** 확인했다. 독본 서가에서 새로 들어온
초3~4 발췌 240편에서 12편을 고르게 뽑아 손으로 판정했다.

| | |
|---|---|
| 쓸 만함 | **7/12 (58%)** |
| 기존 기준선(Cycle 38) | 6/12 (50%) |

쓸 만한 것: 여우가 밭을 가로질러 냄새를 끊는 이야기 · 네덜란드 주부의 대청소 ·
일본 코타츠 · 물소와 아이 · 너구리의 도망 · 여우의 사냥 수업 · 개가 고기를 훔치는 장면.
**교재 지문의 결에 맞는다** — 눈에 보이는 것, 한 편으로 끝나는 이야기.

떨어진 다섯 중 셋은 범죄·폭행 장면(지갑 훔치기 · 소매치기 · 부랑자의 폭행)이고,
나머지 둘은 **새로운 누수**였다:

```
1/4 cup butter 3/4 cup sugar 2 eggs 1/2 cup milk …     ← 조리법 재료란
[Illustration: A. Outer wing of locust. B. …]          ← 그림 설명
```

### 그림 표식은 채택 · 숫자 비율은 **재고 물러섰다**

| 규칙 | 시중 오탐 | 판정 |
|---|---|---|
| `[Illustration|Figure|Plate|Music…]` 표식 | **0 / 207 (0%)** | 채택 |
| 숫자 토큰 비율 | — | **기각** |

숫자 비율이 안 되는 이유가 분명하다:

```
조리법 재료란                   13.8%
날짜·거리가 든 정상 산문         11.8%
시중 지문 최대(초등 12.7·중등)   13.5%
```

**시중 지문이 조리법보다 숫자가 많은 쪽에 있다.** 문턱을 어디에 두어도 하나는 틀린다.
신호는 계속 내되(감시용) 게이트로 쓰지 않는다 — 첫 문장 되짚기 규칙을 물렸을 때와 같은
이유이고, 적어 두는 이유도 같다: **다음 사람이 같은 규칙을 다시 짜지 않게.**

`단위 낱말(cup·tsp) 밀도` 같은 좁은 규칙도 생각했으나 관측 사례가 하나뿐이라 과적합이다.
그 한 편은 손 판정이 걸러 낸다 — 규칙이 못 잡는 몫이 있다는 사실을 숨기지 않는다.

### 재고 (2026-09-05 · 그림표식 88편 정리 후)

| 칸 | 적재 | 게시 가능 | 남은 몫 |
|---|---|---|---|
| 초3~4 | 1,350 | 1,205 | 627 |
| 초5~6 | 2,020 | 1,814 | 18 |
| 초6~중1 | 2,334 | 2,075 | 0 |
| 중1~2 | 2,354 | 2,089 | 0 |
| 중3 | 2,184 | 1,954 | 0 |
| 각색 | 82 | 82 | — |
| **합계** | | **9,219** | **/ 9,160 = 100.6%** |

자를 조였는데 수치가 올랐다 — 88편을 지우는 사이 수확 배치가 그보다 많이 담았기 때문이다.

## Cycle 52 — 각색 적재기는 네 축 중 하나만 보고 있었다

새 자로 적재된 각색 82편을 전수 재판정했다. 한 편이 떨어졌다:

```
The Man Who Trains Moon Walkers
This man was a soldier first. Now he works for a space team. …
```

`This` 가 가리킬 대상 없이 시작한다. 자가 맞다. 그런데 **왜 들어왔나**를 보니 원인이
그 한 편이 아니었다 — `adapt-drain-import` 가 **어휘 대역만** 보고 있었다.
수확기는 네 축(어수창·FK·어휘·자립성)을 다 거치는데, 각색 경로는 한 축이었다.

> 우리가 쓴 글이라고 자를 면제할 이유가 없다. 오히려 **우리 글이야말로 고쳐 쓸 수
> 있으니** 막는 값이 싸다 — 수확은 버리는 것 말고 방법이 없지만 각색은 다시 쓰면 된다.

- `standaloneFit` 을 적재 게이트에 추가(어휘 다음, 각색 게이트 앞)
- 문제의 한 편은 첫 문장을 고쳐 자립하게 만들었다(107어 · 밖 21.3% · 자립 통과)
- 나머지 81편은 전부 통과 — 구멍은 하나였고, 그 하나가 구조를 드러냈다

## Cycle 53 — **앞 사이클의 품질 판정을 정정한다**

Cycle 51 에서 "소재를 바꿔도 품질이 안 깎였다(7/12 → 58%)" 고 적었다. **그 수치는 틀렸다.**
표본을 **최신순**으로 뽑았는데, 최신순은 서가가 섞여 있다. 무엇을 재고 있는지 몰랐던 것이다.

`source_id` 에 책 번호가 들어 있고 카탈로그에 서가가 있으니 **서가로 귀인해 다시 뽑았다**:

| 출처 서가 | 초3~4 적재 | 손 판정 |
|---|---|---|
| children's book series | 720 | (최신순 표본이 대부분 여기였다) |
| children's instructional | 390 | **4/12 (33%)** |
| children's literature | 27 | — 표본이 안 될 만큼 적다 |

Cycle 52 에서 "`children's literature` 가 품질을 떨어뜨렸다" 며 그 서가를 뺐는데,
**그것도 귀인 착오였다** — 그 서가는 27편뿐이었다. 뺀 것 자체는 되돌리지 않는다
(수율은 확인했으나 품질은 표본이 없어 못 쟀다).

> **최신순으로 뽑은 표본으로 서가를 비교하지 말 것.** 무엇이 섞였는지 모르는 표본은
> 숫자를 내지만 답을 내지 못한다.

### 그래서 실제 실패는 무엇인가 — 지도서가 서가에 섞여 있다

`Children's Instructional Books` 는 학생용 독본만 있는 서가가 아니다. 떨어진 여덟 편이
전부 같은 부류였다:

```
교사용 지도서   What would you use to measure the length of the table? A foot measure. …
연습문제 블록   EXERCISES.--What is a thrush? Why was the thrush so happy? …
희곡 대본       Sir Pet. Old enough!--ay, there it is. / Lady Teaz. My extravagance! …
조리법          Beat the eggs until the yolks and whites are well mixed. …
독본 연습 조각   O when Rover went away… / Yes, Mary, I think it is a pretty doll.
종교 지도       Jesus was there--Jesus is here. Peter confessed Him there. John 21:15-17.
```

### 질문 밀도 — 채택 (시중 오탐 0/207)

지도서와 연습문제는 **질문의 연쇄**라는 뚜렷한 꼴을 갖는다. 시중 207쪽으로 쟀다:

| | 중앙 | p90 | p95 | p99 | 최대 |
|---|---|---|---|---|---|
| 시중 초등 | 0 | 14.3 | 18.2 | 27.3 | **33.3** |
| 시중 중등 | 0 | 13.3 | 23.1 | 33.3 | **33.3** |
| 지도서 표본 | | | | | **50.0 · 66.7** |

**시중 최대와 지도서 사이가 비어 있다.** 그래서 백분위를 그대로 쓰지 않고 그 빈 자리인
**40%** 에 두었다 — p95(18~23)로 조이면 질문을 두엇 던지는 정상 설명문이 막힌다.

재고에서 **145편**이 걸렸다(초3~4 60 · 초5~6 44 · 초6~중1 22 · 중1~2 13 · 중3 6).

### 재고 (2026-09-06)

9,577 / 9,160 = **104.6%** · 초3~4 1,605 / 1,832(남은 227) · 초5~6 1,799(남은 33).

### 남는 한계 — 숨기지 않는다

질문 규칙을 넣어도 **희곡 대본 · 조리법 · 독본 연습 조각**은 여전히 통과한다.
손 판정은 그래서 100% 가 되지 않는다. 규칙으로 올릴 수 있는 만큼은 올렸고,
남은 몫은 **사람이 읽어 고르는 자리**다 — 재고 수치를 인용할 때 이 사실을 함께 말한다.

## Cycle 54 — 뺐던 서가를 재서 되돌렸다 · 운문 규칙은 기각

Cycle 52 에서 `children's literature` 를 "품질을 떨어뜨린다" 며 뺐다. **그 판단이 틀렸다.**
그때 그 서가에서 온 것은 **27편뿐**이었고, 판정한 12편은 대부분 `book series` 에서 왔다.

`source_id` 의 책 번호로 서가에 귀인해 다시 뽑으니 둘이 같은 자리였다:

| 서가 | 손 판정 |
|---|---|
| children's instructional | **4/12 (33%)** |
| children's literature | **4/12 (33%)** |

되돌렸다. 남는 교훈은 서가가 아니라 **표본 뽑는 법**이다 — 두 사이클을 이 착오로 썼다.

### 시도했고 물러섰다 — 운문 판별

`literature` 실패 12편 중 **셋이 운문**이었다(생일시 · 자장가 · 정글북 챈트). 운문은
줄머리마다 대문자를 쓰니 '문장당 대문자 수' 로 잡힐 줄 알았는데, 재 보니 겹친다:

```
운문   0.5 ~ 2.0      (정글북 챈트가 0.5)
산문   0.33 ~ 0.75    (고유명사가 많은 이야기가 0.75)
```

**정글북 챈트가 평범한 산문보다 낮다.** 조각을 만들 때 줄바꿈이 사라지므로 운문의 진짜
표지가 남지 않는다. 넣지 않는다 — 숫자 비율 규칙 때와 같은 이유이고, 적어 두는 이유도 같다.

### 지금까지 채택/기각한 규칙 (전부 시중 실측 기준)

| 규칙 | 시중 오탐 | 판정 |
|---|---|---|
| 대화 비중 > 9% | p95 8.5 기준 | 채택 |
| 앞 가리키는 문두 | 3% | 채택(오탐률 명시) |
| 일지·항목 표제 | 0.5% | 채택 |
| 요일 일지 | 0 | 채택 |
| 그림·표 표식 | 0/207 | 채택 |
| 질문 밀도 > 40% | 0/207 | 채택 |
| 첫 문장 되짚기 | 13~19% | **기각** |
| 숫자 토큰 비율 | 구분 안 됨 | **기각** |
| 문장당 대문자(운문) | 구분 안 됨 | **기각** |

기각한 셋을 코드에 남긴 이유는 같다 — **다음 사람이 같은 규칙을 다시 짜지 않게.**

## Cycle 55 — 다섯 칸이 모두 찼다 (107.4%)

| 칸 | 적재 | 게시 가능 | 격리율 | 남은 몫 |
|---|---|---|---|---|
| 초3~4 | 2,027 | **1,832** | 10% | 0 |
| 초5~6 | 2,028 | **1,832** | 10% | 0 |
| 초6~중1 | 2,312 | 2,065 | 11% | 0 |
| 중1~2 | 2,341 | 2,077 | 11% | 0 |
| 중3 | 2,178 | 1,949 | 11% | 0 |
| 각색 | 82 | 82 | — | — |
| **합계** | | **9,837** | | **/ 9,160 = 107.4%** |

처리한 PD 책 **1,430권**. 격리율이 다섯 칸 모두 10~11% 로 고르다 — 한 칸만 오염된 곳은 없다.

### 남는 사실 — 숫자와 함께 말해야 하는 것

**손 판정은 4/12(33%) 수준이다.** 네 축 게이트를 통과해도 사람이 읽으면 셋 중 둘은
교재 지문으로 쓰기 어렵다(희곡 대본 · 조리법 · 독본 연습 조각 · 소설 장면 · 운문).
규칙으로 올릴 수 있는 만큼은 올렸고(채택 6 · 기각 3, 전부 시중 실측 기준), 남은 몫은
**사람이 읽어 고르는 자리**다. 재고 9,837 은 "그중에서 고를 수 있는 후보의 수" 이지
"그대로 실을 수 있는 지문의 수" 가 아니다.

이 사실을 재고 수치와 떼어 인용하면 안 된다 — 그래서 콘솔·도움말·보고서 세 곳에 함께 적었다.

## Cycle 56 — 재측정: 106.8% · 그런데 **재는 경로가 죽어 있었다**

사용자 요청으로 재측정하려는데 CLI 가 죽었다. 오류 메시지가 **비어 있어서** 처음엔
DB 장애로 보였는데, 파고드니 아니었다:

```
REST 엔드포인트        401 · 0.1s          ← 살아 있다
select ... limit 1     0.4s  status 200    ← 읽기는 멀쩡하다
count(*) adapted(82행) 9.8s  status 500    ← 세는 것만 죽는다
```

진짜 메시지는 `canceling statement due to statement timeout` 이었다.
`library_articles` 가 **90,485행**이 됐는데 `feed_id`·`feed_label` **어디에도 인덱스가 없었다**
(기존 8개는 전부 다른 컬럼). 82행짜리 각색 집계조차 90,485행을 훑고 있었다.

### 세 겹이었다 — 하나씩 벗겨야 했다

| # | 증상 | 원인 | 고침 |
|---|---|---|---|
| ① | 모든 count 가 8초 초과 | 인덱스 없음 | `idx_la_feed (feed_id, feed_label)` — 8초 초과 → **47ms** |
| ② | 인덱스를 넣었는데 **여전히 죽음** | 호출부가 `feed_label` 만 걸어 **복합 인덱스의 선두 컬럼이 빠졌다** | `KID_FEED_ID` 를 패키지에서 내보내 양쪽이 함께 걸게 |
| ③ | 그래도 죽음 | 열두 질의 `Promise.all` 이 **서로를 밀어냈다**(하나는 1.2초인데 열둘이면 각자 8초 초과 · 503 섞임) | 순차로 — 열둘이 다 통과, **전체 1.5초** |

②가 특히 값비쌌다. 인덱스를 넣고 EXPLAIN 으로 47ms 를 확인했는데도 스크립트는 계속
죽었다 — **DB 에서 빠른 질의와 코드가 보내는 질의가 다른 질의였다.** 확인은 실제 경로로
해야 한다.

③은 스스로 만든 함정이다. 예전에 "재시도 대기가 줄줄이 더해진다" 며 병렬로 바꿨는데,
표가 커지자 그 병렬이 자기 목을 조였다. **병렬이 늘 빠른 것이 아니다 — 공유 자원에서는
동시성이 곧 비용이다.**

### 재측정 (2026-09-06)

| 칸 | 적재 | 게시 가능 | 격리율 | 남은 몫 |
|---|---|---|---|---|
| 초3~4 | 2,027 | 1,818 | 10% | 14 |
| 초5~6 | 2,028 | 1,819 | 10% | 13 |
| 초6~중1 | 2,312 | 2,053 | 11% | 0 |
| 중1~2 | 2,341 | 2,069 | 12% | 0 |
| 중3 | 2,178 | 1,943 | 11% | 0 |
| 각색 | 82 | 82 | — | — |
| **합계** | | **9,784** | | **/ 9,160 = 106.8%** |

**107.4% → 106.8%.** 다른 세션의 게시 게이트가 계속 판정 중이라 격리가 늘었고(초3~4
195→209 등), 두 칸이 몫에서 14·13편 모자란 자리로 돌아갔다. **재고는 고정된 수가 아니라
게이트와 함께 움직인다** — 그래서 화면에서 볼 수 있어야 하고, 그 화면이 이번에 살아났다.
