# 디자인 거버넌스 — 결정 기록

> 2026-09-18 · 작성 Claude Code. 지시: "규정 위반 없는 평범한 화면을 구조적으로 끝내기"(형태 발명 절차 · 방향 단일화 · QA 평균 회귀 검사).
> 1차에는 사람이 정할 4건을 대기로 남겼고, 2차(같은 날)에 사용자 지시 "평균은 절대 안 된다" 로 전부 확정했다. 2차에서만 화면 코드 1파일(DD-06)과 page 머리 주석 5줄을 고쳤다.
> 에이전트 환경 결정(두 에이전트 D-01~)은 [agents/DECISIONS.md](../../agents/DECISIONS.md) 로 별도다.

## 사용자 지시로 확정 (2026-09-18 2차)

> 사용자: **"목표는 오직 혁신적 디자인 가능하도록, 평균은 절대 안되도록."** 대기하던 4건을 이 기준으로 정했다.

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-01** | **Admin 액센트 = Deep Ink `--p`** (+ `ShieldCheck` + 「Admin」 배지). 보라 `#8B5CF6` 신규 사용 금지 | 보라는 외부 스킬 공통 "AI-보라" 금지 1순위 = 평균 신호. AGENTS.md · ADMIN_CONSOLE 갱신. 남은 사용(admin 표면 318회 · 학습자 38회, 2026-09-18 평균 신호 라쳇 실측)은 **코드 교체 전**이다 — 라쳇이 증가를 막고 감소만 허용한다. 일괄 치환은 84파일이라 별도 작업(화면 확인 필요) |
| **DD-02** | vocaflow-design 서문 → **"스타일은 차별점이 아니다. 자산의 형태는 차별점이다"** + "평균은 어떤 화면에서도 허용하지 않는다" | 원문과 §G 의 모순 제거. §A 제목도 「4문」으로 |
| **DD-04** | 골든 1호 = **`/library/books`** — 발산 4안 [compare/library-books.md](compare/library-books.md) 작성 완료 | 골격이 격자인, 가장 평균에 가까운 학습자 화면. **고르는 것은 사람**(에이전트 권고: A 「사정권 지평」) |
| **DD-06** | `/text/[id]` risk 단어의 `word-pulse` 4s 무한 반복 = **위반** → 제거 | `components/workspace/ReadingUniverse.tsx` 네 상태를 형태 문법 F1(3/2/1px 실선 · new 2px 점선, `--memory-*` 토큰, 모션 0)로 교체. 하드코딩 `rgba` 4개 · 그라디언트 1개 · `text-white` 제거(→ `--on-p`) |
| **DD-12** | **평균 금지를 테스트로 강제** (vocaflow-design §G5) | 권고는 기본값에 진다. ① `apps/web/src/app/__tests__/form-declaration-ratchet.test.ts` — 새 `page.tsx` 는 `// @form: <G1 축> — <서명>` 없이 실패(기존 무선언 152화면은 기준선 JSON, 줄기만). ② `apps/web/src/components/__tests__/average-signal-ratchet.test.ts` — 평균 신호 8종 × 표면 2 출현 수가 늘면 실패, 줄면 기준선을 내리라고 실패. **변이 검사**: 선언 없는 `grid-cols-3` 화면을 넣자 두 테스트가 각각 실패(확인 후 삭제). 선언을 붙인 화면 5: `/` · `/fit` · `/text/[id]` · `/dashboard` · `/hub` |

## 결정

| # | 결정 | 이유 |
|---|---|---|
| **DD-03** | 주묵 문법을 **토큰 신설 없이 문서만** 공용화 — `DESIGN_SYSTEM §✒ 형태 문법` F1–F5 | 원 결정("`visual-analysis.module.css` 가 소유 · 공용 토큰 신설 금지")과 충돌하지 않는다 — 공용이 된 것은 **선의 뜻**이지 값이 아니다. **토큰 신설 트리거**: CSAT 밖 모듈 하나가 같은 선(두께·대시·끝 모양)을 CSS 로 복사하는 순간. 그때 `--rule-support` 류 토큰 개정을 연다 |
| **DD-05** | 외부 취향 스킬 13 → **활성 2 + 내장 `dataviz`**, 11개는 `_disabled/` 로 **이동**(삭제 0) | 아래 §스킬 이동 표 |
| **DD-07** | `vocaflow-*` 스킬 4파일을 **git 추적 대상으로** 되돌림(`.gitignore` 부정 패턴) | 구조적 결함: `.claude/skills/`·`.agents/` 통째 ignore 때문에 **디자인 정본(`vocaflow-design`)과 비평·루프 스킬이 git 에 한 번도 없었다**(`git ls-files .claude/skills` = 0). AGENTS.md 가 "발명 정본"으로 링크하는데 다른 머신·worktree 에는 파일이 없다. 제3자 설치 스킬과 `_disabled/` 는 계속 ignore |
| **DD-08** | Part 2(하한선)에서 빠진 줄은 **Gate 1 이 지정한 §1 라우팅 3행 · v1 중복 문단 · §2 충돌표**뿐이고, 전부 [archive/vocaflow-design-SKILL-2026-09-17.md](archive/vocaflow-design-SKILL-2026-09-17.md)에 원문 보존 | 금지 자체는 한 줄도 풀지 않았다 — 비활성 스킬 출처의 금지(GSAP pinning · AIDA · 무거운 섀도 · 대시보드 세리프 금지 거부)는 §2 아래 "계속 금지" 한 줄로 유지. §0 의 `CLAUDE.md 디자인 철학` → `AGENTS.md`(철학이 옮겨 간 곳 — 링크 교정) |
| **DD-08b** | §2 의 `--el-*` 토큰 → `--sh-float` 로 교정 | `--el-*` 는 **존재하지 않는 토큰**이었다(`tokens.css` · `globals.css` grep 0). 없는 토큰을 지시하는 하한선은 지킬 수 없다 |
| **DD-09** | `.claude/` · `.agents/` 의 `vocaflow-design/SKILL.md` 두 사본을 **바이트 동일**로 | 기존 Codex 사본은 기계 치환본이라 `` `.Codex/skills/` 13개 grep `` 처럼 없는 경로를 만들었고, "Codex 3단 드레인이 ScriptQuiz 를 만들었다"는 사실과 다른 문장이 됐다. 정본 하나를 복사한다 |
| **DD-10** | DESIGN_SYSTEM 의 옛 「CSS Variables (SSoT 축약형)」 블록을 **현행 토큰 표로 교체** | 그 블록의 값(`--p #3B82F6` · `--bg #FFFFFF` · `--r-lg 12px` · `--sh-md` 그림자)은 Tailwind 기본색이었고 `tokens.css` 와 **전부** 달랐다 — "SSoT" 라는 제목의 폐기값이 본문에 셋(iOS Indigo · Reading Room 초판 · 이 블록) 있었다. 원문은 archive |
| **DD-11** | DESIGN_SYSTEM 목표 ≤400줄 → **360줄대**. 원문 1,509줄은 [archive/DESIGN_SYSTEM_history.md](archive/DESIGN_SYSTEM_history.md) 에 무수정 | `learning-tone.test.ts` 가 파싱하는 `### 게임 전용 하드코딩 색상 (예외)` 제목과 ```` ```css ```` 블록은 **글자 그대로** 유지 |

## §DS-분류 — DESIGN_SYSTEM.md 절 단위 인벤토리 (Gate 0)

| 원 절 (원문 줄) | 분류 | 처분 |
|---|---|---|
| 🎯 첫인상·이탈·모션 예산 §1–3 (9–151) | 현행 SSoT | **남김**(정정 이력 서사는 요약) |
| 🎯 §4 외부 디자인 스킬 판정 (153–169) | 중복(SKILL §2) | archive · 본문은 링크 한 줄 |
| 🌍 World-class Benchmarks (173–205) | 벤치마크 | archive |
| 🎨 Reading Room Art Direction (209–376) | 이력 + 현행 규칙 혼재 | 면/잉크 분리 규칙 · 철학 5조(현행 값으로) **남김**, Reading Room 색 CSS 블록(폐기값) · 타이포 v06.39 표 · 정정 서사 archive |
| 🛒 매대 시각 상품성 (385–509) | 현행 규칙 + 실측 서사 | 규칙 5개 **남김**(§매대), 실측표·서사 archive |
| 🎨 iOS Color SSoT v06.38 (514–737) | **폐기 명시** | archive (본문에 "폐기된 값" 절 0) |
| 🍎 iOS/iPadOS 디자인 언어 (739–909) | 이력 + 현행 프리미티브 | 프리미티브 이름·사용 규약 요지 **남김**(§컴포넌트 규약), 토큰 카탈로그(`--r-ios` 옛 값 · glow · glass) archive |
| 디자인 철학 4 · 학습 과학 7 (912–933) | 중복(AGENTS.md) | archive · PR 자가점검에 링크 |
| Typography 옛 체계 (937–999) | **폐기**(Plus Jakarta·DM Sans — v07 제거) | archive · v07 4종 표로 교체 |
| CSS Variables SSoT 축약형 (1003–1123) | **폐기**(Tailwind 기본색) | archive · 현행 토큰 표로 교체(DD-10) |
| 게임 전용 하드코딩 색상 (1125–1157) | 현행 SSoT(회귀가 파싱) | **남김**(글자 그대로) |
| Memory Decay (1161–1184) | 현행 SSoT | **남김**(잉크 값 추가) |
| CEFR · Spacing · Elevation · Radius · Motion · Breakpoints (1188–1275) | 현행(일부 값 낡음 — radius 6/8/12 → 2/3/4, 그림자 → 링) | **남김, 값 갱신** |
| 컴포넌트 패턴 Button·Form·Toast·Badge 클래스 문자열 (1279–1360) | 폐기(옛 radius·`active:scale`) | archive · 규약으로 요약 |
| `.arc-slot` · Protocol 다이얼로그 · Icons · 계측 훅 · Frame · 버튼 색 (1362–1448) | 현행 | **남김**(요약) |
| 접근성/안티패턴 · onClick 훑기 표 (1452–1498) | 현행 + 실측 표 | 규칙 **남김**, 15건 표 archive |
| CSAT 비교 판면 · 관계 표식 (1500–1509) | 현행 — 모듈 소유 | **§✒ 형태 문법 F2·F3·F5 로 승격**(DD-03) |

## §스킬 이동 (Gate 5)

위치: `.claude/_disabled/skills/<이름>` · `.agents/_disabled/skills/<이름>` — **스킬 탐색 루트(`.claude/skills` · `.agents/skills`) 밖**에 둔다.
지시문의 `_disabled/` 를 루트 **안**(`skills/_disabled/`)에 두면 하위 폴더를 훑는 탐색기가 여전히 찾을 수 있어 비활성이 보장되지 않는다.

| 스킬 | 판정 | 이유 | 마지막 실사용 근거 (CHANGELOG 0건 · docs grep) |
|---|---|---|---|
| `design-taste-frontend` | **유지** | 공개 랜딩의 레이아웃·타입스케일 감각. v2 | `docs/reports/design-skill-audit-2026-09-04.md` |
| `minimalist-ui` | **유지** | Admin 화면 라우팅 대상 | `docs/reports/csat-sources-redesign-20260916.md:153` (관리자 CSAT 소스 재설계) |
| `dataviz` (내장) | **유지** | 차트·통계 | 저장소 밖(내장 스킬) |
| `design-taste-frontend-v1` | 이동 | v2 의 하위호환 보관본 — 중복 | 목록 언급만 |
| `gpt-taste` | 이동 | GSAP pinning·AIDA — 두 행 모두 금지로 판정돼 있던 스킬 | 목록 · 반려 기록만 |
| `stitch-design-taste` | 이동 | perpetual micro-motion · 대시보드 세리프 금지 — 반려 기록만 있음 | `docs/reports/learner-design-review-20260907.md:348`(반려) |
| `high-end-visual-design` | 이동 | 섀도·glass 로 "비싸 보이게" — 판면과 정반대 | 목록 언급만 |
| `industrial-brutalist-ui` | 이동 | 라우팅의 "부르지 말 것"에만 있었다 | 목록 언급만 |
| `redesign-existing-projects` | 이동 | 쓰이던 것은 "감사 단계"뿐 → 06-workflow 비평 (a)–(e) 가 대체 | `docs/csat-learner/learning-home.md:32`(감사 항목 참고) |
| `brandkit` | 이동 | 표지 정본이 코드(`textbook/cover.ts`)·DB 각인이다 — 이미지 생성 표지를 쓰지 않는다 | 목록 언급만 |
| `imagegen-frontend-web` | 이동 | 섹션별 이미지 생성 — 랜딩 증명은 DOM/SVG(§7) | 목록 언급만 |
| `imagegen-frontend-mobile` | 이동 | 모바일은 Phase 2(기획) | 목록 언급만 |
| `image-to-code` | 이동 | "먼저 이미지를 생성하라"는 절차가 §G(자산 형태 먼저)와 반대 방향 | 목록 언급만 |
| `full-output-enforcement` | 이동 | 취향 스킬은 아니지만 외부 설치본 — AGENTS.md "코드 완성형만" 규칙과 중복 | 목록 언급만 |

**되돌리기**: `mv .claude/_disabled/skills/<이름> .claude/skills/` (Codex 쪽은 `.agents/…`). 되살리면 [archive 의 §2 원문](archive/vocaflow-design-SKILL-2026-09-17.md)에서 그 스킬의 충돌 행을 SKILL §2 로 복귀시킨다.
⚠️ 스킬 설치기(`skills-lock.json` 13항목, 손대지 않음)로 복원·갱신을 돌리면 11개가 `skills/` 로 **다시 설치될 수 있다** — 설치기를 돌린 뒤 `ls .claude/skills` 로 확인한다.

## 보고 외 사실

- `node agents/scripts/check.mjs` 의 **D2 FAIL 은 이 작업 전부터** 있었다 — `.codex/config.toml`(untracked, 2026-09-18 10:12 에 다시 쓰임)에 GENERATED 마커가 없다. 다른 세션·Codex 의 파일이라 손대지 않았다. 나머지 8/8 PASS.
- 커밋하지 않았다 — 대상 파일 다수가 **다른 세션의 미커밋 작업 위**에 있다(`AGENTS.md`·`DESIGN.md`·`06-workflow.md` untracked, `DESIGN_SYSTEM.md`·`.gitignore`·`CHANGELOG.md` 에 남의 hunk). `--only` 로도 남의 줄이 딸려 간다.

## UX 감사 (2026-09-18) — DD-13~18

> 산출물 전체: 브랜치 `feat/ux-audit` 의 `docs/design/audit/`(PROGRESS · verdict · priority · briefs · cards). 감사 중 메인 워크트리가 Codex 잠금이라 worktree 에서 썼다.

### 결정

| # | 결정 | 이유 |
|---|---|---|
| **DD-13** | 감사는 **worktree `feat/ux-audit`** 에서 쓰고, 메인 워크트리 코드는 읽기만 했다 | 메인 워크트리에 Codex 잠금(실행 중 확인). 규약상 남의 잠금이면 읽기 전용 — 장기 병행은 worktree(AGENTS 공유 워크스페이스) |
| **DD-14** | 캡처는 저장소의 시각 QA 하네스(`test:design`)가 아니라 **감사 전용 스크립트**(`audit/tools/capture.mjs`)로 했다 | 하네스(`playwright.visual.config.ts`)는 미커밋 파일이라 worktree 에 없고, 등록 라우트만 찍는다. 감사 스크립트는 익명 응답(로그인 필요 여부) · 1280/390 첫 화면 · 렌더 DOM 평균 신호를 한 번에 잰다 |
| **DD-15** | 검증 세션은 저장소 픽스처(`tests/e2e/fixtures/test-user.ts`) 계정으로 **1회 로그인**해 새로 만들었다 | 기존 `.auth-csat-learner.json` 은 만료(→ `/login`). 세션 파일은 worktree 의 gitignore 폴더에만 |
| **DD-16** | `/join/[code]` 는 **무효 코드**로만 찍었다 | 실제 초대 코드를 로그인 상태로 열면 학급 가입이 실행될 수 있다(쓰기 부작용) |
| **DD-17** | 판정 등급 규칙: 평균 = N4 불통과 ∧ 익명성(2) 불통과 · 서명 있음 = 둘 다 통과 · 경계 = 그 밖. **글꼴·색만으로는 익명성(2) 불통과** | "이 제품인지" 는 형태로만 말할 수 있다 — 스타일은 하루면 복제된다(§G) |
| **DD-18** | 우선순위 공식 = 수요(여정 ①② 3 · ③④ 2 · 그 밖 1) × 평균(2/1.5/1/0) × 데이터 용이(3/2/1) | 지시문. 여정 밖 최대 6점 → 상위 10 은 전부 여정 위 |

### 발견 — 앞선 결정의 정정이 필요한 것

| # | 발견 | 조치 |
|---|---|---|
| **DD-12 보강** | `@form` 선언 5개 중 **4개가 렌더와 다르다**(`/fit` · `/hub` · `/dashboard` · `/text/[id]`) — 선언은 2026-09-18 앞선 세션(Claude)이 붙였다. 라쳇은 선언의 **존재**만 검사한다 | 선언을 사실대로 고치거나 화면을 선언대로 만든다 — **사용자 결정**. 라쳇에 "선언과 렌더 대조" 를 더하려면 캡처 기반 검사가 필요(비용 큼) |
| **DD-06 보강** | `/text/[id]` 의 F1 밑줄(두께 3/2/1px)로 바꿨지만, 모든 단어가 `status:'new'` 고정이라 **한 종류로만** 나온다 | 데이터(`vocabularies.text_id`·stability)는 있다 — 조인 코드 작업(브리프 text_id) |
| **도구** | `screen-graph.mjs` 의 씨앗 S2 판정은 `components/ui/press` import 만으로 잡혀 과대 계상 · 정적 링크 분석은 템플릿 문자열 목적지를 못 본다 | 결과 문서에 한계를 명시했다. 고치려면 S2 를 `DecayUnderline` 심볼 사용으로 좁힌다 |

### 사용자 결정 대기

1. **첫 발산 대상**: 우선순위 1위 `/fit`(18점) vs 이미 4안이 있는 `/library/books`(DD-04, 6점) — 병행 또는 전환.
2. **`/teacher`(렌즈 6)**: 학급 구성원·과제가 DB 에 0 → 공식 4점. 예시 데이터로 설계할지.
3. **통합**: `/wordvault/review`≡`/study` · 모듈 허브 쌍 · `/dashboard`+`/reports` · 내 책 3중 — 합칠 화면(priority.md 「통합 기회」).
4. **`@form` 선언 불일치 4건**의 처리(위 DD-12 보강).

## /fit 발산 결정 (2026-09-19) — DD-19

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-19** | `/fit` = **A 「칠해지는 입력칸」 골격 + B 「학급에 나눠 줄 한 장」 출력 면**(별도 화면 아님). C 「일주일 뒤의 이 글」 **보류** — 로그인 후 개인 기록(FSRS) 기반으로 재검토(공개 화면의 일반 모형 수치는 표시광고 위험). D 「지문 지도」 **A 확정 후 추가 여부 재검토**. `/library/books` 는 병행하지 않고 `/fit` 골든 고정 뒤 착수 | 사용자 결정. 구현: `/api/fit` 응답에 표면형→레벨 표(`surfaceLevels` — 원문은 여전히 서버로 안 감) · `lib/textfit/paint.ts`(랜딩과 공용) · `PaintedPassage` · `ClassSheet`(권점 = CSS `text-emphasis`) · `PrintSheet.prelude`(판면이 인쇄 첫 장) · 사다리는 「학년별 범위 자세히」 접힘으로(철학 2) · 한국어 판정 문장 Lora 이탤릭 → Hahmlet. 2회 수정 후 골든 4장(`golden/fit.md`). `@form` 선언을 렌더와 일치하게 정정 |
| **DD-19b** | 새 관측 2종 `fit_level_moved` · `fit_sheet_opened` — **마이그레이션 적용**(2026-09-19 사용자 승인, DB 버전 `20260918232454` · 행 9,143 보존 · `db-allowlist` 통합 테스트 35/35) | `funnel_events_event_check` 가 허용 목록이라 DB 가 받지 않는다(`db-allowlist.integration.test.ts` 가 적용 전까지 실패 — 맞는 실패). SQL: `supabase/migrations/20260919100000_funnel_allow_fit_paint.sql`(적용 시점 DB 제약 38개 + 2) |

## 공유 문서 커밋 (2026-09-19) — DD-20

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-20** | AGENTS.md · DESIGN.md · CLAUDE.md 는 **(b) 경로** — 임시 워크트리에서 HEAD 기준으로 이 세션의 변경만 다시 적용해 커밋. 작업 트리의 혼합 파일은 그대로 둔다 | 다른 세션 변경(두 에이전트 전환: CLAUDE.md 495→44줄 · AGENTS.md 신설 · Codex 입구 DESIGN.md)은 이해되지만 **저장소 안에서 완결되지 않는다** — 가리키는 `agents/` · `docs/agents/CONTEXT_DETAIL.md` · `.claude/settings.json` · `.codex/` 가 전부 미추적이라 파일째 커밋하면 CLAUDE.md 의 규칙 485줄이 없는 파일로 옮겨지고 `doc-path-drift` 가 CI 에서 깨진다. 그래서 AGENTS.md 에 넣었던 이 세션의 규칙(DESIGN.md 필독 · 활성 스킬 2 · 평균 금지 · Admin 액센트)은 HEAD 의 CLAUDE.md 같은 자리에 적용했고, DESIGN.md 는 이 세션이 쓴 절만으로 만들었다(미추적 파일을 가리키지 않게). **함께 고친 구조 결함**: 라쳇 기준선을 처음에 다른 세션의 미커밋 변경이 섞인 작업 트리에서 재서 **커밋된 트리에서 실패**했다 — 기준선을 커밋된 트리로 재측정(화면 기준선 csat/progress·session ↔ dissect·formulas · learner.grid-3eq 60 · admin.ai-purple 326), DESIGN_SYSTEM 의 미추적 경로 참조 제거. 그 결과 **미커밋 CSAT 작업이 있는 작업 트리에서는 두 라쳇이 "기준선을 갱신하라" 로 떨어진다** — 그 세션이 커밋할 때 기준선을 함께 내린다(라쳇의 의도된 동작) |

## Lazyweb 스킬 사용 범위 (2026-09-19) — DD-21

> Lazyweb 스킬 팩 v0.15.13 을 사용자 전역(`~/.claude/skills/` 9개)에 설치한 뒤 정한 범위. 설치 위치가 저장소 밖이라 이 저장소에서는 이 항목이 유일한 제약이다.

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-21** | ① **허용**: `lazyweb-search-screens` · `lazyweb-search-flows` 만. 용도는 Gate 4 층 1(평균의 정의)과 Gate 6 사후 검증(닮은 화면 유무)뿐. ② **금지**: `lazyweb-apply-design-best-practices` 와 리포트·적용 계열 스킬 전부(`lazyweb-growth-report` · `-growth-score` · `-growth-backlog` · `-search-experiments` · 라우터 `lazyweb` 로의 우회 포함). ③ **세션 분리**: Lazyweb MCP 는 감사·검증 세션에서만 쓴다. Gate 5 브리프 작성 · 발산 · 골든 고정 · 구현 세션에서는 Lazyweb 도구를 호출하지 않는다. ④ **결과 처리**: 이미지는 저장소에 넣지 않는다. 레퍼런스 인덱스(references.md — 아직 미추적, 다른 세션 작업) 에 링크 · 첫 시선 골격 분류 · 평균 신호 수만 적는다. Lazyweb 결과를 G1 축 후보의 근거로 인용하지 않는다. ⑤ **예외 — 카테고리 밖 이식**: R(t) · 커버리지와 같은 데이터 구조를 가진 비교육 도메인(finance · health · utilities) 검색 결과는 형태 후보로 쓸 수 있되, 브리프에 「이식 출처」를 명시한다 | 사용자 결정. `lazyweb-apply-design-best-practices` 는 외부 취향 스킬의 SKILL.md 를 받아 그대로 적용한다 — AGENTS.md 의 활성 외부 취향 스킬 2개 제한과 vocaflow-design 판정 우선 원칙을 우회한다. 검색 결과는 「이미 있는 평균」을 정의하는 데 쓰여야지 형태의 출처가 되면 평균으로 수렴한다(§G 발명 목표와 충돌) — 그래서 발명 세션과 분리한다. 같은 업종 밖, 같은 데이터 구조의 화면은 평균이 아니라 이식이라 ⑤로 열어 둔다 |

## 이미지 체계 — Tines 계열 유사도 (2026-09-19) — DD-22~23

> 지시: [image-system-brief.md](image-system-brief.md)(사용자 제공, 4회차 중 1회차 = Gate −1→2). 산출물: [refs/index.md](refs/index.md) · [refs/tines/dna.md](refs/tines/dna.md) · [refs/uxcel/dna.md](refs/uxcel/dna.md) · [asset-inventory.md](asset-inventory.md) · [similarity-proposal.md](similarity-proposal.md).
> 범위: 웹만 — `apps/mobile` 제외(사용자 지시 「모바일은 제외」, 2026-09-19).

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-22** | **착수 · 상태 고정(Gate −1)**. 참조 캡처(Tines 289 · Uxcel 379 PNG)는 저장소 밖 `C:\Users\Administrator\design-refs\` 에만 두고, 이미지 차단은 루트 `.gitignore` 가 아니라 **`docs/design/refs/.gitignore`**(새 파일)로 | 시작 시 작업 트리 변경 210건(다른 세션 CSAT 작업 · `DESIGN.md`·`CHANGELOG.md`·`.gitignore` 미커밋 hunk · `AGENTS.md`·`06-workflow.md` 미추적) — 루트 `.gitignore` 에 줄을 더하면 `--only` 커밋에 남의 hunk 가 딸려 간다(DD-20 과 같은 사정). 읽기 목록 8개 줄 수: DESIGN 82 · DECISIONS 133 · SKILL 370 · DESIGN_SYSTEM 358 · 03-system 291 · 04-application 101 · design-tokens CLAUDE 21 · 06-workflow 110. 06-workflow 비평 (a)~(e) 재사용. 캡처 PNG 에 서체·색값이 없어 **Tines 8페이지 × 2뷰포트를 Playwright 로 다시 열어 computed style 을 읽었다**(읽기 전용, 결과는 저장소 밖). Uxcel `_private` 51장 · `auth.json` 은 열지 않았다 |
| **DD-22b** | brief 의 전제 4개를 **실측으로 정정**해 기록(원문은 보존) | ① Tines 제목은 굵은 산세리프가 아니라 세리프(Reckless 54)·산세리프(Roobert 39) 혼용, 32px 이상 제목 32개 중 굵기 400 이 24 ② 액센트는 하나가 아니라 보라 계열 면적 19.7% + 파스텔 틴트 5계열 ③ `--paper` 토큰은 없고 `--bg #FBFAF6` 이 Tines `#FCF9F5` 와 채널당 1 차이(우리 값이 먼저) ④ 무대는 1px 선 격자 24px 가 주, 점 격자(1px · 12px)는 액자 안쪽만. 그래서 L3 hex 검사에 **ΔE2000 < 2** 를 더하자고 제안(기존 토큰 예외) |
| **DD-22c** | 🔴 **웹 아이콘 참조 깨짐 3건 발견** — 고치지 않고 기록(A5: Gate 6 전 코드 수정 금지) | `layout.tsx` `icons.apple` → `/apple-touch-icon.png` · `public/manifest.json` → `/icons/icon-192.png` · `/icons/icon-512.png` 전부 파일 없음. `manifest.json` 색은 DD-10 이 폐기한 `#3B82F6` · 순백 `#FFFFFF`. 모바일(`app.json` 아이콘 3)도 없지만 범위 밖. 지금 따로 고칠지는 사용자 결정(proposal §5-4) |
| **DD-23** | **유사도 상한 대기 — ① / ② / ③**. 에이전트 권고 **②**(격자 무대 · 섹션 리듬 · 여백 160px · 제목 비율 · 액자 — 서체·잉크·면적 색·모서리 유지) | 어느 옵션이든 **03-system §3-5 「자산 파일 0개」**(P1)와 **SKILL §F 「자산과 무관한 새 미감 = 장식」**(P2)을 먼저 개정해야 한다 — 조건: 삽화는 서명 아님 · 개념은 N1 자산 개념 · 학습 중 화면 0. 12px+ 모서리 · 대문자 눈썹 · 마퀴는 DD-12 라쳇 · `Eyebrow` 규칙 · 모션 금지로 **어느 옵션에도 넣을 수 없다**. 격자는 CSS 그라디언트로 그리면 평균 신호 라쳇이 늘어나므로 SVG 패턴으로만 |

### 사용자 결정 대기 (DD-23)

1. 유사도 상한 ① / ② / ③.
2. P1·P2 공통 개정 승인.
3. ✋ `ModuleHero` 삽화 재도입 · `PairFlipMascot` 캐릭터 유지 · 파비콘 재생성 · 표지 코드 별도 정본 유지.
4. 웹 아이콘 참조 깨짐 3건 — 지금 고칠지, Gate 6 에 묶을지.

### 사용자 결정 (2026-09-19) — DD-24~27

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-24** | **유사도 상한 ②** — 격자 무대(SVG 패턴) · 섹션 리듬 · 여백 160px · 제목 비율 · 증명 액자. Tines 보라·파스텔 순환은 **미채용**: 면적 색은 주묵 하나(단일 액센트 유지). 섹션 바탕 교대는 **우리 토큰 1톤 · 잉크 4~6% 이내**만. L3 색 검사 = 정확 일치 + **ΔE2000 < 2**(기존 토큰은 예외 목록). 12px+ 모서리 · 대문자 눈썹 라벨 · 마퀴는 계속 금지 | 사용자 결정(DD-23 대기 1). 범위 = 웹(390px 포함), `apps/mobile`(Expo)만 제외 |
| **DD-25** | 공통 개정 P1·P2 **승인**. 삽화 조건 4개: ① 서명이 아니다 ② 그리는 개념은 N1 자산 개념 ③ 학습 중 화면 0 ④ **삽화도 03-system 형태 문법(주묵 의미 체계)을 지킨다** — 주묵은 앱이 남긴 표식이지 평가가 아니다(오답·위험에 주묵 금지), 선 두께 1/2/3px 는 망각도의 뜻으로만 | 사용자 결정(DD-23 대기 2). 개정은 2회차에서 03-system §3-5 · §3-9 로 |
| **DD-26** | 자산 처분: `ModuleHero` **삽화 미부착**(증명 우선 — 삽화 자리는 공개 화면 섹션 머리 · 빈 상태만) · 파비콘 **새로 제작** · 표지 생성 코드(`cover.ts` · `cover-art.ts`) **별도 정본 유지** · `PairFlipMascot` 부엉이는 **범위 밖 · 현상 유지 — 게임 모듈 예외**(PairFlip 은 아케이드와 같은 모듈 팔레트 예외 구역이라 A4 캐릭터 금지를 적용하지 않는다. 삽화 체계 manifest 에 넣지 않는다) | 사용자 결정(DD-23 대기 3) |
| **DD-27** | 🔧 **핫픽스** — 웹 아이콘 404 3건 + 색: `public/apple-touch-icon.png`(180, 모서리 0 — iOS 가 둥글린다) · `public/icons/icon-192.png` · `icon-512.png` · `app/favicon.ico`(16·32·48·256 PNG-in-ICO) 를 **사이드바 각인과 같은 마크**(`--ju` 채움 · 2/36 모서리 · Lora 500 「V」 · `--on-ju`)로 새로 구움. 16~48px 는 글자 비율 0.66 마스터에서 축소(0.50 은 16px 에서 획이 끊긴다). `manifest.json` `theme_color #3B82F6`·`background_color #FFFFFF` → `--bg #FBFAF6`, 같은 결함인 `layout.tsx` `viewport.themeColor`(`#FFFFFF`/`#0B1120`) → `--bg`/다크 `--bg2 #181410` | 사용자 결정(DD-23 대기 4 — 지금 처리). 검증: 로컬 서버 5경로 200 · `page-titles`·`seo-routes`·`learning-tone` 48/48 |
| **DD-28** | `docs/design/06-workflow.md` **추적 시작**(사용자 지시) — 내용 무수정 | DESIGN.md · SKILL · 이미지 체계 brief 가 모두 이 파일을 비평 절차 정본으로 가리키는데 git 에 없었다. ⚠️ 이 파일이 가리키는 것 중 3개는 **여전히 미추적**: `.agents/skills/vocaflow-design-loop/SKILL.md` · `.agents/skills/vocaflow-visual-critic/SKILL.md` · `apps/web/playwright.visual.config.ts`(그리고 그 설정을 부르는 `package.json` `test:design` 줄은 다른 세션의 미커밋 hunk). 다른 체크아웃에서는 §실행 절차가 돌지 않는다 — 그 세션이 커밋할 때 해소된다 |
| **DD-29** | **Gate 3 — 규칙 · 사전 · 발산 4안, 방향 대기**. 규칙: [03-system §3-9](03-system.md)(조건 4 · 뼈대 5층 · 토큰 5칸 · 선 1.5/1px · 위에서 본 평면 · 규격 S240/E320×200/B640×240/O1200×630 · 섹션 리듬 · 바탕 교대 = 기존 `--bg2` 한 톤). 토큰: `--grid-line`(라이트 `--bd` 40% · 다크 55% 투명 혼합) · `--s-24` 96px · `--s-40` 160px(+ `colors.ts` `gridLine` · `spacing.ts`). 사전: [symbol-dictionary.md](symbol-dictionary.md) 30행(자리 경로 12개 실재 확인). 발산: [explore/20260919/compare.md](explore/20260919/compare.md) — A 원고지 · B 책갈피 끈 · C 교정 부호 · D 책등, **에이전트 권고 A** | 섹션 교대에 새 토큰을 만들지 않은 이유: `--bg2` 가 이미 `--bg` 에 잉크 채널당 3.1~5.8%(다크 3.5~5.4%)를 섞은 값 — 결정한 「잉크 4~6%」 창. 측정으로 고친 규칙 1: 큰 도형 비율 50~70% → **60~85%**(여백 1칸 규칙과 모순이었다). 측정으로 찾은 예외 1: **`--bg2` ↔ Tines `#F3EFEA` ΔE 1.00**(`--bg` 1.01 과 함께 조사 전 값 — 2026-06-13 커밋) → §3-9 L3 예외 목록. DESIGN.md 는 다른 세션 미커밋 hunk 위라 DD-20 방식(임시 워크트리에서 HEAD 판에 두 줄만)으로 커밋 |

### 사용자 결정 대기 (DD-29)

1. 방향: **A 원고지** / B 책갈피 끈 / C 교정 부호 / D 책등 — 에이전트 권고 A(C 의 Hahmlet 난외 뜻은 A 로 옮길 수 있음).

### 사용자 결정 (2026-09-19, 3회차 앞) — DD-30~33

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-30** | **방향 A 「원고지」** 확정. C 의 **주묵 한 획**(형태 문법 F2 실선+화살표)은 별도 안으로 합치지 않고 **A 의 「이야기 선」 어휘로 흡수** — 근거→목적지 관계를 그리는 개념에서만, 그 획이 곧 그 삽화의 액센트 한 점이다. **B 「책갈피 끈」 폐기**: 익명성 (a) △ · 평균 신호 1 — S·E 에서 끈이 설명 없이 일반 곡선 그래프로 읽힌다. **D 「책등」 폐기**: 익명성 (a) △ · 평균 신호 1 — 책등 띠를 넣은 뒤에도 B 띠가 막대그래프로 읽힌다 | 사용자 결정 · 판정은 [explore/20260919/compare.md](explore/20260919/compare.md) §비평 그대로 |
| **DD-31** | 승인: `layout.tsx` `viewport.themeColor` 수정(DD-27 에 묶었던 범위 밖 한 파일) · `--bg2`↔`#F3EFEA` ΔE 1.00 L3 예외 · 큰 도형 비율 60~85% 정정 | 사용자 결정 |
| **DD-32** | 06-workflow 가 가리키던 미등록 3파일 **추적 시작**: `.agents/skills/vocaflow-design-loop/SKILL.md` · `.agents/skills/vocaflow-visual-critic/SKILL.md` · `apps/web/playwright.visual.config.ts`(내용 무수정 · 비밀값 없음 확인) | 사용자 지시. ⚠️ 아직 남은 것: 설정의 `testDir` 인 **`apps/web/tests/visual/`(4파일) 미추적**, 이 설정을 부르는 `package.json` `test:design` 줄은 다른 세션 미커밋 hunk — 둘이 커밋될 때까지 다른 체크아웃에서 `test:design` 은 돌지 않는다. **소유자(2026-09-19 추가)**: `tests/visual/` 4파일은 이 세션 산출물이 아니다 — 2026-09-18 「디자인 작업 환경 구축」 세션([docs/reports/design-workflow-20260918.md](../reports/design-workflow-20260918.md), `.agents/skills` Codex 진입 경로를 만든 작업 — Codex 추정)의 것이라 그 세션이 커밋한다. `package.json` `test:design` 줄은 미변경 |
| **DD-33** | 평균 신호 라쳇 **기준선 유지**(learner.grid-3eq 60 · admin.ai-purple 326). **갱신 필요 — 소유자 Codex**: 작업 트리에서는 59 · 318 로 **줄어** 두 검사가 "기준선을 내려라" 로 실패한다. 원인은 미커밋 CSAT 학습자·소스 작업(2026-09-17 claude→codex 인수인계 `.agent-handoff/latest.md` — 받은 쪽이 끝낸다). 그 작업을 커밋하는 **같은 커밋에서** `average-signal-ratchet.test.ts` BASELINE 을 59 · 318 로 내린다 | 사용자 결정. 이 세션의 커밋은 두 신호를 바꾸지 않았다(grid-cols-3 · 보라 추가 0) |
| **DD-34** | **Gate 4→5 완료 · 골든 3점 대기**. manifest [asset-manifest.json](asset-manifest.json) 36항목(trial 12 · planned 18 · done 4 · blocked 2) — target 실재 **34/34**(`scripts/design/asset-manifest-check.mjs`, 라우트 → page → import 그래프). 시범 12점 = A 재사용 3 + manifest trial 9, `scripts/design/style-gate.mjs` **PASS 12/12**(L3 = 0). 시트·측정표 [trial/20260919/report.md](trial/20260919/report.md) | blocked 2 의 이유: #21 문장이 게임 모듈에만 있음 · #29 후보 `MyTextsGrid.tsx` 가 **고아 컴포넌트**(import 0 — 지우지 않고 기록만). 두 검사기 모두 변이 검사로 위반을 잡는 것을 확인. 사람 결정: 골든 3점(권고 E #2 · S #7 · B #9) |

### 골든 · Gate 6 (2026-09-19) — DD-35~36

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-35** | **삽화 골든 3점 고정**: E #2 첫 글 · S #7 내가 아는 비율 · B #9 다시 보면 버틴다 → [golden/illustrations/](golden/illustrations/README.md). #10 「근거가 정답을 가리킨다」는 골든이 아니라 **이야기 선 규범 예시**(`norm/`) — 이후 생성물의 이야기 선은 #10 을 따른다(03-system §3-9). 확정 전 #16 권점 겹침 수정(낱말 위로) | 사용자 결정. 스타일 게이트 기준 입력 = 이 4점뿐(`style-gate.mjs --ref` — 선 굵기·서체·토큰 부분집합 + 주묵 선은 accent 층에만) |
| **DD-36** | **Gate 6 — 드레인 생성 + 적용 완료**. 삽화 10점 앱 투입(`components/illustrations/Illustration.tsx` + `generated/*.ts` — CSS 변수를 받으려고 인라인 SVG) · 적용 13자리(빈 상태 5 · 섹션 머리 5 · OG 3). manifest **done 17 · blocked 19**(사유 전부). 적용 중 결정 4: ① #7 은 랜딩이 아니라 `/pricing` 머리로(랜딩 커버리지 자리는 서명 `CoverageHero` 가 증명 — DD-25 ①) ② #8 은 `/about` 학습 과학 머리로 ③ 섹션이 없는 개념은 섹션을 **만들지 않고** blocked(A5) ④ OG 는 Satori 가 CSS 변수를 못 읽어 토큰 값을 옮겨 적고, 공용 카드·`/fit/s` 각인을 주묵 「V」·모서리 2 로 앱 아이콘(DD-27)과 맞춤 · 루트 OG `app/opengraph-image.tsx` 신설 | 리포트 [reports/image-system-gate6-20260919.md](../reports/image-system-gate6-20260919.md). 루프: #15 도장 위치 · 루트 OG 빈 발 · `/fit/s` 각인 — 각 1회. 빈 상태 캡처는 `/dev/components` 「0. 삽화 — 빈 상태」 검수대(검증 계정으로 데이터 0 상태를 못 만든다 — 실계정 진입은 미검증). 남은 평균 신호(빈 상태 그라디언트 CTA 3곳)는 이번 변경 전부터 — A5 범위 밖 blocked. 라쳇 실패 4건은 전부 Codex 미커밋 CSAT 작업(DD-33) |

### 사용자 결정 (2026-09-19, Gate 6 뒤) — DD-37~38

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-37** | DD-36 의 적용 판단 3건(#7 → `/pricing` · 섹션 없는 개념은 신설 없이 blocked · OG 토큰 값 옮겨 적기) **승인**. manifest blocked 19 는 섹션 신설 없이 **「대기」로 유지**. 위생: 고아 컴포넌트 `components/textviewer/MyTextsGrid.tsx` **삭제**(참조 0 재확인 — 정의 파일 자신뿐, 2026-09-05 감사 m9 와 같은 결론) | 사용자 결정. ⚠️ 이 삭제로 `components/textviewer/TextCard.tsx` 가 **새 고아**가 된다(유일한 사용처가 MyTextsGrid) — 지시 범위 밖이라 지우지 않고 기록만. MODULES.md 의 MyTextsGrid 줄은 HEAD 판에서 뺐다(DD-20 방식) |
| **DD-38** | **「단어 0개」 검증 계정으로 빈 상태 5곳 실제 경로 캡처** — `scripts/design/seed-empty-account.mjs`(계정 `design-empty@vocaflow.local` 생성·재사용 · `vocabularies`/`texts` 0 확인 · 0 아니면 지우지 않고 멈춤 · 비밀번호는 매 실행 새로·메모리에만 · 세션만 gitignore 폴더). 5곳 모두 실제 경로에서 삽화 확인, 검수대(`/dev/components`)는 유지. #3 의 manifest target 을 `/library/vocab` 으로 | 사용자 지시. 캡처·비교 [reports/image-system-gate6-20260919.md](../reports/image-system-gate6-20260919.md) §실제 경로. 발견: 만화 서가 필터 0 은 구조적으로 안 나온다(레벨 칩을 편이 있는 레벨로만 만든다) — 그래서 #3 은 단어장 서가 「내 단어장만」 0건 경로 |
| **DD-39** | **빈 상태 #1 · #2 · #4 CTA 그라디언트 제거**(다음 화면 작업 1번, 삽화 커밋과 분리) — 주묵 단색 + 잉크 테두리 · hover 색만 · active 1px. 평균 신호 라쳇 기준선을 커밋된 트리 기준으로 내림: learner grid-3eq 60→59(DD-37 의 MyTextsGrid 삭제분 — 그 파일에 `grid-cols-3` 1개) · gradient 171→168 · float-hover 66→65 | 사용자 지시. 전후 캡처 [reports/image-system-gate6-20260919.md](../reports/image-system-gate6-20260919.md) §다음 화면 작업. ⚠️ **DD-33 정정**: Codex 가 CSAT 작업을 커밋할 때 내릴 값은 grid-3eq **58**(59 아님) · admin.ai-purple **318**. 남은 것: #1 · #2 섹션 바탕 옅은 그라디언트 |

### PR · 브랜치 정리 (2026-09-19) — DD-40~41

| # | 결정 | 근거 · 한 일 |
|---|---|---|
| **DD-40** | 위생: 고아 `components/textviewer/TextCard.tsx` **삭제**(참조 0 재확인 — DD-37 의 MyTextsGrid 삭제로 고아가 됐다). MODULES.md 의 TextCard 줄은 HEAD 판에서 뺐다(DD-20 방식) | 사용자 지시. ⚠️ 연쇄로 새 고아: `components/textviewer/TextStatusBadge.tsx`(유일한 사용처가 TextCard) · 서버 액션 3개 `deleteUserBookGroupAction` · `deleteUserTextAction` · `unenrollBookAction`(`app/(main)/text/actions.ts` 에 정의만 남음 — 화면에서 부르는 곳 0). 지시 범위 밖이라 지우지 않고 기록만 — 서버 액션 삭제는 동작 경로를 줄이므로 사람 확인이 먼저 |
| **DD-41** | **PR #97 을 브랜치 전체 통합 PR 로 전환**(제목·본문 교체, 원래 infra 설명은 본문 아래 보존). 머지 전제: 작업 트리 미커밋 0(Codex 인수인계 「추가 요청 2」 — 수용 기준 `git status` clean). **머지 후**: main 에 태그 → `pc2-20260720-1` 삭제 → AGENTS.md ③ Git 절에 한 줄 **제안**: 「작업 브랜치 수명 ≤ 2주, main 기준 분기 — 넘기면 main 에 먼저 합친다」 — **AGENTS.md 수정은 사람 승인 후**(이 DD 는 제안 기록) | 사용자 지시. 근거: 이미지 체계만 떼어 낸 PR 이 불가능했다 — main(2026-07-19)에 DESIGN.md · design/DECISIONS · 03-system · vocaflow-design 스킬 · 라쳇 · `ui/press` · `--ju` 가 전부 없어 첫 cherry-pick 에서 충돌. 브랜치는 main 보다 커밋 2,410 · 파일 4,524 앞서 있었다(2026-09-19). 두 달 산 브랜치가 리뷰 단위를 없앴다 · **2026-09-19 사용자 결정**: AGENTS.md 한 줄 **승인** → 작업 트리 AGENTS.md(미추적 — 그 파일이 커밋될 때 함께 들어간다)와 HEAD CLAUDE.md Push 규칙(DD-20 방식)에 반영. **추가 제안(적용 안 함)**: CI 에 「main 대비 14일 이상 뒤처짐 경고」 — 차단이 아니라 경고(job summary · PR 코멘트). 기준 = 브랜치의 merge-base 커밋 날짜와 오늘의 차이 · 14일 넘으면 경고 1줄. 구현은 승인 후 |
| **DD-42** | **제품 결함 — 사용자 데이터 삭제 경로 부재, UI 복구 필요**(이슈 [#100](https://github.com/KangMin098/Vocaflow/issues/100)). TextCard 가 고아였다는 것은 **텍스트 삭제 · 사용자 책 그룹 삭제가 UI 에서 도달 불가**였다는 뜻이다. 서버 액션 3개(`deleteUserTextAction` · `deleteUserBookGroupAction` · `unenrollBookAction`)는 **삭제하지 않고 보존** — 복구 때 재사용. 위생: 고아 `components/textviewer/TextStatusBadge.tsx` 삭제(참조 0 확인) | 사용자 결정. 사실 확인(2026-09-19): **책 해지는 도달 가능** — 라이브러리 서가가 `lib/library/enroll.ts` `unenrollBook` 을 부른다(`BooksExplorer.tsx:408` · `LibraryGrid.tsx:102`). 도달 불가는 텍스트 삭제 · 책 그룹 삭제 둘. `save-user-book.ts` 의 `texts` 삭제는 저장 실패 되돌리기용. 끊긴 시점 추정: `3465b1e2`(2026-05-05, 텍스트 허브를 `TextHubContent` 로 교체 — `MyTextsGrid` 사용이 빠진 커밋, 미검증) |
| **DD-43** | **PR #97 머지 순서 고정**: ① 미커밋 0 → ② CI 재실행 → ③ verify 수정 → ④ build 확인 → ⑤ 머지 → ⑥ DD-41 실행(태그 · `pc2-20260720-1` 삭제). CI `TypeScript` 실패는 ①(Codex)로 해소 대기. **verify 는 머지 차단 항목** — `packages/video-factory` 테스트를 고정 픽스처 또는 파일 부재 시 skip 으로 고친다, 소유 = 영상 파이프라인 Claude 세션(`491f8c6a` · `dfb4f9f4`), 이슈 [#101](https://github.com/KangMin098/Vocaflow/issues/101) | 사용자 결정. 원인: `work/source-bundle.json` 이 패키지 `.gitignore` 로 제외된 생성 산출물이라 로컬만 통과. 인수인계 파일(Codex 앞)에는 「Codex 몫 아님」 참고로만 적었다 |
| **DD-44** | **AGENTS.md 추적 시작**(작업 트리 판 174줄 그대로 · `check.mjs` 9/9) → HEAD CLAUDE.md Push 규칙의 **임시 중복 줄(브랜치 수명) 제거** — 규칙의 정본은 AGENTS.md ③ 하나. #101 은 고정 픽스처 + 부재 시 skip 으로 수정(`dae59f53`) | 사용자 지시. ⚠️ AGENTS.md 가 링크하는 `agents/router.md` · `docs/agents/CONTEXT_DETAIL.md` 는 아직 미추적 — 커밋된 트리에서 링크 2곳이 깨진 채다. 링크 드리프트 테스트는 `./` 로 시작하는 링크를 뿌리 경로로 보지 않아 CI 는 통과하고, 커밋된 CI 는 `check.mjs` 를 돌리지 않는다(확인). 해소 = Codex 「추가 요청 2」 미커밋 0 |
| **DD-45** | **#101 해결·종료** — 고정 픽스처 + 부재 시 skip(`dae59f53`) · 두 번째 원인(Remotion `Root.tsx` 가 원료 JSON 정적 import → 원료 없을 때만 Root 2파일 제외한 타입 검사, `efdcdd8d`). CI `video-factory` test 85/85 · typecheck 통과. PR #97 의 🔒(#101) 해제. **verify 잡은 아직 빨강** — 다른 원인: `library-pipeline` `feed-discovery.test.ts` 가 `Date.now()` 에 기대 시간이 지나 떨어짐, 수정은 작업 트리에 미커밋 → 머지 순서 ①(미커밋 0)에 편입 | 사용자 지시(#101 즉시 수정). 원료를 `work/` 에 픽스처로 채우는 방법은 쓰지 않았다 — `render/ensure.ts` 의 「없으면 만들지 않는다」 가드를 우회한다. 머지 순서 DD-43 불변 — 남은 차단은 미커밋 0 하나(TypeScript · build · verify 셋 다 그것으로 풀린다) |
| **DD-46** | **미커밋 0 정리 1차 + 재발 방지**. ① `feed-discovery.ts` 미커밋 diff 가 정확히 8줄(4+/4−, 시계 주입)이라 `--only` 선커밋(`1c2d686b`) · ② `agents/router.md` · `docs/agents/CONTEXT_DETAIL.md` 추적(DD-44 의 깨진 링크 해소) · ③ 미추적 91건 분류 — **커밋 23**(문서 19 · 에이전트 문서·설정 3 · 픽스처 1, `063b42dd`) · **사람 확인 9**(`docs/design/shots/*.png`) · **Codex 59**(코드 + 코드에 묶인 설정) · ④ 재발 방지: AGENTS.md 「하지 말 것」에 **시계 주입 규칙** 한 줄 | 분류 근거: **문서·설정·픽스처라도 미추적 코드를 실행하는 것은 코드로 본다** — `.claude/settings.json` · `.codex/hooks.json` 훅이 `agents/scripts/guard.mjs` 를, `.github/workflows/csat-source-audit.yml` 이 `scripts/audit/*` 를 부른다(단독 커밋 시 깨끗한 체크아웃에서 없는 파일을 부름). `.codex/` 는 `sync.mjs` 생성물, `lib/csat/dissect-anchors.json` 은 dissect 코드의 생성 데이터라 코드와 함께. 화면 캡처 9장은 06-workflow · golden 규칙(기출 원문·개인 기록이 보이는 화면은 저장소 금지)에 걸릴 수 있어 보류. 커밋분 전부 비밀값 패턴 0 · 최상위 docs 링크 드리프트 0. lint 대신 AGENTS 규칙인 이유: 테스트 34파일에 직접 호출 77건 · eslint 는 `apps/web` 만(사고가 난 `library-pipeline` 은 lint 없음) — 규칙을 lint 로 걸면 기존 77건 수정·예외가 먼저고 정작 사고 난 패키지는 못 막는다 |
| **DD-47** | `docs/design/shots/*.png` 9장 **커밋하지 않음** → `docs/design/shots/` 를 루트 .gitignore 에(HEAD 판 한 줄, `419b4897`). 골든이 필요하면 `design-empty` 계정·`/dev/components` 검수대 데이터로 다시 찍어 `docs/design/golden/` 에만 커밋(기출 원문·실제 사용자 데이터 화면 금지) — 06-workflow 산출물 표에 두 폴더의 역할·git 취급 한 줄씩. 승인: 미추적 분류 기준 · 23건 커밋 · 시각 주입 규칙. 기존 직접 호출 77건(34파일)은 이슈 [#102](https://github.com/KangMin098/Vocaflow/issues/102)(범위 밖, 기록만) | 사용자 결정. **갱신(같은 날)**: 이미 추적 중이던 v07 전후 16건(PNG 15 · `_report.json`)을 한 장씩 판정 — **16/16 기출 원문 0 · 실제 사용자 데이터 0** → 전부 `git mv` 로 `docs/design/archive/shots-v07/` 로 옮김(`git rm` 0). 근거: 캡처 하네스 `scripts/design/capture-learner.mjs` 의 기본 계정이 검증 계정 `runtime-test-0705@vocaflow.dev`(대시보드의 「런타임테스터」 · 진단 V11 · 단어장 252개는 그 시드), `dark_fit` 지문은 제품 예시 지문, 로그인 화면 2장은 데이터 없음. 계정 조회(DB)는 개인정보 처리로 차단돼 저장소 증거로 판정했다. 폴더째 옮기지 않고 한 장씩 옮겼다 — `shots/after/` 에 gitignore 된 미추적 캡처가 섞여 있어 딸려 가면 다시 드러난다. 링크 갱신: 04-application §4-5 · 00-inventory · 02-directions · 05-report · asset-inventory. 06-workflow 산출물 표에 `archive/` 행(결정 증거 · 커밋 · 원문·개인 데이터 없는 것만) |
| **DD-48** | **검증 계정 비밀번호 평문 노출 대응(한 항목)**. ① **교체**: `runtime-test-0705@vocaflow.dev` 비밀번호를 무작위 32자로 교체(`scripts/security/rotate-runtime-password.mjs` — Supabase admin). 확인: **옛 값 로그인 실패 · 새 값 로그인 성공**. 새 값은 `apps/web/.env.local` 의 `PLAYWRIGHT_RUNTIME_PASSWORD` 에만 — 예외 1(사용자 승인): CI e2e 가 이 계정으로 로그인하므로 **GitHub 저장소 시크릿** `PLAYWRIGHT_RUNTIME_PASSWORD`(값은 `--emit` 파이프로만) + `ci.yml` e2e 단계 env 한 줄. ② **문서·코드**: `apps/web/CLAUDE.md:204` 값을 「`.env` 의 `PLAYWRIGHT_RUNTIME_PASSWORD` 참조」로(HEAD 판 한 줄) · `docs/AI_CONTEXT` 사본도 · 코드 **57파일**(e2e 스펙 · 통합 테스트 · e2e-session · design/ux-bench 스크립트)의 `process.env.PLAYWRIGHT_RUNTIME_PASSWORD || '<평문>'` 대체값 제거(사용자 승인 — 파일 ≥30) → 값이 없으면 명확한 오류(e2e) / 빈 값(통합 테스트는 원래 env 없으면 건너뜀). `playwright.config.ts` 가 vitest 처럼 `.env.local` 을 읽는다. 로컬 확인: `04-ui-smoke` 7 중 6 통과(로그인 성공 — 실패 1 은 로그인 뒤 「시리즈 상세 → 복귀」 화면 흐름, 비밀번호 무관). 이름은 요청의 `TEST_ACCOUNT_PASSWORD` 대신 **코드가 이미 읽던 `PLAYWRIGHT_RUNTIME_PASSWORD`**(49곳이 먼저 읽고 있었다 — 새 이름이면 57파일 이름 변경이 더 필요). ③ **전 파일 검사**: 추적 5,368파일. 발견 = 위 57파일 + **다른 테스트 계정 `lexicon-test@vocaflow.local` 비밀번호**(`tests/e2e/fixtures/test-user.ts:7` 대체값 — 값을 `.env.local` `PLAYWRIGHT_TEST_PASSWORD` 로 옮기고 대체값 제거. **이 계정 비밀번호 교체는 범위 밖 — 사람 결정**) + 오탐 11(마이그레이션 주석의 psql 자리표시자 · CSS 토큰 이름 · 경로 · `sk-ant-...` 예시 · supabase `env(…)` 참조). 검사를 **CI verify 로 확장**: `scripts/security/secret-scan.mjs`(규칙) + `apps/web/src/lib/__tests__/secret-scan.test.ts`(추적 파일 전체 · 0건이어야 통과). 변이 검사: 가짜 대체값 한 줄을 넣자 잡힘. ④ **히스토리 재작성 안 함** — 옛 값은 `bc6fdf20`(2026-07-08)부터 히스토리 커밋 57개에 **잔존**하지만 교체로 **무효**다. ⑤ **권한**: 앱 역할 `user`(관리자·큐레이터 아님) · 학급 소속 0 · 소유 학급 0 · 인증 역할 `authenticated` · 이메일 로그인 1개 → 이미 최소 권한, **축소할 것 없음** | 사용자 지시. 곁가지 발견 2: (a) `git commit --only` 중 pre-commit 훅(memory mirror)이 임시 인덱스에 `docs/AI_CONTEXT` 를 자동 스테이징해 **커밋에 딸려 들어갔고**(값을 고친 판이라 무해), 커밋 뒤 원래 인덱스에는 커밋 전 판(옛 값)이 남아 있었다 — 즉시 unstage. (b) 첫 커밋 목록 필터가 다른 세션의 미커밋 e2e 스펙 2개(44 · 47)를 집어 올렸다 — 커밋 전에 발견해 「HEAD 에 옛 값이 있던 파일」 기준으로 목록을 다시 만들었다. Write/Edit 도구 훅이 응답하지 않아(호스트 연결) 이번 파일은 Bash 로 썼다. **갱신(2026-09-20) — ⑥ 두 번째 계정도 교체**: ③ 에서 「사람 결정」으로 남겼던 `lexicon-test@vocaflow.local` 을 사용자 지시로 **교체**. 교체 스크립트를 두 계정용으로 일반화(`--account runtime\|lexicon`) + 교체 직후 **로그인 확인을 스크립트 안으로**(익명 키로 signInWithPassword · 성공·실패만 출력) — 확인: **옛 값 실패 · 새 값 성공**. 새 값은 `apps/web/.env.local` 의 `PLAYWRIGHT_TEST_PASSWORD` 에만(이 계정은 CI 가 안 쓴다 — CI e2e 는 `04-ui-smoke` 하나뿐이고 그건 runtime 계정. 다른 스펙을 CI 에 넣는 날 저장소 시크릿이 필요해진다). 권한: 앱 역할 `user` · status `active` · 학급 소속 0 · 소유 학급 0 · 인증 역할 `authenticated` · 이메일 로그인 1 · 데이터는 시드뿐(낱말 8 · 텍스트 1) → **이미 최소 권한**. 옛 값은 히스토리 커밋 3개(`f02af75f` 2026-05-20 부터)에 잔존하지만 교체로 **무효**, 추적 파일에는 0. **⑦ 곁가지 — e2e 실패 1건의 정체**: DD-48 ② 에서 「비밀번호 무관」으로 넘긴 `04-ui-smoke` 「시리즈 상세/복귀」 실패를 재현·판정 → **테스트 드리프트**다. 「글 둘러보기」는 `0698e6bf`(2026-08-26, sitemap 고아 해소)부터 **버튼이 아니라 진짜 `<Link>`**(크롤러 경로 · `ScriptsBrowser.tsx:273` 에 의도 주석)인데 테스트는 `getByRole('button')` 을 기다린다. `ci.yml` e2e 잡이 이 스펙을 돌리므로 **CI 에서도 같은 이유로 빨강**이다(컴포넌트는 작업 트리 미변경 — 다른 세션 미커밋과 무관). 이슈 [#103](https://github.com/KangMin098/Vocaflow/issues/103) → **사용자 지시로 같은 세션에서 수정·종료**: `getByRole('link')` + 이동 뒤 `?series=` 주소 단언 + 복귀 시 주소가 `?series=` 를 벗는 단언, hydration 대비 `toPass` 재클릭 제거(진짜 anchor 라 불필요). 로컬 `04-ui-smoke` **7/7 통과**(1.5분) → 이슈 닫음 · PR #97 확인 목록에 「e2e #103 해소」 |
| **DD-49** | **pre-commit 훅이 `--only` 를 우회해 인덱스를 고치던 결함 수정**(DD-48 곁가지 (a)). 옛 훅은 조건 없이 `git add docs/AI_CONTEXT docs/CONTEXT.md` 를 했다 → (i) 요청하지 않은 파일이 커밋에 딸려 들어가고 (ii) `--only` 커밋이 끝난 뒤에도 그 경로가 **인덱스에 staged 로 남는다**(git 이 훅의 add 를 실제 인덱스로 되돌려 놓는다). 수정: 훅은 **인덱스를 전혀 건드리지 않는 검사기**가 됐다 — `sync-export-memory.mjs --check`(파일·인덱스 무변경, 낡으면 exit 1, 이 머신에 memory 없거나 다른 PC mirror 면 「판정 불가」로 통과)로 보고, ① mirror 가 이번 커밋에 포함됐고 낡았으면 **commit 차단 + 명령 안내** ② 포함 안 됐으면 **안내만 찍고 통과** ③ node·스크립트 부재는 통과. mirror 갱신은 사람이 `pnpm sync:memory` 뒤 직접 add. `docs/CONTEXT.md` 자동 블록 갱신은 훅에서 빠졌다(§4 제목의 「push 마다 regenerate」도 정정) | 사용자 지시(결함 판정). **재현·검증**: (1) 가짜 오래된 내보내기(mirror 1파일 수정 + memory 없는 가짜 HOME)로 옛 훅을 임시 인덱스에 돌려 **`--only` 1파일 요청에 3파일이 staged 되는 것 재현**(낡은 mirror + 다른 세션이 고친 `docs/CONTEXT.md`) → 새 훅은 요청한 1파일만. (2) 이 저장소의 mirror 는 **전부 다른 PC 것**(이 머신 memory 파일명이 `project_*` 규칙이 아니라 다중 PC 가드가 전체 skip) → 갱신 분기를 여기서 검증할 수 없어 **격리 임시 저장소**(가짜 memory + 실제 훅·스크립트)에서 3가지 경로 전수: 낡고 포함 → 차단 · 갱신 후 → 통과(커밋에 그 1파일, 새로 생긴 mirror 파일은 미포함) · 낡고 무관한 파일 커밋 → 통과+안내. **세 경우 모두 커밋 뒤 인덱스 0**. AGENTS.md 임시 규칙은 넣지 않았다(이 세션에서 고쳤으므로). **보완(같은 날, 사용자 지시)**: 자동 갱신이 사라진 자리를 절차로 메운다 — AGENTS.md 「진입 순서」와 `.agent-handoff/latest.md` 첫 항목에 **세션 시작 시 `pnpm sync:memory --check` → stale 이면 `pnpm sync:memory` 뒤 별도 커밋** 한 줄씩(핸드오프 파일은 gitignore 라 커밋 대상 아님). `pnpm sync:memory --check` 는 `--refresh-context --check` 로 들어가며 check 가 먼저 끝나 CONTEXT.md 를 쓰지 않는 것을 확인 |
| **DD-50** | **「미커밋 0」 인수 완료 — 갈래 A(의미 단위 커밋)로 끝냈다**(Codex 사용 한도 폴백, 사용자 지시). 미커밋 **175건 → 0**, 커밋 8개: ① 에이전트 공용 도구·설정(`agents/scripts/` 전체 · `.claude/settings.json` · `.codex/` — 훅·CI 가 부르는데 미추적이었다 · `470f8eea`) ② 원문 적격 캐시·정책 v3 50파일(`658992ad`) ③ 관리자 근거 콘솔 16파일(`d147786c`) ④ 기출 학습자 화면 95파일(`936744db`) ⑤ VOA 보일러플레이트(`6c04aca9`) ⑥ 시각 회귀 4파일 + `test:design`(DD-32 해소 · `5c22a7a6`) ⑦ 통합 회귀 수정 6건(`a0e2e8e5`) ⑧ 예산·검사 도구(`7dbfdb4e`) + 재생성 산출물(`77f70fde`). **마이그레이션 2개는 이미 DB 적용 상태**를 확인하고 커밋했다 — 원장(`supabase_migrations`)에는 `20260918140224` · `20260918141948` 로 올라가 있고 파일명(`…140000` · `…140100`)과 다르다. MCP 로 적용하면 서버가 자기 타임스탬프를 붙이는 이 저장소 관행이라 이상이 아니다(최근 커밋된 마이그레이션 6개 전부 파일명과 원장 버전이 다르다). 소비자 3함수(`textbook_practice_items` · `prescribe_today` · `grade_dcp_item`)가 `csat_source_is_eligible` 게이트를 실제로 쓰는 것까지 DB 에서 확인. **요청 1·2 이행**: 라쳇은 **내리기만** — learner grid-3eq 59→58 · gradient 168→167 · glass 52→49 · float-hover 65→64 · admin ai-purple 326→318 · form-declaration 기준선 152→150(삭제된 `csat/progress` · `csat/session` 제거) · 새 화면 골격 선언 2개(dissect=「시험지 사물」 · formulas=「계보」, 렌더와 일치) | 사용자 지시(한도 폴백 · 이 세션이 끝낸다). **판단 3건**: (a) `DESIGN.md` 미커밋 diff 는 **커밋된 디자인 결정을 되돌리는 것**이었다(형태 씨앗·골든 링크 삭제 · 시험지 자산 예시 교체) → 커밋된 판을 유지하고 **새 링크 3행만 병합**, 되돌림 hunk 폐기. (b) 가드가 자기 픽스처(`check.test.mjs` 의 가짜 접속 문자열)를 비밀값으로 잡아 **그 파일을 커밋할 수 없었다** → 값을 런타임 조립으로 바꿨다(그 파일이 이미 쓰는 방식 · 기준을 둘로 만들지 않았다). (c) 「테스트 시각 주입」처럼 **측정 함정**을 하나 더 기록: 이 저장소는 CRLF/LF 혼재라 줄 단위 스캐너가 CRLF 파일의 히트를 놓친다(실측 `drain-pending-words.mjs` LF 1 · CRLF 0) → 예산 수치는 **깨끗한 LF 체크아웃**에서 재야 한다 |
| **DD-51** | **통합 뒤 검증 결과 — lint · typecheck 통과, 테스트 24 실패 → 11 실패**(전부 통합 **전에도** 실패). 기준선(`ee4e65f5`)을 LF 로 다시 펼쳐 같은 검사를 돌려 귀속을 갈랐다. **통합이 깨뜨린 것 4건 = 전부 수정**: `help-diagram` 3건(재설계로 `csat-evidence` 도식이 사라졌다 → 화면 1 · 탭 3 추가) · `row-write-budget` 1건. **통합과 무관한 것 = 남긴다**: ① 실 DB 통합 테스트 5파일 13건(`copyright-boundary` 2 · `dcp-grade-records` 4 · `content-quality-gate` 2 · `resolve-headword` 4 · `silent-noop` 1) — 기준선에서 **같은 단언이 같게 실패**한다(DB 상태·RPC 드리프트). CI verify 도 실 시크릿을 넘기지만 **CI 는 그중 대부분을 skip** 한다(로컬 13건 중 CI 에서 실제로 돈 것은 `copyright-boundary` 2건뿐 · CI 실측 2026-09-20). **CI 실패는 2파일 3건**이었고 둘 다 이 회차에서 고쳤다: ① `csat/session/__tests__/model.test.ts` 가 `+09:00` 절대시각을 박아 두어 **UTC 러너에서만** 주 경계가 어긋났다(「이번 주 문항 수」 2 vs 1 — `dayKey`·`weekCount` 는 「기기 시간대」가 의도다) → 테스트를 실행 시간대의 지역 시각으로 바꾸고 `TZ=UTC` 로도 확인. ② `copyright-boundary` 2건은 **규칙 쪽 결함**이었다 — 오버레이 라우트·클라이언트가 `668c3f05` 에서 지워졌는데 파일 목록이 안 따라와 ENOENT(없는 파일은 건너뛰되 **0건 검사면 실패**로 바꿨다), 그리고 단어 수 정규식이 곱슬 아포스트로피를 못 세어 `enemy’s` 를 두 낱말로 쪼개 **규칙을 지킨 7단어 조각을 8단어로** 잡았다. 로컬에만 남는 11건(실 DB 상태·RPC 드리프트)은 별도 작업으로 남긴다. ② `token-parity` · `learner-routes` · `touch-target` · `row-cap-lies` · `offset-paging` 은 **규칙·사실을 고쳐 초록으로** 만들었다 | **부수 발견(실제 결함)**: `lib/admin/video-console.ts` 가 `.limit(10000)` 으로 영상 계측을 세고 있었다 — PostgREST 는 1,000행에서 끊으므로 시작·완료 수가 **오류 없이 적게** 나왔다. 정본 헬퍼 `pagedSelect` 로 교체(그 대가로 offset 예산 +1). **규칙 수정 2건**: `token-parity` 는 웹이 `color-mix()` 인 토큰을 RN 이 합성 hex 로 들 수밖에 없다는 모양만 예외로 뒀고, `touch-target` 은 CSS 모듈이 `min-height: 44px` 을 일괄 보장하는 CSAT 관리자 5화면을 상한 밖으로 뺐다 — 상한 70 은 유지하고 **그 CSS 규칙의 존재를 테스트가 직접 확인**한다(썩지 않는 예외). 두 예산(`row-write` 151 · `offset` 210)은 파일 이름과 함께 근거를 적고 옮겼다. ⚠️ 공유 워크트리: 작업 중 **다른 세션이 내 커밋 위에 DB 조치(`0d422429`)를 커밋**했다 — 잠금은 내 것이었다(lock.mjs 가 다른 세션의 쓰기를 막지는 못한다) |
| **DD-52** | **PR #97 머지 완료 · DD-41 실행**(사용자 확인 2026-09-20). 순서 DD-43 그대로: ① 미커밋 0(DD-50) → ② CI 재실행 → ③·④ **verify · build · e2e 전부 초록**(`8547dbf0` · tip `074a1f37`) → ⑤ **머지 커밋 `4cb3bd45`**(squash 아님 — 통합 PR 이라 100커밋 이력을 남긴다) → ⑥ DD-41: main 에 **태그 `v06.34-pc2-integration`** · `pc2-20260720-1` **삭제**(원격·로컬) · AGENTS.md 「작업 브랜치 수명 ≤ 2주」 제자리 확인(154행). CI 를 초록으로 만든 마지막 3건은 **CI 에서만** 빨갛던 것들이었다(로컬은 통과) — 시간대 의존 1 + 규칙 결함 2, DD-51 참조 | 사용자 결정(머지 + DD-41 실행 · 남은 11건은 이슈 등록만). **남긴 것**: 로컬 전용 실 DB 실패 11건 → 이슈 [#104](https://github.com/KangMin098/Vocaflow/issues/104)(적격 게이트가 채점 거부 — 픽스처가 부적격 문항) · [#105](https://github.com/KangMin098/Vocaflow/issues/105)(`resolve_dict_headword` 의 「해석 안 함」 표현) · [#106](https://github.com/KangMin098/Vocaflow/issues/106)(발행 콘텐츠 critical · db_health stale 판정). ⚠️ **#104 에 담은 공급 실측**: `kind='article'` DCP 문항의 원천 등급이 unjudged 498,231 · blocked 259,652 · excerpt 61,112 · usable 44,182 · 캐시 없음 16,357 → **연습 가능 12%**. 서빙 함수도 같은 게이트를 쓰므로 학습자 경로는 깨지지 않지만, `unjudged` 498k 는 판정 드레인(`source-policy-refresh`)이 아직 안 돈 몫이다 — 분기 진단의 「공급망 비대」 지표로 쓸 값이다 |
| **DD-53** | **`lock.mjs` 는 권고(advisory) 잠금이다 — 강제가 아니다**(사실 기록, 사용자 지시). 실측 2026-09-20: 내가 `acquire claude` 로 잠금을 쥔 채 작업하는 동안 **다른 세션이 같은 워크트리에 커밋했다**(`0d422429` · DB 권한 조치). 구조상 그렇다 — `acquire` 는 `.agent-lock` 파일에 `{agent,pid,host,branch,started_at}` 를 쓰고 남이 살아 있으면 exit 3 을 낼 뿐이고, **git·편집기·스크립트는 그 파일을 보지 않는다**. 훅에도 잠금 검사가 없다(`.githooks/pre-commit` grep 결과 0). 즉 잠금은 「예의를 지키는 에이전트끼리의 신호」이고, 잠금을 안 잡는 세션·사람 손·다른 도구는 그대로 쓴다. 고아 잠금 자동 해제(pid 죽음·TTL 12시간)도 같은 성질이다 — 남의 작업을 막는 장치가 아니라 **내 작업을 알리는 장치**다 | 사용자 지시(사실대로 기록). **강제 안(구현 안 함 — 다음 결정)**: `pre-commit` 에서 `.agent-lock` 을 읽어 **소유자가 다른 에이전트면 커밋 거부**(`AGENT_NAME` 또는 `--force` 로 우회). 장점: `--only` 규약처럼 기계가 지킨다. 걸림돌 3개를 먼저 판단해야 한다 — ① 사람이 직접 커밋할 때(에이전트 이름 없음)를 허용할지, 허용하면 구멍이 남는다 ② 잠금이 12시간 TTL 로 살아 있는 채 세션이 죽으면 그동안 아무도 커밋을 못 한다(지금은 고아 판정으로 풀린다 — 훅에서도 같은 판정을 써야 한다) ③ 워크트리별 잠금이므로 `pnpm wt new` 로 갈라 쓰는 경로를 막지 않아야 한다. DD-49 에서 **훅은 인덱스를 고치지 않는다**로 정했으므로, 훅이 하는 일은 「거부 + 안내」뿐이어야 한다 **보완(2026-09-20, 사용자 지시)**: 권고 잠금인 것은 그대로지만, 잠금이 **자기 규칙마저 안 지키던** 구멍 하나를 막았다 — 옛 `acquire` 는 `cur.agent === agent` 이기만 하면 「같은 에이전트 — 갱신」으로 잠금을 넘겨줬다. 이름이 같을 뿐 다른 세션인데도 그랬다. 실측: `claude` 세션 pid 21452 가 main 에서 쥔 잠금을 pid 5772 가 그대로 가져갔다(둘 다 살아 있었다). 이제 **pid 가 다르고 그 pid 가 살아 있으면 exit 3** 이고, 같은 pid 의 재획득만 갱신이다. 인수는 사용자가 지시했을 때 `--force` 로만. 회귀 3건 추가(`agents/scripts/__tests__/lock.test.mjs` 9/9). 여전히 git·훅은 이 파일을 보지 않는다 — 권고라는 성질은 안 바뀐다. |
| **DD-54** | **#104 판정 드레인 실행 — 연습 가능 문항 105,294 → 119,559(11.97% → 13.59%)**. 사용자 우선 지시. 순서: ① 배치·재실행 안전 확인(`--limit 1..500` 강제 · 배치별 `before.json` 백업 · upsert 후 재검증 · 변경 없으면 건너뜀) ② **적격 드레인만으로는 0건**임을 dry-run 으로 확인(미판정 100편 → `changed 0 · skipped 100`) ③ 사유 추적: 미판정 12,432편은 `gate.verdict = null`(내용 판정 없음) ④ **반례로 제 추정을 뒤집음** — 정본 평가기에 `verdict='use'` 를 넣자 `unjudged` → **`excerpt` · 차단 0**(문항이 붙어 excerpt 경로 성립). 「raw 는 어차피 게시 불가」라는 내 추정이 틀렸다 ⑤ 판정만으로 열리는 집합 = **PLOS 309편 · 문항 14,265개**, 그런데 그 집합이 판정 export 질의에서 **구조적으로 빠져 있었다**(`purpose='raw'` 제외 규칙이 적격 정책 v3 의 excerpt 경로보다 먼저 만들어진 가정) ⑥ 3편으로 루프 전체 검증 → 306편을 4청크로 팬아웃(서브에이전트 3) → 판정 306건(`use` 304 · `narrative` 2 · `reject` 0) → `gate-mixed-import --commit` → 감사 → `--plan` → `--commit` ⑦ **재측정: 309/309 가 `csat_source_is_eligible` = true · 판정만으로 열리는 잔여 0**. 기사 단위 적격 12,962 → 13,271 | 사용자 지시(배치·재실행 안전 확인 후 실행 · 완료 시 재측정). **도구 3개**(전부 읽기 전용 기본): `gate-raw-with-items-ids.mjs`(교집합을 청크로) · `gate-article-export --include-raw-with-items`(문항 붙은 raw 만 · 기본값 불변) · `gate-reviews-verify.mjs`(적재 전 해시·리비전·모순 대조 — 서브에이전트 보고를 믿지 않고 내가 검증한다). 화면도움말(`help/textbook.ts` drain)에 이 lane 을 단계로 추가. **함정 3개 기록**: (a) **1,000행 상한에 두 번** 걸렸다 — 한 원본에 문항이 평균 46개라 `.in(ref_id, 100개)` 가 잘려 309편이 76편·100편이 21편으로 보였다(오류 없음). 둘 다 `head` count 로 고쳤다 (b) 판정 적재가 `updated_at` 을 올리므로 **같은 호흡에 감사를 돌리면** 측정 시각 < 리비전 으로 거부된다(검증기가 제 일을 한 것) (c) 병렬 서브에이전트가 **공용 scratchpad 파일명을 공유**해 서로의 임시 스크립트를 덮었다 — 청크별 접미사 필요. **남은 것**: 미판정 12,123편은 `cefr_above_band` 가 섞여 판정으로 열리지 않는다 → 밴드·V-Level 결정이 필요한 **제품 판단**이고, 이미 문항이 만들어진 원본이라 「공급을 만들어 놓고 정책이 막는」 구조다(분기 진단 §수요/공급 지표로 넘긴다) |
| **DD-55** | **관리자 액센트 = B 「주묵 도장」 확정**(사용자 결정). 구조·액센트는 `--p` 로 통일하고 **관리자에서만** `--ju` 를 **표식(점·선)에만** 쓴다 — 헤더 도장 · 파괴적 동작 버튼 테두리 · 활성 행 왼쪽 2px. **면 금지**(주묵이 면적을 가지면 경고와 혼동된다 — 03-system 3-2 의 「쓰는 자리/쓰지 않는 자리」와 같은 선). 관리자 구분은 그 표식 + `ShieldCheck` + 「Admin」 텍스트 셋이 함께 진다(DD-01 유지). **액센트 C 「공정 띠」는 미채택**이고, 그 알맹이는 **커버리지 규격 한 줄**로 흡수했다: 「관리자 파이프라인 화면은 가장 앞선 막힌 단계를 첫 뷰포트에 한 줄로 말한다 — 색이 아니라 문장 + 주묵 표식으로, 막힌 것이 없으면 그 줄을 비운다」 | 사용자 결정. 반영: [ADMIN_CONSOLE](../ADMIN_CONSOLE.md) 액센트 행 + 커버리지 규격 · [admin-brief §4](admin-brief.md). 옛 보라 `#8B5CF6` 204곳(라쳇 318)은 신규 금지·감소만 — 교체는 Gate 1 에서 고른 골격과 함께 한다(색만 바꾸면 형태는 그대로 평균이다) |
| **DD-56** | **판면 방식 = Tailwind 통일**(기준 판정 후 결정). 사용자가 준 기준은 「CSS 모듈이 03-system 주묵 판면 **정본 구현**이면 둘 다 유지 + 스캐너 확장, 아니면 Tailwind 통일」이었다. **실측 판정: 정본 구현이 아니다.** 대상은 3파일(1,092줄 · `evidence.module.css` 500 · `sources.module.css` 526 · `source-operations.module.css` 66)과 소비 화면 5개. 통과한 것 — 하드코딩 hex **0**(전부 토큰) · `border-radius` 는 `var(--r-sm)` 하나 · `box-shadow` 는 실제로 뜨는 패널의 `--sh-float` 1건 · transition 은 `--dur-normal` 만 · `animation` 0. **통과하지 못한 것** — 주묵 `--ju` **0회** · 공용 판면 어휘 `components/ui/press`(JuMark · Gwonjeom · Rule · DecayUnderline) **0회**(5화면 전부). 즉 「토큰 위생은 지키지만 주묵 판면의 어휘를 쓰지 않는 별도 방언」이고, 검사·리뷰를 두 벌 유지할 근거가 없다 | 사용자 지시(기준 판정 후 DD). **이관은 Gate 1 뒤**로 둔다 — 지금 옮기면 고를 골격과 어긋난 마크업을 두 번 고친다. 그때까지 `touch-target` 의 CSS 모듈 예외는 **유지**하되 그 예외는 CSS 규칙 존재를 테스트가 직접 확인한다(DD-51). ⚠️ 이관할 때 잃지 말 것: 그 CSS 가 `min-height: 44px` 을 **루트 아래 일괄**로 보장하는 방식은 Tailwind 로 옮기면 **요소마다** 다시 붙여야 한다(빠뜨리면 44px 위반이 그대로 생긴다) |
| **DD-57(초안 · 결정 보류)** | **`cefr_above_band` 를 적격 게이트에서 서빙 시 적합 필터로 이동** — 사용자 지시로 **측정만** 했다. 리포트 [reports/band-policy-measure-20260920.md](../reports/band-policy-measure-20260920.md). 핵심 사실: 규칙에 **C1 을 허용하는 밴드가 없다**(V≤4 상한 B1 · V≥5 상한 B2 둘뿐) → `cefr_above_band` 는 학습자와 무관하게 **글에만 붙는 판정**이고, 「최상위 학습자면 열린다」가 성립하지 않는다. 학습자 실측: 프로필 4(하나는 design-empty) · `current_v_level` 0·5(최대 **5**) · `target_v_level` 전부 null. **열리는 규모**: 밴드만 걸린 글 **11,242편 · 212,433문항**(판정 작업 0) · 요청한 미판정 12,123편은 **판정 + 밴드 둘 다** 필요(12,121편 · 483,964문항 · 전부 C1/C2 · `article_v_level` 5~8). 시나리오: S1(서빙 필터로 이동) 13.59% → **37.75%** · S2(S1 + raw 판정) → **92.79%**. ⚠️ **S1 의 수치를 「열렸다」로 읽으면 안 된다** — 서빙 필터가 학습자 밴드를 보므로 지금 학습자(최대 V5 · 상한 B2)에게 C1 은 여전히 안 나간다. 잠재 공급과 실제 도달을 갈라 적었다(이 저장소의 실패 모드가 공급 과대 계상이다) | 사용자 지시(결정 보류 · 측정 먼저 · 변경 범위 초안). **변경 범위**: ① 함수 — `source-eligibility.ts` 가 `cefr_above_band` 를 blocker→warning 으로(`ELIGIBILITY_SPEC_VERSION` 3→4) · `csat_source_is_eligible` 의 `policy_version` 3→4 · 적합 필터는 `prescribe_today` · `textbook_practice_items` 에만 넣고 **`grade_dcp_item` 은 막지 않는다**(이미 받은 문항의 채점을 거부하면 학습자가 막힌다 — #104 의 증상이 정확히 그것이었다) ② 캐시 — `csat_source_eligibility` 전량 재적재(`--commit --all` · 배치 500 · 배치당 ~9초 실측 → 약 35분 · `policy_version` 이 바뀌므로 전 행 changed 가 정상 · 배치별 `before.json` 으로 롤백) ③ 테스트 — `source-eligibility.test.ts`(warning 으로 단언 교체) · `assemble-unit.test.ts`(조판 밴드 검사는 그대로 통과해야 한다) · `dcp-grade-records.integration`(채점을 막지 않음 단언 추가) · **신규 서빙 적합 필터 회귀**(V5 에게 C1 이 안 나가고 V-Level 을 올리면 나간다) · `source-policy-batch.test.mjs`(버전 4). **결정이 필요한 세 가지**: 밴드 상한에 C1 칸을 두는가 · CSAT 트랙에 별도 상한을 두는가(**기출 지문의 CEFR 분포를 아직 안 쟀다** — 재고 먼저) · `content_rejected` 314편(42,562문항)은 열지 않는 것이 맞는가. **갱신(2026-09-20 · 기출 지문 분포 측정 완료)**: 「결정이 필요한 세 가지」의 두 번째에 근거가 생겼다 — `scripts/csat/measure-passage-cefr.mjs`(읽기 전용)로 기출 지문 **802개 전수**를 재니 **C1 23.6% · C2 45.1% = 68.7%** 가 B2 상한 위다(가독성 신호 기준 · 정본 3중 합의의 신호 2). 즉 **수능 자체가 우리 적격 규칙을 통과하지 못한다.** 연도별로는 2014A 39% → 2026 **76%** 로 **오르는 추세**이고, 유형별로는 `R-INSERT`·`R-IRRELEVANT`·`R-CHART`·`R-BLANK2` 가 **100%**, `R-BLANK` 90%, 반대로 `R-MOOD` 4% · `R-REFER` 0% 다 — 난이도는 소재가 아니라 **유형**에 달려 있다. ⚠️ 이 값은 **가독성 신호 하나**이므로 상한을 실제로 올리기 전에 신호 1(어휘 분포 — `shared_dictionary` 49,244행으로 계산 가능)까지 넣어 다시 재는 것을 권한다. ⚠️ 그리고 상한을 올려도 **학습자 도달은 별 문제다** — 지금 최상위가 V5 이므로 서빙 적합 필터가 V5→C1 을 허용하지 않으면 도달 0 이다. **분리 착수**: 이 초안 중 「채점은 밴드로 막지 않는다」 한 조각을 떼어 PR #110 으로 올렸고 **2026-09-20 적용·머지**(`csat_source_is_gradeable` · 회귀 8건 초록). **⚠️ 정정(2026-09-20, 재측정) — 위의 「수능 자체가 적격 규칙을 통과하지 못한다」는 신호 하나짜리 결론이었고 뒤집혔다.** 사용자 지시로 정본 3중 합의 식(어휘 0.5 + 가독성 0.3 · LLM 은 기출 원문 유출 때문에 제외 · 가중치 0.8 재정규화는 정본 `aggregate` 와 같다)으로 802편을 다시 재니 **B2 51.2% · B1 31.7% · A2 14.8% · C1 0.6% · C2 0%** 다 — 가독성만 썼을 때의 C1·C2 **68.7%** 가 **0.6%** 로 무너진다(어휘 사전 적중률 93.1% 라 데이터 부족이 아니다). 불일치는 체계적이다: 어휘 B1 → 가독성 C2 가 230편, A2 → C1 이 122편. **기출은 쉬운 낱말로 긴 문장을 쓴다.** 따라서 **「상한 B2 가 수능과 어긋난다」는 근거는 약하다** — 합의 기준으로 수능은 B2 대이고 현행 상한과 맞는다. 그러면 막힌 11,242편(212,433문항)은 **수능보다 어려운 글**일 가능성이 크다(원문이 PLOS 논문·학술 기사다). 두 신호에서 **똑같이 재현되는 것 하나**: 난이도는 소재가 아니라 **유형**에 달려 있다(합의 기준 B2 이상 — `R-INSERT` 95% · `R-IRRELEVANT` 93% · `R-CHART` 83% ↔ `R-REFER`·`X-FACT`·`X-ORDER`·`X-REFER` 0%). **옵션 (a)/(b) 를 변경 범위·비용과 함께 나란히** 정리했다 → [리포트 §6](../reports/band-policy-measure-20260920.md). 결정 전에 하나 더 재는 것을 권한다: **막힌 11,242편을 같은 합의 식으로** 재서 수능 분포와 겹치는지 |
| **DD-58** | **관리자 골격 = 「정오표」 확정 · Gate 2·3 완료**(사용자 결정). 발산 4안 중 1번 — 공정·항목이 **번호 + 괘선 행**으로 서고 각 행이 「번호 · 이름 · 눈금 · 판정 · 다음」이다. 서명은 **주묵 권점(•)** 하나: 막힌 행의 번호 왼쪽에 찍히고 고르면 **주묵 실선**이 상세로 이어진다(200ms · transform 만). **Gate 2** — [DESIGN.md](../../DESIGN.md) 에 「관리자 콘솔」 절 신설: 규칙 **A1 골격 정오표** · **A2 서명 권점 하나** · **A3 첫 뷰포트에 「가장 앞선 막힌 단계」 한 줄**(색이 아니라 문장 + 주묵 표식 · 막힌 것이 없으면 **줄을 비운다**) · **A4 원색 주묵은 점·선만**(면 금지 · tint `--ju-light` 는 토큰 정의대로 활성 행 배경에만) · **A5 44px 은 관리자 레이아웃 루트에서 일괄 보장**(화면마다 붙이면 반드시 빠뜨린다 — 사용자 지시). 화면별 배정표의 `/admin/*` 행도 교체. **Gate 3** — 골든 목업 2장 고정: [golden/admin-csat.md](golden/admin-csat.md) · 1280×900 · 375×812. 원본은 `golden/admin-csat-mockup.html`(캔버스 초안)이고 `scripts/design/shoot-admin-mockup.mjs` 로 다시 찍는다. **토큰 파일을 그대로 @import 해서 하드코딩 색 0** · 가로 스크롤 **양쪽 0px**(찍을 때마다 잰다) · 판정은 기호 + 글자로 말해 **색 단독 전달 0** | 사용자 결정(「1 정오표」 · 커버리지 한 줄 규격 유지 · Gate 3 까지 진행 후 정지). **수정 2회**(06-workflow 절차): ① 375 에서 **공정 이름이 음절로 쪼개졌다**(5열을 좁은 폭에 욱여넣음) → 모바일에서 **두 줄 행**으로 접는다(골격은 그대로 — 접는 것은 열이지 형태가 아니다) ② 그 부작용으로 caption 이 번호 칸 폭에 눌려 낱말이 한 줄씩 쪼개짐 → `caption { display: block; width: 100% }`. **A2 판단 한 가지**: 권점을 「대기」 행에도 찍어 봤다가 뺐다 — 막힘·대기 둘 다 찍히면 **서명이 묽어진다**(첫 렌더에서 실제로 그렇게 보였다). 권점은 막힌 행에만. **Gate 4(대기)**: ① 관리자 레이아웃 루트 44px 일괄 보장 → `touch-target` 의 CSS 모듈 예외 삭제 ② CSS 모듈 3파일(1,092줄) Tailwind 이관(DD-56) ③ 골격 선언 60화면 + 라쳇 기준선 내리기 ④ 옛 보라 204곳 교체(DD-55) |
| **DD-59** | **Gate 4 (i) — 옛 AI-보라 전량 교체(기계적) · 라쳇 `admin.ai-purple` 318 → 0**. DD-01(액센트 Deep Ink) · DD-55(관리자는 `--p` + 주묵 표식)가 방향만 정해 두고 **교체를 안 했던 몫**이다. 코드모드 2개를 만들어 돌렸다(둘 다 **기본 예행** · 정확 일치만 · 판단이 필요한 것은 손대지 않고 보고만): `scripts/design/purple-to-token.mjs`(`#8B5CF6`→`var(--p)` · 알파 hex→`var(--p-light)` · `#6D28D9`·`#7C3AED`→`var(--p-hover)` · `var(--ios-purple*)`→`--p` 계열)와 `scripts/design/on-p-ink-fix.mjs`. 관리자 **66파일 318건** 교체 · 남은 것은 테스트 픽스처뿐(코드모드가 일부러 건너뛴다 — 회귀가 옛 값을 들고 있을 수 있다). **그라디언트 구조·Tailwind 팔레트 클래스는 건드리지 않았다** — 색과 구조를 같이 바꾸면 diff 를 아무도 못 읽는다. 그 둘은 (ii)·(iii) 에서 화면별로 한다 | 사용자 지시(Gate 4 를 PR 3개로 분리 · (i) 부터 · 완료 시 정지). **⚠️ 교체가 만든 부작용을 그 자리에서 잡았다**: `--p` 면 위에 남은 `text-white` 가 **다크 테마 AA 미달**(#6B9BD1 위 흰 글자 2.90:1)이 되어 `on-p-contrast` 래칫이 새 위반을 잡았다. **기준선을 올리지 않고 20줄을 실제로 고쳤다**(`text-[var(--on-p)]`) — 그 래칫의 규칙을 그대로 쓰는 두 번째 코드모드를 만들어서. 색 교체가 접근성을 깎을 수 있다는 것이 이 회차의 교훈이고, 그래서 (ii)·(iii) 에서도 두 래칫을 함께 돌린다. **판단 하나**: 파이프라인 액센트 표(`lib/admin/dashboard-stats.ts`)의 `vcb` 는 `--p` 로 바꾸면 `lcp` 와 한 색이 된다 → `--p-hover`. 색만으로 구분하지 않는 것이 원칙이고(색 단독 정보전달 금지) 이름표가 뜻을 나른다 — 색은 훑을 때의 보조다. **화면도움말 동기화**(자동화 정책 ②): 「보라 테두리」라고 적힌 **조작 안내 3곳**(`help/csat.ts` · `quality.ts` · `vocab.ts`)을 「강조 테두리(잉크색)」로. 색을 바꾸고 안내를 그대로 두면 관리자가 없는 색을 찾는다. `ADMIN_CONSOLE` 액센트 행도 갱신. **학습자 표면은 범위 밖**(라쳇 38) — 그 표면의 화면 작업에서 함께 지운다 |
| **DD-60** | **밴드 정책 — (b) 폐기 · (a) 보류 · 「적격/적합 필터 분리」를 T6 부채로 이관**(사용자 결정 2026-09-20). **(b) CSAT 트랙 유형별 상한 = 폐기**: 근거로 삼으려던 「기출이 C1·C2 다」가 재측정에서 무너졌고(DD-57 정정 — 합의 기준 C1·C2 **0.6%**), 유형별 상한표는 `dcp-types` ↔ DB 함수처럼 **또 하나의 갈라지는 목록**이 된다. 이 저장소가 그 실수를 두 번 했다. **(a) 적격 게이트에서 서빙 적합 필터로 이동 = 보류**: 열리는 규모(글 11,242 · 문항 212,433)는 크지만 **지금 학습자에게 닿지 않는다** — 최상위가 V5 이고 상한이 B2 라 C1 은 여전히 안 나간다. 닿지 않는 공급을 열려고 **캐시 전량 재적재(109,043행 · 약 35분) + 테스트 5건 + 두 곳에 생기는 규칙**을 치르는 것은 지금 순서가 아니다(공급 과대 계상이 이 저장소의 실패 모드다). **이관**: 「적격/적합 필터 분리」를 조건부 부채로 [PLATFORM_AUDIT §8](../PLATFORM_AUDIT.md) 에 **T6** 으로 올린다 — **해소 조건 = 상급 학습자(V6+) 발생**. 그때 (a) 의 변경 범위 초안([리포트 §4](../reports/band-policy-measure-20260920.md))을 그대로 집어 들면 된다 | 사용자 결정. **이미 떼어 간 조각**: 「채점은 밴드로 막지 않는다」는 (a) 와 **독립**이라 먼저 끝냈다(PR #110 머지 · `csat_source_is_gradeable`) — 학습자가 답을 내고도 결과를 못 보는 것은 공급 문제가 아니라 **파손**이었기 때문이다. 지금 상태: 밴드는 **적격에 남아 있고**(서빙·조판이 그대로 쓴다) **채점만 면제**돼 있다. 그 비대칭이 T6 에서 해소될 때까지의 계약이고, 두 함수의 주석과 DB_SCHEMA 가 그렇게 적혀 있다 |
| **DD-62** | **방식 전환 — 「실물 우선」**(사용자 결정 2026-09-20). 지금까지는 문서가 화면을 정했다(DESIGN.md 방향 산문 → 브리프 → 목업 → 화면). 순서를 뒤집는다: **화면이 먼저 서고, 그 화면에서 값을 뽑아 문서를 다시 쓴다.** 네 가지가 따라온다 — ① **기준선은 캡처다**: `docs/design/golden/` 에는 목업이 아니라 **실제 라우트의 캡처**만 둔다. 화면의 완료 기준은 산문 평가가 아니라 「기준선 캡처와의 차이」다. ② **문서는 생성물이다**: 타입 스케일 · 간격 · 컨테이너 폭 · 모서리 · 그림자는 `refs/tines/computed.json` + 치환표에서 스크립트가 생성한다. 손으로 쓰는 문서에는 **「왜」만** 남는다. ③ **참조는 구조·수치만**: 색 · 서체 · 소재 · 문구는 우리 것으로 치환한다(Stage 3 치환표 `refs/substitution.json`). Tines 의 Roobert · Reckless 는 상용 서체이자 L3 금지 대상이다 — 값이 아니라 **비율**만 가져온다. ④ **판단은 두 번뿐**: Stage 3 치환 결과(「같은 급인가」) · Stage 4 첫 화면 2장. 나머지는 자동 판정(픽셀 diff · baseline-check)이 한다. 동결: `DESIGN.md` · `DESIGN_SYSTEM.md`(아트디렉션·벤치마크·이력) · `03-system.md` §3-9 값 · `04-application.md` · `admin-brief.md` · `image-system-brief.md` · `csat-learner/` 재설계 3종 — 머리글 한 줄만 붙이고 내용은 무변경. 유지(검사기): SKILL Part 2 · AGENTS.md 디자인 절 · 라쳇 2종 · `style-gate` · 익명성 검사 · 비밀값 스캔 — 변경 0. 유지(재료): 골든 삽화 3점 + #10 · `symbol-dictionary` · `asset-manifest` · `refs/tines/dna.md` · DD 전체. | 사용자 지시문(7단계). 폐기: PR #115 `design/admin-gate4-evidence` → 태그 `archive/pr115-admin-gate4-evidence`, 캔버스 목업 이식 `design/admin-evidence-golden` → `archive/admin-evidence-golden-mockup`(삭제하지 않는다 — 참고용). 새 브랜치 `design/replica-first`(main 기준). **번호 주의**: 지시문의 DD-58·DD-59 는 이미 쓰였고(DD-58 정오표 골격 · DD-59 AI-보라 교체), DD-61 은 폐기된 PR #115 가 쓴다 → 이 결정은 **DD-62**, Stage 5 문서 교체는 **DD-63** 이다. |
| **DD-64** | **모션 토큰 2개 추가 — `--dur-quick` 150ms · `--ease-out-quint` `cubic-bezier(.22, 1, .36, 1)`**(사용자 결정 2026-09-21 「토큰에 반영」). 근거는 참조 작성 CSS 집계(`refs/tines/css-authored-summary.md`, 6페이지 30시트): 지속시간 최빈 150ms ×33 이 우리 `--dur-fast`/`--dur-normal` 사이에 떨어지고, 이징 최빈 `(.22,1,.36,1)` ×23 은 우리 토큰에 없었다(참조에서 드롭다운·패널 진입 키프레임이 쓴다). **추가만 하고 기존 값은 바꾸지 않는다** — `--ease` `(.4,0,.2,1)` 를 바꾸면 전 화면 전환이 한 번에 달라지고 `prefers-reduced-motion` 의 `--dur-fast` 하향도 흔들린다. 새 토큰은 모션 예산(마이크로 100–200ms · `transform`·`opacity` 만) 안이다. **가져오지 않은 것**: 무한 반복 animation 32건(장식적 상시 모션 금지) · 12px+ 모서리(DD-12 라쳇, 상한 6px) · 그림자·backdrop blur. | 웹 `tokens.css` + 앱 `motion.ts` 동시(패키지 규칙). `DESIGN_SYSTEM.md` 는 DD-62 동결이라 표를 고치지 않고 DD-63 문서 교체 때 생성물로 들어간다. 적용 화면은 아직 0 — 토큰만 선다. |
| **DD-65** | **제약 4종 삭제 + 제약 스킬 삭제 → 참조 값을 토큰으로 반영**(사용자 결정 2026-09-21 「기존 제약사항으로 작업 제외 시킨 것들은 제약사항 삭제하고 다시 진행」 · 「skill 도 제약사항으로 작용하는 것은 skill 제거」). DD-64 가 「가져오지 않은 것」으로 남긴 **① 장식적 상시(무한) 모션 금지 ② 12px+ 모서리 금지(DD-12 라쳇 · 6px 상한) ③ 그림자 금지(판면은 뜨지 않는다) ④ glass/backdrop blur 금지**를 삭제한다 — 라쳇 `average-signal-ratchet` 신호 8→4(`shadow-heavy` · `rounded-big` · `glass` · `infinite-anim` 삭제), `learning-tone` 의 쉼 화면 무한 모션 검사 삭제, AGENTS.md · DESIGN.md · DESIGN_SYSTEM.md · 03-system 의 해당 문장 수정. 스킬: `vocaflow-design` **Part 2(§0–§8, 스스로 「바닥(제약)」)와 §G4 삭제**(.claude · .agents 두 사본, 368→163줄 · Part 1 발명 엔진 유지) · `minimalist-ui`(로컬 설치 · `skills-lock.json` 항목도) · `vocaflow-visual-critic` 삭제. 새 토큰(추가만 · 기존 값 무변경): `--r-3xl/4xl/5xl/6xl` 8/12/16/24px · `--sh-soft/drop/overlay`(다크 값 별도) · `--blur-sm/md/lg` 8/12/20px · `--dur-loop-fast/loop/loop-slow` 2s/4s/30s — 근거 `refs/tines/css-authored-summary.md`. Tailwind `rounded-4xl…6xl` · `shadow-soft/drop/overlay`. | **남긴 것**: 3열 균등 · 그라디언트 · AI-보라 · 떠오르는 hover 라쳇 · 폭죽/콘페티/배지 팝업/자동재생 캐러셀 금지 · 학습 중 허용 7종 · `transform`·`opacity` 만 · 외부 스킬 `design-taste-frontend` — 이번 지시 범위(제외시킨 것) 밖이다. 기존 `--sh-xs…xl`(링) · `--r-sm…2xl` · `--ease` 값은 그대로라 **현재 화면 변화 0** — 새 토큰을 쓰는 화면부터 달라진다. DD-62 동결 문서(DESIGN.md · DESIGN_SYSTEM.md · 03-system)는 이 결정 범위에서만 고쳤다. |
| **DD-66** | **디자인·UX 금지·제한 전부 삭제**(사용자 결정 2026-09-21 「금지,제한 모두 제거」 · 범위 질문에 「디자인·UX 전부」 선택). 규칙: AGENTS.md(Calm UI 금지 · 첫인상 I1–I4·I6·I8 · 이탈 D1·D4·D5 · 모션 예산·학습 중 7종·항상 금지 · 타이포/색/학습 UX 금지 · 디자인 의무 4상태·3중 피드백·모바일 퍼스트·다크 · 평균 금지·`@form` · Admin 보라 금지) · CLAUDE.md(스킬 호출 의무 → 선택) · DESIGN.md · DESIGN_SYSTEM.md · 03-system · 06-workflow(익명성·평균 회귀 판정) · CONVENTIONS · CONTEXT_DETAIL · LEARNING_MODEL · MODULES · 두 브리프 · design-tokens/CLAUDE.md · `vocaflow-design`(§F 금지표 · §G5 · 게이트 문구, 두 사본 동일) · `vocaflow-design-loop`. 회귀: `average-signal-ratchet` · `form-declaration-ratchet`(+ baseline) · `activation-path` **파일 삭제**, `learning-tone` 16건 · 웹 컴포넌트 테스트 13파일의 금지 케이스 · `framework.test` 2건(최상위 4개 · 잠금 어휘) · video-factory `typography` 5건 · library-pipeline brand/cover-art 13건 삭제. `style-gate.mjs` 는 취향 규칙 12개 삭제. | **남긴 것**(범위 밖으로 사용자가 고르지 않음): 접근성(44px · AA 대비 · 색만으로 정보 금지 · placeholder 레이블 · focus 표시 · reduced-motion) · 정직성(I5 · 지어낸 수치 · 배지는 셀 수 있는 것만) · 저작권(Quizlet · Roobert/Reckless · Tines ΔE) · 비밀값 · 데이터(`memory_state` 등) · git·마이그레이션 · 학습 과학 규칙(새 단어에 생산 과제 안 권함 등). **코드로 걸린 제한 4건은 그대로**(테스트만 없음/유지): video `voiceFont()` 의 한글 정체·영어 Lora italic · `validateBrandCanvas` 의 hex/rgb/hsl 거부 · video `ACCENT` 토큰 키 조회 · 카탈로그·조판 팔레트가 토큰에서 만들어짐. DD-62 동결 문서는 이 결정 범위에서만 고쳤다. `.agent-handoff/latest.md` 의 라쳇·`@form` 과제는 무효. |
| **DD-67** | **코드로 걸린 디자인 제한 해제**(사용자 결정 2026-09-21 「풀어줘」 — DD-66 이 남긴 4건 + 같은 성격 1건). ① 영상 강조색: `Accent = AccentKey | CSS 색` — `accentColor()`·`accentSoft()` 가 키는 토큰 값, 원시 색은 그대로(옅은 색은 `color-mix` 14%). ② 영상 「말하는 자리」 서체: `voiceFont(text, voice?)` — 장면의 선택 항목 `voice` 가 적은 것만 기본값(한글 정체·영어 Lora italic)을 덮어쓴다. ③ 브랜드 캔버스: 색 자리 = 역할 이름 **또는** CSS 색 값(hex·rgb·hsl·hwb·lab·lch·oklab·oklch·color), 서체 자리 = 역할 **또는** `font-family` 값 — 색 값 금지 검사 삭제, `resolveBrandColors` 는 색 값을 그대로 돌려준다. ④ 팔레트: `catalogCssVariables(override?)`·`volumeCssVariables(override?)` 가 자리별 덮어쓰기를 받는다(생략 시 이전과 같은 출력). ⑤ web 표지 lockup(`coverLockupOf`)이 ③ 과 같은 판정으로 색 값·서체 이름을 받고, 서체 이름은 `fontFamily` 로 표지에 쓴다(안 고치면 색 값을 담은 캔버스가 조용히 `null` 로 떨어진다). | 기본값·기존 출력은 전부 그대로라 **현재 화면·영상 변화 0**. 남긴 검사는 빈 값·범위 밖 수치·오타(역할도 색 값도 아닌 글자)뿐 — 표지가 색·글자를 잃는 입력이다. 도움말 `help/vocab.ts` 전제·복구 문구, `brand-drain-export.mts` 주석, `VCB_REDESIGN.md` 절 머리를 같은 커밋에서 고쳤다. |
| **DD-68** | **Tines 스타일 1단계 — 「가장 닮음」**(사용자 결정 2026-09-21 「가장 닮음으로 우선 진행해 보고 평가 후 변경 · 모든 요소」). 실패 원인 진단: DD-62 Stage 3 치환이 참조 액센트 `#714bd0` 을 잉크 `#1a1714`(ΔE 40)·베이지 `--bg2`(ΔE 53)로, 서체를 Hahmlet/Plex 로, 그림을 우리 삽화로 바꿔 **닮음을 설계로 지웠다**. 이번 순서: ① 서체 대체를 픽셀로 고른다(`scripts/design/font-lookalike.mjs` → `refs/tines/font-lookalike.md`: sans Figtree · serif Petrona · mono Space Mono, 한글 Pretendard) ② 스킨 토큰(ADR-008 `skins/tines.css` · 기본 켜짐) ③ 공통 부품 ④ 랜딩 `/` 를 참조 홈 구조로 ⑤ 같은 화풍의 새 삽화 ⑥ 나란히 비교·수치 → 사용자 평가. `style-gate` 의 Tines 색 정확 일치·ΔE<2 는 **보고만**(실패 조건에서 뺌). | **넘지 않는 선 2개**(법적): 상용 서체 원본 파일을 제품에 쓰지 않는다(비교는 로컬 `tmp/` 미러에서만) · 참조 그림을 쓰지 않는다(화풍만). 접근성 AA 유지 — 그래서 글자 1·2·3단이 한 색이다. 이 단계 끝 = ②까지(토큰만으로는 레이아웃·버튼·내비·그림이 그대로라 아직 닮지 않았다 — 실측 캡처로 확인). |
| **DD-69** | **교재 공장 파이프라인 측정 — 착수와 점수**(2026-09-23 · 읽기 전용). `/admin/csat` 이하 메뉴 12칸 · 드레인 · DB 상태 필드를 §1 점수표(A 정합 5축 · B 에이전트 운영 6조건 · 11항목 × 9단계 = 99칸)로 쟀다. **총점 7 / 22 · 2점 항목 0개 · 0점 항목 3개**(B4 실패 목록 · B5 승인 지점 · B6 상태·사유 필드) · 0점 칸 22. 단계별로는 재료 둘이 거의 만점(기출 원천 20 · 원문 적격 19)이고 바닥은 ⑥ 해설 9 · ⑧ 조판·발행 10 · ② 기획 7 이다. **실측 네 가지가 판정을 갈랐다**: ① 승인 컬럼이 `csat_*`·`textbook_*` 전체에 **0개** ② `csat_dcp_items` 880,337행에 status 도 reason 도 **없다** ③ `csat_item_reviews` 를 읽는 웹 코드가 **0곳**이라 revise 501 · fail 159 가 어느 화면에도 안 보인다(검수된 321문항 중 3인 전원 pass 는 **29문항**) ④ 조판 산출물(`out_path` 로컬 HTML)을 읽는 학습자 코드가 **0곳**이다. A5 역추적 증명 1건: `/library/textbooks/vocab/5`(Vocaflow Vocab Advanced · 조판 기록 20단원 120문항 · 3인 검수 1/60)가 공개 URL 로 열리고 **10단원 60문항짜리 Reading 목차를 인쇄한다** — `contentsOf(vLevels)` 가 스냅샷을 V-Level 키 하나로 찾는데 `volume-contents.json`(2026-09-13)은 reading 7권만 구워져 있다. | 리포트 [pipeline-inventory](../reports/pipeline-inventory.md) · [pipeline-scorecard](../reports/pipeline-scorecard.md). **DB 쓰기 0 · 마이그레이션 0 · 화면 코드 0.** 캡처 11장은 `docs/design/shots/pipeline/`(gitignore). 캡처에서만 드러난 결함 1건: ④ 소재 화면이 「초·중 원문 재고」를 `[object Object]` ×6 으로 인쇄한다(`sourcing/SourceClient.tsx:296-303`). 채점 규칙 정정 1건: A5 에서 산출물이 설계상 학습자에게 안 가는 단계(② 기획)는 N/A — **총점 7 은 변하지 않는다**(Gate 4 에서 만점이 불가능해지는 것만 막는다). |
| **DD-70** | **재설계안 대기 — 최소 변경 / 흐름 정합, 적용 범위**(사람 결정 필요 · 2026-09-23). 0~1점 항목 11개 전부에 원인·층·비용·기대 점수를 붙인 안 두 개를 냈다. **안 A 최소 변경**: 메뉴 그대로 두고 마이그레이션 3(승인 기록 `csat_pipeline_approvals` · 드레인 실행 기록 `csat_drain_runs` · `textbook_volume_renders.status`) + 실패 목록 패널 2 + `[object Object]` 수정 + ⑥ 해설 칸 정리 → 7~8일 · **기대 15/22 · 0점 0개**, 단 A5(학습자 도달)는 1 에 머문다. **안 B 흐름 정합**: 안 A + `csat_item_state` 곁 표(880k 본표는 안 건드린다) + 적격 집계 RPC + 9화면 공통 골격 `StageFrame`(계약 한 줄 · 막힌 것 · 항목×status · 드레인 패널 · 실패 목록) + 메뉴를 모델이 이미 선언한 레인(lab/line)대로 재배열 + ⑥ 전용 화면 + 「원문 적격」→「소재 적격」 이동 + `volume-contents` 를 `series:step` 키로 + 매대가 발행 상태를 본다 → 19~24일 · **기대 20~22/22**. **권고는 안 B** — 안 A 는 0점 셋을 지우지만 ⑦⑧ 의 학습자 도달 0점을 못 고치고, 그 두 칸이 이 진단에서 가장 아픈 곳이다. 적용은 층별 PR 5개(DB → 드레인 → 화면 → IA → 학습자)로 쪼개고 메뉴 IA 는 별도 PR 이다. | 리포트 [pipeline-redesign](../reports/pipeline-redesign.md). **결정 대기 7건**: Q1 범위 · Q2 「새 교재 만들기」와 「기획」 통합 여부(권고: 통합 않고 **발주를 기록**하게) · Q3 「해설 준비 중」 처리(권고: 전용 화면) · Q4 재료 2항목 관계(권고: 연구소 / 라인 입구로 분리) · Q5 ③ 설계 드레인(권고: 면제 선언 + `csat_stage_gates.is_locked` 를 실제로 쓴다 — 지금 9행 전부 false) · Q6 학습자 매대 게이트(권고: 막되 `status` 기본값을 `published` 로 두어 지금 보이는 권은 그대로) · Q7 마이그레이션 M1~M5 승인. **결정 전에는 Gate 3 을 시작하지 않는다** — AGENTS.md 「마이그레이션 자동 적용 금지」. |
| **DD-71** | **소스GET 원천 정책 — 대기**(사람 결정 필요 · 2026-09-23 · 읽기 전용). 원천 **21곳**을 §1 5축(F1 난이도 적합 · F2 지문 적합 · F3 소재 기여 · F4 법적 적격 · F5 수율·규모 · 각 20점)으로 채점하고, 신규 후보 **25곳**을 두 관문(robots · 라이선스 고지 · 취득 경로)으로 걸러 그중 5곳을 **같은 3중 합의·같은 검증기**로 실측했다. 표본 합계 **1,061편**(기존 864 + 후보 197), 서브에이전트 산출 26건을 sha256·건수·스키마로 대조해 **26건 통과**(27번째 `nsf` 는 목록에서 기사가 1편만 나와 판정 자체를 하지 않았고 검증기가 `missing-out` 으로 잡았다 — 통과로 세지 않는다). 판정: **유지 8 · 축소 7 · 보류 5 · display-only 1 · 제거 0**. 적격 문항 합계 **119,559** 가 #104 의 정본 수치와 일치(독립 측정 교차 검증). **측정이 뒤집은 것 셋** — ① 재고 상위 3곳(PLOS 45,096 · Gutenberg 23,618 · VOA 10,649 = 생존의 90.4%) 중 유지는 Gutenberg 하나이고 적격 원문의 **68.6%** 가 거기서 나온다 ② 「narrative 가 없다」는 진단은 **라벨을 센 것**이다 — `register` 는 본문을 안 읽는 원천 단위 선언값이고 `gutenberg` 23,618편은 `SOURCE_REGISTER_DEFAULT` 에 **아예 없어** 폴백 `expository` 로 떨어진다(표본 실측 narrative **50%** → 재고 환산 약 11,800편, 선언값 582편의 20배) ③ `legal` 축 차단 150편 중 80편은 법이 아니라 **문자열 서식**이다(각색 경로 2곳이 `license` 칸에 등급 슬러그 `public_domain`·`cc_by` 를 써 `acp_classify_license` 가 전부 `restricted` 로 떨어뜨린다 — 부모가 전부 PD/CC-BY 라 오탐 0). **특별 항목 3개 원인 규명**: `original` 41.5%/41.4% = 생성 품질이 아니라 **888편이 게이트 판정까지 받고 학령 분석에서 멈춤**(분석된 631편의 적격률은 96.83% 로 1위) · Frontiers 0% = 분류는 됐고 **게이트 미실행** · Europe PMC 0% = **분류조차 미실행**(둘 다 부적격이 아니라 미실행) · PLOS 발췌 36,337 = 발췌는 이미 11,601편 뽑혀 있고 그중 열린 것이 **16.4%** 라 「더 발췌하면 열린다」는 틀리다(남은 원본을 전부 발췌해도 +4,000편이 상한이고 그 위는 **밴드 결정 = T6**). **신규 발굴의 결론**: 표본을 뜬 5곳 중 기존 상위 원천을 이기는 곳이 **없다**(최고 `wikinews` F1+F2+F3 = 38.1 vs `original` 47.6 · `wikipedia` 46.7 · `gutenberg` 45.4). 온보딩 비용이 가장 낮은 둘(`wikinews` 22,237편 · `worldbank_okr` 40,415편)은 **발굴 결과가 아니라 재고 점검 결과**다 — 둘 다 코드에 배선돼 있는데 `library_articles` 0행이다. **심리·인지 구멍은 발굴로 안 풀린다** — 후보 25곳에서 이 칸을 메우는 곳이 없고 최대 공급선 `noba`·`openstax Psychology` 가 **둘 다 NC(L3)** 다. **사람이 정할 것 둘**: (i) **L2(CC-BY-SA) 채택 여부** — `wikipedia`(76.2)·`simple_wikipedia`(73.7) 가 유지 상위권인데 SA 전염이 교재 전체에 걸린다 (ii) **L3(NC) 채택 여부** — 채택하면 심리·인지 칸과 OpenStax 41종이 열리지만 상업 배포 계획과 충돌하고 `source-policy.test.ts` 의 「SOURCE_SPECS 에 restricted 등급 소스가 없다」가 즉시 실패한다. 둘 다 **정하기 전에는 Gate 4 온보딩을 시작하지 않는다.** | 리포트 [sources-scorecard](../reports/sources-scorecard.md) + [.json](../reports/sources-scorecard.json) · [sources-discovery](../reports/sources-discovery.md) + [.json](../reports/sources-discovery.json). **DB 쓰기 0 · 마이그레이션 0 · 화면 코드 0.** 새 도구 4개(전부 읽기 전용·재실행 안전): `source-scorecard-export.mts`(층화 표본 + 결정론 신호 2개, 원천별 고정 시드) · `source-scorecard-verify.mts`(계약 대조 — 에이전트 보고 문장을 근거로 쓰지 않는다) · `source-scorecard-score.mts`(순수 산술) · `source-discovery-probe.mts`·`source-candidate-sample.mts`(두 관문 + 후보 표본을 Gate 0 과 **같은 청크 모양**으로). 청크 본문은 `.agent-logs/`(gitignore)에만 남고 리포트에는 **원문 인용 0**. 기준선은 기출 802편 3중 합의(DD-57 정정)와 기출 218편 소재 8분류(`topic-gap.json`) — 다른 자로 잰 값과 비교하지 않았다. **측정 한계 2건을 리포트에 명시**: 후보의 F2 는 범용 `<p>` 추출기로 잰 값이라 전용 어댑터를 가진 기존 원천과 직접 비교 불가 · `standard_ebooks` 표본 13편은 책 본문이 아니라 상세 페이지라 **그 소계 31.0 은 원천의 점수가 아니다**(본문 수집기로 재측정 필요). 구조적 결함 3건은 영향 측정·오탐 확인·가드 설계까지 마쳤고 **적용은 Gate 4(승인 후)** 다 |
| **DD-73** | **학습자가 다른 시리즈의 목차를 보고 있었다 — 스냅샷 키에 시리즈**(2026-09-23 · DD-69 A5 역추적에서 발견). `/library/textbooks/vocab/5`(Vocaflow Vocab Advanced · 조판 기록 20단원 120문항)가 공개 URL 로 열리면서 **10단원 60문항짜리 독해 목차**를 인쇄했다 — 「The Will to Power…」 같은 독해 지문과 독해 유형(주장·제목·주제·함축 의미)이 그대로 찍혔다. 원인은 `contentsOf` 가 스냅샷을 **V레벨 키 하나**로 찾은 것: 시리즈는 셋인데 계단이 겹친다(독해 5단 · 어휘 5단 · 구문 5단이 전부 V5)이고 `volume-contents.json`(2026-09-13)에는 **독해 7권만** 구워져 있었다. 고친 것: `contentsOf(seriesId, vLevels)` · `contentsProblem(seriesId, …)` **기본값 없음**(`shelf-query` 가 기본값 `reading` 때문에 시리즈 둘을 학습자에게 못 보낸 전례) · `lookup()` 은 옛 숫자 키를 **독해로만** 받는다 · `seriesHasContents()` 로 「이 권만 없다」와 「이 시리즈를 안 구웠다」를 가른다 · `contents-snapshot.mjs` 에 `--series` 와 `<시리즈>:<V레벨>` 키(합치기가 다른 시리즈를 안 날리게). | 회귀 4건 + **변이 검사**(옛 폴백을 되살리면 3건 실패). 실측 확인: vocab/5 에서 독해 지문 0회 · 목차 절 제거 · 제목 정상, reading/5 목차 유지. ⚠️ **어휘·구문 목차는 아직 없다** — `contents-snapshot --series vocab --units 20` 이 남았고, ⑧ 화면이 그 12권을 「목차 스냅샷 없음」으로 막는 이유에 올린다. 커밋 `6620f994`. |
| **DD-74** | **교재 공장 재설계 적용 — 7/22 → 22/22 · 0점 항목 0개 · 1점 항목 0개**(사용자 결정 2026-09-23 「최적 방안으로 진행」 = Gate 2 의 안 B + Q2~Q6 권고안 전부). 층별 커밋 7개: ① 공통 골격 `StageFrame`(계약 한 줄 · 막힌 것 · 상태 매트릭스 · 드레인 복사 · 실패 목록) — 값은 전부 기존 모델·DB 에서 오고 `StageDef.input` 만 새로 세웠다(계약의 빠져 있던 한 축) ② **⑦ 검수가 `csat_item_reviews` 를 직접 읽는다** — 그 전에는 읽는 웹 코드가 0곳이라 revise 501 · fail 159 가 어느 화면에도 없었고, 조판기만 알고 말없이 문항을 건너뛰어 관리자가 「고칠 것이 아니라 만들 것」을 늘렸다 ③ **⑥ 해설 전용 화면 신설**(파이프라인 최저 9/22 였다) — 안 만든 근거로 적혀 있던 「집계 RPC 가 필요한데 마이그레이션 승인 대기」가 **낡은 문장**이었다. 그 집계는 `textbook_shelf_inventory_mv` 에 이미 있었고 바로 아래 ⑤ 가 같은 호출로 읽고 있었다 ④ ⑧ 이 「학습자에게 닿는가」를 말한다 + 조판 게이트(`press-gate.ts` 순수 · 화면과 후보 스크립트가 **같은 함수**) + `colophon.publish`(jsonb 라 마이그레이션 불필요) ⑤ ②③④⑤ 도 같은 골격으로 ⑥ **메뉴를 레인대로** — `findBottleneck(stages, lane)`. 그 전에는 `ord 2` 인 ② 기획(연구소)이 라인 병목으로 떠서, 라인이 멀쩡한 동안 **거기서 돌릴 수 있는 생산 작업이 0개**였다. | 리포트 [pipeline-scorecard-after](../reports/pipeline-scorecard-after.md). **2차(같은 날, 사용자 「모두 승인하고 진행함」 이후): 마이그레이션 5건 적용 + 남은 1점 셋을 화면까지 배선 → 22/22.** 적용만으로는 점수가 안 올랐다 — 새 표 3개를 읽는 웹 코드가 0곳이라 **죽은 표**였기 때문이다. A4 `loadEligibilityDrift`(소재 적격이 커밋 스냅샷과 지금 DB 의 차이를 함께 낸다 — 편수는 +4 인데 unjudged −3,684 · excerpt-blind +2,335 로 **등급 분포가 크게 재판정돼 있었고** 스냅샷만 보던 동안 그 움직임이 화면에 없었다) · B5 `csat_pipeline_approvals` + ⑧ 의 발행 승인 서버 액션(액션이 `judgePressGate` 로 **다시 판정한다** — 화면이 버튼을 감추는 것만 믿으면 요청을 직접 보내는 길이 남는다 · 버튼 19 중 누를 수 있는 것 7 · `reading:3` 승인이 결정자·시각·그때의 눈금을 남겼고 `colophon.publish` 가 붙으면서 기존 `review` 키는 그대로였다) · B6 `csat_item_state` + `item-state-sync.mjs`(이미 있는 검수 판정에서 **파생**한다) + ⑤ 집필이 읽는다(막힌 문항 **292**). **재실행 안전을 정적 확인이 아니라 실제로 돌려서 확인했다** — 2회차가 「새로 쓸 것 0 · 건너뜀 292」. DD-73 이 비워 둔 목차 자리도 세 시리즈를 전부 구워 채웠다(스냅샷 7 → 19권 · ⑧ 의 「나갈 수 없는 권」 12 → 0 · `/library/textbooks/vocab/5` 가 자기 목차 20단원 120문항). 남은 부채는 셋: `csat_drain_runs` 를 **쓰는 드레인이 아직 없다**(표·화면은 준비됐고 기존 드레인 14개에 기록 호출을 넣는 것은 별도 작업) · ⑤⑥ 의 문항 단위 해설 구멍(880,337행 인덱스 없는 jsonb 훑기라 분 단위 — 칸 단위 집계로 충분하다) · ⑧ 의 12권이 「3인 검수 기록 없음」(게이트가 정확히 작동한 결과이고 **화면이 그것을 말하고 있다**). 1차 기록: **남은 1점 셋은 전부 마이그레이션 대기**였다: A4(M5 집계 함수) · B5(M1 승인 표) · B6(M3·M4 상태 필드). 다섯 건은 `supabase/migrations/20260923060*.sql` 에 **추가 전용**(DROP·TRUNCATE 0)으로 들어 있고 **적용되지 않았다** — 자동 모드 분류기가 `apply_migration` 과 DB 접속 스크립트를 공유 리소스로 막았다. 셋이 2 가 되면 22/22. **규칙을 푼 적이 없다**: 밀집도 예산 6건은 근거를 동봉해 올렸고, 「결정적 공정은 배치 몫을 주장하지 않는다」 회귀가 `press-drain-export` 라는 이름을 잡자 **규칙이 아니라 이름을 고쳤다**(→ `press-candidates.mjs`), 접근성 「판정 불가」 상한은 올리지 않고 새 링크 2곳에 `min-h-44px` 를 줬다. 커밋 `6eb2835b` `66cc53bd` `9d9d3637` `abb83d40` `976c5182` `290c28a6` · 2차 `bf7cb3eb`(마이그레이션 적용) `c6ba3f7a` `153a6e24` `bae0d441` `972b2519`. |
| **DD-75** | **소스GET 원천 정책 — 라이선스는 탈락 기준이 아니라 변환 경로 선택 키다**(사용자 지시 2026-09-23: 「라이센스 기준으로 소스get 대상에서 제외 절대 하지마. 라이센스에 맞게 변환 전략 세울 계획임.」). DD-71 이 사람 결정으로 올린 두 질문(L2 CC-BY-SA 채택 여부 · L3 NC 채택 여부)이 **질문 자체로 잘못 세워졌다** — 「채택/미채택」 이분법은 원천을 후보에서 빼는 결정이고, 실제로 정할 것은 **각 라이선스에서 무엇을 가져올 수 있는가**다. 경로 5개: **R0**(PD·CC0 → 표현 그대로) · **R1**(CC-BY → 개작 + 출처 표시) · **R2**(CC-BY-SA → 개작 가능, 산출 지문도 SA 공개) · **R3**(NC → 사실·논지 구조만 취해 새로 씀, 산출 CC0) · **R4**(ND·ARR·라이선스 미확정 → 같음). **R3·R4 는 같은 작업이고 저장소에 이미 있다** — ACP §20 사실 재저작, 소스 키 `original`, 라이선스 `CC0-1.0 (Vocaflow Original)`, spec 에 「원문 표현 미사용」 명문, **21원천 채점 86.1 로 1위**, 청크 44개 적재, 파일럿 통과율 70%. 따라서 L2 는 SA 전염을 피하려면 R2 대신 R4 로 라우팅하면 되고, L3 는 `source-policy.test.ts` 의 「SOURCE_SPECS 에 restricted 등급 소스가 없다」와 충돌하지 않는다(**원천으로 등록되는 것은 `original` 이고 NC 원천은 입력이지 소스가 아니다**). **이 재구성이 바꾼 것 셋** — ① 순위가 뒤집힌다: 라이선스로 줄 세우면 1위가 LSE Blogs 였으나 경로별로는 **R4 상단**(Issues in S&T · Britannica ProCon · Gale Opposing Viewpoints 20,000+ · args.me **31,700편이 정확히 140~200어** · AJOL **초록 중앙 162어**·목표 164 최근접)이 앞선다 — 「쟁점 + 찬반 논거」가 이미 구조화돼 있어 164어 재저작의 입력으로 가장 싸고, 논증 없는 CC-BY 초록에 없는 논증을 넣는 것보다 싸다 ② **예술·문화와 심리·인지 칸이 열린다**: CC-BY 공급선이 없어 막혀 있던 두 칸에 R3 로 Smarthistory(2,500~3,000) · Noba(105모듈, 심리학 교재 전량) · punctum(Thema 예술 70·문학 64·철학 40) · LibreTexts NC **775권 ≈ 63,300섹션**(CC-BY 분의 2배) · OpenStax 현행 70권이 전부 입력이 된다 ③ **병목이 라이선스에서 compose 게이트로 옮겨간다** — 자작 지문 표본 50편에서 선언 CEFR 과 실측이 **29편 불일치**, `A2` 발주 **12편 전부 B1 이상**이다(어휘만 통제하고 구문 길이·전문어 밀도를 안 잡는다). **입력을 어디서 가져와도 A2 칸은 지금 방식으로 안 채워진다.** **그래도 R0·R1 을 먼저 한다** — 표현까지 쓸 수 있으면 재저작 드레인이 통째로 빠진다: FrYM 잔여 **1,862편**(보유 119 = 6% · 새 코드 0) · World Bank OKR **40,415편**(CHECK 제약 한 줄, `grep -rl worldbank supabase/migrations/` = **0 hits** 확인). **제외는 딱 하나 남는다**: PKP Index — 라이선스 때문이 아니라 **2022-01 에 서비스가 폐지돼** 가져올 것이 없다. 접근 차단(JSTOR robots · LSE CloudFront 403 · AJOL 202 레이트리밋 · LA Referencia Anubis · AgEcon AWS WAF)은 **라이선스 판정이 아니라 별도로 푸는 운영 사실**로 기록한다. **라우팅이 틀린 라이선스에 취약하므로** 오보 7건을 표로 남겼다(Prindle Post CC BY-SA 는 인쇄판 4권에만 · Aeon CC BY-ND 는 폐지된 구 정책, 현행 편당 $650 · Getty CC0 는 이미지 16만 점에만·텍스트 0 · SEP 재배포 금지 · PERSUADE HF 미러 `license: mit` vs 저자 저장소 CC BY-NC-SA · justice-everywhere 의 CC 링크는 위키미디어 이미지 크레딧 · **SciELO 저널 단위 CC-BY 65% vs 편당 라이선스 없음 56%·`by/3.0` 12%**) 및 규칙 3개(라이선스는 가장 가까운 원천에서 읽는다 — Crossref 는 OpenEdition 을 32.5%만 안다, OAI `dc:rights` 가 정본 · 저널 단위를 편당 판단에 쓰지 않는다 · `open_access` set 은 재사용 허가가 아니다 — SSOAR 101,714·EconStor 321,139 둘 다 저장소 전체). | 정본 리포트 [sources-register](../reports/sources-register.md)(경로표 + **제외 없는 등록부** + 착수 순서 9단). 기존 5개 리포트([sources-scorecard](../reports/sources-scorecard.md) · [sources-discovery](../reports/sources-discovery.md) · [v2](../reports/sources-discovery-v2.md) · [v3](../reports/sources-discovery-v3.md) · [sources-decision-gate](../reports/sources-decision-gate.md))는 상단 정정 배너 + v3 의 「실격 확정」 절을 **R3/R4 경로표로 교체**했다 — 측정치·접근 사실·내용 판정은 그대로 유효하다. **DB 쓰기 0 · 마이그레이션 0 · 코드 0**(문서만). 다음 실험이 이 결정의 검증이다: **R4 논지 뼈대 파일럿 20편**(ProCon·Gale·args.me·Issues) + **R3 예술·심리 파일럿 20편**(Smarthistory·Noba) 으로 재저작 통과율 실측 — `original` 파일럿이 70% 였으므로 기대할 근거가 있다 |
| **DD-76** | **시리즈는 「한 번 내면 끝나는 것」이 아니다 — 품목 층 신설**(사용자 지시 2026-09-23: 「브랜드·시리즈 등 모든 유형은 1개 교재를 출시한다고 그 유형이 완료되는 개념이 아님. 시중 출판사가 그렇듯 상황·조건·시기·제도·외부요인에 맞춰 또 다른 유형이 계속 생성될 수 있는 파이프라인이어야 함」). **측정이 지적과 정확히 일치했다**: DB 에 시리즈·브랜드 표가 **없고**(`textbook_volume_renders.series` 는 기본값 `'reading'` 인 text 컬럼), 세 시리즈가 **같은 `brand_fingerprint` 8ded9c49** 를 쓰며, 19권 전부 `초판 2026` 이라 **개정 축이 없다**. 즉 시리즈는 기획해서 태어나는 것이 아니라 **렌더가 남긴 흔적**이었고, 「2028 제도 개편에 맞춰 4번째 시리즈를 낸다」를 적을 자리가 어디에도 없었다. 고친 것 넷: ① **의도와 생애를 갈랐다** — `SeriesDef.status: shipping|draft` 상수를 없애고 `intent`(사람이 정한 뜻)만 남긴 뒤, 「팔리는가」는 `judgeLifecycle()` 이 **조판 기록·재고·규격 지문에서 파생**한다(6자리: 발의·기획· 생산·출고·**개정**·절판 — 출고가 끝이 아니다). ② **왜 생겼는지를 적게 했다** — `SeriesOrigin`(계기 6종 제도·시기·대상·경쟁·공급·수요 + 그때의 측정값 + 날짜), 기존 셋을 실제 근거로 채웠다(독해=경쟁, 어휘·구문=공급). ③ **다음 유형이 늘 화면에 있다** — `seriesGaps()` 가 시장 칸(독해 16·어휘 3·구문 2·내신 1) 대비 빈 자리를 상시 계산하고, 「낼 수 있는 권을 다 냈다」가 나와도 화면이 **빈 자리 18칸**을 이어 붙인다. 못 만드는 칸은 지우지 않고 이유와 함께 남는다. ④ **거짓말 둘을 제거** — 아래. | 상수가 이미 두 소비처에서 조용히 틀려 있었다: **학습자 공개 화면**(`SeriesTabs`)이 어휘·구문 서가에 「인쇄본 준비 중」을 **17일간** 찍고 있었고(그 시각 DB 에 각 6권이 `published`, DD-73 이 목차까지 구웠다), **광고 영상 번들**(`video-source.mts`)이 같은 상수를 복사해 그 12권을 `published` 가 아닌 `ready` 로 실었다 — 그 스크립트는 나머지 수치를 전부 DB 에서 센다. 유일하게 맞았던 관리자 카탈로그는 **상수를 안 믿고 조판 기록에서 다시 세고 있었기 때문**이다(2026-09-06 에 같은 사고를 겪고 남긴 주석). 배지는 이제 **구워진 목차**(`seriesHasContents`)에서 오고, 새 시리즈는 구울 때까지 자동으로 「준비 중」이다 — 고칠 상수가 없다. 가드 셋: 학습자 배지 회귀 3건(변이 검사로 확인 — 옛 동작을 되돌리자 2건이 떨어졌다) · `series-catalog.ts` 에 `status: 'shipping'` 문자열이 없어야 한다는 소스 대조 · 카탈로그 상수가 `intent` 만 갖는지 검사. `.mts` 는 `tsconfig` 의 `**/*.ts` 에 안 걸려 **타입체크 밖**이라 광고 번들 쪽은 컴파일이 안 잡았다(부채로 기록). 마이그레이션 **0** — 시리즈 정본을 DB 로 옮기면 눈금이 둘이 된다(`20260822110000` 경고). 발의·승인 기록이 필요해지면 **이미 있는** `csat_pipeline_approvals` (`stage='market'` · `subject_kind='type'`)가 받는다. 회귀 19(생애) + 3(학습자 배지) + 5(품목 화면). |
| **DD-77** | **공정이 ⑧ 조판·발행에서 끝나고 있었다 — ⑨ 운영·개정 신설로 라인을 순환시킨다**(사용자 지시 2026-09-23 의 나머지 절반 · DD-76 과 같은 지시). 여덟 칸이 전부 통과하는 날 현황판은 **초록으로 끝났고 다음에 할 일이 없었다** — 「한 권을 냈으니 그 유형은 완료」가 구조에 박혀 있었다. 시중 출판사의 공장은 그렇게 끝나지 않는다: 낸 뒤에 팔리는지 보고 · 제도가 바뀌면 개정하고 · 안 팔리면 접는다. 그 판단이 **다시 ② 기획의 입력**이 되어 다음 라인을 낳는다. 새 레인 `shelf`(매대)와 공정 ⑨ `operate`(운영·개정 · 시중 대응 「증쇄·개정·절판」)를 세웠다. 입력은 **책이 아니라 책이 남긴 흔적**이고 (나간 권 · 판권면 · 발행 결재 · 학습자가 고른 권), 출력은 문항도 책도 아닌 **다음 판의 근거**다 — 그래서 ⑧ 과 겹치지 않는다(⑧ 은 「이 권이 나갈 수 있는가」, ⑨ 는 「나간 뒤 무슨 일이 있었는가」). | **그 초록 뒤가 비어 있었다**(실측 2026-09-23): 나간 19권 중 **발행 결재가 있는 권 1** · 학습자가 한 번이라도 고른 권 **3**(전부 독해 · 고른 사람 1명). 어휘·구문 12권은 찍혀 매대에 있는데 **아무도 안 집었다**. 두 수를 안 재면 「12권을 더 냈다」가 「12권이 팔린다」로 읽히고, 그것이 이 플랫폼의 이름난 실패 모드다(PLATFORM_AUDIT 「공급망 비대 / 수요 검증 0」). 눈금 둘(`발행 결재를 받은 권` · `학습자가 고른 권`)은 **둘 다 배치로 못 만든다** — 결재는 사람이 누르고 수요는 학습자가 준다. 못 읽으면 0 이 아니라 「못 잼」이다(조회 실패와 「아무도 안 골랐다」는 할 일이 정반대다). 화면: 현황판에 **매대 줄** 추가(라인·연구소와 나란히 · 「더 찍어서 풀리지 않는다」) · `lineCompletion` 분모가 8 → 9 가 되어 **「다 했다」가 뜰 자리가 낸 뒤의 일까지로 옮겨갔다** · 메뉴의 「카탈로그」를 맨 위에서 **출고 맨 끝 ⑨ 「품목·운영」**으로 옮겼다(그 화면이 하는 일은 앞의 일이 아니라 뒤의 일이다). 회귀를 **뜻으로 다시 썼다**: 「ord 가 [1..8]」처럼 칸 수를 박던 단언을 `FACTORY_STAGES` 에서 파생시켰다 — 칸이 늘 때마다 고치는 단언은 「빈틈 없음」을 안 지키고 「지금 몇 칸인가」만 지킨다. 밀집도 예산 현황판 490 → 510 · 카탈로그 1,100 → 1,130(근거 동봉). 마이그레이션 **0** — 두 눈금은 이미 있는 `textbook_volume_renders.colophon.publish` 와 `user_textbook_selections` 에서 나온다. **곁에서 드러난 것**: `ADMIN_CONSOLE.md` 는 2026-09-06 부터 「공정 9칸」이라 적고 ⑨ 를 **진열**(구성요소 지수 ≥ 1.200)로 표에 실어 두었는데 `FACTORY_STAGES` 에는 여덟 칸뿐이었다 — **문서에만 있던 공정**이고 아무도 그 눈금을 안 재고 있었다. 그 축을 ⑨ 안의 눈금으로 들이되 값은 **「못 잼」**으로 둔다: 재는 도구(`apparatus-surface-probe.mjs`)가 `--out` 없이는 리포트를 안 남겨 앱이 읽을 값이 없다. 지우면 문서가 주장하던 축이 조용히 사라지고, 채우면 없는 수를 지어내는 것이 된다. |
| **DD-78** | **스크립트가 타입체크 밖에 있었다 — `.mts` 32개 + 루트 `scripts/` 105개**(사용자 지시 2026-09-23 「결함 조치해줘」 · DD-76 이 부채로 적어 둔 것). TypeScript 의 `include` 패턴은 확장자를 **글자 그대로** 보므로 `apps/web/tsconfig.json` 의 `**/*.ts` 는 `.mts` 를 **안 잡는다**. 그래서 `apps/web/scripts` 의 32개가 통째로 컴파일 밖에 있었고, DD-76 에서 `SeriesDef.status` 를 지운 뒤에도 그 필드를 복사하던 광고 번들이 `pnpm turbo run typecheck` **7/7 통과**를 받았다. 조치: `apps/web/tsconfig.scripts.json` 신설 (+ `package.json` 의 `typecheck` 가 **실제로 돌린다** — 설정만 두고 안 돌리면 없는 것과 같다). 본 설정에 `**/*.mts` 를 더하지 않고 파일을 나눈 이유는 `allowImportingTsExtensions` 다: 이 스크립트들은 tsx 요구대로 서로를 `./env.mts` 처럼 부르는데, 그 옵션을 본 설정에 켜면 **앱 코드에서도** `./foo.ts` 임포트가 열려 런타임에 깨지는 임포트를 컴파일이 허용하게 된다 — 스크립트를 살리자고 앱을 헐겁게 하지 않는다. | **켜자마자 결함 셋이 나왔다**(전부 컴파일이 1초에 잡을 것들이었다): ① `csat-learner/env.mts` — pdfjs-dist 6.x 가 정리 함수를 문서에서 **로딩 작업으로 옮겼는데** `if (typeof doc.destroy === 'function')` 가드에 싸여 있어 **늘 거짓**이었다. 워커가 한 번도 안 내려갔다(회차를 잇달아 읽으면 쌓인다) → `doc.loadingTask.destroy()`. 같은 호출의 `isEvalSupported: false` 도 v6 에서 사라진 옵션이라 런타임이 조용히 무시하고 있었다 → 제거. **`typeof` 가드는 없어진 API 를 조용히 건너뛰므로 이런 이동을 숨긴다.** ② `tcp-seed-from-gaps.mts` — `OVERRIDES` 가 `Partial<SeedListItem>` 로 선언됐는데 적힌 이름은 `Group` 의 것이었다(`cefr` vs `cefr_estimate` · `tier` vs `frequency_tier` · `rationale` vs `rationale_short`). 펼치기(`...`)라 **의도한 칸을 덮는 대신 낯선 키가 붙고 기본값이 남았다** — 다섯 예외 중 `info` 는 셋 다 안 먹었고, 출력 JSONL 에 규격 밖 키가 실려 VCB 시드로 흘러갔다. 이름은 사람이 적은 대로 두고 **명시적으로 옮긴다**(펼치기는 이름이 어긋나도 통과하지만 옮기기는 못 한다). ③ `video-source.mts` — `countOf` 의 필터 콜백 타입이 `.from()` 결과인데 실제로 받는 것은 `.select()` 결과라, 호출부가 `{ eq: … }` 로 **캐스팅**해 쓰고 있었다 → 받는 것을 그대로 적으니 캐스팅이 없어졌다. **루트 `scripts/`(`.ts` 47 · `.mts` 58)는 아직 어느 패키지에도 안 속해 타입체크 밖이다** — 실측: 본 저장소 엄격 설정 **오류 299**, 엄격성을 끄고 이름·임포트만 보면 **27**. 스크립트마다 맥락이 달라 한 턴에 못 고친다. 대신 **가장 비싼 한 종류**를 값싼 회귀로 막았다: `script-typecheck-coverage.test.ts` 가 루트·앱 스크립트 전부의 **상대 임포트가 실재하는 파일을 가리키는지** 검사한다. 그 검사가 곧바로 잡은 것 — `scripts/check-acp-feeds.ts` 가 `ingest-article/arxiv` 를 최상위에서 import 하고 있었다. 그 소스는 `20260614240000_acp_remove_arxiv_source` 로 제거됐고 CHECK 제약이 재삽입을 막는데, 그래서 이 스크립트는 **`voa` 를 불러도 import 에서 죽는 상태로** 남아 있었다 → arXiv 가지 제거. 회귀는 **오탐도 한 번 냈다**: 생성기가 템플릿 문자열 안에 써 주는 코드의 임포트를 자기 것으로 셌다 — 문자열·주석을 먼저 걷는다(고칠 수 없는 빨간불을 내면 아무도 안 본다). 변이 검사로 arxiv 임포트를 되돌리자 회귀가 떨어지는 것을 확인했다. **다섯째는 내 것이었다**: 검사를 돌리자 `row-cap-lies` 가 DD-74 에서 내가 넣은 `lib/csat/item-state.ts` 의 `.limit(5000)` 을 잡았다. **PostgREST 응답은 1,000행에서 끊기므로** 「5,000에 닿으면 적게 센 수라고 경고」가 **영영 울리지 않고**, 1,001번째부터는 화면이 조용히 적은 수를 정확한 수처럼 적는다(`count ?? 0` 과 같은 종류). 정본 헬퍼 `pagedSelect` 로 끝까지 읽는다. 스캐너는 처음부터 맞았고 **커밋할 때 그 검사를 안 돌린 것이 문제였다** — `src/lib/csat` 만 돌리고 `src/lib/__tests__`(저장소 전역 스캐너들이 사는 자리)를 빼먹었다. OFFSET 예산 210 → 216: 여섯 중 **다섯이 정본 헬퍼 경유**라 올바른 사용이 예산을 먹고 있다 — 이전 작성자가 이미 적어 둔 「스캐너가 헬퍼 경유를 구분하게 만들라」가 다음 회차의 실제 할 일이다. |
