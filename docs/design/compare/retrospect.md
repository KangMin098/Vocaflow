# compare — 회고 `/dashboard` (+ `/reports`) (우선순위 9위)

> vocaflow-design §G3 발산 4안 · 2026-09-19 · 작성 Claude Code(Opus, 고노력) · 화면 재설계 실행 세션(질문 없이 B4).
> **선택(B4)**: **A 「기억의 지층」**(환경 변형 — 선언만 있던 골격을 실제로 세운다) — [DECISIONS](../DECISIONS.md) DD-29 · 골든 [golden/retrospect.md](../golden/retrospect.md).
> 왜 9위인가: `audit/priority.md` — 브리프 [audit/briefs/retrospect.md](../audit/briefs/retrospect.md).

## 현재 (감사 2026-09-18 · 2026-09-19 재확인)

- `/dashboard` 는 `@form 환경 변형 — 단어가 버티는 기간의 사다리 층` 을 선언했지만 렌더는 **흰 카드 7장**(`rounded-ios-2xl` + `shadow-ios-2`) · 5칸 숫자 격자(`lg:grid-cols-5`) · 12px 누적 막대 하나. 사다리의 "층" 은 색 칸 5개로만 있었다 — 층에 **단어가 없다**.
- 런타임 계정 실측(첫 뷰포트 1280): 01 사다리 카드(하루 130 · 사흘 6 · 한 주 **0** · 한 달 **0** · 계절 **0**) → 02 같은 폭 카드 2장. 0 카운터 3개가 첫 화면에 선다(브리프 제약 "0/0/0 카운터 금지" 위반).
- `/reports`: 3D 달력 이모지 · 가운데 정렬 빈 상태 카드 · `grid-cols-3` 숫자 상자 · **조회 실패를 빈 목록으로 삼킨다**(`fetchRecentReports` 가 `error` 를 버림) · 머리에 `N분` — 같은 원천의 분을 `/dashboard` 는 "기록되지 않는 값" 이라 안 쓰는데 여기선 쓴다(두 화면 기준 불일치).
- 정적 신호 `/dashboard` 5 · `/reports` 3 · 렌더 1280 `0/1/1/6/4/0`.

## 자산 (A6 판정)

| 자산 | 상태 | 쓰는 안 |
|---|---|---|
| 낱말별 `stability` | **있음** — `fetchMemoryHorizon` 이 이미 전량 읽는다(버리고 개수만 낸다) | A · C |
| 이번 주 다시 만나 맞힌 낱말 id | **있음** — 같은 함수의 7일 `learning_records` | A · C |
| 지난주 시점의 낱말별 stability | **없음** — 이력 테이블이 없다. `learning_records` 로 FSRS 를 재생해야 한다(드레인급) | B |
| `weekly_reports` | 있음(1행) | D |

## 네 안

| | **A 「기억의 지층」** | **B 「지난주의 나 × 오늘의 나」** | **C 「28일 필사본」** | **D 「주간 장부」** |
|---|---|---|---|---|
| **골격 (G1)** | 환경 변형 — 버티는 기간별 **층**이 쌓인 단면, 층 안에 실제 낱말 | 망각 × 과거의 나 — 두 R(t) 곡선 겹침 | 채색 지문 — 28일 동안 만난 낱말을 날짜 순 원고처럼 | 시험지 사물 — 주마다 한 줄 괘선 장부 |
| **첫 시선** | 「단어 절반이 N 버텨요」 한 문장 아래, 위(하루)에서 아래(계절)로 얇아지는 지층. 층 두께 = 낱말 수, 층 안에 낱말이 줄로 선다 | 곡선 둘 · 그 사이 면적 | 날짜별 낱말 줄, 버틴 만큼 진하게 | 표 |
| **서명** | 이번 주에 다시 만나 맞힌 낱말에 **권점**(허브 「오늘 다시 볼 낱말」 과 같은 표식) · 층을 누르면 그 층의 낱말이 펼쳐진다(모션 0 · 200ms 색) | 과거 곡선 위에 손을 얹으면 그 주 | — | — |
| **N4** (자산 없이 불가능?) | 통과 — 낱말별 FSRS 없이는 층도 권점도 없다 | 통과 | 부분 — 날짜 색은 어느 앱이나 | 불통과 |
| **빈 상태 (렌즈 4)** | 층이 비면 **층 이름만 괘선 한 줄**(0 을 쓰지 않는다). 전부 비면 문장 + 1차 행동 | 곡선 하나뿐 | 빈 원고 | 빈 표 |
| **새 작업** | 조회 0 — 이미 읽은 행에서 층별 낱말을 싣는다(응답 확장) | FSRS 재생(드레인) | 조회 1 | 0 |
| **`/reports`** | 같은 문법 — **주가 한 겹**: 최신 주가 위, 겹 두께 = 그 주 복습 수. 카드·이모지·3칸 숫자·분 제거, 실패와 빈 상태 구분 | — | — | 흡수 |

## 자기검토

| | (a) 익명성 — 로고를 가려도 이 앱인가 | (b) 앞 골든과 같은 문법 | N4 |
|---|---|---|---|
| A | 예 — 층 안에 내 낱말, 권점 | **예** — 허브 권점 · `/text/[id]` 의 R(t) 로 칠한 낱말 | 통과 |
| B | 예 | 예(곡선) — 하지만 과거 곡선을 **지어내야** 한다(I5) | 통과(데이터 없음) |
| C | 부분 | 부분 | 부분 |
| D | 아니오 | 아니오 | 불통과 |

## B4 선택

(1) 새 데이터: A·D 0, C 1, B 드레인 → B 보류. (2) 앞 화면과 같은 몸짓: **A**(권점 = `/hub` · 낱말을 R(t) 로 = `/text/[id]` · 누르면 펼침 = `/flashcard/play` 의 손을 얹으면 보임). D 는 몸짓 없음. (3) A 자기검토 통과. → **A**.

**관측**: `retrospect_layer_opened { rung: 'day'|'few'|'week'|'month'|'season', words: number }` — 브리프의 `weeksBack` 은 B 의 속성이라 A 에 맞게 바꿨다(닫힌 열거형 + 숫자, D3). DB 허용 목록은 `_pending_funnel_allow_hub_curve.sql` 에 한 줄 더해 **승인 대기**.
