# compare — 게임 허브 `/wordblitz` · `/pairflip` (B1 학습자 넷째 단위)

> vocaflow-design §G3 발산 4안 · 2026-09-19 · 작성 Claude Code(Opus, 고노력) · 화면 재설계 실행 세션(질문 없이 B4).
> **선택(B4)**: **A 「이번 판 낱말」**(시험지 사물 — 모듈 허브와 같은 문법 · 권점 몸짓) — [DECISIONS](../DECISIONS.md) DD-35 · 골든 [golden/game-hubs.md](../golden/game-hubs.md).
> 단위: 두 허브가 `GamePoolPanel` 을 같이 쓴다. `RecentScoresList` 는 `/spellforge`(DD-34 완료)와 공용 — 이번에 평평하게 바꿔도 이미 끝난 화면의 판정은 그대로다(시작 아래 기록 목록).

## 현재 (감사 2026-09-18)

- `/wordblitz`: 단어 알약 칩 나열 · **같은 시작 버튼 둘**(네이비 「바로 시작」 · 빨강 「지금 한 판」 — 빨강은 전역 「진단 시작」 과 같은 색) · 트로피 상자 두 장 · 한글 이탤릭 3곳.
- `/pairflip`: 5열 균등 난이도 타일(선택 = 네이비 그라디언트 + 그림자) · 네이비→보라 그라디언트 CTA 금색 글씨 · 390 난이도 타일 가로 잘림 · 판면 톤과 단절된 게임몰 스킨 · 정적 신호 11.
- 매칭 모드 두 번째(영영 정의)는 「준비 중」 인데 선택지로 보였다.

## 네 안

| | **A 「이번 판 낱말」** | **B 「게임 판 미리보기」** | **C 「기록판」** | **D 「게임몰 유지·톤다운」** |
|---|---|---|---|---|
| **골격 (G1)** | 시험지 사물 — 게임이 실제로 쓸 낱말이 괘선 두 단(영어 — 뜻) | 게임 화면 축소판(카드 격자) | 점수 표 | 타일 |
| **서명** | PairFlip 난이도를 고르면 그 판의 쌍 수만큼 권점이 옮겨 찍힌다(모듈 허브 · `/hub` 와 같은 몸짓) | 카드 뒤집기 미리보기(모션 예산 밖) | — | — |
| **N4** | 통과 — `fetchDueGameWords`·`fetchDuePairs` 없이는 목록이 없다 | 부분 | 불통과(기록 0 계정) | 불통과 |
| **새 작업** | 0 — 이미 온 목록 | 0 | 0 | 0 |

## B4 선택

(1) 새 데이터: 전부 0. (2) 같은 몸짓: **A**(모듈 허브 골든 14호와 같은 권점 · 같은 HubStartCard). (3) A 통과. → **A**.

결정: 시작은 **하나**(판면 머리의 주묵 1차 — WordBlitz · HubStartCard — PairFlip). 준비 중인 매칭 모드 선택지는 걷었다(고를 것이 하나뿐). `HubStartCard` 에 `cta.onStart` 추가 — PairFlip 은 설정을 주소가 아니라 sessionStorage 로 넘긴다. 쓰이지 않게 된 `PairFlipLevelSelector` · `PairFlipModeSelector` 와, 그것만 쓰던 옛 `PairFlipStartScreen` 을 지웠다(도달 불가 모듈 라쳇).
