# compare — `/teacher` 클래스 (B1 학습자 둘째 단위)

> vocaflow-design §G3 발산 4안 · 2026-09-19 · 작성 Claude Code(Opus, 고노력) · 화면 재설계 실행 세션(질문 없이 B4).
> **선택(B4)**: **A 「교실에 붙일 초대장」**(시험지 사물) — [DECISIONS](../DECISIONS.md) DD-33 · 골든 [golden/teacher.md](../golden/teacher.md).
> 왜 지금인가: `priority.md` 점수 4 — 학급 구성원·과제가 0 이라 데이터 용이도가 낮지만 "렌즈 6 — 유일한 성장 경로" 라 전략상 중요도가 높다고 적혀 있다. 남은 학습자 화면 중 점수가 있는 것은 이것뿐(`/join` 은 판정 보류).

## 현재 (감사 2026-09-18)

- 첫 시선 = 같은 폭 카드 2장(만들기 / 참여) 입력칸 + 버튼 · 점선 빈 상태 상자 · 개설 버튼이 회청(`--p`) 비활성이라 1차 행동 색과 따로 논다. 교사 채널(렌즈 6)의 첫 화면이 입력 폼 2개와 빈 상자(감사 평균).
- 이 화면이 실제로 만드는 **사물** — 학생 30명을 데려오는 초대 — 은 코드 칩 하나(`TSTEM1`)로만 있었다.

## 자산 (A6)

| 자산 | 상태 |
|---|---|
| 반 이름 · 초대코드 · 학생 수 | 있음 |
| 초대 링크 QR | 있음 — `lib/worksheet/qr.ts`(인쇄 검증된 인코더, `/join/<code>` 32자 → 복사본에서도 읽힘) |
| 학급 활동(과제 진척) | 있음(보낸 단어) — 다만 DB 에 과제 0 |

## 네 안

| | **A 「교실에 붙일 초대장」** | **B 「우리 반 칠판」** | **C 「첫 과제 미리보기」** | **D 「학급 대시보드」** |
|---|---|---|---|---|
| **골격 (G1)** | 시험지 사물 — 반 이름 · 큰 코드 · QR 한 장 | 칠판 — 학생 이름이 하나씩 적힘 | 채색 지문 — 예시 지문을 학년별로 칠한 과제 | 숫자 타일 |
| **첫 시선** | 초대장. 반이 없으면 같은 종이가 미리보기로 서고 반 이름 칸이 종이의 제목 | 빈 칠판 | 예시 지문 | 0/0/0 |
| **서명** | 반 이름을 적는 대로 종이 제목이 채워진다(입력칸이 곧 결과 — `/text/new` · `/fit` 과 같은 몸짓) · 복사 → 「복사했어요」 | 학생이 들어올 때마다 한 줄 | 슬라이더 | — |
| **N4** | 통과 — 초대코드·QR 없이는 종이가 없다 | 통과(학생 0 이라 빈 칠판) | 부분(예시) | 불통과 |
| **새 작업** | 0 — QR 인코더 재사용 | 0 | 0 | 0 |

## B4 선택

(1) 새 데이터: 전부 0. (2) 같은 몸짓: **A**(입력칸이 곧 결과 · 시험지 사물 = `/practice` 연습지·`/fit` 학급 시트와 같은 사물 계열). B 는 학생 0 인 지금 첫 화면이 빈 칠판(D4 위반 위험). (3) A 통과. → **A**.

참여(학생 쪽 입구)는 이 화면의 1차가 아니다 — 한 줄 폼으로 내렸다(학생은 대부분 `/join/<code>` 링크로 온다).
