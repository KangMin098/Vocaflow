# compare — 모듈 허브 `/flashcard` · `/spellforge` (B1 학습자 셋째 단위)

> vocaflow-design §G3 발산 4안 · 2026-09-19 · 작성 Claude Code(Opus, 고노력) · 화면 재설계 실행 세션(질문 없이 B4).
> **선택(B4)**: **A 「오늘 담길 낱말」**(망각 — `/hub` 골든의 권점 몸짓) — [DECISIONS](../DECISIONS.md) DD-34 · 골든 [golden/module-hubs.md](../golden/module-hubs.md).
> 단위: 공용 부품 `components/hub/*`(ModuleHero · HubStartCard · TodayQueue · GamePoolPanel · RecentScoresList)을 쓰는 라우트는 `/flashcard` · `/spellforge` · `/pairflip` · `/wordblitz` · `/dictate` · `/text` — **전부 남은 평균 화면**이라 부품을 바꿔도 A5 안이다. 이 단위는 같은 큐를 쓰는 두 허브를 끝내고, 나머지 넷은 머리(ModuleHero)만 함께 바뀐 채 각자 단위로 남는다.

## 현재 (감사 2026-09-18)

- `/flashcard`: 4열 균등 숫자 타일(0·0·8·0) + 전폭 회색 막대(전부 new 라 진행률 바와 구별 안 됨) · 영어/한국어 혼용 · 15px h1.
- `/spellforge`: 첫 두 블록이 `/flashcard` 와 **픽셀 단위로 같다**(CTA 색만 다름) · 기록 0 인 「최근 기록」 이 첫 화면에 트로피와 · 한글 이탤릭 두 곳.
- 시작 카드: bg2 상자 · 그림자 세그먼트 · 떠오르는 CTA(hover scale) · 모듈마다 다른 CTA 색(핑크 `#BE185D` · 게임 파랑 `#4A9FCF`).

## 자산

| 자산 | 상태 |
|---|---|
| play 가 제시할 순서의 세션 낱말 + 낱말별 기억 상태(R(t) 동적) | **있음** — `SessionQueue.words`(이미 클라이언트에 온다, 타일은 개수로 접었을 뿐) |

## 네 안

| | **A 「오늘 담길 낱말」** | **B 「기억 곡선 위의 큐」** | **C 「카드 더미」** | **D 「타일 유지·정리」** |
|---|---|---|---|---|
| **골격 (G1)** | 망각 — 카드가 나오는 순서의 낱말 줄, 밑줄 = 기억 상태 | 망각 — 7일 곡선(`/hub` 의 곡선) 재사용 | 사물 — 쌓인 카드 | 숫자 타일 |
| **서명** | 세션 길이를 고르면 앞 N개에 권점이 옮겨 찍힌다(`/hub` 골든과 같은 몸짓 · 200ms 색만) | 슬라이더 → 곡선 들어 올림 | 더미 두께 | — |
| **N4** | 통과 | 통과 | 부분 | 불통과 |
| **새 작업** | 0 — 이미 온 배열 | 조회 1(곡선용 S) | 0 | 0 |
| **두 허브 구별** | 같은 문법, 문구·기록만 다름 — "같은 큐를 다른 손으로 푼다" 가 사실이다 | 같음 | 같음 | 같음 |

## B4 선택

(1) 새 데이터: A·C·D 0. (2) 같은 몸짓: **A**(권점 = `/hub` · 밑줄 = `/text/[id]` · `/hub` 낱말 줄 — 세 골든의 표식을 그대로). (3) A 통과. → **A**.

**공용 부품 결정**: ModuleHero 는 면(그라디언트·상자)을 칠하지 않는 **판면 머리**로(다른 골든과 같은 두꺼운 괘선 · 26/32px 제목) — `/dictate`(하늘→파랑 띠) · `/pairflip` 의 머리도 함께 조용해진다. HubStartCard 의 1차 행동은 모듈 색이 아니라 **주묵 하나**(화면마다 1차 색이 달랐다). TodayQueue 는 지웠다(두 허브만 썼다 — 타입은 `session-queue.ts` 로).
