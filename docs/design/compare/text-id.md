# compare — `/text/[id]` 읽기 워크스페이스 (우선순위 7위)

> vocaflow-design §G3 발산 4안 · 2026-09-19 · 작성 Claude Code(Opus, 고노력) · 화면 재설계 실행 세션(질문 없이 B4).
> **선택(B4)**: **A 「내 기억으로 칠한 원문」** — [DECISIONS](../DECISIONS.md) DD-27 · 골든 [golden/text-id.md](../golden/text-id.md).
> 왜 7위인가: `audit/priority.md` — 수요 2(여정 ③ 본체) × 평균 2 × 데이터 3(조인 키가 컬럼으로 있음) = 12. 브리프 [audit/briefs/text_id.md](../audit/briefs/text_id.md).
> 데이터 판정(A6): **조인 필요** — `vocabularies`(word · stability · last_review_at)와 챕터 낱말을 낱말로 조인. 드레인 불필요 → 진행.

## 현재 (감사 2026-09-18 · 2026-09-19 재확인)

- 첫 시선 = 무채색 산문. 원문 낱말이 **전부 `status: 'new'` 하드코딩**(`word-enrichment.ts` `toWord`) — 읽기 화면은 이미 3/2/1px·점선을 그릴 줄 아는데 늘 점선 하나만 받았다. `@form 채색 지문` 선언과 렌더 불일치.
- 지어낸 수치(I5): 인사이트 패널 기억 통계 `{97, 28, 14, 17}` 상수 · 「Page 3까지 왔어요. 이번 chapter 의 1/4」 고정 문장 · 모드 알약 진행(듣기·읽기 「완료」)이 모든 학습자에게 같다.
- 한국어 격려 문장 이탤릭 · 모드 알약 1280 잘림 · 390 가로 넘침 349px(도구 줄 + 알약 줄 + 원문 URL + 아래 장 이동).
- 첫 출현만 표시되는 챕터 학습 낱말(약 30개)은 대개 **첫 화면 아래**에 있다(런타임 계정 실측).

## 네 안

| | **A 「내 기억으로 칠한 원문」** | **B 「난외의 7일 곡선」** | **C 「한 장의 판면」** | **D 「읽고 나면 얇아지는 밑줄」** |
|---|---|---|---|---|
| **골격 (G1)** | 채색 지문 × 망각 | 망각 | 시험지 사물 | 환경 변형 |
| **첫 시선** | 원문 바로 위 **이 챕터의 낱말 줄**(나오는 순서, Lora, 지금 R(t) 의 밑줄 두께) + 상태별 수. 원문 속 같은 낱말도 같은 두께 | 원문 옆 난외에 이 글의 흐려질 낱말 7일 곡선 | 모드 알약 13개 대신 한 장의 판면, 모드는 난외 기호 | 끝까지 읽으면 이 글의 밑줄이 한 단계 얇아진다 |
| **서명** | 낱말 줄의 낱말을 누르면 원문의 그 자리로(즉시 · 포커스) — 밑줄 두께가 두 곳에서 같은 말을 한다 | 다시 읽기 → 곡선 들어 올림 | 기호 → 모드 | 페이지 페이드 300ms |
| **N4** | R(t) 없이는 두께가 없다 | 통과 | 약함 | "읽음 = 복습" 은 FSRS 가 정하지 않는다 — 지어낸 상태 전이 |
| **새 작업** | 레이아웃에 조회 1(챕터 낱말만 `in`) · 조립 경로에 상태 전달 | 조회 + 예보 계산 + 난외 판면 | 헤더·모드 전면 재구성 | 진행 행 연동 + 상태 전이 규칙(없음) |
| **다른 셋과** | 제품의 본체가 선언대로 칠해진다 | 시간 축 | 모드 비용 제거 | 성장을 환경으로 |

## 자기검토

| | (a) 앱 종류 | (b) 이 제품인가 | N4 |
|---|---|---|---|
| A | 예 — 원서 읽기 | **예** — 3/2/1px 밑줄 문법 · 허브 낱말 줄과 같은 조판 | 통과 |
| B | 예 | 예 | 통과 |
| C | 예 | 부분 | 약함 |
| D | 예 | 예 | **불통과 위험**(지어낸 전이) |

## B4 선택

(1) 새 데이터 최소: **A**(조회 1). (2) 앞 화면과 같은 몸짓: A(허브 낱말 줄 · `/flashcard/play` 상태 밑줄) · B(허브 곡선). (3) 자기검토: A·B 통과. → (1) 에서 **A**.

## 하한선 (A)

- 상태는 요청마다 R(t) 로 계산(`memory_state` 저장 금지) · 같은 낱말 여러 행이면 가장 잘 기억한 행.
- 조회 실패는 "전부 처음" 으로 삼키지 않고 낱말 줄과 인사이트 패널이 밝힌다.
- 지어낸 수치 전부 제거(I5) · 한국어 이탤릭 제거 · 390 가로 넘침 0 · 44px.
- 모션 신설 0 — 낱말 찾기는 즉시 스크롤 + 포커스.
