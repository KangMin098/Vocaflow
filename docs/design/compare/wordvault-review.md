# compare — `/wordvault/review` (+ `/wordvault/study`) 복습 (우선순위 8위)

> vocaflow-design §G3 발산 4안 · 2026-09-19 · 작성 Claude Code(Opus, 고노력) · 화면 재설계 실행 세션(질문 없이 B4).
> **선택(B4)**: **A 「이 단어의 기억선」**(`/flashcard/play` 골든과 같은 부품) — [DECISIONS](../DECISIONS.md) DD-28 · 골든 [golden/wordvault-review.md](../golden/wordvault-review.md).
> 왜 8위인가: `audit/priority.md` — 수요 2(여정 ③ 복습 루프) × 평균 2 × 데이터 3 = 12. 브리프 [audit/briefs/wordvault_review.md](../audit/briefs/wordvault_review.md).

## 현재 (감사 2026-09-18 · 2026-09-19 재확인)

- 두 라우트가 **같은 컴포넌트 · 같은 쿼리**(주석만 "due+new") — 픽셀까지 같았다.
- 중앙 단일 카드(`rounded-3xl` · `shadow-md` · 장식 방사형 그라디언트 원) · 레이블 없는 거북이 · 파란 글로우 · 재생 중 무한 글로우.
- 평가 버튼 아래 간격 **상수**(10 min · 1 day · 3 days · 7 days · 14 days) — I5.
- **숨은 데이터 결함**: 낱말마다 DB 의 FSRS 카드(`srs`)가 실려 오는데 `StudyMode` 가 쓰지 않고 세션 캐시가 비면 `createNewCard` 로 평가했다 — 이 화면의 복습은 매번 안정도를 리셋했다.

## 통합 결정 (브리프 "합칠지 구분할지 먼저")

**구분한다** — review = **다시 볼 낱말**(`attention` = 흐릿해요 + 익숙해요, 상단 리본 "다시 볼" 칸과 같은 집합), study = 전체(급한 순 · `?filter=` 로 좁힘). 쿼리 함수는 그대로(`fetchStudyVocabularies(…, 'attention')`). 합치면 리본의 "다시 볼" 칸이 갈 곳을 잃는다.

## 네 안

| | **A 「이 단어의 기억선」** | **B 「곡선 위의 큐」** | **C 「차오르는 바탕」** | **D 「오늘의 복습 한 장」** |
|---|---|---|---|---|
| **골격 (G1)** | 망각 | 망각(큐 전체) | 환경 변형 | 시험지 사물 |
| **첫 시선** | 카드 아래 이 단어의 R(t) 시간축 · 오늘 상태 | 큐 전체가 R 낮은 순 곡선 위 점들, 점을 열면 카드 | 세션 동안 바탕 톤이 risk → stable 로 | 번호 붙은 복습지 |
| **서명** | 평가에 손을 얹으면 다음 곡선·다음 만남 눈금(모션 0) — **`/flashcard/play` 와 같은 몸짓** | 평가 → 점이 오른쪽으로 이동 200ms(§5.2 밖 — 개정 필요) | 톤 전환 | 기표 |
| **N4** | 통과 | 통과 | 약함 — 톤은 자산 없이 흉내 | 약함 |
| **새 작업** | 0 — 부품 재사용 + 카드를 DB FSRS 로 | 큐 전체 R 계산 + 새 판면 | 0 | 0 |

## 자기검토

| | (a) | (b) | N4 |
|---|---|---|---|
| A | 예 — 간격 반복 | **예** — 플래시카드·허브와 같은 곡선 문법 | 통과 |
| B | 예 | 예 | 통과(모션 개정 필요) |
| C | 부분 | 부분 | 약함 |
| D | 예 | 부분 | 약함 |

## B4 선택

(1) 새 데이터: A·C·D 0. (2) 앞 화면과 같은 몸짓: **A**(같은 부품), B 비슷하나 모션 개정 필요. (3) A 통과. → **A**.
