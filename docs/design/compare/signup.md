# compare — `/signup` 가입 (우선순위 6위 · 인증 4화면 대표)

> vocaflow-design §G3 발산 4안 · 2026-09-19 · 작성 Claude Code(Opus, 고노력) · 화면 재설계 실행 세션(질문 없이 B4).
> **선택(B4)**: **A 「칠해진 지문 옆의 가입」** — [DECISIONS](../DECISIONS.md) DD-26 · 골든 [golden/signup.md](../golden/signup.md).
> 왜 6위인가: `audit/priority.md` — 수요 3(여정 ①→② 경계) × 평균 2 × 데이터 2 = 12. 브리프 [audit/briefs/signup.md](../audit/briefs/signup.md).

## 현재 (감사 2026-09-18 · 2026-09-19 재확인)

- 빈 바탕 한가운데 **그림자 카드 폼**(`Card variant="elevated"` · `rounded-xl`) + 중앙 H1 2줄. **1차 행동 「가입하고 학습 시작하기」 가 1280·390 모두 첫 화면 밖**.
- 로그인 ↔ 가입 전환 링크가 복귀 경로(`next`)를 떨어뜨린다 — 초대 링크로 온 학생이 로그인/가입을 오가면 학급 연결이 끊긴다.
- 신호: 정적 6 · 렌더 1280 `0/2/0/0/2/0` · 390 `0/1/0/0/2/0`.

## 자산

| 자산 | 값 | 어디 |
|---|---|---|
| 칠할 지문 | 랜딩 데모 지문(낱말별 V-Level, 사전 계산 — 계정 무관) | `lib/marketing/hero-demo.ts` `buildHeroDemo` |
| 칠 부품 | `/fit` 과 같은 `PaintedPassage`(학년 슬라이더 · 눈금 = 학년별 %) | `components/textfit/PaintedPassage.tsx` |
| "가입하면 무엇이 바뀌나" 의 근거 | 진단 결과로 같은 지문을 내 수준으로 칠한다 — `/diagnostic` 골든(DD-23)이 실제로 한다 | 골든 [diagnostic](../golden/diagnostic.md) |
| 방금 본 증명(공유 payload) | 서버 저장 없음 — 가입 화면까지 따라오지 않는다 | `share.ts` |

## 네 안

| | **A 「칠해진 지문 옆의 가입」** | **B 「첫 서가 짓기」** | **C 「원서 한 장 서명」** | **D 「방금 본 결과 이어받기」** |
|---|---|---|---|---|
| **골격 (G1)** | 채색 지문 | 환경 변형 | 시험지 사물 | 채색 지문(받은 payload) |
| **첫 시선** | 두 단 — 왼쪽: 랜딩과 같은 지문이 학년 슬라이더로 칠해진 채 "지금은 학년 기준 · 가입하고 진단하면 내 수준으로", 오른쪽: 폼. 모바일은 칠해진 두 줄 → 폼 | 입력할 때마다 빈 서가에 이름표·첫 책 자리가 선다 | 가입서가 판면 — 이름 칸에 도장 | `next` 에 `/fit/s/…` 가 있으면 그 결과를 폼 옆에 |
| **서명** | 학년 슬라이더 → 낱말 면 색 200ms — **`/fit`·랜딩과 같은 몸짓(같은 부품)** | 입력 → 서가 칸 페이드 | 제출 → 도장 페이드 | 같은 칠 |
| **N4** | 낱말별 레벨 없이는 못 칠한다 | 서가 모양은 자산 없이 그려진다 — **약함** | 약함 | 통과 — 단, 공유 경로로 온 사람만(대부분은 결과 없이 온다) |
| **새 작업** | 서버 page 로 나눠 데모 지문을 props 로(쿼리 0 · 랜딩과 같은 계산) | 0 | 0 | payload 파싱 · 대부분의 진입에 골격이 없다 |
| **다른 셋과** | 누구에게나 같은 증명 | 가입 과정이 형태 | 사물 | 개인화 — 범위가 좁다 |

## 자기검토

| | (a) 앱 종류 | (b) 이 제품인가 | N4 |
|---|---|---|---|
| A | 예 — 읽기·어휘 도구 가입 | **예** — 랜딩·`/fit` 의 칠 | 통과 |
| B | 부분 | 부분 | 약함 |
| C | 예 | 부분 | 약함 |
| D | 예 | 예 | 경로 한정 |

## B4 선택

(1) 새 데이터: B·C 0, A 쿼리 0(page 분리만), D payload. (2) `/fit` 골든과 같은 몸짓: **A**·D. (3) 자기검토: A 통과, D 는 대부분의 방문자에게 골격이 없다 → **A**.

## 하한선 (A)

- 1차 행동은 첫 화면 안(1280 · 390) · `next` 보존(가입↔로그인 링크 양쪽) · 약관 필수 유지 · 44px.
- 약속은 사실만: "가입하고 진단하면 내 수준으로 칠해요" — `/diagnostic` 이 그렇게 칠한다(DD-23).
- 다른 인증 3화면(로그인·재설정·메일 확인)은 이번에 **형태를 바꾸지 않았다** — 레이아웃의 모바일 위 여백만 줄었다(`py-s-8 → py-s-5`). 같은 결정으로 옮기는 것은 다음 순서의 일.
