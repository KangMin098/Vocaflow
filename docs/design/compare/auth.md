# compare — 인증 3화면 `/login` · `/reset-password` · `/verify-email` (여정 ①② 나머지)

> vocaflow-design §G3 발산 4안 · 2026-09-19 · 작성 Claude Code(Opus, 고노력) · 화면 재설계 실행 세션(질문 없이 B4).
> **선택(B4)**: **A 「칠해진 지문 옆의 폼」**(`/signup` 골든 6호와 같은 두 단 · 같은 부품) — [DECISIONS](../DECISIONS.md) DD-31 · 골든 [golden/auth.md](../golden/auth.md).
> 왜 지금인가: 상위 10 다음 B1 순서는 여정 ①② 위의 화면 — 남은 것은 `/verify-email`(② 가입 → 메일 확인) 하나. 브리프 [signup.md](../audit/briefs/signup.md) 가 "로그인·재설정·메일 확인은 같은 틀이라 이 브리프의 결정을 따른다" 고 적었으므로 셋을 한 단위로 한다.

## 현재 (감사 2026-09-18)

| 화면 | 첫 시선 | 정적 | 판정 |
|---|---|--:|---|
| `/login` | 빈 바탕 한가운데 그림자 카드 · 중앙 H1 「로그인」 · 「안전한 인증 · 산업 표준 암호화」(아무것도 말하지 않는 안심 문구) | 6 | 평균 — 뷰포트 약 70% 빈 공간 |
| `/reset-password` | 그림자 카드 · 좁은 카드에 두 줄 중앙 H1 · 아이콘 원 · 정보색 안내 상자 | 11 | 평균 — 공개 화면 정적 신호 최다 |
| `/verify-email` | 메일 아이콘 타일 + 주황 알림 점 · 번호 원 3단계 상자 · 주소를 모를 때 「재발송할 수 없어요」 옆에 **활성처럼 보이는** 잠긴 버튼 | 6 | 평균 |

세 화면 모두 공개 화면인데 첫 화면에 **실제로 수행한 결과**가 없다(I1–I2).

## 네 안

| | **A 「칠해진 지문 옆의 폼」** | **B 「내 서가가 기다려요」** | **C 「한 줄 인증」** | **D 「원고 봉투」** |
|---|---|---|---|---|
| **골격 (G1)** | 채색 지문 — `/signup` 과 같은 두 단 | 서가 — 로그인하면 돌아갈 책 표지 | 없음(폼만, 여백 정리) | 시험지 사물 — 봉투·우표 |
| **첫 시선** | 왼쪽 칠해진 지문 + 한 문장(화면마다 다름), 오른쪽 폼 | 표지 줄 | 폼 | 봉투 그림 |
| **서명** | 학년을 옮기면 칠이 바뀜 200ms(같은 부품) | — | — | — |
| **N4** | 통과 | 부분 — 로그인 전에는 **내** 서가를 모른다(공용 표지 = 평균) | 불통과 | 불통과(장식) |
| **새 작업** | 0 — `buildHeroDemo` · `SignupProof` 그대로 | 조회 1 | 0 | 0 |

## B4 선택

(1) 새 데이터: A·C·D 0. (2) 앞 화면과 같은 몸짓: **A**(`/signup` 골든과 한 틀 — 인증 4화면이 같은 판면이 된다). (3) A 통과. → **A**.

**구조**: 세 `page.tsx` 는 `'use client'` 였다 → 서버 페이지(메타데이터 + `AuthSpread`) + 클라이언트 폼(`LoginForm` · `ResetPasswordForm` · `VerifyEmailClient`)으로 나눴다(`/signup` 과 같은 분할). 폼 로직은 그대로다. `SignupProof` 는 이제 인증 4화면이 같이 쓴다 — 여전히 `(auth)` 그룹 안.
