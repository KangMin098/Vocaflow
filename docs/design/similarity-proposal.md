# 유사도 상한 결정안 — ① / ② / ③ (Gate 2 · 사람 결정 대기)

> 2026-09-19 · 작성 Claude Code · 지시: [image-system-brief](image-system-brief.md) Gate 2 · 근거: [refs/tines/dna.md](refs/tines/dna.md) · [refs/uxcel/dna.md](refs/uxcel/dna.md) · [asset-inventory.md](asset-inventory.md).
> **이 문서는 아무것도 바꾸지 않는다.** 아래 diff 는 초안이고, DESIGN.md(다른 세션의 미커밋 수정이 있다 — A8)·토큰·03-system 은 사람이 옵션을 고른 뒤 2회차(Gate 3)에서만 고친다.
> 줄 번호는 2026-09-19 작업 트리 기준이다. 범위: 웹(`apps/web`)만 — `apps/mobile` 제외(사용자 지시).

## 0. 기준선 — 지금 정본이 정한 것 (Gate −1)

**06-workflow 비평 재사용**: (a) 익명성 · (b) 평균 회귀 · (c) 골든 대조 · (d) 포트폴리오 · (e) 규칙 — 전부 현행본에 있다([06-workflow.md:15-23](06-workflow.md)). 새 비평 절차를 만들지 않고 이것을 쓴다.

**DESIGN.md 화면별 골격·서명**(DESIGN.md:37-49 을 옮김 — 삽화 체계는 이 표의 서명을 **바꾸지 않는다**. 삽화는 서명이 아닌 자리에만 들어간다):

| 화면 | 골격 | 서명 | 삽화가 들어갈 수 있는 자리 |
|---|---|---|---|
| `/` 랜딩 | 채색 지문 | 슬라이더 → 단어 면 색 200ms | 히어로 **아래** 섹션 · OG |
| `/fit` | 채색 지문 | 학년 슬라이더 → 낱말 면 색 · 「학급에 나눠 줄 한 장」 | 빈 상태(입력 전)만 — 골든 1호라 판면은 건드리지 않는다 |
| `/text/[id]` | 채색 지문(목표) | 밑줄 두께 3/2/1px | 없음(학습 중) · 허브 빈 상태(`textviewer/EmptyState`) |
| `/csat` 분석 · 홈 | 주묵 문법 · 시험지 사물 | 지지 실선 200ms · `=` 합류 | 없음(학습 중) |
| `/dashboard` | 환경 변형(목표) | 사다리 층 | 빈 상태 |
| `/hub` · 홈 | 망각(목표) | 밑줄 두께 | 모듈 머리(`ModuleHero` ✋) |
| `/wordvault` | 망각(목표) | — | 빈 상태(`WordVaultEmptyState`) |
| `/library/books` | — (다음 발산) | 표지 식별색 | 빈 서가(`ShelfEmptyState`) · 표지는 별도 정본 |
| `/flashcard` 등 학습 중 | 망각(목표) | 카드 뒤집기 | **없음** — 학습 중 삽화 0 (Calm UI · 모션 7종) |
| `/admin/*` | 화면별 | — | 대상 아님 |

## 1. 모든 옵션에 공통으로 먼저 풀어야 할 두 줄

brief 는 "① 은 DESIGN.md 무변경" 이라 했지만, **삽화 파일을 들이는 것 자체**가 두 정본 줄과 부딪힌다. 어느 옵션을 고르든 아래 둘은 개정된다.

| # | 부딪히는 줄 | 왜 | 개정 초안 |
|---|---|---|---|
| P1 | [03-system.md:228](03-system.md) 「브랜드 마크 … **자산 파일 0개로 서명이 된다** (`public/` 에 이미지가 하나도 없던 상태에서 파일을 늘리지 않는 선택)」 | 삽화 SVG 가 `public/` 또는 컴포넌트로 들어온다 | 서명(워드마크·낙관·권점)은 계속 파일 0개. **삽화는 별도 행**으로 허용하고 조건을 단다(아래 diff) |
| P2 | [vocaflow-design SKILL §F](../../.claude/skills/vocaflow-design/SKILL.md) 「자산과 무관한 새 미감 = 장식(N1 불통과)」 | 삽화 양식 자체는 스타일이다 | **삽화가 그리는 개념이 N1 자산 개념일 때만** 장식이 아니다 — 망각(R(t)) · 칠해진 지문(커버리지) · 주묵 선 · 시험지 · 서가. manifest `concept` 는 이 다섯 중 하나를 가리켜야 한다. SKILL 본문은 고치지 않고 03-system 새 절에서 인용한다 |

```diff
 # docs/design/03-system.md §3-5
 | 브랜드 마크 | **주묵 각인 + Lora 워드마크 + 권점 하나.** 자산 파일 0개로 서명이 된다 (…) |
+| 삽화 | **서명이 아니다.** SVG 만 · 색은 토큰 5개 · 개념→사물→동사 3칸(manifest)이 비면 들이지 않는다 ·
+|      | 개념은 N1 자산 개념(망각·지문·주묵 선·시험지·서가) 중 하나 · 학습 중 화면에는 넣지 않는다 — §3-9 (DD-2x) |
```

비용(공통): 03-system 2줄 + 새 절 §3-9(Gate 3) + DD 1건.

## 2. L2 항목별 — 차용하면 무엇이 바뀌는가

| L2 항목 | Tines 값(dna) | 차용 시 바뀌는 줄 | 충돌 정도 | 들어가는 옵션 |
|---|---|---|---|---|
| **따뜻한 중성 바탕** | `#FCF9F5` | **없음** — `--bg #FBFAF6`(tokens.css:123)가 이미 채널당 1 차이. brief 의 `--paper` 토큰은 존재하지 않는다 | 0 | 이미 충족 |
| **격자 무대** | 선 1px · 24px · 대비 ≈1.06 / 점 1px · 12px(액자 안) | DESIGN.md:14 에 한 문장 · tokens.css 새 토큰 `--grid-line` 라이트/다크 2줄 · `colors.ts` 동기(design-tokens CLAUDE.md 절차 2) · DESIGN_SYSTEM 토큰 표 1행 | 낮음 — 시험지 세계(모눈·원고지)와 같은 사물. ⚠️ **CSS `linear-gradient` 로 그리면 평균 신호 라쳇 `gradient` 가 는다**(`average-signal-ratchet.test.ts:51`) → SVG data-URI 패턴으로만 | ② ③ |
| **제품 화면 액자** | 1px 선 · 12~14px · 그림자 0 · 0° | 없음 — `Panel` + 헤어라인 링(DESIGN.md:14)이 이미 같은 문법. 모서리는 6px(`--r-2xl`) 유지 | 0 | ① ② ③ |
| **섹션 리듬** 제목 → 그림 → 본문 | 4패턴 | 없음 — SKILL §3.2 「증명 → 근거 → 다음 문」과 같은 방향. 공개 화면·빈 상태에만 | 0 | ② ③ |
| **여백 리듬** | 섹션 간 156px | tokens.css `--s-40: 160px` 신설(현재 최대 `--s-16` 64px) | 낮음 | ② ③ |
| **제목/본문 비율** | 4.0 / 2.4 | 공개 화면 h1 42→56px · 28→34px(`app/page.tsx:76` — 화면 코드, Gate 6) · 03-system §3-1 에 비율 1줄 | 낮음 | ② ③ |
| **큰 도형 비율** | 스팟 15~31% · 머리 60% · 배너 30% | 03-system §3-9 규격 4종(새 절) | 0 | ① ② ③ |
| 제목 굵기 400 · 자간 −0.03em | 400 · −0.03em | `globals.css:295-296`(`font-weight: 600` · `-0.005em`) — 03-system §3-1 이 **세리프는 같은 굵기에서 무겁게 읽혀 800→600** 으로 정한 근거를 뒤집는다 | 중간 | ③ |
| 제목 서체(세리프+산세리프 혼용) | Reckless + Roobert | **바꿀 것이 없다** — 우리도 세리프(Hahmlet·Lora) 제목 + 산세리프(IBM Plex Sans KR) UI 짝. brief 가정("굵은 산세리프로 교체")은 실측과 다르다. 산세리프 제목으로 가면 v07 4종 규칙·SKILL §2 「Serif discipline 거부」 행 개정 | 높음(그리고 Tines 에 가까워지지도 않는다) | 권하지 않음 |
| 파스텔 섹션 면 5~6색 | 라벤더·초록·복숭아·분홍·노랑 틴트 | DESIGN.md:11-12 「면적을 가진 색은 주묵 **하나**」 · 03-system §3-2 「왜 색 하나인가」 폐기 · 새 토큰 5×2 | **높음** — 방향 문단 자체 | ③ |
| 브랜드색 글자 잉크 | 제목·본문이 보라 | DESIGN.md:11 `--t1` 잉크 → `--p` 계열. 대비 재측정 · `learning-tone.test.ts` 영향 | 높음 | ③ |
| 12~24px 모서리 | 12~14 · 24px | DESIGN.md:14 「모서리 2–6px」 · 03-system §3-3 · **평균 신호 라쳇 `rounded-big` 증가 = 실패**(DD-12 「기준선을 올려 통과시키지 않는다」) | **차단** — DD-12 를 뒤집어야 한다 | 어느 옵션에도 넣지 않음 |
| 대문자 넓은 자간 눈썹 라벨 | 모노 대문자 | 03-system §3-4 `Eyebrow` 「대문자·넓은 트래킹 금지」(04-application 에서 145곳을 걷어냈다) | 차단 | 어느 옵션에도 넣지 않음 |
| 마퀴 · 자동 재생 영상 | 로고·인용 줄 · 히어로 영상(추정) | AGENTS.md 모션 절 「자동재생 캐러셀 · 장식적 상시 모션」 금지 | 차단 | 어느 옵션에도 넣지 않음 |

## 3. 옵션 셋

### ① L1 만 — 삽화 규칙만 닮는다

- **바뀌는 것**: §1 공통(P1·P2) · 03-system §3-9 삽화 렌더 규칙(Gate 3). 토큰 0 · DESIGN.md 0.
- **삽화**: 개념→사물→동사 · 토큰 5색 · 선 2종(1.5/1px) · 면 평면 · 그라디언트 0 · 규격 4종. 무대는 **맨 지면**(`--bg`) — 격자 없음.
- **비용**: 문서 3곳(03-system 2줄 + 새 절 · DD). 화면 코드 0.
- **한계**: Tines 와 닮는 건 "그림 그리는 법"뿐 — 나란히 놓아도 계열로 안 읽힌다([D] L2 전부 0).

### ② L2 부분 — 무대·구성까지, 서체·색 방향은 유지 (**권고**)

- ① + 격자 무대 · 섹션 리듬 · 여백 리듬 · 제목/본문 비율 · 액자.
- **서체·굵기·잉크·모서리·면적 색은 지금 그대로.**

```diff
 # DESIGN.md:14 (다른 세션 미커밋 파일 — 2회차에 이 한 줄만 추가, 나머지 hunk 는 건드리지 않는다)
 구획은 그림자 대신 `0 0 0 1px #E0DBD0`(헤어라인 링, `--bd`), 모서리 2–6px(`--r-sm`…`--r-2xl`),
+무대는 1px 모눈(`--grid-line`, 24px) — 삽화 · 증명 액자 · 공개 히어로 · 빈 상태의 바탕에만(학습 중 화면 0).
 뜨는 것은 모달·시트·토스트·팝오버만(`--sh-float`). …

 # DESIGN.md:28 세계의 사물
-| 시험지 | 2단 격자 · 문항 번호 · 괘선 | CSAT 코퍼스 | … |
+| 시험지 | 2단 격자 · 문항 번호 · 괘선 · 모눈 무대(삽화·액자 바탕) | CSAT 코퍼스 | … |
```

```diff
 /* packages/design-tokens/src/tokens.css — :root */
+  --grid-line: color-mix(in srgb, var(--bd) 40%, transparent); /* 모눈 무대 — 대비 ≈1.1:1 목표, 실측 후 확정 */
+  --s-40: 160px;                                                 /* 섹션 리듬(공개 화면) */
 /* [data-theme="dark"] */
+  --grid-line: color-mix(in srgb, var(--bd) 55%, transparent);
```
+ `src/colors.ts` 같은 값 · DESIGN_SYSTEM 토큰 표 2행 · 03-system §3-1 에 「공개 화면 h1/본문 비율 desktop 3.5 · 390 2.3」 1줄.

- **비용**: 토큰 2(+다크) · 문서 5곳 · DD 1. 화면 코드는 Gate 6 에서 manifest 대상만(랜딩 h1 크기 · 빈 상태 5 컴포넌트 · OG 무대).
- **라쳇 영향**: 0 — 격자는 SVG 패턴, 모서리·그림자 불변.
- **왜 권고인가**: Tines 와 닮은 **골격 문법**(모눈 위 사물 하나 · 증명 액자 · 넓은 섹션 간격 · 큰 제목 비율)은 다 가져오고, 익명성 (a) 을 지키는 세 요소(주묵 하나 · 세리프 판면 · 2–6px 모서리)는 남긴다. 모눈은 원고지·시험지라는 **우리 세계의 사물**이라 소재 차용이 아니다.

### ③ L2 전부 — 색 방향까지 (Reading Room 부분 피벗)

- ② + 파스텔 섹션 면 5색 · 브랜드색 잉크 · 제목 굵기 400/자간 −0.03em.
- 모서리 12px+ 는 ③ 에서도 **넣을 수 없다**(DD-12 라쳇). 서체 교체는 Tines 에 가까워지지 않으므로 넣지 않는다.

```diff
 # DESIGN.md:11-12
-지면 … 위에 잉크 `#1A1714`(`--t1`)로 쓰고, 브랜드 행동은 Deep Ink `#0F2540`(`--p`),
-면적을 가진 색은 주묵 `#C0392B`(`--ju`) 하나다. 골드 … 5% 미만.
+지면 … 위에 제목·본문을 Deep Ink(`--p`)로 쓰고, 섹션 면은 틴트 5색(`--tint-1…5`)이 번갈아 맡는다.
+주묵(`--ju`)은 서명·1차 CTA 의 점으로 줄어든다. 골드 … 5% 미만.
 # globals.css:295-296
-  font-weight: 600;  letter-spacing: -0.005em;
+  font-weight: 400;  letter-spacing: -0.03em;
```
+ tokens.css 틴트 10줄(라이트·다크) · 03-system §3-2 「왜 색 하나인가」 폐기 · §3-1 굵기 근거 폐기 · `learning-tone.test.ts` 주묵 검사 재검토 · 대비 재측정 전 화면.

- **비용**: 토큰 11+ · 문서 7곳 · 테스트 1 · **학습자 70 라우트 전체 재캡처·재비평**(04-application §4-1 의 "아래로 흐르는 지점" 이 색이라 전 화면이 바뀐다).
- **위험**: ① 틴트 면 + 브랜드색 잉크 + 세리프·산세리프 짝이 겹치면 own-portfolio 판정 「셋 중 둘 공유 = 쌍둥이」를 Tines 에 대고 해도 걸린다(팔레트 방식 · 서체 짝 구성). ② brief 의 앞선 진단 "문제는 방향이 아니라 생성 장치" 와 반대 방향. ③ DESIGN.md 방향 문단은 2026-09-18 거버넌스(DD-01~12)가 막 확정한 것이다.

## 4. [D] 측정 — 옵션별 예상

측정 가능한 것은 값, 구현 뒤에만 잴 수 있는 것은 "목표".

| 층 | 항목 | Tines | ① | ② | ③ |
|---|---|---|---|---|---|
| L1 | 개념 설명(동사 있음 %) | 50% | 목표 100% | 목표 100% | 목표 100% |
| L1 | 액센트 면적(삽화 안) | 19.7%(화면) | `--ju` ≤10% | ≤10% | ≤10%(주묵) + 틴트 면 별도 |
| L1 | 팔레트 수 | 6~8 | 5 | 5 | 5(삽화) · 화면은 10 |
| L1 | 선 굵기 종류 | 2 | 2 | 2 | 2 |
| L1 | 액자 | 사용 | 사용 | 사용 | 사용 |
| L2 | 격자 무대 사용률 | 33%(페이지) | 0 | 삽화 100% · 공개 히어로+빈 상태 | 같음 |
| L2 | 섹션 리듬 준수 | 4패턴 | — | 공개·빈 상태 100% 목표 | 같음 |
| L2 | 제목/본문 비율 | 4.0 / 2.4 | 2.6 / 1.9(현재) | 3.5 / 2.3 (창 안) | 3.5 / 2.3 |
| L2 | 여백 리듬 | 156px | 64px 최대 | 160px | 160px |
| L2 | 큰 도형 비율 | 15~31 · 60 · 30% | 창 안 | 창 안 | 창 안 |
| L3 | 소재 재현 | — | 0 목표 | 0 목표 | 0 목표 |
| L3 | hex 정확 일치 | — | 0 | 0 | 0 |
| L3 | hex ΔE2000 < 2 (제안) | — | 0(기존 `--bg` 예외) | 0(같음) | **틴트 5색이 Tines 틴트와 가까워질 위험** — 색 선정 때 검사 필수 |
| L3 | 서체 | Roobert · Reckless | 0 | 0 | 0 |
| L3 | 경로 유사 | — | 0 | 0 | 0 |

**L3 검사 보강 제안**: 정확 일치만 보면 `#FCF9F5` ↔ `#FBFAF6` 처럼 채널 1 차이가 통과한다. Gate 5 `style-gate` 는 **ΔE2000 < 2 도 일치로 센다**. 이 조사 전에 있던 토큰(`--bg` 등, `git log` 로 날짜 확인)은 예외 목록에 두고 **새로 만드는 색만** 검사한다.

## 5. 사람이 정할 것

1. **유사도 상한**: ① / ② / ③ — 에이전트 권고 **②**.
2. **P1·P2 공통 개정 승인**(어느 옵션이든 필요): 삽화를 "서명 아님 · N1 개념만 · 학습 중 0" 조건으로 들인다.
3. ✋ 인벤토리 확인 4건: `ModuleHero` 에 삽화를 다시 얹을지(v06.30 장식 제거 이력) · `PairFlipMascot` 부엉이 캐릭터 유지 여부(A4) · 파비콘 재생성 여부 · 표지 코드(`cover.ts`·`cover-art.ts`)는 별도 정본 유지(권고).
4. 🔴 웹 참조 깨짐 3건(`/apple-touch-icon.png` · `/icons/icon-192.png` · `/icons/icon-512.png`, 그리고 `manifest.json` 의 폐기 색)은 옵션과 무관하게 고쳐야 한다 — Gate 6 까지 기다릴지, 지금 따로 고칠지.
