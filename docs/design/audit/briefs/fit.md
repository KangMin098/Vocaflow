# 브리프 — `/fit` 지문 진단 (S003) · `/fit/s/[payload]` 공유 결과 (S004)

> 생성 2026-09-18 · Claude Code · 근거: [카드](../cards/public/fit.md) · [카드 공유](../cards/public/fit_s_payload.md) · [판정](../verdict.md). 4안은 만들지 않았다.
> 공유 결과 화면은 같은 클라이언트(`PublicFitClient`)를 재사용하므로 한 브리프로 묶는다.

## 목적 · 여정 위치
- JTBD(교사): "수업에 쓸 지문을 골랐을 때, 우리 반 학년에 맞는지 바로 알고 싶다, 그래서 3분 안에 학급에 던진다"
- **여정 ① 첫 방문 → 증명**의 도구 본체(랜딩 CTA 의 목적지) · 로그인 없이 열린 유일한 도구(D1) · 렌즈 6(교사의 3분).

## 현재 골격 (판정: 평균)
- 첫 시선 = **무채색 산세리프 textarea**(첫 뷰포트 절반 — 영어에 산세리프 금지) + 학년 **막대 목록**. 판정 문장은 한국어 이탤릭(금지), 「60.2%」 는 CTA 와 같은 빨강.
- **선언 `@form 채색 지문` 과 렌더 불일치** — 입력은 칠해지지 않고(`PublicFitClient.tsx:227`), 고스트 마커는 `/fit` 이 import 하지 않는 `TextFitVerdict` 에만 있다. 선언을 쓴 것은 2026-09-18 앞선 세션(PROGRESS FAIL 표).
- 공유 결과: 받은 사람이 보러 온 **결과가 빈 입력칸 뒤로 밀린다**(코드 추정, 유효 payload 미캡처).
- 랜딩 `/` 는 같은 자산으로 **서명 있음** — 도구가 광고보다 평범하다.

## 자산

| 자산 | 값 | 어디 |
|---|---|---|
| 붙여 넣은 지문의 커버리지 | 코드 — 700ms 디바운스 후 분석 | `lib/textfit/coverage.ts` · `PublicFitClient.tsx:116,148` |
| 학년별 % | 코드 | 현재 막대 목록 |
| 공유 payload | URL(서버 저장 없음) | `share.ts:231` |
| 랜딩의 채색 지문 렌더 | 코드 | 씨앗 **S3** `CoverageHero.tsx` |
| 고스트 마커 | 코드 | 씨앗 **S4** `TextFitVerdict.tsx` |

## G1 축 후보
1. **채색 지문** — 붙여 넣은 지문이 **그 자리에서 칠해지고**(입력칸 = 결과), 학년 슬라이더를 움직이면 색이 바뀐다 — 랜딩 S3 의 몸짓을 도구에 그대로.
2. **시험지 사물** — 결과를 **학급 배포용 한 장**(2단 판면 · 어려운 단어 난외 주석 · 학년 판정 도장)으로 — 교사가 바로 인쇄/공유(`fit_worksheet_printed` 이미 있음).

## 서명 후보
- 학년 슬라이더 이동 → 지문의 모르는 낱말 면이 200ms 에 바뀐다(랜딩과 같은 서명 — 제품 전체 서명의 통일).

## 제약
- I1–I8(공개 화면 증명 규칙) · 입력 0 으로도 예시가 칠해져 있어야(I2) · 영어는 Lora · 한국어 이탤릭 금지 · break-keep(I7 — 캡처에서 「바/로」 쪼개짐).
- 랜딩의 "빨간 물결 밑줄" 이 맞춤법 오류처럼 읽힌다는 판정 — 면 채색으로 통일할지 이 발산에서 같이 정한다.

## 성공 지표
- 기존: `fit_viewed` · `fit_analyzed` · `fit_shared` · `fit_share_opened` · `fit_signup_clicked` · `fit_worksheet_printed`.
- 현재 실측: `fit_viewed` 64(계정 1) · `fit_analyzed` 1 — 표본 부족. 지표 변경 없이 비율(analyzed/viewed)로 본다.
