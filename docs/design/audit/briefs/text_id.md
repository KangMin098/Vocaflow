# 브리프 — `/text/[id]` 읽기 워크스페이스 (S053)

> 생성 2026-09-18 · Claude Code · 근거: [카드](../cards/reading/text_id.md) · [판정](../verdict.md) 2차 캡처 · DB 질의. 4안은 만들지 않았다.

## 목적 · 여정 위치
- JTBD: "영어 원문을 읽을 때, 내가 아는 단어와 흐려진 단어를 글 위에서 바로 보고 싶다, 그래서 모르는 것만 골라 붙잡는다"
- 제품의 L0–L2 본체. 여정 ③(허브 TodayReading → 여기) · 모드 알약 13개(Listen→Read→…→Dict)의 중심.

## 현재 골격 (판정: 평균)
- 첫 시선 = **무채색 산문 지문** — 칠해진 단어·3/2/1px 밑줄 0. 모든 단어가 `status:'new'` 로 하드코딩(`word-enrichment.ts:91`)이라 F1 밑줄이 한 종류(2px 점선)로 무너지고, 캡처에서는 그마저 보이지 않았다.
- 패널 수치 일부가 지어낸 값(`memoryStats` `page.tsx:471` · softQuote `:659` · `MODE_STATUS` 목업 `:47-60`) — **I5 위반**.
- 한국어 격려 문장이 **이탤릭**(금지) · 모드 알약 13개가 1280 에서 잘리고 390 에서 4개만 보인다 · 390 플레이어 바가 지문 아래 절반을 덮는다.
- `@form 채색 지문` 선언과 렌더 불일치(선언은 2026-09-18 앞선 세션이 붙임).

## 자산 (DB 실측)

| 자산 | 값 | 어디 |
|---|---|---|
| 글 ↔ 내 단어 | `vocabularies.text_id` · `lemma` — **조인 키가 이미 있다** | 2,249행 |
| 단어별 R(t) | `stability · last_review_at` 전부 보유 | `lib/srs/fsrs.ts` |
| 밑줄 두께 문법 | F1 3/2/1px · new 점선 | `DecayUnderline` · DESIGN_SYSTEM §✒ F1 |
| 글 커버리지 | 코드 | `lib/textfit/coverage.ts` |

→ "모든 단어가 new" 는 **데이터 부재가 아니라 코드 공백**이다(조인만 하면 된다 — 드레인 불필요).

## G1 축 후보
1. **채색 지문 × 망각** — 원문 자체가 조작면(렌즈 2): 모르는 낱말 면 + 아는 낱말 밑줄 두께 = 지금의 R(t). 낱말을 누르면 그 자리에서 뜻·곡선.
2. **망각** — 여백(난외)에 **이 글에서 흐려지는 단어들의 7일 곡선** — 다시 읽기가 곡선을 들어 올린다.
3. **시험지 사물** — 모드 알약 13개 대신 **한 장의 판면에 난외 주석**(Listen/Words/Quiz 는 난외 기호) — 모드 전환 비용 제거(원칙 6).

## 서명 후보
- 한 번 읽고 나면(끝까지 스크롤) 이 글에서 흐려졌던 낱말들의 밑줄이 한 단계 얇아진다 — 페이드 300ms(7종 중 페이지 페이드 범위 — §5.2 확인).

## 제약
- 지어낸 수치 제거(I5) · 한국어 이탤릭 제거 · 영어 Lora · 모션 7종 · 390 에서 지문이 플레이어에 가리지 않을 것 · 색 단독 정보 금지(두께·sr-only 병행 — F1 이미 충족).

## 성공 지표
- 기존: `screen_viewed`(text / text-read 30).
- **신설 필요**: `reading_word_opened { state: 'stable'|'shaky'|'risk'|'new' }`(닫힌 열거형) · 끝까지 읽음은 진행 행에서 파생(D4).
