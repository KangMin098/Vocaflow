# 브리프 — `/diagnostic` 어휘 진단 (S022)

> 생성 2026-09-18 · Claude Code · 근거: [카드](../cards/home/diagnostic.md) · [판정](../verdict.md) · DB 질의. 4안은 만들지 않았다.

## 목적 · 여정 위치
- JTBD: "처음 왔을 때, 내 영어 수준을 5분 안에 알고 싶다, 그래서 지금 읽을 수 있는 책부터 시작한다"
- **여정 ② 활성화 3단계** — 결과 후 추천 세트 구독 → `/flashcard/play` 직행(`DiagnosticClient.tsx:517-535`). 전역 헤더 문구도 "5분 진단이 끝나면 312권 중 지금 읽을 수 있는 책이 정해져요" 로 이 화면을 약속한다.

## 현재 골격 (판정: 평균)
- 첫 시선 = **남색 SaaS 히어로 카드**(금색 배지·칩·흰 버튼, 코드상 보라 그라디언트 `:615` · glass 알약 `:622`) + 같은 모양 행 카드 반복. 결과는 72px 「V{n}」 숫자(`:916-918`).
- 빈 상태 "사용 가능한 진단이 없어요." 에 다음 한 걸음 없음 — **D5 위반**(`:763`).

## 자산 (DB 실측)

| 자산 | 값 | 어디 |
|---|---|---|
| 도서별 레벨 커버리지 | 발행 312권 중 **311권**이 V1–V10 각각의 아는 단어 % | `library_books.lexical_coverage`(jsonb 키 1…10) |
| 도서 난이도 | 312/312 | `book_v_level` |
| 진단 결과 | V-Level | 진단 RPC(카드) |
| 커버리지 계산 | 코드 | `lib/textfit/coverage.ts` |

## G1 축 후보
1. **채색 지문** — 진단 문항에 답할 때마다 옆의 실제 지문이 내 추정 레벨로 **다시 칠해진다**(S3 의 조작을 진단 자체에 이식). 진단이 곧 증명.
2. **환경 변형** — 진단이 진행될수록 **312권 서가가 읽을 수 있는 책부터 불이 켜진다**(`lexical_coverage` × 추정 V). 결과 화면 = 켜진 서가, 숫자 「V{n}」 는 부제.

## 서명 후보
- 한 문항 답할 때마다 사정권(95–98%) 안으로 들어오는 책 수가 서가 위에서 켜진다 — 페이드 200ms(모션 7종 중 "진행률" 의 형태 변형으로 해석 가능한지 §5.2 확인 필요).

## 제약
- 문항 수·시간 예산(5분 약속) 유지 · D7 전환 수 유지(결과 → 학습 직행) · 보라/그라디언트 제거(평균 신호 라쳇) · 빈 상태 D5.

## 성공 지표
- 기존: `screen_viewed`(diagnostic) · `wayfinder_cta_clicked`.
- **신설 필요**: `diagnostic_completed { vLevel: number; booksUnlocked: number }` · 결과 → 첫 학습 시작 전환(D7 경로 — 파생 가능하면 DB 행으로).
