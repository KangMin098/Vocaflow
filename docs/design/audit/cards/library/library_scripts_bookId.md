# S033 `/library/scripts/[bookId]` — 짧은 글 공개 미리보기 + 학습 리졸버

> 생성 2026-09-18 · Claude(감사 서브에이전트) · 근거: 메인 워크트리 코드 읽기 + `tools/screen-graph.mjs` 결과. 카드 ≤ 40줄.

## 목적
- JTBD: "검색으로 글 하나에 도착했을 때, 로그인 없이 본문을 읽고 이 글의 낱말과 다음 글을 보고 싶다, 그래서 계정을 만들어 학습으로 잇는다"
- 주 사용자: 방문자(비로그인 SEO 착지 `page.tsx:5-18`) — 로그인 학생은 즉시 `/text` 로 넘어감(`:174-177`) · 인지 계층: L0
- 이름 주의: 파라미터 이름은 `bookId` 지만 값은 **글 id**(`ArticleCard.tsx:97`)

## 흐름
- 진입: 정적 in=0 은 오판 — 템플릿 링크 `ArticleCard.tsx:97,173,217` · 자기 링크(이어 읽기 `page.tsx:372`) · sitemap(주석 `page.tsx:12`, 추정)
- 단계: 1. 「Dispatches」 뒤로+읽는 시간 2. 제목 h1+메타(CEFR·V·단어 수) 3. 본문 문단 4. 출처·라이선스 5. 이 글의 낱말(`:321-350`) 6. 이어서 읽을 글 3(`:363-385`) 7. 로그인 CTA(`:387-400`)
- 완료 조건: 로그인 → `startArticleLearning` → `/text/[id]?mode=read`
- 1차 행동: 「로그인하고 …」(`page.tsx:394`) — 본문 **아래 맨 끝** · 보조: 이어 읽기, 원문 링크
- 나가는 길: /library/scripts(`:229`) · 다음 글 · /login · 원문(`:295`) · 글이 아니면 /library/books/[id](`:166`)

## 상태 5종
| 상태 | 있나 | 근거 file:line | D5(다음 한 걸음) |
|---|---|---|---|
| 빈 | 있음 | 본문 없음 「본문을 준비하고 있어요」 `page.tsx:283-286` · 없는 id → 도서 라우트로 `:166` | ✗ 문구만(아래 이어 읽기가 있을 때만 출구) |
| 로딩 | 전역만 | `app/loading.tsx` | — |
| 오류 | 폴백 | 학습 시작 실패 시 미리보기로 떨어짐 `page.tsx:175-176` | ○ 미리보기 유지 |
| 부분 | 있음 | 낱말·이어 읽기 0 이면 섹션 생략 `:321, :363` | — |
| 완료 | 있음 | 로그인 사용자 → `/text` redirect `:176` | ○ |

## 자산
- N1 자산: V-Level 칩(`page.tsx:249-251`) · 사전 낱말 샘플(`fetchArticleVocabPreview` `:142-145`) · 같은 V-Level 이어 읽기(`:113-136`) — 역할: **칩·목록**
- 형태 씨앗: 없음
- 학습과학 원칙: #5 Context-Dependent(글 안의 낱말) · #3(같은 V-Level 다음 글)

## 첫 시선 (코드 기준 — Gate 3 캡처로 확정)
- 첫 뷰포트: 720px 단일 컬럼 — 제목 24/30px + 메타 한 줄 + 본문 첫 문단(`page.tsx:209, 240-288`)
- 골격 판정: **산문**(읽기 판면). G1 아님 — 다만 이 그룹에서 장식이 가장 적다
- 평균 신호(정적): 0

## 근거
- `apps/web/src/app/(main)/library/scripts/[bookId]/page.tsx:161-185` — 리졸버 분기(글 아님/로그인/미리보기)
