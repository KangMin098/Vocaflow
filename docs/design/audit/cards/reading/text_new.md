# S055 `/text/new` — 새 스크립트 추가

> 생성 2026-09-18 · Claude Opus 5 (서브에이전트) · 근거: 메인 워크트리 코드 읽기 + `tools/screen-graph.mjs` 결과. 카드 ≤ 40줄.

## 목적
- JTBD: "내가 고른 영어 글이 있을 때, 붙여넣어 학습 자료로 만들고 싶다, 그래서 그 글로 읽기·단어 학습을 시작한다"
- 주 사용자: 학생(자기주도) · 교사(추정, `SendToClassButton` 경유) · 인지 계층: L0 입구

## 흐름
- 진입: `/text` 행동 카드·빈 상태 `TextHubContent.tsx:188` · `EmptyState.tsx:41` · `/dictate` · `/teacher` · `/wordvault`
- 단계: 1. 입력 방식 탭(`page.tsx:276`) 2. 구조 선택 단일/책(`:282-320`) 3. 제목·본문(또는 샘플 `:386-393`) 4. (선택) AI 단어 추출(`:396-404`) 5. 저장(`:453-460`)
- 완료 조건: 저장 성공 → 토스트 + `/text` 로 이동(`page.tsx:160-161,175-179`) — 방금 만든 글이 아니라 허브로 간다(D5 경로 +1 화면)
- 1차 행동: 저장(`page.tsx:453-460`) · 보조: 샘플 적용 · 초안 이어 쓰기(`:244-272`)
- 나가는 길: 「허브」 `/text`(`:202-210`) · `/teacher`(`SendToClassButton.tsx:46`)

## 상태 5종
| 상태 | 있나 | 근거 file:line | D5(다음 한 걸음) |
|---|---|---|---|
| 빈 | ○ | 빈 폼 + 샘플 스크립트 `page.tsx:386-393` | ✓ 샘플 적용 |
| 로딩 | ○ | 「저장 중...」 `page.tsx:196` · 버튼 disabled `:456` | — |
| 오류 | ○ | 저장 실패 배너 `page.tsx:444-450` | △ 문구만, 재시도는 같은 버튼 |
| 부분 | ○ | 초안 복구 배너 `page.tsx:244-272` · 파일/URL 「준비 중」 `:422-438` | ✓ 「직접 입력으로 이동」 |
| 완료 | ○ | 토스트 후 `/text` `page.tsx:160-161` | △ 새 글을 여는 대신 허브 |

## 자산
- N1 자산: 커버리지 적합도(`TextFitVerdict` `ExtractionPanel.tsx:518`) · VRL 다축 추출 — 역할: **칩·눈금**, 추출 결과가 있을 때만(`:516`)
- 형태 씨앗: S4 적합도 눈금 + 고스트 마커 — 폼 아래 패널 안(△)
- 학습과학 원칙: #6 Cognitive Load(적합도로 「이 글이 맞나」 먼저) · #5 Context-Dependent(내 글)

## 첫 시선 (코드 기준 — Gate 3 캡처로 확정)
- 첫 뷰포트: 60px 헤더 + 설명형 h2 「직접 입력한 스크립트를 내 라이브러리에 추가합니다」(`page.tsx:232-240`) + 입력 탭 + 구조 토글
- 골격 판정(코드 추정): **폼** — 평균. 붙여넣는 순간 지문이 채색되는(S3) 자리인데, 적합도는 추출 후 패널 안에만
- 헤더 부제 「직접 입력 · 파일 · URL」(`page.tsx:196`)과 파일/URL 준비 중(`:428`)이 어긋난다
- 평균 신호(정적, 자기 트리): 21 (rounded-big 9 · ai-purple 4 · shadow-heavy 3 — 예: 저장 버튼 `hover:shadow-lg` `:457`)

## 근거
- `apps/web/src/app/(main)/text/new/layout.tsx:10-13` — metadata(페이지가 client)
- `apps/web/src/app/(main)/text/new/page.tsx:145-185` — handleSave 단일/책 분기
