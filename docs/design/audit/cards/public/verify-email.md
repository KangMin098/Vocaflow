# S012 `/verify-email` — 이메일 인증 대기

> 생성 2026-09-18 · Claude Opus 5 · 근거: 메인 워크트리 코드 읽기 + `group-public.json`. 카드 ≤ 40줄.

## 목적
- JTBD: "가입 직후 인증 메일을 기다릴 때, 무엇을 하면 되는지·메일이 안 오면 어떻게 할지 알고 싶다"
- 주 사용자: 신규 가입자 · 인지 계층: 없음(인증)

## 흐름
- 진입: S010 가입 후 **세션이 없을 때만** `router.replace`(`(auth)/signup/page.tsx:171-173`) — 정적 링크 0 은 정상(고아 아님). 현 프로젝트는 autoconfirm=true 라 실제 도달은 드묾(`signup/page.tsx:9-10`, 추정)
- 단계: 1. 주소 확인 2. 3단계 안내(메일 열기 → 링크 클릭 → 자동 로그인) 3. 안 오면 재발송(쿨다운)
- 완료 조건: 메일 링크 → `/api/auth/callback` → 복귀 경로(`verify-email/page.tsx:7`) — 이 화면 밖에서 끝남
- 1차 행동: 「인증 메일 다시 보내기」(`:165-181`) · 보조: 다른 이메일로 다시 가입(`:196`), 로그인 화면으로(`:209`)
- 나가는 길: `/signup?next=` · `/login?next=`(`loginUrlWithReturn`) — 둘 다 복귀 경로 유지

## 상태 5종
| 상태 | 있나 | 근거 file:line | D5(다음 한 걸음) |
|---|---|---|---|
| 빈 | ✅ `?email` 없음 → 재발송 비활성 + 이유 문구 | `:36,49,185-190` | ✅ 다시 가입 링크 |
| 로딩 | ✅ "발송 중..." · 쿨다운 초 카운트 · Suspense | `:171-179,235` | — |
| 오류 | △ 토스트로만 | `:80,87` | △ |
| 부분 | n/a | — | — |
| 완료 | ✅ 재발송 성공 토스트 | `:85` | — |

## 자산
- N1 자산: **없음** — 역할: 없음
- 형태 씨앗: 없음
- 학습과학 원칙: 해당 없음(7 — "마지막 한 단계만 남았어요")

## 첫 시선 (코드 기준 — Gate 3 캡처로 확정)
- 첫 뷰포트: elevated 카드 — 큰 메일 아이콘 원 · H1 "이메일을 확인해주세요" · 주소 · 번호 3단계 목록 · 재발송 버튼
- 골격 판정: **폼형 안내 카드**(단계 목록 `<ol>`). G1 축 없음 — 대기 화면이라 합당
- 평균 신호(정적 6): heavy shadow 2 · big radius 3 · float-hover 1
- 좋은 점: 로그인·가입 링크가 모두 복귀 경로를 들고 감(`:196,209`) — S006·S010 은 서로에게 넘길 때 버림

## 근거
- `apps/web/src/app/(auth)/verify-email/page.tsx:10-13` — 재발송 무반응·예외 삼킴 수정 경위
- `apps/web/src/app/(auth)/verify-email/page.tsx:52-58` — 쿨다운 타이머
