# S010 `/signup` — 회원가입

> 생성 2026-09-18 · Claude Opus 5 · 근거: 메인 워크트리 코드 읽기 + `group-public.json`. 카드 ≤ 40줄.

## 목적
- JTBD: "써 보고 마음에 들었을 때, 빠르게 계정을 만들고 원래 가려던 곳(초대 학급 등)으로 이어지고 싶다"
- 주 사용자: 신규 학생(초대 학생 포함 `:17-19`) · 교사 · 인지 계층: 없음(인증)

## 흐름
- 진입: 9개 화면 — S001(`LandingCta.tsx:42`) · S002 · S003/S004(`LevelProfilePanel.tsx:269`) · S005(`join/[code]/page.tsx:107`, `next` 동반) · S007 · S006 · S009 · S012
- 단계: 1. 이름 2. 이메일 3. 비밀번호 4. 약관·개인정보 필수 체크 + 마케팅 선택 5. 제출 → 세션 유무 분기(`:157-174`)
- 완료 조건: 세션 있음 → `router.replace(returnTo)` 기본 `/hub`(`:163`, `lib/auth/redirect.ts:26`) · 없음 → `/verify-email?email=&next=`(`:171-173`)
- 1차 행동: 「가입하고 학습 시작하기」(`:365-385`) · 보조: 로그인(`:201`), 약관/개인정보 링크(`:315,334`)
- 나가는 길: `/login` · `/terms` · `/privacy` · 로고 `/`

## 상태 5종
| 상태 | 있나 | 근거 file:line | D5(다음 한 걸음) |
|---|---|---|---|
| 빈 | ✅ 제출 시 필드별·약관 오류 | `:82,91-97` | ✅ |
| 로딩 | ✅ "가입 중..." · Suspense 스켈레톤 | `:373-376,393-404` | — |
| 오류 | ✅ 인라인 배너 · 이미 가입된 이메일 감지 | `:140-153,208-215` | △ "이미 가입된 이메일입니다" 에 로그인/재설정 링크 없음(`:151`) |
| 부분 | n/a | — | — |
| 완료 | ✅ 토스트 "환영합니다 🎉" 후 이동 | `:159,170` | ✅ `/hub` 또는 복귀 경로 |

## 자산
- N1 자산: **없음** — 역할: 없음. `/fit` 에서 본 결과를 가입 후로 넘기는 장치 없음(추정: `fit_signup_clicked` 링크에 `next` 없음 `LevelProfilePanel.tsx:269`)
- 형태 씨앗: 없음
- 학습과학 원칙: 해당 없음(7 — "지금은 모든 기능이 무료" 부담 완화 `:190-192`)

## 첫 시선 (코드 기준 — Gate 3 캡처로 확정)
- 첫 뷰포트: elevated 카드 — 라벨 · H1 "영어 학습을 시작하세요" · 입력 3 + 체크 3 · 버튼 (390px 에선 버튼이 접힘 아래 — 추정)
- 골격 판정: **폼(중앙 카드)**. 입력 6개 — 가입 전 가치 확인(D1)은 `/fit` 이 담당
- 평균 신호(정적 6): heavy shadow 3 · big radius 2 · float-hover 1 (login 과 같은 Card elevated 구성)
- 누락: 「로그인」 링크가 `next` 를 버림(`:201`) — 초대 학생이 "이미 계정 있음"으로 갈아타면 반 연결 소실(추정). 가입 계측 이벤트 없음(`track` 0 — grep)

## 근거
- `apps/web/src/app/(auth)/signup/page.tsx:125` — `emailRedirectTo` 에 `next` 실음
- `apps/web/src/app/(auth)/signup/page.tsx:22-23` — 동의 시각을 user metadata 에 임시 저장(TODO)
