# S009 `/reset-password` — 비밀번호 재설정 (요청 · 새 비밀번호 2모드)

> 생성 2026-09-18 · Claude Opus 5 · 근거: 메인 워크트리 코드 읽기 + `group-public.json`. 카드 ≤ 40줄.

## 목적
- JTBD: "비밀번호를 잊었을 때, 메일 링크로 새 비밀번호를 정하고 싶다, 그래서 학습 기록을 잃지 않고 돌아간다"
- 주 사용자: 기존 학생·교사 · 인지 계층: 없음(인증)

## 흐름
- 진입: S006 「비밀번호 찾기」(`(auth)/login/page.tsx:169`) · `/settings` · 메일 recovery 링크 → 콜백 → `?mode=update`(`reset-password/page.tsx:3-10,55`)
- 단계(request): 1. 이메일 입력 2. `resetPasswordForEmail`(`:96`) 3. "이메일을 보냈어요" 안내(`:256-300`)
- 단계(update): 1. 새 비밀번호·확인 2. `updateUser`(`:123`) 3. `/hub`
- 완료 조건: `router.replace('/hub')`(`:129`) + 토스트 "비밀번호가 변경되었습니다"
- 1차 행동: request=「재설정 링크 받기」(`:370-380`) / update=저장(`:216`) · 보조: 다른 이메일로 다시 시도(`:291-297`)
- 나가는 길: `/login`(`:303,394`) · `/signup`(`:400`) · `/hub`

## 상태 5종
| 상태 | 있나 | 근거 file:line | D5(다음 한 걸음) |
|---|---|---|---|
| 빈 | ✅ 제출 시 이메일 검증 | `:90` | ✅ |
| 로딩 | ✅ 모드 판정 스피너 · Suspense | `:141-154,411-420` | — |
| 오류 | △ **토스트로만**(인라인 없음) — login 은 인라인 배너 | `:101,108,125,133` vs `login/page.tsx:131` | △ 토스트 사라지면 흔적 없음(추정) |
| 부분 | n/a | — | — |
| 완료 | ✅ 발송 안내 + "다음 단계" 박스(1시간 유효) | `:283-290` | ✅ 로그인·재시도 |

## 자산
- N1 자산: **없음** — 역할: 없음
- 형태 씨앗: 없음
- 학습과학 원칙: 해당 없음(7 — "걱정마세요, 도와드릴게요" 공감 카피 `:328`)

## 첫 시선 (코드 기준 — Gate 3 캡처로 확정)
- 첫 뷰포트: elevated 카드 — 라벨 · H1 "비밀번호를 잊으셨나요?" · 입력 1 · 버튼 · 하단 링크 2
- 골격 판정: **폼(중앙 카드)** — 단계별 원형 아이콘 배지(`KeyRound` 원 `:162-164`)
- 평균 신호(정적 11, 그룹 최고): heavy shadow 5 · big radius 5(Card elevated `rounded-xl` 4벌 `:143,160,258,325,413`) · float-hover 1
- 접근성: 하단 「← 로그인으로」「회원가입 →」 11px 텍스트 링크에 44px 높이 없음(`:393-404`) — login 의 같은 링크는 `min-h-[44px]` 로 고쳐짐(`login/page.tsx:170-171`)

## 근거
- `apps/web/src/app/(auth)/reset-password/page.tsx:51-70` — `?mode=update` 우선, 세션은 보조
- `apps/web/src/app/(auth)/reset-password/page.tsx:100` — 미가입 이메일 침묵(계정 열거 방지)
