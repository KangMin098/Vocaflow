# S002 `/about` — 소개 (철학 4 · 학습과학 7 · 모듈 7)

> 생성 2026-09-18 · Claude Opus 5 · 근거: 메인 워크트리 코드 읽기 + `group-public.json`. 카드 ≤ 40줄.

## 목적
- JTBD: "가입을 망설일 때, 이 제품이 어떤 원칙으로 가르치는지 알고 싶다, 그래서 믿고 시작할지 정한다"
- 주 사용자: 방문자(특히 교사) · 인지 계층: 없음(공개)

## 흐름
- 진입: 셸 헤더(`(marketing)/layout.tsx:43`) · S001 `/`(`page.tsx:62,158`) · S003 `/fit`(`fit/page.tsx:208`)
- 단계: 1. 미션 히어로 + 소개 영상 2. 다른 점 영상 3편 3. 철학 4 카드 4. 과학 7 카드 5. 모듈 7 타일 6. 안티패턴 5 7. CTA
- 완료 조건: `/signup` 클릭(`about/page.tsx:412`) — 계측 이벤트 없음(추정: 이 화면 자체 `track` 0, 영상만 `ComponentVideo.tsx:57,70`)
- 1차 행동: 「무료로 시작하기」 `/signup` · 보조: 「요금제 보기」, 모듈 타일 7개(로그인 필요 라우트 — 추정)
- 나가는 길: 모듈 링크 `/text` `/wordvault` … `/dashboard`(`:126-132`), `/video/[id]`(`:194`)

## 상태 5종
| 상태 | 있나 | 근거 file:line | D5(다음 한 걸음) |
|---|---|---|---|
| 빈 | △ 영상 미발행이면 영상 자리 무표시 | `about/page.tsx:179` · `components/video/ComponentVideo.tsx:81` | ✅ 하단 CTA 존재 |
| 로딩 | 전역만(정적 페이지) | `app/loading.tsx` | — |
| 오류 | 전역만 | `app/error.tsx` | ✅ |
| 부분 | ✅ 영상 없이 산문만 | `:173,179` | — |
| 완료 | n/a | — | — |

## 자산
- N1 자산: **없음** — 전부 상수 배열(`:34-133`). 영상만 파이프라인 산출물
- 형태 씨앗: 없음
- 학습과학 원칙: 1–7 을 **설명**만 함(`:65-115`) — 화면이 원칙을 수행하지 않음
- 사실 오류: Flashcard "SM-2 간격 반복"(`:128`), "Ebbinghaus, SM-2"(`:76`) — 실제 스케줄러는 FSRS(`ts-fsrs`, AGENTS.md). 모듈 "7개" 이나 실제 9 모듈

## 첫 시선 (코드 기준 — Gate 3 캡처로 확정)
- 첫 뷰포트: 그라디언트 배경(`:146`) 위 pill 배지+Sparkles(`:148-150`), 52px H1 + 그라디언트 글자(`:154`), Lora 인용문, 소개 영상
- 골격 판정: **산문 + 카드 격자** (2열 `:222` · 3열 `:188,273` · 7열 이모지 타일 `:323`). G1 축 없음
- 평균 신호(정적 10): 그라디언트 4(`:146,154,402` 등) · 3열 격자 3 · float-hover 2(`:330` scale-110) · AI purple 1 · 카드 안 radial blur 광원(`:232-233`)
- 모순: CTA 주석은 "그라데이션을 걷었다"(`:399`)인데 바로 아래 `bg-gradient-to-br`(`:402`)

## 근거
- `apps/web/src/app/(marketing)/about/page.tsx:126-132` — 모듈 아이콘이 이모지(📖🃏⚡) + 하드코딩 `#4A9FCF`(`:129`)
- `apps/web/src/app/(marketing)/about/page.tsx:365-369` — 안티패턴 목록(✕/✓)
