# S005 `/join/[code]` — 학급 초대 링크

> 생성 2026-09-18 · Claude Opus 5 · 근거: 메인 워크트리 코드 읽기 + `group-public.json`. 카드 ≤ 40줄.

## 목적
- JTBD: "선생님이 보낸 초대 링크를 눌렀을 때, 어느 반인지 확인하고 한 번에 들어가고 싶다, 그래서 과제를 받을 수 있다"
- 주 사용자: 학생(대부분 신규 가입자 `:12`) · 인지 계층: 없음(관문)

## 흐름
- 진입: 교사 허브 복사 버튼 `inviteUrl`(`components/teacher/TeacherClient.tsx:99`) · 워크시트 QR(`components/teacher/ClassWorksheet.tsx:65`) — 정적 링크 0 은 정상(고아 아님)
- 단계: 1. `peek_class_by_code` 로 반 이름·인원 확인(`:54,82-89`) 2a. 로그인 상태 → 「이 클래스에 참여하기」 폼 제출 2b. 비로그인 → 가입/로그인(`next` 유지 `:106-117`) 3. `/teacher` 로 이동
- 완료 조건: `joinClassByCode` 후 `redirect('/teacher')`(`:63-70`)
- 1차 행동: 「이 클래스에 참여하기」 / 「가입하고 참여하기」(`:97,110`) · 보조: 「이미 계정이 있어요」
- 나가는 길: `/teacher` · `/signup?next=` · `/login?next=` · 무효 코드 시 `/`

## 상태 5종
| 상태 | 있나 | 근거 file:line | D5(다음 한 걸음) |
|---|---|---|---|
| 빈 | ✅ 무효 코드 화면(가입 권하지 않음) | `:60,135-152` | △ 주석은 "`/teacher` 를 알려 준다"(`:133`) 인데 링크는 `/`(`:146`) |
| 로딩 | 서버 렌더(force-dynamic `:40`) — 전역 스피너 | `app/loading.tsx` | — |
| 오류 | ❌ 참여 실패를 삼킴(`void res`) | `:65-68` | △ `/teacher` 목록이 사실을 보여준다는 가정 |
| 부분 | ✅ 인원 0 → "첫 번째 참여자예요" | `:86` | — |
| 완료 | 이 화면엔 없음(`/teacher` 로 이동) | `:69` | — |

## 자산
- N1 자산: **없음**(학급 이름·인원·코드뿐) — 역할: 없음
- 형태 씨앗: 없음
- 학습과학 원칙: 해당 없음(7 Emotional — "첫 번째 참여자예요" 정도, 추정)

## 첫 시선 (코드 기준 — Gate 3 캡처로 확정)
- 첫 뷰포트: 520px 카드 1장 — 라벨·반 이름 H1·인원/코드·버튼 1–2개(`:76-120`)
- 골격 판정: **폼(단일 카드)**. G1 축 없음 — 관문 화면이라 합당(추정)
- 평균 신호(정적 0)
- 누락: 계측 이벤트 0(D2 — 공개 화면 진입 이벤트 없음, 서버 컴포넌트에 `track` 없음). 로그인 화면의 「회원가입」 링크가 `next` 를 버림(`(auth)/login/page.tsx:124`) → 초대→로그인→가입 경로에서 반 연결 소실(추정)

## 근거
- `apps/web/src/app/(marketing)/join/[code]/page.tsx:42-49` — noindex
- `apps/web/src/lib/teacher/invite-link.ts` — `invitePath`/`normalizeInviteCode`
