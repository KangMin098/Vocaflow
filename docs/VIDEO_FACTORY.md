# 영상 공장 (Video Factory)

> `packages/video-factory` — **플랫폼 구성요소를 영상으로 찍어 내는 공장.**
> 학습자 화면 임베드 · 광고 · YouTube 채널이 같은 설비에서 나온다.
> 최초 작성 2026-09-12. 여기 있는 수치는 전부 그날의 실측이다.

---

## 0. 한 줄 요약

영상 73편이 **어디에도 손으로 적혀 있지 않다.** 있는 것은 규칙 여덟이고,
플랫폼이 자라면 편수가 같이 는다.

```
설계도 73편 × 규격 3 = 컴포지션 219개
  소개 1 · 장점 3(=DIFFERENTIATORS) · 커리큘럼 1 · 시리즈 3(=SERIES_CATALOG)
  · 유형 28(=TYPE_GUIDE) · 활동 26(=framework/registry)
  · 학습방법 7(=총론 1 + FACETS 6) · 권장안 4(=STAGES 이동)
```

**실제로 두 번 그렇게 자랐다** — 2026-09-13 에 유형 설명 15종을 채우자 코드 한 줄 안 고치고
설계도가 47 → **62편**이 됐고, 같은 날 프레임워크(단계·면·처방 임계)를 번들에 실으니
규칙 둘이 더해져 62 → **73편**, 컴포지션 186 → **219개**가 됐다.

⚠️ 학습방법·권장안이 그때까지 **한 편도 없었던 이유는 규칙이 아니라 원료였다** —
번들에 단계와 임계값이 안 들어 있었고, 공장은 번들에 있는 것만 만든다. 「무엇을 더 만들까」를
물을 때 규칙부터 보면 답이 안 나온다. **원료를 먼저 본다.**

시리즈를 하나 만들면 코드 한 줄 안 고쳐도 영상이 **3개**(가로·세로·정사각) 는다.
회귀 `packages/video-factory/src/__tests__/factory.test.ts` 가 그 약속을 잠근다 — 구성요소가 늘었는데 영상이 안 늘면 실패한다.

---

## 1. 왜 Remotion 인가 (그리고 그 값)

| 후보 | 라이선스 | 한계 |
|---|---|---|
| **Remotion 4.0.524** ✅ | 오픈소스 + **4인 이상 법인은 유료** | 프레임마다 브라우저 스크린샷 — 느리다 |
| Motion Canvas | MIT | Canvas 2D — **제품 컴포넌트·CSS 토큰을 못 쓴다** |
| Revideo (Motion Canvas 포크) | MIT | 같은 한계. 렌더 API 가 붙어 있을 뿐 |

**고른 이유는 미감이 아니라 자산이다.** 이 공장의 값어치는 "제품이 실제로 쓰는 색·서체·계산"을
그대로 영상에 올리는 데 있다(`@vocaflow/design-tokens` 를 그대로 import 한다). Canvas 2D 도구를
고르면 디자인 시스템을 **한 벌 더** 구현해야 하고, 그 순간부터 제품과 광고가 갈린다.

⚠️ **라이선스**: Remotion 은 **3인 이하 팀은 무료**, 4인 이상 영리 법인은 좌석당 월 \$25(Creators)
또는 렌더당 \$0.01(Automators, 월 최소 \$100). 팀이 넷이 되는 날 정산 대상이다.
([remotion.pro/license](https://www.remotion.pro/license) · 2026-09-12 확인)
그래서 **설계도(`src/spec/`)에는 Remotion 이 한 글자도 없다** — 엔진을 바꿔도 설계도는 산다.

---

## 2. 큐 — 이게 없어서 「파이프라인」이 아니었다 (2026-09-13)

영상 62편을 찍어 발행했는데 **그 과정이 어디에도 남지 않았다.** 진행은 오직 **파일의 존재**로만
표현됐다(mp4 가 있으면 찍은 것). 그래서:

| 없던 것 | 무슨 일이 생기나 |
|---|---|
| 작업 단위 | 어느 편이 어느 단계인지 모른다 — Admin 이 manifest 와 파일을 **비교해 추론**했다 |
| 실패 기록 | 렌더 실패가 콘솔에 한 줄 찍히고 **사라진다**. 다음 사람은 실패가 있었는지도 모른다 |
| 이력 | 언제 찍었는지 모른다 — 수치가 얼마나 묵었는지의 근거가 없다 |

이 저장소의 다른 파이프라인(`book_quiz_jobs` · 큐레이션 잡)은 전부 이걸 갖는다.

```
video_jobs  — 편당 한 행
  stage: queued → voiced → rendered → packaged → published   (+ failed)
```

**단계는 앞으로만 간다.** 그냥 UPDATE 를 허용하면 늦게 끝난 단계가 먼저 끝난 단계를 덮어써
뒤로 돌린다(렌더를 두 번 돌리면 실제로 그렇게 된다). 그래서 순서를
`video_job_advance()` 한 곳에서만 정의하고, 낮은 단계로 가는 호출은 **조용히 무시**한다.

### 큐가 본 작업을 인질로 잡지 않는다

기록은 곁가지다. 그래서 큐 클라이언트는:

- 호출마다 **8초 타임아웃** — 이게 없으면 망이 느릴 때 기록 한 줄이 렌더를 멈춰 세운다
  (실측 2026-09-13: DB 가 응답하지 않는 동안 `enqueue` 가 10분을 넘겨도 안 끝났다)
- **한 번 막히면 그 실행 동안 끈다** — 없는 것은 다음 호출에도 없다. 62편에 62번
  타임아웃을 기다리지 않는다
- 예외를 **던지지 않는다** — 실측: 큐가 통째로 막힌 상태에서 렌더 1편 34.9초·실패 0

## 2-2. 기획·평가 — 파이프라인의 나머지 두 단계 (2026-09-14)

`/admin/video` 는 **기획·제작·평가**를 관리하는 자리인데, 오래 **제작만** 있었다.

### 기획 — 다음에 무엇을 찍을 것인가

「밀린 것」과 다르다. 공장은 번들에 있는 것을 **전부** 설계도로 만들므로 정상 상태에서
「안 만든 편」은 늘 0 이고, 화면은 "다 했다" 고 말한다. 그런데 **설계도 규칙이 아예 없는
후보**가 남아 있었다 — 교재 **권별**이 그랬다. 없는 것이 목록에 안 보이면 영원히 안 만들어진다.

```
주소 가능한 자리 92 · 설계도 있음 73 · 다음 18 · 막힘 1 · 덮개 80%
```

**순서는 재고로 정한다.** 재생 수로 정하는 것이 맞지만 지금 재생은 **0** 이다(채널이 없다).
없는 신호로 순위를 매기는 대신 지금 관측되는 것을 쓴다 — 그 구성요소 뒤에 있는 문항 수
(`volume-reading-6` 318,477 → `volume-syntax-2` 3,586).

⚠️ **재고 0 은 「순위가 낮은 것」이 아니라 「지금 만들면 안 되는 것」이다.**
실측 2026-09-14: `Vocaflow Reading Starter`(reading 1단)가 문항 0 이다. 찍으면 **빈 서가를
광고**하게 되므로 후보가 아니라 **막힘**으로 따로 낸다. 덮개 분모에서도 뺀다 —
만들 수 없는 것 때문에 영영 100%가 안 되면 그 수치로 아무 결정도 못 한다.

정본 `packages/video-factory/src/catalog/plan.ts` (순수) · `pnpm video plan` ·
Admin 「기획」 탭. Admin 은 커밋된 원천 + **DB 실측**으로 같은 판단을 다시 한다 —
번들은 커밋하지 않으므로 운영에는 없다(`lib/video/components.ts` 와 같은 이유).

### 평가 — 찍은 것이 규격 안인가

**외부에 공개된 규격이 있는 축만 합격/불합격을 매긴다.**

| 축 | 규격 | 출처 |
|---|---|---|
| 음량 · 트루 피크 | −14 LUFS ±1 · ≤ −1 dBTP | YouTube |
| Shorts 길이 | ≤ 60초 (세로 규격에만) | YouTube Shorts |
| 자막 노출 길이 | 0.83~7초 | Netflix TTSG |
| 자막 읽기 속도 | ≤ 13 CPS(학습자) · 17(광고) | Netflix TTSG |
| 자막 없는 컷 · 수치의 출처 | 0개 · 전부 있음 | 이 저장소 규칙 |
| **첫 컷 길이** | **없음 — 재기만 한다** | — |

마지막 줄이 이 설계의 핵심이다. "첫 3초가 중요하다" 는 말은 널리 쓰이지만 **근거로 댈 공개
수치가 없다.** 숫자를 정해 두면 그 숫자가 다음 사람에게 근거처럼 보인다. 재생 데이터가
쌓이면 그때 **우리 데이터로** 선을 긋는다(지금 재생은 0이다).

**「못 잰 축」은 합격이 아니다.** 음량은 파일을 읽어야 나오는데 그 파일이 없으면 못 잰다 —
0 으로 접으면 조용히 통과한다. 그래서 합격·불합격·**못 잼** 셋을 따로 센다.

정본 `packages/video-factory/src/spec/evaluate.ts` (순수) · `pnpm video evaluate [--full]` ·
결과는 `video_jobs` 의 평가 열에 남고 Admin 「평가」 탭이 읽는다.
**단계(`stage`)는 건드리지 않는다** — 평가는 제작의 단계가 아니라 그 위의 판정이고,
섞으면 「평가에서 떨어졌다」가 「아직 안 찍었다」로 읽힌다.

### 붙이자마자 나온 결함 — 자막 7초 초과 15컷

컷 길이는 **나레이션 실측**이 정하는데, 그 자리(`voice/edge-tts.ts`)가 최소값(5/6초)만 지키고
**최대 7초는 검사하지 않았다.** 스펙이 보장한 가독성이 제작 단계에서 조용히 뒤집힌 것이다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 7초 초과 큐 | **15 / 269컷** (최대 8.1초) | **0** |
| 공개된 자막 최대 길이 | 8.1초 | **4.93초** |

고친 방법이 구멍 하나를 더 메웠다 — Edge TTS 의 **낱말 경계 타임스탬프**(`VoiceClip.words`)는
처음부터 있었는데 쓰이지 않았고 이 문서에도 「데이터는 있는데 안 씀」으로 적혀 있었다.
이제 상한을 넘는 큐를 **말이 실제로 끊기는 자리**에서 나눈다(`render/cue.ts`).
문장 단위는 그대로 둔다 — 낱말 단위로 깜빡이면 한국어 자막은 읽기가 **더** 어렵다.
낱말 수가 안 맞으면(나레이션이 자막과 다른 문장이면) **눈금을 쓰지 않고** 글자 수 비례로
나눈다 — 어긋난 눈금으로 나누면 안 나눈 것보다 나쁘다.

⚠️ 자막이 **화면에 구워져** 있으므로 플레이어의 VTT 트랙에 `default` 를 걸지 않는다 —
같은 글이 아래쪽 같은 자리에 두 번 그려진다(실측 2026-09-14에 그러고 있었다).
트랙 자체는 남긴다(켜고 싶은 사람 · YouTube 업로드 · 크롤러).

---

## 3. 3단 드레인 (CLAUDE.md §🤖 와 같은 구조)

| 단계 | 명령 | 하는 일 | 재실행 안전 |
|---|---|---|---|
| **기획** | `pnpm video plan` | 주소 가능한 자리 전부 · 다음에 찍을 것 | **아무것도 안 바꾼다**(관측만) |
| ⓪ 큐 | `pnpm video enqueue` | 설계도를 `video_jobs` 에 올린다 | **안전** — 단계를 되돌리지 않는다 |
| ① 원료 | `pnpm video:source` | DB 실측 → `work/source-bundle.json` | **안전** — 같은 DB면 같은 파일 |
| ② 음성 | `pnpm video voice [<id>]` | Edge TTS 나레이션 + 낱말 타임스탬프 | **안전** — 문장·음성이 같으면 건너뛴다 |
| ③ 렌더 | `pnpm video render <id>` | mp4 (`out/<규격>/<id>.mp4`) | 덮어쓴다 |
| ④ 음량 | `pnpm video loudness --fix` | YouTube 규격으로 맞춤. **포장보다 먼저** | **안전** — 규격 안인 편은 안 건드린다 |
| ⑤~⑦ | 썸네일 · 포장 · 발행 | (아래 화면도움말의 드레인 절차 참조) | **안전** |
| **평가** | `pnpm video evaluate [--full]` | 규격 대비 판정 → `video_jobs` 에 기록 | **안전** — 판정만 다시 쓴다 |

보조: `pnpm video list`(목록·길이·음성 진척) · `pnpm video check`(설계도 검사) ·
`pnpm video stale`(**발행본 ↔ 설계도 어긋남**) · `pnpm video thumbs`(썸네일) ·
`pnpm video:studio`(브라우저 미리보기).

**테스트와 원료**(2026-09-19 · 이슈 #101): `work/source-bundle.json` 은 커밋하지 않으므로 깨끗한 체크아웃(CI)에는 없다. 원료가 필요한 두 테스트(`factory` · `plan-evaluate`)는 `src/__tests__/test-bundle.ts` 로 읽는다 — `work/` 가 있으면 실측, 없으면 `tests/fixtures/source-bundle.json`(줄인 **고정 원료 — 테스트 전용**, 영상·광고에 쓰지 않는다). 둘 다 없으면 `vitest.config.ts` 가 두 파일을 경고와 함께 뺀다. 제품 경로(`loadBundle` · `render/ensure.ts`)는 픽스처로 떨어지지 않는다. 번들 스키마를 바꾸면 픽스처도 같은 커밋에서 고친다. 타입 검사도 같은 원칙이다 — `scripts/typecheck.mjs` 가 원료가 없으면 `src/remotion/Root.tsx` · `index.ts`(원료 JSON 을 번들 시점에 import)만 경고와 함께 빼고 검사한다(`tsconfig.no-bundle.json`). 원료를 픽스처로 `work/` 에 채우지 않는다 — `render/ensure.ts` 의 「없으면 만들지 않는다」 가드를 우회하게 된다.

⚠️ **기획이 맨 앞인 이유**: 렌더 전량이 90분이다. 90분을 쓰고 나서 "이걸 왜 찍었지" 가 되면
그 90분은 되돌릴 수 없다. **평가가 맨 뒤인 이유**: 판정하려면 찍힌 파일이 있어야 한다
(음량은 파일을 읽어야 나온다 — 없으면 「못 잼」이고 그건 합격이 아니다).

### `stale` — 이 공장의 마지막 구멍

`out/` 을 커밋하지 않으므로 저장소에 남는 것은 manifest 한 장이다. 그래서 설계도의 문구를
고쳐도 **이미 올라간 mp4 는 옛 내용을 말하고, 아무도 안 알려 준다** — 화면은 멀쩡히 뜨고
영상만 거짓말을 한다. `stale` 이 셋을 가른다:

| | |
|---|---|
| 없어진 편 | manifest 에 있는데 설계도에 없다 → 화면이 죽은 링크를 그린다 |
| 안 올린 편 | 설계도에 있는데 manifest 에 없다 → 새 영상이 화면에 안 뜬다 |
| 낡은 편 | 제목·부제가 다르거나 길이가 어긋난다(문구를 고치면 음성 길이가 바뀐다) |

⚠️ **재고 수치가 달라진 것은 낡음으로 세지 않는다.** 영상은 찍은 날의 스냅샷이고 화면에
출처와 날짜가 함께 박혀 있다. DB 가 자랄 때마다 186편을 다시 찍을 수는 없다.

`work/` 와 `out/` 은 **커밋하지 않는다.** 번들에는 그날의 재고가 박혀 있어서, 묵으면
틀린 수치로 광고를 찍게 된다. 다시 뽑는 데 30초다.

---

## 3. 원료 — 무엇이 영상에 들어가는가

`apps/web/scripts/video-source.mts` 가 뽑는다. **앱이 뽑아서 패키지에 넘긴다** —
패키지가 앱을 import 하면 의존이 거꾸로 선다.

| 항목 | 출처 | 2026-09-12 실측 |
|---|---|---|
| 커버리지 증명 | `lib/marketing/hero-demo.ts` (랜딩 히어로와 **같은 지문·같은 계산**) | 61 러닝워드 · 초등고 67% → 적정 7레벨 |
| 유형 재고 | `textbook_shelf_inventory()` RPC | 유형 13 · 표본 10 |
| 시리즈 단 재고 | 같은 RPC를 **단의 V레벨로 좁혀** 합산 | reading 1단 0 … 6단 185,296 |
| 실제 문항 | `csat_dcp_items` 중 **가장 짧은 것** | payload 5계열 정규화 |
| 활동 | `lib/framework/registry.ts` | 26 |
| 플랫폼 수치 | 테이블 카운트 | 사전 49,244 · 발행도서 312 · 문항 879,830 |

### 쓰지 않는 것

- **`getDuration()` / `estimatedDuration`** (Edge TTS) — 4.4초 mp3 를 **0.37초**로 답한다.
  길이는 **마지막 낱말 경계의 끝**에서 구한다.
- **`count ?? 0`** — 없는 테이블도 head 요청엔 `count=null` 로 답한다. 0 과 구분이 안 된다.
- **유형만으로 센 재고** — 그러면 한 시리즈의 **모든 단이 같은 수**를 보고한다(실측으로 겪었다).

---

## 4. 품질 규칙 — 타입과 회귀가 강제하는 것

| 규칙 | 강제 방법 |
|---|---|
| 모든 컷에 자막 | `SceneBase.caption` 이 **필수 필드** — 자막 없는 컷을 만들 수가 없다 |
| 모든 수치에 출처 | `Evidence.source` 필수 + `validateSpec` 이 빈 값을 막는다 |
| 지어낸 숫자 0 | 회귀가 화면 수치를 **번들 안 실재 값과 대조**한다 |
| 색은 토큰만 | 회귀가 `remotion/` 전체에서 hex 리터럴 0 을 확인 |
| 한글에 Lora·이탤릭 금지 | `voiceFont()` + 회귀. 한글은 **왼쪽 세로선**으로 "사람 목소리"를 표시 |
| 한글 `keep-all` | 틀(`Frame.tsx`)이 깔고, 회귀가 확인 |
| 닫는 컷 = 다음 한 걸음 | `validateSpec` 의 `no-closing` |

### 자막·컷 길이의 근거

Netflix Timed Text Style Guide (2026-09-12 확인) — 자막 **최소 5/6초 · 최대 7초**,
읽기 속도 **성인 17 CPS · 아동물 13 CPS**. 학습자 영상은 초등까지 내려가므로 **13 CPS 를 바닥**으로,
광고는 17 CPS 를 허용한다. 음성을 구운 뒤에는 **실측 길이가 이 계산을 덮는다.**

### 영상 모션 예산은 화면 예산과 다른 표다

CLAUDE.md §모션 예산의 "총 1초 초과 금지"는 **학습 중 화면**의 규칙이다. 영상은 존재 자체가
모션이므로 그 숫자를 그대로 쓰면 아무것도 못 만든다. 대신 **성질을 물려받는다** —
같은 이징(`cubic-bezier(.4,0,.2,1)`) · 진입 300ms · 퇴장 200ms · 스태거 50ms ·
`transform`·`opacity` 만 · **폭죽·트로피·콘페티 금지** · 정지 구간은 완전 정지.

---

## 5. 음성 (Edge TTS)

키 없이 무료로 한국어 신경망 음성 3종을 쓴다 — `ko-KR-SunHiNeural`(여) ·
`ko-KR-InJoonNeural`(남) · `ko-KR-HyunsuMultilingualNeural`(남).

| 쓰임 | 음성 | 속도 | 왜 |
|---|---|---|---|
| 학습자 | SunHi | **-6%** | 새 개념을 설명하는 소리다 (학습원칙 6 Cognitive Load) |
| 광고 | InJoon | 0% | 스크롤 경쟁에서 느린 소리는 넘겨진다 |

얻는 것이 소리만이 아니다 — **낱말 경계 타임스탬프**가 컷 길이를 실측으로 정하고,
앞으로 자막을 낱말 단위로 동기시킬 수 있다.

### 음량 — 이 공장에서 유일하게 「시중」과 같은 자로 잴 수 있는 축

「시중 광고보다 우위」는 대부분의 축에서 **측정이 성립하지 않는다** — 경쟁사의 카피·구성·색을
우리가 관측할 수 없고, 볼 수 있는 것은 그들의 결과물뿐이라 비교값이 아니라 인상이 된다.
음량만은 다르다. **YouTube 가 규격을 공개했고 재생 시점에 강제한다.**

| 항목 | 값 | 출처 |
|---|---|---|
| 통합 음량 | **-14 LUFS** | YouTube 라우드니스 정규화 (2026-09-13 확인) |
| 트루 피크 | **-1 dBTP** | 같은 규격 |
| 허용 오차 | ±1.0 LU | `loudnorm` 2패스의 실측 재현 오차 |

결정적인 성질 하나: **큰 소리는 줄이지만 작은 소리는 키워 주지 않는다.**
목표보다 조용하면 같은 피드에서 **실제로 작게 재생된다** — 소리를 키우려 손이 한 번 더 가는
사이에 넘겨진다.

**실측 (2026-09-13, 186편 전량)**

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 규격 안 | **0 / 186** | **186 / 186** |
| 통합 음량 범위 | -16.4 ~ -19.6 LUFS | -14.0 LUFS |
| 편끼리 편차(최대-최소) | **3.4 LU** | **0.0 LU** |

편차가 더 아팠다 — 우리 영상 두 편을 이어 보면 음량이 튀었다.

**어떻게** — `pnpm video loudness [--fix]`. 2패스(먼저 재고, 그 값을 필터에 넣어 다시 건다.
한 번만 걸면 실시간 추정이라 목표를 1~2 LU 빗나간다). 영상 스트림은 `-c:v copy` 로
**다시 그리지 않는다** — 90분짜리 렌더를 되풀이하지 않고 소리만 고친다.
재실행 안전: 이미 규격 안인 편은 건드리지 않는다(다시 인코딩하면 세대손실만 쌓인다).

⚠️ **포장(`package`)보다 먼저** 돌린다 — 포장이 manifest 에 바이트 수를 적는데,
뒤에 음량을 고치면 파일이 바뀌어 그 수가 틀어진다.

판정 규칙은 `packages/video-factory/src/render/loudness.ts`(순수부)에 있고 회귀 16종이 잠근다. 붙이는 동안
**같은 종류의 사고를 세 번** 겪었고 셋 다 회귀로 박았다:
① 템플릿 리터럴에서 `\s` 가 `s` 로 줄어 정규식이 아무것도 못 맞혔다(186편 전부 「못 잼」) ·
② `execFileSync` 는 성공하면 stdout 만 주는데 `loudnorm` 요약은 **stderr** 로 나온다 ·
③ 같은 출력의 `Output Integrated` 를 읽으면 **무엇을 재도 합격**한다(그 값은 필터를
통과시켰다면 나왔을 값이라 늘 -14 근처다).

---

## 6. 컷 10종

| 컷 | 무엇을 보여 주나 | 어떤 자산이 필요한가 |
|---|---|---|
| `hook` | 여는 물음 | — |
| `coverage` | **지문 위에 아는 낱말이 칠해진다** | 사전 49,244 + 커버리지 계산 |
| `decay` | `R(t)` 곡선이 두 임계를 지난다 | FSRS 상태 |
| `ladder` | 7단이 아래에서 차오른다 | 단별 재고 |
| `shelf` | 권이 하나씩 꽂힌다 | 시리즈 카탈로그 |
| `item` | **실제 문항**이 풀린다 | `csat_dcp_items` |
| `stat` | 실측 수치 + 출처 | 테이블 카운트 |
| `statement` | 장점 + 근거 | `DIFFERENTIATORS` |
| `progression` | **단어 하나가 거치는 자리들**이 아래에서 켜진다 | `axes.ts` STAGES + FACETS |
| `closing` | 다음 한 걸음 | — |

`coverage` · `decay` · `item` 셋이 **경쟁사가 못 만드는 그림**이다. 나머지는 누구나 만든다.

---

## 7. 알려진 구멍 (2026-09-12)

| 구멍 | 크기 | 무엇이 막혔나 |
|---|---|---|
| ~~학습자용 설명이 없는 문항 유형~~ | **해소 2026-09-13** | 13 → **28종**. 설계도 47 → 62편 · 문항 표본 10 → 25 |
| V1(초등) 유형 재고 0 | `rhyme` · `word_meaning` · `spell_blank` | 스냅샷(2026-09-08)은 1,981문항이라 적는데 DB 는 0. **드리프트** |
| 문항 표본 못 만든 유형 | 3 (위 셋) | 재고가 0이라 당연 |
| ~~학습자 화면 임베드~~ | **해소 2026-09-13** | §9 참조 — `/video` + 시리즈 서가 + 헤더·푸터 |
| ~~음량이 YouTube 규격 밖~~ | **해소 2026-09-13** | 0/186 → **186/186** · 편차 3.4 → **0.0 LU** (§5) |
| ~~학습방법·권장안 영상 0편~~ | **해소 2026-09-13** | 규칙 둘 추가 → **11편**. 원인은 규칙이 아니라 원료였다(§0) |
| **교재 권별 영상 없음** | 시리즈 3 × 계단 7 = 최대 21편 | 지금은 시리즈 단위(3편)까지만. 권별 재고·표본은 번들에 이미 있다 |
| ~~큐가 없다~~ | **해소 2026-09-13** | `video_jobs` 적용 · **73행 등재**(8종 전부). 한동안 이 머신에서 DDL 경로 셋이 다 막혀 있었다 |
| ~~낱말 단위 자막 동기~~ | **해소 2026-09-14** | 7초 넘는 큐를 낱말 경계에서 나눈다(§2-2) |
| ~~기획·평가 단계 없음~~ | **해소 2026-09-14** | `pnpm video plan` · `evaluate` + Admin 탭 2개 (§2-2) |
| **재생 0** | 가장 큰 구멍 | 73편을 찍어도 본 사람이 0 이면 공급만 늘린 것이다. YouTube 채널은 계정 주인만 만들 수 있다 |

---

## 8. 성능 실측

| 항목 | 값 |
|---|---|
| 헤드리스 셸 최초 내려받기 | 113MB (1회) |
| 번들 | 약 20초 (캐시 후 빠름) |
| 렌더 (24.8초 · 1920×1080 · 음성 포함) | **43~46초** · 2.55MB |
| Edge TTS 한 문장 | 약 0.4~1초 |

⚠️ **종료코드를 믿지 않는다.** 다른 세션의 dev 서버가 3000 을 잡고 있으면 렌더러가
그 앱을 자기 번들로 착각하는데 **exit 0** 으로 끝난다. 그래서 CLI 는 끝난 뒤
**파일 존재와 크기**를 직접 확인한다.

⚠️ **포트는 고정하지 않고 찾는다.** 한 번호로 못 박았더니 앞 렌더를 중단한 직후 그 포트가
아직 붙들려 있어 **186편이 전부 즉시 실패**했다(2026-09-13). 더 나쁜 것은 그 다음이다 —
빈 포트를 찾는 코드를 넣었는데 `127.0.0.1` 로만 시험해서, **0.0.0.0 로 잡힌 포트를
"비었다"고 답했다**(Windows). 렌더러가 잡는 방식과 같은 방식(호스트 미지정)으로 시험해야 한다.

---

## 9. 학습자에게 닿는 길 (2026-09-13 추가)

찍은 것이 화면에 없으면 **공급만 늘린 것**이다(§D). 그래서 전달까지를 공장의 일로 본다.

```
video render-all → video package → video publish → (manifest 커밋) → 화면
```

| 단계 | 명령 | 내는 것 |
|---|---|---|
| 포장 | `pnpm --filter @vocaflow/video-factory package` | 포스터 jpg(mp4 에서 ffmpeg 로 추출) · **WebVTT 자막** · 설명글(YouTube 설명란용) · `apps/web/src/lib/video/manifest.json` |
| 발행 | `pnpm --filter @vocaflow/video-factory publish` | Supabase Storage 공개 버킷 `video` 업로드 + manifest 에 `baseUrl` 기록 |

**재실행 안전**: 포장은 이미 있는 포스터를 건너뛰고, 발행은 **같은 크기**로 올라간 파일을 건너뛴다.

### 화면에 붙는 자리

| 자리 | 무엇 |
|---|---|
| **`/admin/video`** | **운영 콘솔(VFP)** — 밀린 것·유실·재생 실적. 탭 3. [ADMIN_CONSOLE.md](./ADMIN_CONSOLE.md) |
| **`/video/[id]`** | **편별 페이지 62개** — 자막 전문 서버 렌더 + `VideoObject` JSON-LD. 색인 가능 면 1 → **63** |
| `/video` | 영상 서가 — 종류별 전체 목록 (공개) |
| `/about` | 소개 1편 + 장점 3편 (그 전까지 처음부터 끝까지 산문이었다) |
| `/pricing` | 커리큘럼 1편 + 시리즈 3편 — "얼마인가" 다음 질문이 **"무엇을 사는가"** 다 |
| `/library/textbooks/[series]/[step]` | 그 단원의 **첫 유형** 영상 한 편. 유형 28편이 닿는 자리 |
| `/` (랜딩) | **영상을 넣지 않는다** — 첫 화면의 자리는 작동하는 증명(CoverageHero)이고 영상은 말하기다(I1). 헤더·푸터 링크로만 잇는다 |
| `/library/textbooks/[series]` | 그 시리즈 영상 한 편 (`ShelfScreen`, 420px 카드) |
| 마케팅 헤더 · 랜딩 푸터 | `/video` 링크 |

`components/video/ComponentVideo.tsx` 한 곳이 모든 자리를 그린다 —
**자동재생 없음 · 반복 없음 · `preload="none"` · 자막 기본 켜짐 · 재생 버튼 64px**.

### 발행 실적 (2026-09-13)

| | |
|---|---|
| 설계도 | **62편** · 규격 3 = **186 컴포지션** · 총 **14분** |
| 렌더 | **186/186 성공 · 실패 0** · 175.8MB · 편당 25~45초 |
| 음성 | 62편 결번 0 (Edge TTS · 낱말 타임스탬프로 컷 길이 실측) |
| 썸네일 | 62장 (1280×720) |
| 발행 | **558개 파일** → `video` 버킷. 공개 URL 200 확인(mp4·jpg·vtt 전부 올바른 MIME) |
| baseUrl | `…/storage/v1/object/public/video` |

`out/` 과 `dist-media/` 는 커밋하지 않는다. 커밋되는 것은 **manifest 한 장**뿐이고,
그게 화면이 보는 전부다.

### 발행 전에는 아무것도 안 그린다

`manifest.baseUrl` 이 `null` 이면 모든 조회가 `null` 을 돌려주고 컴포넌트가 **아무것도 렌더하지 않는다.**
"영상 준비 중" 자리를 만들지 않는 이유: 지키지 못할 약속을 화면에 두지 않는다.
`/video` 만 예외로 **다음 한 걸음**(`/fit`)을 보여 준다(D5 — 막다른 화면 금지).

### id 규칙은 한 곳에만 있다

`@vocaflow/video-factory/ids` — 공장이 짓는 id 와 앱이 찾는 id 가 **같은 함수**에서 나온다.
두 곳에 적으면 갈리고, 갈려도 **오류가 안 난다**(영상만 조용히 사라진다).
회귀 `apps/web/src/lib/video/__tests__/catalog.test.ts` 가 앱 쪽에 id 문자열이 다시 적히는 것을 막는다.

### 마이그레이션 3건 — **적용 완료 (2026-09-13, 승인 후)**

| 파일 | 무엇을 열었나 |
|---|---|
| `20260912235900_funnel_events_video.sql` | `video_started`·`video_completed` 를 `funnel_events_event_check` 에 추가(17 → 19종). 없었다면 수신구가 204 를 주며 **한 건도 안 쌓였다** |
| `20260913000100_video_bucket.sql` | 공개 버킷 `video` + 정책 3(`video_public_read` · `video_admin_write` · `video_admin_update`) |
| `20260913120000_video_jobs.sql` | **큐** — `video_jobs`(편당 한 행) + `video_job_advance`(단계는 앞으로만) + `video_jobs_overview`. 이게 없어서 진행이 「파일의 존재」로만 표현됐다 |

확인: 버킷 1 · 정책 3 · 허용목록 통과 · 계측 회귀 57/57 · 큐 **73행 등재**(8종 전부).

⚠️ `video_jobs.kind` 의 CHECK 는 `spec/types.ts` 의 `VideoKind` 와 **같은 목록이어야 한다.**
승인 시점의 SQL 에는 여섯 종만 있었는데, 같은 날 `method`·`advice` 가 생겨 적용 직전에 넓혔다 —
안 넓혔으면 그 11편의 큐 등록이 CHECK 위반으로 죽는다.
상세는 [DB_SCHEMA.md](./DB_SCHEMA.md) §영상.
