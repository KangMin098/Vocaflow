# 원문 점검·판정 기준 — 개정 기록

> 현행 기준: [criteria.md](./criteria.md). 회차마다 개정 이유와 **근거 사례**를 적는다. [추론]만으로 개정하지 않는다.

## v1 — 2026-09-24 (회차 0: 설계)

회차 루프 이전에 확정한 것. 근거는 모두 2026-09-24 실측·조사다.

| 개정 | 이유 | 근거 |
|---|---|---|
| 정본을 `docs/SOURCE_JUDGMENT_CRITERIA.md` → `docs/source-check/criteria.md` 로 옮기고 버전 표기 | 과제 명세의 산출물 위치 · 판정 기록에 기준 버전을 남기기 위해 | 사용자 과제 명세(2026-09-24) |
| 보관 판정 **보관 / 보류 / 폐기** 3단 | 2단(보관·폐기)에서는 「기준에 없어서 갈린 것」이 어느 쪽으로든 조용히 섞였다 | [측정] 서론·고찰 판정 800편에서 청크별 보관 75~99% — 판정자 차이(retention-criteria-study-20260924) |
| **쓰임새 칸**(연령 × 유형 × 목적 × 난이도 + 플랫폼 속성)을 보관 판정의 첫 기준으로 · 채울 칸이 없으면 폐기 | 옛 기준은 「일반 독자에게 읽히는 단락이 있는가」 하나라 보관 90% 가 나왔고, 보관 판정이 발췌 수율을 거의 예측하지 못했다 | [측정] 적격 발췌를 낸 원본은 100%, 못 낸 원본도 94% 보관(retention-criteria-study) |
| **가공 가능성을 샘플 문단에 실제로 시도** | 인상 판정은 판정자마다 갈렸다 | [측정] 위와 같음 |
| 부분 읽기 금지(전문만) | 앞 800어는 보관할 17편 중 12편을 버렸고, 서론·고찰은 버릴 5편을 남겼다 | [측정] PLOS 30편 세 방식 대조 |
| 길이·어휘·V-Level·CEFR·기계 점수로 버리지 않음 · 저작권도 보관 기준이 아님(권리 태그만) | 버린 원문은 흔적이 없어 잴 수 없다 · 저작권 해소는 후속 파이프라인의 일 | [측정] 수집기 20여 곳이 길이·라이선스로 기록 없이 버리고 있었다 · 사용자 결정 DD-75 |
| 권리 태그를 원천별로 · 컬렉션 라벨 일괄 적용 금지 | 일괄 라벨은 확인한 값이 아니다 | [측정] PLOS 47,938편이 `CC BY 4.0` 하나로 일괄 표기 |
| 플랫폼 고유 칸 5종(`audio` · `data-claim` · `myth-rebuttal` · `claim-evidence` · `then-now`) | 기존 시험에 없는 유형의 원천 속성 | **[추론] — 잠정.** 회차 1 에서 속성 보유율을 재서 확정·폐기 |
| 망설여질 때: 보관 판정은 `hold` | 「망설여지면 남긴다」는 보관 비율을 부풀렸을 가능성이 있다 | [측정] 그 문구가 있는 배치 90% · 없는 30편 73% (단, 판정자 차이와 분리 못 함 — 회차 1 에서 이중 판정으로 잰다) |

## 다음 회차에서 확인할 것

- 플랫폼 고유 칸 5종의 원천 보유율(소스별) — 너무 드물거나(1% 미만) 판정자 일치가 낮으면 폐기.
- `hold` 비율과 `hold_reason` 분포 — `criteria-gap` 이 몰리는 자리가 개정 대상.
- 이중 판정 κ — 0.6 미만이면 문구부터 고친다.
