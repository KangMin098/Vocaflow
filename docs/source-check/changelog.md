# 원문 점검·판정 기준 — 개정 기록

> 현행 기준: [criteria.md](./criteria.md). 회차마다 개정 이유와 **근거 사례**를 적는다. [추론]만으로 개정하지 않는다.

## v6 — 2026-09-25 (회차 10 반영)

근거: [round-10.md](./round-10.md) §5-1 — wikinews 이중 판정 일치 94% · **κ 0.550**. 어긋난 6편 중 셋이 짧은 사실 단신이었다.
검증: 회차 10 wikinews 이중 판정 청크 4개(100편)를 v6 로 두 판정자가 다시 판정 — κ 0.6 이상이면 셋째 몫(회차 11)으로 간다.

| 개정 | 이유 | 근거 사례 |
|---|---|---|
| 짧은 사실 단신: 원인·결과·반응·절차 중 하나라도 문장으로 이어지면 `content_match` 보관, 수치·이름·일정만 되풀이되면 `reference` | v2 「칸 하나만 수치 나열」이 사건 단신에도 걸리는지 판정자마다 달랐다 | [측정] wikinews 「House honors John Dingell」(A 보관 · B 폐기) · 「Earthquake shakes western Mexico」(A 보관 · B 폐기) · 「Teenager stabbed to death in east London」(A 보류 · B 보관) |

## v5 — 2026-09-25 (회차 7 반영)

근거: [round-7.md](./round-7.md) §5-3 — nih_news_in_health 일치 85% · **κ 0.211**. 기사 본문은 전부 일치했고 어긋난 세 건이 모두 기관 공지문이었다.
검증: 회차 9 에서 회차 7 nih 청크를 v5 로 재판정 + 새 표본 20편(판정자 둘).

| 개정 | 이유 | 근거 사례 |
|---|---|---|
| 기관 공지문은 「누가·누구에게·무엇을 하라고」가 서면 `purpose` 칸으로 보관, 주소·경로·목록 나열뿐이면 `reference` | 같은 공지문이 보관(목적)·보류·폐기로 흩어졌다 | [측정] nih 「Spanish-Language Health Materials」(A 보관 · B 폐기) · 「Considering Hip Replacement?」(A 보류 · B 폐기) · 「Mind and Body Therapy for Fibromyalgia」(A 보관 · B 보류) |

**v5 후보로 남긴 것**(이번엔 개정하지 않음 — 한 원천에서만 두 회차 관찰): 한쪽 인용 위주 진행 중 정치 기사(global_voices 튀니지) · 가벼운 폭력의 연령 칸 하한(gdl 체벌). 판정 자체는 갈리지 않았거나 한 건뿐이다.

## v4 — 2026-09-25 (회차 6 반영)

근거: [round-6.md](./round-6.md) §5-3 — global_storybooks 이중 판정 일치 80% · **κ 0.470**. 어긋난 네 건이 세 자리로 모였다.
검증 방법: 회차 7 에서 **회차 6 global_storybooks 청크를 v4 로 다시 판정**(판정자 둘)해 바뀐 건수와 κ 를 보고한다(§10-6).

| 개정 | 이유 | 근거 사례 |
|---|---|---|
| 초등 칸 최소선 — 문장 사이 사건·이유·순서 연결이 하나라도 있어야 보관, 무관한 특징 나열은 `reference` | 짧은 나열형이 「초등 어휘 칸만 보관」과 「폐기」로 갈렸다 | [측정] global_storybooks 「Teeth」 · 「Cooking」(A 보관 · B 폐기) |
| 폭력·성·죽음이 교훈의 중심이면 보류가 아니라 연령 칸 제한(`high1`↑) 보관 | 같은 글이 「고등 이상만 보관」과 「보류」로 갈렸다 | [측정] 「The girl with one breast」(A 보관 · B 보류) · wikinews 뭄바이 연쇄살인(A 보관 · B 보류) |
| 설화 속 고정관념은 요지·결말이면 `bias` 폐기, 한두 구절이면 보관 + 가공 때 걷어냄 | 같은 글이 보류와 폐기로 갈렸다 | [측정] 「Our wonderful world」(A 보류 · B 폐기) |

## v3 — 2026-09-24 (회차 2 반영)

근거: [round-2.md](./round-2.md) — simple_wikipedia 이중 판정 일치 65% · κ 0.442. 어긋난 7건이 전부 같은 자리였다.
**사람 확인(§4 표본)은 아직 반영하지 않았다.**

| 개정 | 이유 | 근거 사례 |
|---|---|---|
| `incomplete-source` 는 중간에서 끊긴 글만 · 짧음·도입부만은 불완전이 아니다 | 같은 도입부 글이 보류(불완전)와 보관(받은 단락으로 선다)으로 갈렸다 | [측정] simple_wikipedia `183e6e80` · `c1fb7092` · `e633bfed` · `cc16bbfc` · `b8100be6`(A 보류 · B 보관), `f3652d09` · `a2173e0a`(A 보류 · B 폐기) |
| **파생물(발췌·도입부·개작)은 원천이 아니다** — 회차에서 빼고 원천의 판정을 따른다(`derivativeKind`) | 원천으로 저장된 행 상당수가 수집 단계에서 잘린 조각이었다 — 조각으로는 원천 단위 판정이 성립하지 않는다 | [측정] 파생물 europe_pmc 1,211/1,300 · space_place 54/59 · storyweaver 77/136 · simple_wikipedia 59/99 · plos 발췌 11,602 |

**수집 결함으로 넘긴 것**: frym 152편은 키가 원천 모양(`frym:<DOI>`)인데 본문이 초록(중앙값 140어)뿐이라 규칙으로 가릴 수 없다 — 수집기가 전문을 받아야 한다.
europe_pmc·space_place·storyweaver·simple_wikipedia 의 파생물도 원천(부모)이 저장돼 있지 않은 것이 많다 — 수집기가 원천을 먼저 받고 조각은 뒤에서 만든다.

## v2 — 2026-09-24 (회차 1 이중 판정 반영)

근거는 회차 1 이중 판정(noaa · the_conversation · african_storybook)의 불일치를 나란히 읽은 것이다 — [round-1.md](./round-1.md) §3.
**사람 확인(§4 표본)은 아직 반영하지 않았다** — 그 결과로 v3 를 낸다.

| 개정 | 이유 | 근거 사례 |
|---|---|---|
| 칸 하나만 수치·순위 나열로 채우는 원천은 `discard`/`reference` | 「칸이 하나라도 있으면 보관」이 수치 요약을 두고 판정자마다 갈렸다 | [측정] NOAA `01bb8d5a` · `a5bec486` (A 보관 「내용 일치 칸 하나」 · B 폐기 「논지·흐름 없음」), `9eaf355e`·`8190995d`·`812e3701`(A 보관 · B 보류) |
| 그림·지도·조작 전제 글은 `discard`/`fragmentary`(보류 아님) | 같은 글이 보류와 폐기로 갈렸다 | [측정] NOAA `1290db35`(포스터 소개) · `48d8daf3`(「파란 화살표를 누르라」) |
| 한 판정이 95% 이상이면 κ 대신 일치율 + 불일치 대조 | κ 가 계산상 무너진다 | [측정] the_conversation 일치 95% · κ 0.000 |

**재판정 결과 [측정]**([round-1-v2.md](./round-1-v2.md)): 같은 442건 중 **61건** 바뀜(보류→보관 13 · 보류→폐기 19 · 보관→폐기 16 · 보관→보류 10 · 기타 3). 보관 85.1→82.4% · 보류 11.3→6.8% · 폐기 3.6→10.9%. 이중 판정 κ: noaa **0.125→0.818** · europe_pmc 0.864 · nasa 1.000 · storyweaver 0.918 · the_conversation 일치 95%(κ 무의미 — 한쪽 쏠림). 다섯 곳 모두 일치율 90% 이상.

## v1 — 2026-09-24 (회차 0: 설계)

회차 루프 이전에 확정한 것. 근거는 모두 2026-09-24 실측·조사다.

| 개정 | 이유 | 근거 |
|---|---|---|
| 정본을 `docs/SOURCE_JUDGMENT_CRITERIA.md` → `docs/source-check/criteria.md` 로 옮기고 버전 표기 | 과제 명세의 산출물 위치 · 판정 기록에 기준 버전을 남기기 위해 | 사용자 과제 명세(2026-09-24) |
| 보관 판정 **보관 / 보류 / 폐기** 3단 | 2단(보관·폐기)에서는 「기준에 없어서 갈린 것」이 어느 쪽으로든 조용히 섞였다 | [측정] 서론·고찰 판정 800편에서 청크별 보관 75~99% — 판정자 차이(retention-criteria-study-20260924) |
| **쓰임새 칸**(연령 × 유형 × 목적 × 난이도 + 플랫폼 속성)을 보관 판정의 첫 기준으로 · 채울 칸이 없으면 폐기 | 옛 기준은 「일반 독자에게 읽히는 단락이 있는가」 하나라 보관 90% 가 나왔고, 보관 판정이 발췌 수율을 거의 예측하지 못했다 | [측정] 적격 발췌를 낸 원본은 100%, 못 낸 원본도 94% 보관(retention-criteria-study) |
| **가공 가능성을 샘플 문단에 실제로 시도** | 인상 판정은 판정자마다 갈렸다 | [측정] 위와 같음 |
| 부분 읽기 금지(전문만) | 앞 800어는 보관할 17편 중 12편을 버렸고, 서론·고찰은 버릴 5편을 남겼다 | [측정] PLOS 30편 세 방식 대조 |
| 길이·어휘·V-Level·CEFR·기계 점수로 버리지 않음 · 저작권도 보관 기준이 아님(권리 태그만) | 버린 원문은 흔적이 없어 잴 수 없다 · 저작권 해소는 후속 파이프라인의 일 | [측정] 수집기 20여 곳이 길이·라이선스로 기록 없이 버리고 있었다 · 사용자 결정 DD-75 |
| 권리 태그를 원천별로 · 컬렉션 라벨 일괄 적용 금지 | 일괄 라벨은 확인한 값이 아니다 | [측정] PLOS 47,938편이 `CC BY 4.0` 하나로 일괄 표기 |
| 플랫폼 고유 칸 5종(`audio` · `data-claim` · `myth-rebuttal` · `claim-evidence` · `then-now`) | 기존 시험에 없는 유형의 원천 속성 | **[추론] — 잠정.** 회차 1 에서 속성 보유율을 재서 확정·폐기 |
| 망설여질 때: 보관 판정은 `hold` | 「망설여지면 남긴다」는 보관 비율을 부풀렸을 가능성이 있다 | [측정] 그 문구가 있는 배치 90% · 없는 30편 73% (단, 판정자 차이와 분리 못 함 — 회차 1 에서 이중 판정으로 잰다) |

## 다음 회차에서 확인할 것

- 플랫폼 고유 칸 5종의 원천 보유율(소스별) — 너무 드물거나(1% 미만) 판정자 일치가 낮으면 폐기.
- `hold` 비율과 `hold_reason` 분포 — `criteria-gap` 이 몰리는 자리가 개정 대상.
- 이중 판정 κ — 0.6 미만이면 문구부터 고친다.
