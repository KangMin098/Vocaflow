# P4 rollback 절차

본 Phase 는 helper 함수 신설 + select_*_vocab 리팩토. 롤백 시:

## 1. select_*_vocab 본문 P2 본문으로 복구

`docs/AI_CONTEXT/rollback/P2_select_book_chapter_vocab_원본.sql` (P3 사이 변경 없음, P2=P3=P4 직전 본문 동일)
`docs/AI_CONTEXT/rollback/P2_select_article_vocab_원본.sql`

각 파일 apply.

## 2. helper 함수 DROP

```sql
DROP FUNCTION IF EXISTS public._extract_composite_score(int, int, int, smallint, boolean, text, smallint, smallint);
```

## 검증

- `pg_proc` 에서 `_extract_composite_score` 부재 확인
- `select_*_vocab` body 에 `_extract_composite_score` 호출 부재 확인
- Les Misérables composite_score 분포 = P2 결과
