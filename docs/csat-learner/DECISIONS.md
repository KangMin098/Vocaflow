# /csat 학습자 재설계 — 결정 기록

> 지시문: [docs/csat-learner-brief.md](../csat-learner-brief.md). 지시문이 "질문하지 않고 여기에 적는다"고 정했다.
> 한 줄 = 한 결정. **지시문과 다르게 간 것은 왜 다른지를 함께 적는다.**
> 게이트 리포트: `gate0-routes.md` · `gate1-report.json`(전 회차) · `gate1-sample.json`(표본) · `gate2-report.json` · `gate3-report.json` · `gate4-critique.md`

| # | 날짜 | 결정 | 근거 |
|---|---|---|---|
| D1 | 09-17 | `/frontend-design` 스킬은 이 저장소에 없다 → 정본 `vocaflow-design` 과 `docs/design/03-system.md`(v07 주묵 판면)를 따른다 | CLAUDE.md 「UI 작업 전 그 스킬을 먼저 읽는다」. 학습 화면에서 외부 취향 스킬은 비활성(§1 라우팅). 강의 지시문 L1 과 같은 판단 |
| D2 | 09-17 | 문항 경계는 **새로 찾지 않고** 커밋된 좌표 색인(`anchor-data/<회차>.json`)을 쓴다. 회차 식별은 파일 이름이 아니라 **sha256** | 색인은 30개 문제지에서 번호 45/45 · 고아 기호 0 으로 검증돼 있다. 같은 해시 = 같은 좌표. 지시문 C1 의 「첫 페이지 텍스트 파싱」은 이름·조판이 회차마다 흔들려 온 전력이 있어(`build-anchor-data.mjs`) 해시보다 약하다. 해시가 모르는 파일이면 그때 회차를 고르게 한다(C1 의 드롭다운) |
| D3 | 09-17 | 선지 분리 규칙은 **코퍼스 빌드에서 그대로 옮긴다**(`choiceStart` · `INLINE_SYMBOL_TYPES`) | 그 규칙은 두 번 조용히 틀린 끝에 얻은 것이다(M1809#30 이 첫 `①` 에서 잘림). 새로 짜면 같은 사고를 다시 겪는다. 두 목록이 같은지는 회귀가 대조한다 |
| D4 | 09-17 | 「문장 앵커 매칭」은 순번 대조가 아니라 **길이열 정렬**(Gale–Church 식 DP)로 잰다 — 화면도 같은 정렬로 강의 큐 `sentence:k` 를 reflow 문장에 잇는다 | 1차 실측 73%. 원인은 reflow 가 아니라 **DB 지문**이었다 — 장문(43~45)에 쪽 번호 `8` 이 섞여 `it. 8 (B)` 에서 문장이 안 끊겨 그 뒤 번호가 전부 밀렸다. 서버에는 문장 길이뿐이라(저작권 경계) 글자 비교는 못 한다. 정렬 후 99.4% |
| D5 | 09-17 | Gate 1 텍스트 비교는 **한글 음절 단위** 토큰, 선지 불일치는 「DB 잡음 / reflow 결함」으로 **갈라서** 보고한다 | DB 선지가 `Zurich 에서` · `체 육 지 도 자` 처럼 공백이 끼어 있어 낱말 단위로 세면 reflow 가 맞게 뽑은 것을 틀렸다고 센다. 갈라 보니 어긋난 68건 전부 DB 쪽(쪽 번호·깨진 글리프 꼬리) — reflow 선지 정확도 100% |
| D6 | 09-17 | reflow 결함 셋을 실측으로 잡아 고쳤다: ① 바닥글 규칙 `/저작권/` 이 선지 「디지털 창작물의 **저작권** 보호」를 버림 → 바닥글 **문장**으로 좁힘 ② `※` 로 자르기가 안내문 본문 `※ All passes include…` 에서 지문을 자름 → 「※ 확인 사항」·「답안지의 해당란」으로만 ③ 묶음 머리글 발문이 두 줄(`[38～39] … 가장` / `적절한 곳을 고르시오.`) → 종결 표지까지 잇는다 | 셋 다 **오류 없이 짧게** 뽑히는 종류라 화면만 봐서는 안 보인다. 회귀 `reflow.test.ts` 가 합성 조각으로 잠근다(원문 없이) |
| D7 | 09-17 | 풀이 기록·복습 큐는 **저장소 인터페이스** 뒤에 두고, 이번에는 **기기 저장(IndexedDB)** 어댑터로 간다. DB 테이블 마이그레이션은 SQL 을 만들어 두고 **적용하지 않는다** | 알맞은 테이블이 없다(`csat_item_attempts` 는 dcp 문항용 · `csat_trap_attempts` 는 선지 단위). CLAUDE.md 「마이그레이션 자동 적용 금지」가 지시문 「질문 금지」보다 위다. 세션 규칙은 순수 함수라 어댑터만 갈아 끼우면 서버 저장으로 옮겨진다 |
| D8 | 09-17 | 학습자 라우트 3개 외 화면 중 **분석 뷰**(허브·유형·문항·지형·계획·사정권)는 `/admin/kice/*` 로 옮기고, **drill · overlay** 는 삭제한다 | 지시문 §3 「지형·패턴·경향은 관리자 evidence 쪽의 일」. `/admin/csat` 는 「교재 공장」(다른 제품 라인)이라 섞지 않았다 — 구조를 그대로 옮겨 접두사(`/csat` → `/admin/kice`)만 바뀐다. 문항 화면은 강의 검수(Gate 2 재생 하네스)가 쓰므로 지우지 않는다. drill 은 세션의 ①풀기가, overlay 는 reflow 가 대체한다 |
| D9 | 09-17 | 정오 확인 전 분석 미렌더(A4)는 **데이터가 브라우저에 오지 않는 것**으로 지킨다 — 답을 고르면 `/api/csat/session/reveal` 이 그때 정답·해설을 준다 | 화면에서 숨기기만 하면 개발자 도구·RSC 페이로드에 다 있다(오버레이 패널 계약과 같은 판단). 정답 번호도 제출 뒤에만 온다 — 제출 전 페이로드에 답이 있으면 「먼저 푼다」가 코드로 지켜지지 않는다 |
| D10 | 09-17 | 주 버튼은 **먹색 채움**(`--t1`) 하나, 주묵(`--ju`)은 **근거 밑줄**에만 쓴다 | 지시문 D2 「색 의미는 정답·오답·근거 셋」과 v07 「1차 CTA = 주묵」이 부딪힌다. 주묵을 버튼과 근거 둘에 쓰면 한 색에 뜻이 둘이 된다. 근거는 앱이 지면에 남기는 표식이라 주묵의 원래 뜻과 맞는다. 오답 자리(점선)·함정 자리(물결)는 무채색 — 모양이 말한다 |
| D11 | 09-17 | 「받기」 링크는 EBSi 가 아니라 **평가원**으로 둔다 | 저장소에 검증된 원본 링크는 평가원 것뿐이다(`kice-source.ts` — 2026 수능만 직접 링크, 나머지는 목록). 확인 안 한 외부 주소를 학습자에게 주지 않는다. 받은 파일이 EBSi 판이어도 해시가 다를 뿐 그 자리에서 번호를 찾는다(detect.ts · 29회차 100%) |
| D12 | 09-17 | `/csat/session` 은 **풀스크린**(셸 걷음), `/csat` · `/csat/progress` 는 셸 띠만 걷는다 | 375px 실측: 셸 띠의 빨간 「Flashcard 시작」이 세션·홈의 주 행동과 겨뤘다(A2). 홈·기록에서 사이드바·탭바까지 걷으면 나갈 길이 없어진다 |
| D13 | 09-17 | 세션 분 어림 = 유형 권장 시간 + 문항당 60초(이해·한 줄) | 90초로 두면 첫 세션이 「약 11분」이 되어 지시문 「≤ 10분」을 스스로 어긴다. 실제 소요는 `csat_session_finished.seconds` 가 잰다 — 거기서 다시 맞춘다 |
| D14 | 09-17 | F1 은 **프로덕션 빌드 · 세 번 재서 중앙값**으로 판정한다 | 개발 서버(비압축 번들)는 19.8초였다 — 화면이 아니라 번들 모드를 잰 것. 프로덕션 한 번 재기는 1.3초와 11.1초가 둘 다 나왔다(공유 DB 가 바쁜 순간). 원인인 카탈로그 재조회는 프로세스 캐시(10분)로 없앴다 |
| D15 | 09-17 | 게이트 스크린샷은 저장소 밖(`apps/web/test-results-csat-learner/` · gitignore) | reflow 화면에는 평가원 지문이 찍힌다. 저장소에 올리면 원문 사본이 된다 |
| D16 | 09-17 | 옛 이벤트 6종(`csat_overlay_*` 4 · `csat_drill_*` 2)은 **코드 목록에서만** 뺀다 | 보내는 화면이 없어져 「목록에만 있고 아무도 안 보낸다」 회귀가 걸렸다. DB 허용 목록에는 남긴다 — 이미 쌓인 행을 읽을 때 이름이 필요하다 |
| D17 | 09-17 | **판단 필요(지시문 §5)** — reflow 는 학습자 PDF 의 문항 글을 큰 글자로 다시 보여 준다. 서버는 아무것도 받지 않지만 「텍스트 재조판」이 이미지 크롭보다 민감하게 읽힐 수 있다 | 상업화 전 변호사 자문 항목에 추가할 것. 지금 설계가 지키는 선: 원본·추출 글을 서버로 보내지 않음 · 해설 얹힌 파일 내려받기 없음 · 원본 바이트 기기 저장 없음 · 스크린샷 저장소 밖 |
| D18 | 09-17 | **사용자 승인** — 계측 허용 목록(`20260917190000`)과 서버 기록 표(`20260917200000`)를 적용하고, 기기 기록을 서버와 잇는다. **복습 큐는 서버로 옮기지 않고 합친 풀이를 시간순으로 다시 돌려 만든다** | 두 기기가 각자 복습 단계를 들고 있으면 「어느 쪽이 맞나」 규칙이 하나 더 필요하다. 큐는 풀이의 함수이므로 풀이만 합치면(고유 `(user,item,answered_at)`) 충돌이 사라지고 규칙은 `model.ts` 한 곳에 남는다. 기기가 먼저 쓰는 순서는 유지 — 서버가 막혀도 세션은 돈다. 적용 뒤 보안 권고(anon 에게 표 노출)로 `REVOKE ALL … FROM anon` 을 더했다 |
