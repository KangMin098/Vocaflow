# Gate 4 · 자기비평 — 화면마다 요소 하나 제거 (2026-09-17)

> 지시문 [E] Gate 4: 「화면마다 요소 하나 제거. 제거 목록·이유 기록. 최종 스크린샷 3장.」
> 스크린샷에는 평가원 지문이 찍혀 저장소 밖에 둔다: `apps/web/test-results-csat-learner/final-{1-home,2-session,3-progress}.png`
> 수치: [gate4-shots.json](./gate4-shots.json) (375px · 프로덕션 빌드 · `<main>` 안 보이는 조작 요소 수)

## 뺀 것

| 화면 | 뺀 요소 | 이유 | 남긴 이유가 있었나 |
|---|---|---|---|
| 홈 `/csat` | 스트릭 0일 때의 「첫날이에요」 | 아무 정보도 없는 위로였다. 스트릭 줄은 **숫자가 있을 때**(「2일째」)와 **끊겼을 때**(「오늘 다시 시작해요」)만 말할 것이 있다 | 없음 — 첫 방문자는 온보딩 한 장을 이미 봤다 |
| 세션 `/csat/session` ② 이해 | 「물결 밑줄은 함정 자리예요 — 눌러 보세요」 안내 줄 | 설명을 설명하는 줄. 물결 밑줄 자체가 누를 수 있는 모양(`role=button` · 포커스 링)이고, 누르면 제목이 「함정 · …」이라고 말한다. 지시문 B 는 함정을 「작은 마커 1개」로 정했다 — 안내 줄은 마커가 아니라 산문이다 | 발견 가능성 — `csat_session_explained{kind:'tempt'}` 가 0 에 머물면 되살린다(계측으로 판정) |
| 기록 `/csat/progress` | 막대 아래 「오늘의 세션으로」 주 버튼 | 지시문 B 「숫자 3개와 막대 하나. 끝.」 돌아가는 길은 셸(사이드바·탭바)에 있다 | **빈 상태의 버튼은 남겼다** — 기록이 없을 때는 그 버튼이 다음 한 걸음이다(D5 · CLAUDE.md D4) |

## 빼려다 남긴 것 (왜 남겼나)

| 화면 | 후보 | 남긴 이유 |
|---|---|---|
| 홈 | 「파일은 이 기기 밖으로 나가지 않아요.」 | 「여기 놓기」를 누르기 직전의 **망설임에 답하는 유일한 줄**이다(서버 전송 없음은 이 설계의 전제). PDF 가 기기에 있으면 줄째 사라진다 |
| 홈 | 「기록 보기」 링크 | `/csat/progress` 로 가는 학습자 쪽 유일한 문이다. 지우면 기록 화면이 도달 불가가 된다(오버레이가 한 번 그렇게 묻혔다) |
| 세션 | 판정 한 줄(「① 을 골랐어요. 답은 ② — 밑줄 친 문장을 눌러 보세요.」) | 카드 색·아이콘과 겹치지만 **`role=status`** 라 스크린리더가 정오를 듣는 자리이고, 다음 행동(밑줄 문장)을 가리키는 유일한 줄이다 |
| 세션 | 타이머 | 지시문 B 가 명시한 요소. 색이 변하지 않는다(압박 없음) |
| 세션 | 「설명 N개 더」 접힘 | 지시문 B 「기본 5개 이하, 나머지는 더 보기」의 구현 — 한 문장에 설명이 둘 이상 겹치면 여기로 온다 |

## 실측 (최종 · 프로덕션 · 375px)

| 화면 | 보이는 조작 요소 | 보이는 글자 |
|---|---|---|
| 홈(PDF 필요 상태) | 4 (받기 · 놓기 · 시작 · 기록 보기) | 80 |
| 세션 ① 풀기 | 7 (선지 5 · 모르겠어요 · 세션 닫기) | 704(대부분 지문) |
| 세션 ② 이해(설명 1개 펼침) | 14 | 931 |
| 기록 | 0 | 46 |

게이트(최종 빌드에서 다시): Gate 2 **19/19** · Gate 3 **9/9**(F1 탭 1 · 3G 중앙값 1.4초 · Lighthouse 100/100/100).
