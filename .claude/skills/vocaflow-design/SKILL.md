---
name: vocaflow-design
description: Vocaflow 의 디자인·기능·프로세스 발명 정본. Part 1(§A–§G)은 혁신 판정 4문(자산 의존·즉시 증명·학습과학 개선·형태 판정) + 5축 조합 발명 엔진 + 디자인 렌즈 6 + 공급/수요 게이트 + 드레인 프로세스 규칙 + 형태 발명(자산이 화면의 골격이 되는 절차)이다. Part 2(§0–§8)는 활성 외부 취향 스킬 2개(design-taste-frontend · minimalist-ui)와 내장 dataviz 가 AGENTS.md 4철학 · docs/DESIGN_SYSTEM.md 토큰과 충돌할 때의 판정표·라우팅·모션 예산이다(나머지 11개는 _disabled 로 이동). 새 화면·새 기능·새 파이프라인을 제안하거나 랜딩·히어로·온보딩·학습 화면·모션·감성 카피를 만들 때, 그리고 "혁신" "임팩트" "이탈" "첫인상" 요청이 올 때 읽는다.
---

# Vocaflow Design — 발명 엔진 + 병합 정본

> **이 스킬은 금지 목록이 아니다.** 앞쪽 Part 1(§A–§F)이 목표이고, 뒤쪽 Part 2(§0–§8)는 바닥이다.
> 제약만 지킨 화면은 "규정 위반이 없는 평범한 화면"이지 혁신이 아니다.
> 외부 취향 스킬(2026-09-04 기준 13개 설치 · 2026-09-18 부터 활성 2개)은 *생성 장치*(Design Read · Variation Engine · 개념법)를 갖고 있는데,
> 그 장치들은 전부 **시각 변주**를 만든다. 이 제품에서 **스타일은 차별점이 아니다. 자산의 형태는 차별점이다** —
> 그래서 Part 1 은 같은 자리에 **학습 메커니즘 변주 엔진**(§A–§F)과 **형태 발명 엔진**(§G)을 놓는다.
> **목표는 혁신뿐이고, 평균은 어떤 화면에서도 허용하지 않는다**(2026-09-18 사용자 지시) — §G5 가 그것을 테스트로 강제한다.
> (2026-09-18 추가) 메커니즘만 발명하면 형태는 기본값(카드·표·3열)으로 떨어진다 — 그 빈 칸을 **§G 형태 발명**이 채운다.
> 복제되는 것은 *스타일*이고, **N1 자산의 형태**(R(t) 곡선 · 채색 지문 · 주묵 선)는 자산 없이 못 그린다.
> 정본 수치는 전부 저장소·DB 에서 읽은 실측값이다(출처를 각 줄에 적었다).

---

# Part 1 — 발명 (목표)

## §A. 혁신 판정 4문 — 통과 못 하면 그건 장식이다

새 화면·기능·프로세스를 제안할 때 **코드보다 먼저** 세 줄로 답한다.
(`design-taste-frontend` §0.B 의 "Design Read" 를 학습 제품용으로 바꾼 것이다.)

| # | 질문 | 통과 기준 | 불합격 예 |
|---|---|---|---|
| **N1** | **자산 의존** — 우리만 가진 실측 자산 없이는 성립 불가한가 | `shared_dictionary` 48,969 · `library_books` 401 · R(t) 동적 계산(`lib/srs/fsrs.ts`) · 커버리지 계산(`lib/textfit/coverage.ts`) · 4축 V-Level · CSAT 코퍼스 중 **최소 1개**에 의존 | 폰트·여백·그라디언트만 바뀐 안 — 경쟁사가 하루면 베낀다 |
| **N2** | **즉시 증명** — 200ms 안에 화면에서 눈으로 확인되는가 | 방문자가 값을 바꾸면 결과가 바뀐다(§3 I3) | "AI 가 분석해 드립니다"라고 쓰고 결과는 로그인 뒤에 두는 것 |
| **N3** | **학습과학 개선** — 7원칙 중 **어느 것**을 더 잘 하는가 | 원칙 번호를 하나 대고 이전보다 나아지는 이유를 한 줄 | "재미있어서" · "요즘 트렌드라서" |
| **N4** | **형태 판정** — 이 화면의 골격(첫 시선이 닿는 형태)을 N1 자산 없이 그릴 수 있는가 | 없이는 못 그린다(R(t) 곡선·채색 지문·주묵 선·서가 변형 중 하나가 골격) | 자산은 데이터로만 쓰이고 골격은 카드 목록·표·3열 격자인 화면 — N1 통과, N4 불통과 = "자산을 담은 평범한 화면" |

세 줄을 못 쓰겠으면 그 안은 아직 발명이 아니다. **N1 이 가장 자주 탈락한다.**
화면(UI)을 만드는 안이면 **N4 까지 네 줄**이다 — N4 의 답은 §G2 절차로 만든다.

---

## §B. 발명 엔진 — 5축 조합 (같은 칸을 두 번 만들지 않는다)

외부 스킬(`imagegen-frontend-*` §22 · `image-to-code` §12)의 Combinatorial Variation Engine 은
Theme / Hero Architecture / Motion Language 를 조합해 **시각**을 변주한다.
여기서는 같은 방식으로 **학습 메커니즘**을 조합한다.

| 축 | 값 |
|---|---|
| 1. 인지 계층 | L0 노출 · L1 이해 · L2 맥락 · L3 수집 · L4a 재인 · L4b 시각생성 · L4c 청각생성 · L5 정복 · L6 완성 · L7 회고 |
| 2. 증거 표면 (무엇을 보여줘야 "내 것"이 되는가) | 지문 채색 · 서가 · 회상률 곡선 · 오답 계보 · 시간대 분포 · 커버리지 % · 난이도 사정권 |
| 3. 주체 | 혼자 · 교사→학급 · 또래 · **과거의 나** |
| 4. 시간 | 지금(세션) · 오늘 · 이번 주 · 학기 |
| 5. 자산 | 사전 48,969 · 도서 401 · 챕터 퀴즈 2,453 · CSAT 코퍼스 · 만화 969호 · FSRS 상태 |

**규칙**: 각 축에서 하나씩 뽑는다. 뽑힌 조합이 이미 있는 화면과 같으면 **버리고 다시 뽑는다.**

> 9개 모듈이 거의 전부 `(혼자 · 지금 · 재인)` 근처에 몰려 있다 —
> **그래서 10번째 모듈은 아무리 잘 만들어도 새롭게 느껴지지 않는다.**
> 비어 있는 칸이 발명 지점이다: 「과거의 나 × 학기 × 회상률 곡선」,
> 「교사→학급 × 이번 주 × 커버리지」, 「또래 × 오늘 × 난이도 사정권」.

---

## §C. 디자인 발명 렌즈 6 — 일반 랜딩 취향으로는 나오지 않는 것

화면을 새로 만들 때 이 여섯을 순서대로 대 본다. 하나라도 걸리면 그쪽이 대개 더 좋은 안이다.

1. **망각을 보여준다** — `R(t) = exp(ln(0.9)·t/S)` 는 연속 함수인데 UI 는 스냅샷(4색)만 보여준다.
   시간을 축으로 꺼내면 "지금 안 하면 목요일에 이만큼 잃는다"가 **눈에 보인다.** 경쟁사엔 이 곡선이 없다.
2. **지문이 곧 인터페이스** — 컨트롤을 지문 바깥 툴바에 두지 않는다.
   아는 단어/모르는 단어가 칠해진 지문 자체가 조작면이다.
3. **성장은 환경으로** (철학 4 Implicit Progress) — 게이지를 그리고 싶어질 때마다
   "대신 무엇이 **변형**되는가"를 답한다. 서가가 차오르고, 색이 risk→stable 로 옮겨가고, 배경 톤이 달라진다.
4. **빈 상태가 최고의 전시장** — 기록이 아직 없을 때가 **실측 데이터로 남의 결과를 보여줄 유일한 기회**다
   (`lib/marketing/hero-demo.ts` 가 그 예). 빈 화면에 "데이터가 없습니다"를 쓰는 것은 발명의 포기다.
5. **실패의 계보** — 오답을 낱개로 세지 않는다. 어떤 단어가 어떤 오답을 낳고 그게 어느 문항에서 다시
   나오는지 **사슬**로 보여준다. `library_chapter_quiz` 2,453 과 FSRS 상태를 동시에 가진 곳만 할 수 있다.
6. **교사의 3분** — 허용 CAC 가입당 ₩400 이라 광고 경로가 성립하지 않고 **교사→학급 경로만 남는다**
   (`docs/PLATFORM_AUDIT.md`). 그러므로 모든 공개 표면은 *"교사가 3분 안에 학급에 던질 수 있는가"* 를
   통과해야 한다. 통과 못 하면 예뻐도 성장에 기여하지 않는다.

---

## §D. 기능 혁신 — 공급이 아니라 수요 쪽에서 찾는다

이 저장소의 실패 모드는 **공급 비대 · 수요 0** 이다 (공급:수요 **3,480:1**, 가입자 3).
새 기능을 제안할 때 **한 줄로 먼저 답한다**: *"이건 공급을 늘리는가, 수요를 늘리는가."*

- **공급 증가**(새 소스 · 새 코퍼스 · 새 콘텐츠 유형)면 **순위를 내린다.** 이미 충분하다.
- **수요 증가**(교사 유입 · 재방문 · 완료율 · 공유)면 올린다.
- **새 테이블을 만들기 전에 기존 자산의 새 결합을 먼저 시도한다.** 대부분의 발명은 **조인 하나**에서 나온다.
  예: `CSAT 코퍼스 × 개인 V-Level` = "이 지문이 네 수능에서 몇 등급 자리인가".

---

## §E. 프로세스 혁신 — 드레인이 이 저장소의 진짜 발명이다

Claude Code 3단 드레인(export → 내가 채움 → import)이 ScriptQuiz **1,292문항** · 사전 **466낱말** ·
PDCP **969호**를 만들었다. 새 작업을 설계할 때 먼저 묻는다:
**"이걸 드레인으로 돌릴 수 있게 export/import 를 먼저 만들 수 있나?"**

- **재실행 안전**(이미 채운 건 건너뛴다) · **빈 값 import 금지** — 둘 중 하나만 빠져도 구멍이 영영 남는다.
- **계측을 같은 커밋에** — 새 공개 표면은 진입 + 내부 상호작용 이벤트를 함께 넣는다(§4 D2).
  **안 재면 혁신인지 아닌지 영원히 모른다.**
- Admin 화면도움말도 같은 커밋에 — 프로세스가 바뀌었는데 도움말이 낡으면 잘못된 조작을 부른다.

---

## §F. 혁신처럼 보이지만 아닌 것

| 장식 | 왜 아닌가 |
|---|---|
| 자산과 무관한 새 미감·새 폰트·새 그라디언트 | N1 불통과. 하루면 복제된다. (자산의 형태를 골격으로 세우는 것은 §G — 장식이 아니다) |
| 새 애니메이션 · 3D 배경 | Calm UI 위반이고 LCP 를 깎는다 (§5 모션 예산 · §7 3D) |
| 10번째 학습 모듈 | §B 축에서 기존과 같은 칸이면 모듈 수만 늘어난다 |
| 새 콘텐츠 소스 추가 | 공급 측. 3,480:1 을 더 벌린다 |
| "AI 로 ~해 드립니다" 카피 | N2 불통과 — 증명이 로그인 뒤에 있으면 없는 것과 같다 |
| 외부 스킬의 Awwwards 연출 이식 | 청중이 다르다. 교사·수험생은 수상작을 보러 오지 않는다 |

---

## §G. 형태 발명 — 자산이 골격이 된다 (시각 변주 엔진의 자리)

외부 스킬의 Variation Engine 은 Theme/Hero/Motion 을 조합해 스타일을 변주한다. 이 제품에서 그 자리는 비어 있었고,
빈 자리는 기본값(카드·표·3열)으로 채워졌다. 여기서는 스타일이 아니라 **자산의 형태**를 변주한다.
씨앗 인벤토리(경로·쓰이는 화면·골격 여부): [docs/design/00-form-seeds.md](../../../docs/design/00-form-seeds.md) —
2026-09-18 실측 씨앗 10개 중 화면의 골격인 것은 3개뿐이었다.

### G1. 형태 축 — 우리만 그릴 수 있는 것

| 축 | 자산 | 형태 | 이미 있는 곳 |
|---|---|---|---|
| 망각 | R(t) 동적 계산 | 시간축 위 감쇠 곡선, 4색 구간 · 밑줄 두께 | 렌즈1 · `lib/srs/fsrs.ts` · `components/layout/MemorySparkline.tsx` · `DecayUnderline`(`components/ui/press`) |
| 채색 지문 | 커버리지 계산 | 지문 자체가 조작면, 아는/모르는 단어의 면 | 렌즈2 · `lib/textfit/coverage.ts` · `components/marketing/CoverageHero.tsx` |
| 주묵 문법 | CSAT 분석 자료 | 실선+화살표=지지, 점선+막대=배제, ○•✓↻ 기록 지도, 막대 길이=문장 길이 | DESIGN_SYSTEM §✒ 형태 문법 · `components/csat/session/visual-analysis.module.css` |
| 환경 변형 | FSRS 상태·서가 | 서가가 차오르고 배경 톤이 옮겨간다(Implicit Progress) | 렌즈3 · `components/dashboard/DurabilityLadder.tsx`(부분) |
| 계보 | 챕터 퀴즈×FSRS | 오답이 다음 문항으로 이어지는 사슬 | 렌즈5 · (코드 0) |
| 시험지 사물 | CSAT 코퍼스 | 2단 격자·문항 번호·출제자 펜·정오표·바를 정 회독 | (신규) · 가장 가까운 것 `components/csat/session/learning-home.module.css` |

### G2. 절차 — 코드 전에 세 줄

1. **골격 선언**: 이 화면의 첫 시선 형태는 G1 중 무엇인가. 하나만. (카드·표·격자는 골격이 될 수 없다 — 그것들은 정보를 담는 그릇이지 형태가 아니다)
2. **서명 선언**: 그 골격에서 기억에 남을 요소 하나(예: 대조 순간 주묵 실선이 그려지는 200ms). 화면당 하나.
3. **N4 답**: 이 골격을 자산 없이 그릴 수 있는가 — "없다"가 답이어야 진행.

### G3. 큰 재설계의 발산

새 화면·전면 재설계는 G1 축이 서로 다른 4안을 먼저 만든다(최고 성능 모델·고노력 — 싼 모델은 이름만 넷인 한 안을 돌려준다).
각 안: 이름 · 골격(G1 축) · 서명 · 토큰명으로 적은 색 4~6 · 타입 역할 3 · 모션 1곳(요소·트리거·ms) · "다른 셋과 무엇이 다른가".
자기검토: 로고를 가리고 (a) 앱 종류 (b) 이 제품인지 알 수 있는가. (b) 불가면 탈락. [own-portfolio.md](../../../docs/design/own-portfolio.md) 와 쌍둥이면 표시.
산출물은 `compare.md` 한 장(4안 표 + 자기검토 결과). 고르는 것은 사람. 고른 안을 2회 수정해 골든 스크린(스크린샷 3장, [docs/design/golden/](../../../docs/design/golden/README.md))으로 고정한다. 이후 화면은 골든을 닮게 만든다.

### G4. 하한선과의 관계

§G 는 Part 2 를 한 줄도 넘지 않는다. 모션은 §5 예산 안, 색은 토큰만, 학습 화면 모션은 7종 안에서 서명을 고른다
(예: "정답 scale" 대신 "주묵 실선 그리기"가 필요하면 §5.2 화이트리스트에 항목을 **추가하는 개정**을 먼저 한다 — 규칙이 정당한 형태를 걸면 규칙을 고친다).

### G5. 평균 금지 — 권고가 아니라 실패하는 테스트

권고는 기본값에 진다. 그래서 아래 둘은 **CI 에서 떨어진다**(`pnpm turbo run test`):

| 장치 | 막는 것 | 파일 |
|---|---|---|
| **골격 선언 라쳇** | `page.tsx` 첫 20줄에 `// @form: <G1 축> — <서명>` 이 없는 **새 화면**. 축은 G1 여섯 중 하나(redirect 전용만 `경유`). 선언이 없던 기존 152화면은 기준선 목록 — **줄기만** 한다 | `apps/web/src/app/__tests__/form-declaration-ratchet.test.ts` |
| **평균 신호 라쳇** | 3열 균등 격자 · Tailwind 그림자 · 12px+ 둥근 카드 · 그라디언트 · AI-보라 · glass · 떠오르는 hover · 끝나지 않는 모션 — 학습자/관리자 표면별 출현 수가 **하나라도 늘면** 실패, 줄면 기준선을 내려 잠근다 | `apps/web/src/components/__tests__/average-signal-ratchet.test.ts` |

- **N4 불통과 화면은 머지하지 않는다.** 예외 없음 — 관리자 화면도(축은 같은 여섯에서 고른다. 파이프라인 화면이면 대개 `환경 변형` 또는 `주묵 문법`).
- 기준선을 **올려서** 통과시키지 않는다. 올려야 한다고 느끼면 그 화면의 골격이 틀린 것이다 — §G2 로 돌아간다.
- 두 라쳇은 **후퇴만** 막는다. 앞으로 가는 것은 §G3 발산 · 골든 · 06-workflow 비평 (a)(b)가 한다.

---

# Part 2 — 바닥 (제약)

> 아래는 **어겨선 안 되는 하한선**이다. 여기를 지켰다는 것만으로는 §A 를 통과하지 못한다.

## 0. 우선순위 — 충돌하면 위가 이긴다

| 순위 | 출처 | 예 |
|---|---|---|
| 1 | **법·표시광고** | 지어낸 후기·이용자 수·평점 금지 (`lib/marketing/trust-signals.ts` 가 DB 에서 읽은 값만) |
| 2 | **접근성** | 44px 터치타겟 · `prefers-reduced-motion` · 색상 단독 정보전달 금지 |
| 3 | **AGENTS.md 디자인 철학 4** | Calm UI · Progressive Disclosure · Empathetic Feedback · Implicit Progress |
| 4 | **docs/DESIGN_SYSTEM.md 토큰** | `--p` 축약형 · Lora/Plus Jakarta/DM Sans · Memory Decay 4색 |
| 5 | 설치된 외부 취향 스킬 | 위 넷을 안 건드리는 범위에서만 |

**5번이 1~4번을 이기는 경우는 없다.** 외부 스킬이 시키는데 위와 부딪히면 §2 표를 따른다.

---

## 1. 어떤 스킬을 언제 부르는가 (라우팅)

| 작업 | 부를 것 | 부르지 말 것 |
|---|---|---|
| 공개 랜딩 / `/fit` / `/pricing` 히어로 | `design-taste-frontend` (레이아웃·타입스케일 감각) + 본 스킬 §3 | `gpt-taste` (GSAP pinning·scrubbing — Calm UI 위반) |
| 학습 중 화면 (`/text` `/flashcard` `/dictate` …) | **본 스킬만.** 외부 취향 스킬 전부 비활성 + **§G 로 형태 생성**(골격 = G1 축 하나 · 서명 하나 · N4) + `docs/design/03-system.md`(v07 「주묵 판면」) · DESIGN_SYSTEM §✒ 형태 문법 | 전부 |
| Admin 화면 | `minimalist-ui` (플랫·저자극이 콘솔에 맞다 — 단 §2 의 폰트·`#EAEAEA`·앰비언트 행 적용) | `industrial-brutalist-ui` (analog degradation = 노이즈) |
| 차트·통계·리포트 | 내장 `dataviz` 스킬 | `high-end-visual-design` (섀도·그라데이션이 데이터를 왜곡) |
| 표지·브랜드 보드 이미지 | **스킬 없음** — 교재 표지는 코드 정본 `textbook/cover.ts`, 단어장 표지는 DB 각인(DESIGN_SYSTEM §매대). `brandkit` · `imagegen-frontend-web` 은 `_disabled` (필요하면 [다시 켜는 법](../../../docs/design/DECISIONS.md)) | — |
| 3D / 성장 시각화 | 본 스킬 §5 (three 0.184 + r3f 8.17 고정) | 최신 r3f 9.x 전제 외부 스킬 |
| 기존 화면 개선 | [06-workflow](../../../docs/design/06-workflow.md) 비평 (a)–(e) — 익명성 · 평균 회귀 · 골든 대조 · 포트폴리오 · 규칙 | `redesign-existing-projects`(`_disabled`)의 "premium 상향" 처방 |

**중복 해소 (2026-09-18)**: `design-taste-frontend-v1` 은 `_disabled` 로 옮겼다 — **v2(`design-taste-frontend`)만 쓴다.**
활성 외부 취향 스킬 = `design-taste-frontend` · `minimalist-ui` + 내장 `dataviz`. 이동 목록·이유: [docs/design/DECISIONS.md](../../../docs/design/DECISIONS.md) DD-05.

---

## 2. 판정된 충돌 — 외부 스킬이 시키지만 Vocaflow 에서는 금지

활성 2개(`design-taste-frontend` · `minimalist-ui`)의 원문 grep(2026-09-18)으로 확인한 것만 적는다.
비활성 11개 기준의 원래 표(2026-09-04 실측 — GSAP pinning · AIDA · stitch 의 "dashboard 세리프 금지" 행 포함)는
[archive/vocaflow-design-SKILL-2026-09-17.md](../../../docs/design/archive/vocaflow-design-SKILL-2026-09-17.md) §2 에 그대로 있다. 스킬을 다시 켜면 그 행도 함께 복귀시킨다.

| 외부 지시 | 어디서 | 왜 금지 | 대신 |
|---|---|---|---|
| **perpetual micro-interactions** (Pulse·Float·Shimmer, `MOTION_INTENSITY > 5` 이면 허용) | `design-taste-frontend` §Perpetual | 철학 1 Calm UI 정면 위반. 학습 중 주변시야 움직임은 작업기억을 깎는다(원칙 6 Cognitive Load) | 상태 **변화 시점에만** 모션. 정지 상태는 완전 정지. 로더·스켈레톤만 예외(끝나는 상태가 있다) |
| **Background Ambient Motion** — 20s+ 떠도는 radial 블롭 · 섹션 배경 radial-gradient | `minimalist-ui` §Motion·§Backgrounds | 끝나는 상태가 없는 장식 모션 + 의미 없는 그라디언트 | 정지 지면(`--bg`/`--bg2`). 깊이는 괘선(`Rule`)과 톤차로 |
| Scroll Entry `translateY(12px)` **600ms** · 모든 블록에 | `minimalist-ui` §Motion | §5 표준 전환 200–300ms 초과 · 학습 화면 7종 밖 | 공개 랜딩 1회 진입 페이드 ≤300ms(`--dur-slow`). 학습 화면 0 |
| 카드 hover 그림자 상승 · `active:scale(0.98)` | `minimalist-ui` §Motion | v07 판면은 뜨지 않는다 — 스케일은 글자를 흐린다 | hover 는 색만 · `active:translate-y-[1px]` (DESIGN_SYSTEM §판면 6) |
| 테두리 `1px solid #EAEAEA` · 표면 `#FFFFFF`/`#F9F9F8` 하드코딩 | `minimalist-ui` §Color·§Bento | 하드코딩 색 금지 · 순백 금지 · 다크 모드 붕괴 | `--bd` · `--bg` · `--bg2` 토큰 |
| **Serif discipline** — 세리프를 기본값으로 쓰지 말라 · Fraunces/Instrument Serif 금지 | `design-taste-frontend` §Typography | Lora(영어)·Hahmlet(한글)가 Vocaflow 의 Dual Coding 시그니처다(`tailwind.config.ts` `editorial`) — 이 제품은 그 스킬이 말한 "genuinely editorial / publication" 예외에 해당한다 | 영어 원문·표제어·히어로 = Lora · 한글 제목·감성 문장 = Hahmlet |
| glassmorphism · Liquid Glass (조건부 허용) | `design-taste-frontend` §Glass | 판면 방향(조용한 지면)과 어긋나고 대비를 깎는다 | `--r-lg` + 1px `--bd` + 배경 톤차. 뜨는 것만 `--sh-float` |
| Marquee · 캐러셀로 "폭" 보여주기 · bento 셀에 그라디언트/이미지 다양화 강제 | `design-taste-frontend` §Layout | 자동재생 캐러셀 금지 · 장식 그라디언트는 N1 불통과 | 격자 진열(DESIGN_SYSTEM §매대) · 골격은 §G1 축 |
| 폰트 자유 선택 (Geist / Satoshi / Cabinet Grotesk · SF Pro / Switzer / Lyon / Newsreader) | 두 스킬 공통 | 폰트는 SSoT 다 | **v07 개정**: Hahmlet(한글 디스플레이) · IBM Plex Sans KR(UI·본문) · Lora(영어 원문) · JetBrains Mono **4종 고정**. ⚠️ 이전 4종(Plus Jakarta·DM Sans·Lora·JetBrains)은 **전부 latin subset** 이라 한글 UI 를 OS 기본꼴로 두고 있었다 — 화면 글자의 48~75%(실측). 이 칸은 "폰트를 마음대로 고르지 말라" 는 뜻이지 "한글을 폴백에 맡기라" 는 뜻이 아니었다. 근거: `docs/design/00-inventory.md` §0-2 |

**비활성 스킬이 시키던 것도 계속 금지다** — 출처 스킬이 꺼졌다고 하한선이 풀리지 않는다:
GSAP pinning·scrubbing·stacking(스크롤 납치) · AIDA 세일즈 구조와 거대 여백(§3 증명 먼저) · 무거운 섀도로 "비싸 보이게" ·
대시보드 세리프 금지 지시(거부 — Lora/Hahmlet 유지).

**아래는 외부 스킬과 일치하므로 그대로 채택한다** (충돌 아님):
Inter · Roboto · Arial 금지 · AI-보라 그라데이션 금지 · 동일 3카드 배치 금지 ·
"Acme / Jane Doe" 류 플레이스홀더 금지 · 가짜 사회적 증거 금지.

---

## 3. 첫인상 — "말하지 말고 증명하라" (첫눈 임팩트 규칙)

이 제품의 주장은 **"내가 아는 비율"** 이고, 그건 본질적으로 **눈으로 보이는 것**이다
(지문 위에 아는 단어/모르는 단어가 칠해진 그림). 그런데 산문으로 설명하면 증명이 사라진다.

### 3.1 규칙 (검증 가능)

| # | 규칙 | 검사법 |
|---|---|---|
| I1 | 공개 화면의 **첫 화면(above the fold)** 에 제품이 하는 일을 **실제로 수행한 결과**가 하나 이상 있어야 한다 | 히어로에 실데이터 렌더 요소가 있는가 |
| I2 | 그 증명에 도달하는 데 필요한 클릭 = **0**. 입력 요구 = **0** | 진입 직후 스크린샷에 보이는가 |
| I3 | 증명은 **조작 가능**해야 한다 — 방문자가 값을 바꾸면 결과가 즉시 바뀐다 (자기 것으로 느껴져야 남는다) | 컨트롤이 1개 이상 있고 200ms 내 반응하는가 |
| I4 | 히어로 본문(부제) ≤ **2문장 / 90자**. 설명은 증명 뒤로 | 글자 수 |
| I5 | 수치는 **DB 실측 또는 그 자리에서 계산한 값**만. 상수 박기 금지 | `no-hardcoded-stats` 회귀가 잡는다 |
| I6 | 증명 요소는 **서버 렌더 HTML 에 남아야** 한다 — 크롤러가 읽을 것이 있어야 한다 | `curl` 결과에 텍스트가 있는가 |

### 3.2 순서 (세일즈가 아니라 도구 순서)

```
증명(작동하는 것) → 왜 그런가(근거 1줄) → 다음 문 → 신뢰 수치 → 상세
```
`gpt-taste` 의 AIDA(Attention→Interest→Desire→Action)를 쓰지 않는 이유:
교사·학생은 광고를 보러 온 게 아니라 **오늘 쓸 도구**를 찾으러 온다. 욕구를 만드는 단계가 낭비다.

---

## 4. 이탈 방지 — 못 재면 방지도 없다

### 4.1 규칙

| # | 규칙 | 근거 |
|---|---|---|
| D1 | **막는 것을 먼저 없앤다.** 가치 확인 앞에 로그인·입력·모달을 두지 않는다 | `/fit` 이 로그인 없이 열려 있는 이유와 같다 |
| D2 | 새 공개 화면에는 **진입 이벤트 + 내부 상호작용 이벤트**를 같은 커밋에서 넣는다. 진입만 재면 "왔다 갔다"밖에 모른다 | `lib/analytics/events.ts` 닫힌 목록에 추가 |
| D3 | 이벤트 속성은 **숫자·불리언·닫힌 열거형만**. 자유 문자열 금지 (지문 유출 차단이 타입으로 보장돼 있다) | `events.ts` 헤더 계약 |
| D4 | 파생 가능한 것은 수집하지 않는다 (구독·완료는 DB 행에 남는다) | `lib/admin/retention-math.ts` 결정 |
| D5 | 빈 상태(empty state)에 **다음 한 걸음**이 반드시 있어야 한다. 막다른 화면 = 이탈 | — |
| D6 | 실패·오답 화면에서 **비난 금지**. 정답률 빨간 글씨·경고 아이콘 금지 | 철학 3 Empathetic Feedback |

### 4.2 첫 세션 예산

가입 직후 **첫 학습 1회 완료까지 화면 전환 ≤ 3**. 넘으면 그 경로를 줄이거나 건너뛰게 한다.

---

## 5. 모션 예산 — 숫자로 못 박는다

Vocaflow 토큰(`packages/design-tokens/src/tokens.css:200-204`)은 이미 업계 권장 대역 안에 있다.
아래는 그 사실을 **규칙으로 고정**한 것이다.

| 항목 | 값 | 토큰 |
|---|---|---|
| 마이크로(호버·프레스·토글) | **100–200ms** | `--dur-fast` 100ms · `--dur-normal` 200ms |
| 표준 전환(패널·모달·페이지) | **200–300ms** | `--dur-normal` · `--dur-slow` 300ms |
| 이징 | `cubic-bezier(.4, 0, .2, 1)` | `--ease` |
| 스태거 | **50ms** | DESIGN_SYSTEM §Motion |
| 이동 거리 — 마이크로 | **4–16px** | — |
| 이동 거리 — 리빌 | **20–40px** (현 `translateY 20→0` 준수) | — |
| 총 지속 | **1초 초과 금지**. 예외: `--dur-breath` 4s 앰비언트(정지 배경 한정) | — |
| 프레임 | `transform` · `opacity` 만 애니메이트 (레이아웃 스래시 0) | — |

### 5.1 reduced-motion 은 "끄기"가 아니라 "낮추기"

```css
@media (prefers-reduced-motion: reduce) {
  /* 위치 이동·회전·스케일은 제거하되, 상태 변화는 여전히 보여야 한다 */
  .x { transition: opacity var(--dur-fast) var(--ease); transform: none !important; }
}
```
전부 `0.01ms` 로 죽이면 **무엇이 바뀌었는지 알 수 없게 되어** 오히려 접근성이 나빠진다.
페이드는 남긴다. (외부 `accessible-animation` 계열의 tiered 처리와 같은 입장)

### 5.2 학습 화면 모션 화이트리스트

학습 중에는 아래 **7가지 외 모션 금지**:
카드 뒤집기 · 정답 `scale(1.05)→1` · 오답 shake 3회 · 진행률 바 · 점수 카운트업 ·
페이지 전환 페이드 · 포커스 링. (`DESIGN_SYSTEM.md §Motion 사용 매핑`)

**금지 명시**: 폭죽 · 트로피 · 콘페티 · 배지 팝업 · 루프 애니메이션 · 자동재생 캐러셀.

---

## 6. 감성 — 어디서 사람의 목소리가 나는가

| 자리 | 서체 | 말투 |
|---|---|---|
| 격려·회고·빈 상태 — **한국어** | **Hahmlet(세리프) · 이탤릭 없음** | 사람이 옆에서 말하듯. "오늘 잘 마쳤어요" |
| 격려·회고·인용 — **영어** | **Lora italic** | 진짜 이탤릭 자형이 있는 쪽 |
| 지표·수치 | Plus Jakarta / tabular-nums | 말을 얹지 않는다. 숫자만 |
| 오답 직후 | IBM Plex Sans KR + 한국어는 Hahmlet 1줄 | 원인을 말하고 다음 한 걸음. 평가하지 않는다 |

**Implicit Progress** — 성장은 게이지보다 **환경**으로 보인다: 서가가 차오르고, 지문의 색이
`risk → shaky → stable` 로 바뀌고, 배경 톤이 미세하게 달라진다. 숫자 100%에 폭죽을 터뜨리지 않는다.

---

## 7. 3D / 제너러티브 — 쓸 자리와 안 쓸 자리

스택 고정: `three` 0.184 · `@react-three/fiber` **8.17** · `drei` 9.122
(외부 Three.js 스킬 다수가 r3f 9.x / three r16x 전제다 — API 가 다르므로 그대로 복사하지 않는다.)

| 쓴다 | 안 쓴다 |
|---|---|
| 성장 시각화(서재·풍경) — **결정론적 시드**로 학습자마다 재현 가능하게 | 학습 화면 배경 |
| 아케이드 게임 표면 | 랜딩 히어로 (LCP 를 깎는다 — 히어로 증명은 DOM/SVG 로) |
| 표지·앰비언트 정지 이미지 | 스크롤 연동 3D 카메라(모바일에서 프레임이 무너진다) |

제너러티브를 쓸 때는 **해시 시드 결정론**을 지킨다 — 같은 학습자·같은 상태면 같은 그림이 나와야
"내 것"이 된다. 매번 달라지면 성장 신호가 아니라 노이즈다.

---

## 8. 착수 전 체크리스트 (공개 화면)

- [ ] 첫 화면에 **작동하는 증명**이 있다 (I1·I2)
- [ ] 그 증명을 **조작**할 수 있다 (I3)
- [ ] 히어로 부제 ≤ 90자 (I4)
- [ ] 화면의 모든 수치가 실측·계산값이다 (I5)
- [ ] 서버 HTML 에 내용이 남는다 (I6)
- [ ] 진입 + 내부 상호작용 이벤트를 넣었다 (D2), 속성은 숫자/불리언/열거형뿐 (D3)
- [ ] 모션이 §5 예산 안이고 `prefers-reduced-motion` 에서 **페이드는 남는다** (5.1)
- [ ] 44px 터치타겟 · 4상태(hover/active/focus/disabled) · `data-theme="dark"` 대응
- [ ] 폰트 4종·`--p` 축약형 토큰만 · 하드코딩 색 0

### 8.1 그리고 — 통과했다고 끝이 아니다

위 항목은 **하한선**이다. 마지막으로 §A 를 다시 본다.

- [ ] **N1** 이 화면은 우리 실측 자산 없이는 못 만드는가 (아니면 경쟁사가 하루면 베낀다)
- [ ] **N2** 증명이 200ms 안에 눈에 보이는가
- [ ] **N3** 학습과학 7원칙 중 무엇을 더 잘 하는지 한 줄로 말할 수 있는가
- [ ] **§B** 뽑은 5축 조합이 기존 화면과 다른 칸인가
- [ ] **§D** 이건 공급이 아니라 수요를 늘리는가
- [ ] **§G 골격**이 G1 축 중 하나이고 카드·표·격자가 아니다
- [ ] **§G 서명**이 첫 뷰포트에 있고 화면당 하나다
- [ ] **N4** 이 골격은 자산 없이 못 그린다
- [ ] **§G5** `page.tsx` 에 `// @form:` 선언을 적었고, 평균 신호 라쳇이 그대로거나 줄었다(올리지 않았다)
