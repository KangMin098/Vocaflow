# DB 실측에서 나오는 원료와 구운 산출물 — 커밋하지 않는다.
#
# 왜: 번들에는 그날의 재고·커버리지가 박혀 있다. 커밋해 두면 6개월 뒤 누군가 그 수치로
# 광고를 찍는데, 그때는 이미 틀린 값이다(이 저장소가 CLAUDE.md 수치에서 이미 겪은 일).
# 필요하면 `pnpm --filter web video:source` 로 **다시 뽑는다** — 30초면 된다.
work/
out/
