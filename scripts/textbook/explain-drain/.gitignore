# 드레인 청크는 **작업 중간물**이다 — 산출물은 DB(`csat_dcp_items.answer_key.explanation_ko`)에 있다.
# 저장소에 넣으면 밴드별 폴더가 계속 늘고, 이미 적재된 낡은 `.out.json` 이 남아
# 다음 import 가 그것까지 다시 읽는다. export 로 언제든 다시 만든다.
chunk-*.json
v*/
